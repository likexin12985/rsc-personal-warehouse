# 日终审核浏览器与 PG16 联验（8.13）

本批使用当前私有网页构建、真实 FastAPI 路由、合成 JWT/人员及全新自有 PostgreSQL 16.15，数据库 HEAD 为 `20261113_0134`。**已通过下列六个局部场景，尚未完成附件到总部审核的浏览器全流程验收。** 未提交、推送或部署。

## 结果与代码修复

1. 区域人员通过网页开启第一份档案；当前权限、原回执复查完成后显示第 1 版。
2. 切换档案时，不再把上一份档案的成功提示显示在新档案下。正式改动仅 `DailyReviewActions.tsx` 和相应回归测试；消息绑定截止档案，身份变化通知仍全局显示，无当前选中档案时可显示恢复结果。
3. 第二份档案收到真实 API 的 HTTP 200 后，使用本地页 CDP Fetch 故障注入丢弃响应。刷新后待核验标记仍在，点击查询找回原第 1 版；数据库只有一次开启事件。
4. 第三份档案在请求到达 API 前断线。刷新后查询返回未观察到原提交，待核验记录继续保留。
5. 明确勾选确认并终结第三份原请求，返回永久终结证明；审核仍为第 0 版，没有开启事件。
6. 切换到合成外区人员，页面显示“暂无可见日终档案”。

第二轮数据库精确回读为 **2 条开启事件、1 份终结证明**。212 张表中 197 张摘要保持；其余 15 张仅合成人员/会话/授权、审核投影、事件、终结及审计。库存、日终截止档案和附件保持。两轮实例及 API 都已停止，两个临时标签页关闭，故障拦截清除。

59 项聚焦（3 文件，会话 `33208`）和完整前端 **1,346 项 / 80 文件**（`41100`）退出 0。TypeScript 与私有构建退出 0；私有 bundle 原有 >500 kB 提示仍在。公开构建逐字节保持 8.12，星星 `/xx` 入口保持。1,456 个正式输入绑定当前摘要，仅上述两个前端输入变更；后端和迁移未变。

## 失败记录与证据边界

第一轮 `62290` 退出 1：收尾脚本误查 `daily_review_events.operation`，真实结构为 `payload_jsonb` 内的请求字段。浏览器观察和前后表摘要保留，但整轮不列为通过。该实例已停止，未重启旧数据目录。修正仅在新的第二轮工件；第二轮 `16667` 退出 0。

工具曾拒绝未指定资源类型的 Fetch 拦截、`Fetch.disable` 和 DOMStorage 读取。随后按工具要求限定 `Fetch` 类型并用空 patterns 清除；**没有取得浏览器原始 localStorage 内容的实测证据**。第一次等待了与实际提示不一致的文案，已改为读取真实 status，没有重放业务命令。

API 绑定随机的 `127.0.0.1` 端口，数据库仅本地 Unix socket。合成身份入口仅用于本批测试；正式身份路由及当前 HMAC 会话校验被执行。命令配置允许本地 HTTP，未执行整个生产应用的 KMS/启动门禁。对象存储为 FakeStorage，未进行浏览器附件上传。本批不代表真实 OSS、短信登录、生产启动或远端 CI 成功。

证据目录：

- 首轮：`artifacts/daily-review-browser-20260921/`，原始失败、59 项聚焦和构建日志保持。
- 通过子集：`artifacts/daily-review-browser-20260921-attempt02/`，包含 `verification.json`、`result.json`、源码/构建摘要、表摘要及 `browser-observations.json`。
- `browser-observations.json` 是本任务 CUA 实测摘要，非伪造的自动化测试报告。两个目录中的 `STOP` 已存在，脚本拒绝原位重复运行；下次复制到新的独立工件目录并建立新实例。

## 接续工作

继续补浏览器真实附件上传、部分解释、总部退回、补证及独立审核；需要有效 HTTPS 对象传输测试环境，不能放宽正式上传 HTTPS 校验或越过浏览器证书警告。补跨标签竞争、授权变化与窄屏检查，再把本地原生用例纳入正式 CI。当前八角色容器恢复、准确最终候选完整后端/GitHub 门禁、真实来源/身份/OSS 及三天对账仍待完成。公开目录仍 `pending`、0 条。
