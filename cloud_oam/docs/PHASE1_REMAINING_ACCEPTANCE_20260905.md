# 一期剩余交付与验收边界（2026-09-05）

本清单是 V1.0 下的一期交付切分，不替代设计基线，也不授权外部系统操作。
一期重点为盘点、需求提报/逐行审批及原物料供给计划处理。
依据当前源码复核；已存在的模块不重复建设，新增候选源码未通过准确 SHA 的 PostgreSQL 16
门禁前不计入发布完成。

## 2026-09-06 小程序页面显式永久封存本地候选

提交 `e77a6bd` 将“确认未执行并永久封存”按钮接入小程序非期初过账恢复页面：只有原哨兵坐标、
当前身份、授权版本和 `can_post` 完全一致时才允许操作；请求不带幂等键，严格校验七字段回执，
成功回执前保留哨兵。小程序全量回归 `714 passed`，Web 全量回归 `803 passed`。提交 `5e88298`
把封存服务与迁移专门回归加入 PostgreSQL 16 release gate 静态套件。该切片仍是本地候选，
准确 SHA 的远程 Client/PG16 门禁、直接 SQL 真库竞争样本、微信真机生命周期和一期 UAT 尚未完成。

## 2026-09-07 封存坐标修正与货源候选只读目录本地候选

提交 `c457aaf` 新增 0066 前向迁移，将服务端迟到过账保护从任务级修正为 `task_id + request_reference`
精确坐标；新过账完成事实和 `posted` 结果必须互相绑定请求坐标，历史未知坐标保持只读兼容。
新增回归 `148 passed, 19 skipped`（迁移与正式盘点相关套件），并复核小程序 `716 passed`、Web `804 passed`。
提交 `15a9290`、`820f722`、`5ca5fbf` 新增需求明细的只读来源库存候选目录：重证最终审批、当前修订、
库存投影连续性和期初建账，只返回 active/available/正余额及版本游标，不创建分配、占用或履约事实；
相关后端回归 `85 passed`。以上均为本地候选，准确 SHA 的远程 Client/PG16 门禁、真库竞争样本、真机生命周期
和一期 UAT仍未完成。

随后提交 `0b08717` 增加 0067 任务行锁，将直接 SQL 的封存/完成竞争纳入同一数据库序列化边界；新增迁移
契约回归并将准确 head 更新为 0067。该项仍未取得远程 PG16 动态竞争样本，不能替代真库验收。

提交 `2816665` 将该只读候选目录接入 Web 适配器：请求行 UUID 绑定、`no-store` 读策略、响应版本/投影状态、
库存货源字段和数量精度均做严格校验；未接入分配、占用或履约写入。前端 TypeScript 构建及 Web 全量
`808 passed`。提交 `bca784e` 已在需求详情接入只读候选展示，`d13fc70` 增加页面无写入回归；仍未接入分配/占用写入；准确 SHA 的远程
Client 门禁、真实库存候选 UAT 和后续履约状态轴仍未完成。提交 `7fed5a2` 将页面入口绑定到访问上下文
明确计算的 `admin`/`provincial_manager` 能力，避免把工程师库存只读权限误当成货源分配权限。
提交 `84bb25b` 增加该角色能力的适配器回归；提交 `0b5caac` 进一步在客户端重证批准量、取消量、可分配量和候选物料的一致性，异常响应保持失败关闭。

## 2026-09-06 `a00f591` 门禁后基线

提交 `a00f591deb894e748f45806c442ee13ff42432e0` 已推送且本地与远端一致。准确 SHA 的
[Client gate 34021817679](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34021817679)
和 [PostgreSQL 16 gate 34021817686](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34021817686)
均通过。复核命令状态独立读端及其持久版本对、owner graph、审计链和篡改 fail-closed
检查已纳入代码与回归；此前“尚未交付独立 endpoint”的旧记录仅保留为历史，不再作为
当前状态。

当前仍不能宣称一期 UAT 或生产放行。供给任务纵向链路尚未开始；审批、分配、占用、出库、
发货、物流签收、OAM 收货、RSC/个人仓入库、通知送达和对账同步必须继续作为独立状态轴
开发和验收。门禁期间未访问或写入任何外部生产系统。

## 2026-09-06 Web 持久恢复续交付

客户端提交 `83f2223d8fa5031718cb5b8250c3b2796d4667fd` 已通过
[Client gate 34025174245](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34025174245)：
Web `795` 项、小程序 `670` 项、类型、构建和仓库安全均通过。Web 日常非期初计数与区域/总部
复核已经用独立 task/round/scope 或 review-stage 哨兵实现跨刷新恢复；历史未知只读查询，
不重放 POST、不换幂等键，确认历史事实后才清理。该提交不改数据库，因此 PG16 仍以未变更后端
[34021817686](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34021817686)
为最新真库证据。

下一项仍不是供给履约，而是完成小程序非期初过账恢复、服务端提交级“未执行封存”协议，
以及期初可信库存控制投影和启动前恢复锚点。所有审批、分配、占用、出库、发货、物流签收、
OAM 收货、RSC/个人仓入库、通知送达、对账同步继续独立建模和验收。

## 2026-09-06 0063 本地续开发（未计入发布完成）

0063 复核命令状态前向迁移已在工作树实现，拓扑为 `0062 → 0064 → 0063`，但本轮尚未取得
新的 PostgreSQL 16 真库门禁，因此不计入正式发布完成。对新非期初复核，PG/SQLite 版本对均
明确拒绝双向半空和非连续值；旧 0047 启动降级阻断与新 0063 复核降级阻断已在门禁流程中隔离。

generic `stocktake_query` 仅对非期初 `TASK_TYPES` 做状态迁移/审计 payload 重证，缺失或篡改
即阻断；这不是独立 review command-status endpoint，也不是完整 owner-graph + audit-chain
读端。审批、分配、占用、出库、发货、物流签收、OAM 收货、RSC/个人仓入库、通知送达和对账
同步继续保持独立状态轴。当前定向回归为 `67 passed`；完整后端静态套件（不含动态 PG16）为
`718 passed in 168.02s`，本机动态 PG16 按环境 `1 skipped`。0063 改动已形成本地提交但尚未
推送；完整套件、提交 SHA 和远程门禁不能以旧记录代替。

## 2026-09-06 小程序复核恢复本地候选（未计入发布完成）

本地提交 `9b7a4b172bd0cbf66d691742ee68f39ffcf68d08` 已将日常盘点区域/总部复核接入独立
持久恢复哨兵、只读 `command-status` 查询和精确详情责任链；未知结果、身份/权限漂移、存储故障
和坐标不一致继续失败关闭并保留原坐标，不重放或换键。小程序全套 `675 passed`，Web `795
passed`，TypeScript/Vite 构建、仓库安全及后端复核/迁移/历史定向 `289 passed`。

该提交只改客户端和文档，本机 PostgreSQL 16 动态门禁按设计跳过（无 Docker/psql，未伪造
GitHub disposable 环境），尚无该准确 SHA 的远程 Client/PG16 门禁。因此小程序复核恢复只计
本地候选，不改变当前 PG16 真库证据或一期/UAT/生产状态；服务端“未执行永久封存”、真机生命周期、
SN/cutoff replay 多范围及通知/履约状态轴仍按下文保持未完成。

## 2026-09-06 服务端封存迟到过账阻断本地候选

提交 `316c340` 将 `sealed_not_executed` 结果接入正式过账服务：相同请求坐标的迟到 POST
在业务锁图内失败关闭，成功过账写入 `posted` 结果坐标；0065 迁移新增 PostgreSQL
`ENABLE ALWAYS` 与 SQLite 触发器，直接插入过账完成事实也会被已封存任务拒绝。新增
封存后迟到 POST、posted 状态恢复和迁移契约回归。相关后端定向 `19 passed`，正式盘点 API、
任务策略、数据库安全、迁移及封存回归 `476 passed, 19 skipped`。

这仍是本地候选：尚未取得该准确 SHA 的远程 Client/PG16 门禁，直接 SQL 的真实 PostgreSQL
竞争样本、真机生命周期和一期 UAT 仍待完成；OAM/RSC 入库、通知送达和对账继续保持独立状态轴。

## 2026-09-06 两端显式永久封存本地候选

提交 `223b04d`、`f626855`、`e6e9401` 已将服务端封存协议接入 Web 与小程序恢复流。两端仅在
原哨兵坐标、当前身份、授权版本和 `can_post` 完全一致时允许显式确认；请求不携带幂等键，
严格校验 7 字段封存回执，只有成功回执才清理哨兵。Web `802 passed`、小程序 `711 passed`，
TypeScript/Vite 构建和仓库安全检查通过。远程 Client/PG16 门禁、真机生命周期、直接 SQL
真实竞争样本和一期 UAT 仍未完成。

## 2026-09-06 小程序过账恢复只读契约本地候选（未计入发布完成）

本地提交 `bbb5657` 新增非期初过账历史命令状态的严格小程序校验：固定字段、任务/人员/授权版本、
版本递增、差异计数、流水游标连续性、数量关系及 `posted/closed` 详情投影边界均按 Web/后端契约
收紧；适配器同步投影总部管理员的独立 `can_post` 权限。该切片没有新增小程序过账按钮、POST
接线或重放路径，`post` 仍由现有小程序正式契约拒绝。小程序全套 `688 passed`，本机 PG16
动态门禁仍按设计跳过，尚无该准确 SHA 的远程 Client/PG16 门禁。

## 2026-09-06 小程序过账恢复前置契约本地候选（未计入发布完成）

提交 `43fb3b5`、`859303a`、`dff1013` 完成非期初过账恢复前置层：小程序持久坐标哨兵、单任务租约、写后回读、
存储故障锁存、canonical `post-differences` 路径、过账写响应校验、command-status 只读投影校验和单次 POST/后续只读恢复协调器。
小程序全套回归 `688 passed`，未接入页面按钮或实际过账 POST；现有页面仍保持过账关闭。服务端
“未执行永久封存”、准确 SHA 远程门禁、真机生命周期和一期 UAT 仍未完成。

## 当前验收基线

### 2026-09-06 当前续验收（0063 之前的历史基线）

安全提交 `27c445c030b887c935359f7458234eee6baf399f` 的 Client run
`34009213016` 和 PostgreSQL 16 run `34009213014` 均已通过。PG16 静态为
`2133 passed, 1 skipped, 1 warning`，动态为 `1 passed, 1 warning`。

本轮补齐并验收 0064 组织终结器锁的真实 owner/ACL/SECURITY/search_path、readiness
0062→0064 指纹、降级/再升级及锁竞争；并完成小程序 durable count recovery 的 no-replay、
存储故障锁存、物理 key 完整性、页面卸载证据清理和 late-result 竞态保护。0064 真库使用的
总部组织为 disposable 测试夹具，验证后删除，不计生产数据。

在该 0064 历史记录时，0063 复核命令状态迁移尚未开始；其后已在工作树按
`0062 → 0064 → 0063` 线性拓扑实现最小迁移和非期初证据校验，当前是否可发布仍以本文首节及
新的准确 SHA/PG16 真库门禁为准。需求供给纵向链路及审批、分配、占用、出库、发货、物流签收、
OAM 收货、RSC/个人仓入库、通知送达、对账同步继续保持独立状态和未完成边界。

### 2026-09-06 最新续验收记录（`e5ea7fe`）

提交 `4c0f74a` 已通过 Client release gate `33996107590` 与 PostgreSQL 16 release gate
`33996107518`。该轮动态真库样本已覆盖非期初串码、cutoff replay、两范围独立游标、串码逐件回放、
差异评估、审批、零移动过账、对账和关闭；动态库位的零余额允许缺失 `stock_balances` 行，首笔合法入库
后再验证延迟创建的余额投影，未绕过正式库存规则。

提交 `e5ea7fe` 已通过 Client release gate `33997085112` 与 PostgreSQL 16 release gate
`33997085137`（静态与动态均通过）。非期初过账现在在库存批次及来源审计验证之后、写入不可变过账完成事实
之前重新读取并比较完整 principal、总部管理员 assignment、角色范围和有效权限坐标；任何漂移统一以
`stocktake_posting_authorization_changed`/412 失败并回滚同一事务。新增锁序静态契约及安全过账回归，
未新增迁移、ACL 或外部系统写入。

上述门禁仍不等于生产放行：尾部身份竞争与原 POST 锁序尚需专门 PostgreSQL 真库竞争样本；Web 日常盘点
过账命令的持久哨兵/只读恢复已在本地实现并等待本次远程门禁，小程序暂不接入。复核、分配、占用、出库、发货、物流签收、
OAM 收货、RSC/个人仓入库、通知送达和对账同步仍保持独立状态，不能由盘点过账状态代替。

### 2026-09-06 续验收记录

提交 `c7f98cb282a99456b923a4fcbfede943d5cc6482` 已通过 Client release gate（run
`33990582028`）和 PostgreSQL 16 release gate（run `33990581994`）。本轮将过账历史恢复回归纳入
正式静态门禁，并以真实 post/close 后历史查询验证 posted 事实仍可重证；同时为后续动态 SN 试验
增加独立 replay 库位、账户和串码，先完成正式 opening establishment，再写入合法串码库存种子。
该轮只证明串码种子满足正式期初前置且恢复回归可执行，尚未把非期初 SN、cutoff replay、多范围、
尾部身份竞争或原 POST 锁序宣称为真库已验收。

准确父提交 `d95a3fe61eaa656d0d144d307a4ab9348c048194` 已通过 Client release gate
（run `33979437995`）及 PostgreSQL 16 release gate（run `33979438009`）；静态契约提交
`18dfbd72b7d578aff5886029304a5afadb75b8f2` 的 Client run `33981032052` 与 PostgreSQL 16
run `33981032060` 均已通过。当前 HEAD 可能是文档后继，最近文档 Client release gate（run `33985348396`）已通过；已验收功能代码提交为 `8046c78`，其 Client gate `33985935542` 与 PostgreSQL 16 gate `33985935553` 已通过。本次修复过账命令状态查询的锁后身份复核及 posted→closed 历史重证兼容，并新增两条回归测试；前一功能基线为 `f482b8e`，其 PostgreSQL 16 gate（run `33982740333`）已通过。本次仅新增
盘点历史 cutoff replay、SN/逐件、多范围、尾部身份重验和 owner-lock 顺序的静态契约预检，
没有把源码存在误报为 PostgreSQL 真库场景已验收，也没有新增迁移或改变生产业务逻辑。
当前真库样本仍是单范围、hard freeze、非 SN；下一项 P0 仍为准确 SHA 的 SN + cutoff replay
多范围真库样本，随后才是尾部身份竞争、原 POST 锁序和两端非 count 命令持久恢复。

尝试提交 `03a66ea` 直接向串码账户写入库存，被 PG16 真库按 `inventory_opening_not_established` 拒绝；回退提交 `e227696` 后 Client gate `33988119885`、PG16 gate `33988119864` 全绿。该失败样本确认动态 SN 验收必须先建立独立 opening establishment，不能用测试 seed 绕过正式期初建账。

复核命令状态查询已完成只读设计审查但未实现：现有 `stocktake_reviews` 未持久化可重建的历史
`task_version`，直接复用当前任务版本会制造错误恢复证据。正式实现前必须新增前向迁移，明确
结果版本字段及其审计、触发器、权限和安全清单同步；在此之前不得返回“已复核”历史状态。
具体字段、兼容策略、函数指纹和验收矩阵见 `cloud_oam/docs/REVIEW_COMMAND_STATUS_MIGRATION_PLAN.md`。

随后提交 `f482b8e` 的过账恢复切片已通过 PostgreSQL 16 release gate（run `33982740333`）：
非 opening 盘点新增只读
`/{task_id}/post-differences-command-status`，只接受精确 `X-Request-ID`，并重证过账审计、
状态转换、审批封印、过账完成及库存事实；9 项定向测试通过，未新增迁移。该接口不扩展为
review/recount/disposition/close 的通用状态查询，未知证据仍保持阻塞。

历史已验收代码 `b57f3c3`（迁移head0062）已由后续功能代码基线取代；其真实零期初6步独立提交夹具仍是当前真库门禁的基础。
新增7项静态回归、相关76项及既有期初2项通过；完整本地3045项通过、1项环境跳过（814.07秒）。
准确 SHA PG16 run`33970055640`和客户端 run`33970055644`全绿，含清理；PG静态2112、
动态复合门禁1项，Web776、小程序647。
PG job于`2026-09-05T14:09:10Z`完成。该夹具修正不改38b35e6业务、迁移和ACL。
旧人员变化由合成SQLite回归覆盖；真库覆盖三轮跨附件到过账/关闭、合法冻结释放与有界owner竞争；
SN/截止回放、多范围、尾部身份竞争、原POST锁序和两端持久恢复仍未完成。详细范围以最新终态验收文件为准。

以下为此前两轮失败历史。第二轮修正 `1f7322c`：新增21项版本感知目录回归、相关69项通过，客户端
run `33968901267` 全绿；修正后本地全量3038项通过、1项环境跳过（848.28秒），
PG16 run `33968901288` 静态2105项通过、动态在+1盘盈被正确拒绝（专用夹具账户缺正式期初建账）。
此缺口现已由b57f3c3补真实期初前置修正，但旧run不计全绿。
首次业务候选 `38b35e6` 新增 0062 历史 owner 与终态来源；本地完整回归3017项通过、1项跳过，
准确 SHA 客户端 run `33967807404` 全绿；PG16 run `33967807410` 静态2084项通过，动态
因旧0058目录caller断言遗漏0062失败，随后已修正并重跑。最新验证见
`DAILY_COUNT_HISTORY_TERMINAL_ACCEPTANCE.md`。旧人员/已释放冻结历史模式已实施，
旧失败run不能计发布完成；SN/截止回放专用真库样本、多范围和尾部身份竞争，以及两端持久恢复
仍待完成。以下为更早验收历史。

三轮 PG 历史查询测试 `976cdba` 暴露第二轮只读表 FOR UPDATE 权限错误，run `33963127979`
动态失败，不计全绿。`ee0d0ed` 修复17处锁后回读，但 run `33964225622` 在第二轮封轮因父状态时序失败。
已验收修正提交 `2691ab3` 改为同一事务先 flush round，再更新 task；两次修复均未改迁移/ACL。
相关 103 项、脱敏诊断 13 项通过；本地完整回归 `2939 passed, 1 skipped`（909.10 秒）。
准确 SHA PG16 run `33965156443` 全绿：静态2006项（569.04秒）、动态1项（186.61秒）通过，
含容器清理，于 `2026-09-05T12:21:41Z` 完成；客户端 run `33965156446` 全绿（Web776、小程序647）。
该历史 SHA 累计新增13项历史查询回归；其迁移 head 为0061。
彼时的历史冻结、旧人员及 owner 锁集合缺口见 `DAILY_COUNT_HISTORY_PG16_ACCEPTANCE.md`，
对应修复已由上方0062候选实施；该历史三轮样本不能替代新的终态或并发恢复验收。

日常 count 历史查询服务端 `8cabfdecb0e8734403fb8863e90d06951383f164` 已验收推送，62 项
新增定向回归通过，准确 SHA 客户端 run `33958198902` 全绿；本地后端/边缘全量
`2926 passed, 1 skipped`（539.43 秒），退出码 0，测试前后源码冻结。
准确 SHA 的 PG16 run `33958198821` 全绿：静态 `1993 passed, 1 skipped, 1 warning`
（737.46 秒），动态 `1 passed, 1 warning`（223.17 秒），含容器清理，
`2026-09-05T09:48:21Z` 完成。首次 `812debc` 的 run `33957264704` 动态失败，
已修正新查询对截止/冻结时间的错误等同，保留失败记录，不修改历史迁移。
只证明一笔原 scope count，不产生写入，不解除 `not_observed`。
详见 `DAILY_COUNT_COMMAND_STATUS_ACCEPTANCE.md`；该 SHA 尚无复盘真 PG 服务链，后继见上方。
在该历史 SHA 时，客户端持久恢复、深层历史来源与终态兼容均未完成；后继修复与剩余边界见上方。

客户端修复 `a78d5ad` 已实现两端日常盘点任务加载更多、扫码标识类型与逐件 SN 校验、
过期页面回调隔离及未决意图禁止隐式重放。本地 Web 776、小程序 647、类型/构建通过；
准确 SHA 的客户端 run `33956352741` 已回读全绿。未改后端/迁移/部署/边缘；不代表
日常盘点跨进程恢复已完成。契约与下一切片见 `DAILY_STOCKTAKE_CLIENT_ACCEPTANCE.md`。

此前已验收后端功能 `262b07a34cf508930c3835f2dad993a141920d97` 仅新增控制库存离线证据一致性
验证，195 项新增定向通过，本地冻结后端/边缘 `2864 passed, 1 skipped`（947.35 秒）。准确
SHA 的 PG16 run `33953508516` 全绿（静态1931、动态1项通过，含容器清理，
`2026-09-05T08:03:12Z` 完成）；客户端 run `33953508544` 同 SHA 全绿（Web745、小程序631）。
成功不认证来源/目录、不发布控制 run、不开放启动；迁移/ACL仍为0061基线，客户端未改。
还需可信目录/采集证据、正式控制投影及启动恢复。
契约见 `INVENTORY_CONTROL_EVIDENCE_ACCEPTANCE.md`，不把本切片算作完整控制库存链已完成。

`2677546f5ef6041ecb92acee75d1af868c9d80ff` / `0061` 的 GitHub PostgreSQL 16 run
`33924289472` 已成功：静态 `1234 passed, 1 skipped, 1 warning`（560.26 秒），动态
`1 passed, 1 warning`（184.68 秒）。原物料供给计划、历史命令核验、权限/数量/并发约束和
PC/小程序最小界面已经落地；完整范围与历次失败/取消记录见供给发布清单。
后端新一轮本地完整回归 `2266 passed, 1 skipped`（826.43 秒）。后续独立客户端脱敏提交
`ca9fac76761d6fc400ef75421c2123d2440927d1` 的 Web 425 项、小程序 216 项通过，未改后端。
后续恢复修复 `e2043bbfcaa9fedbf7320adb4f7a773bfa4be0c6`：Web 全套 476 项、TypeScript/构建、
小程序全套 220 项、后端路由定向 95 项通过。该提交只改客户端和测试，不改后端业务代码、迁移或 ACL；
它的精确 SHA 已通过官方 run `33926859550`：静态 `1234 passed, 1 skipped, 1 warning`
（505.14 秒），动态 `1 passed, 1 warning`（185.73 秒）；不挪用前一提交的成功结果。
这不是一期或生产完成：未知结果恢复、可用盘点入口、待办/通知及真实 UAT 仍需以下工作。

后续已保存客户端修复 `ff9611e97ce53adb98c6e05a9af15b59176b6c8a`：小程序首次直接明确拒绝
白名单、覆盖确认弹窗的调用租约和未知后拒绝保留原坐标，全套 229 项通过。
复盘人员目录及 PC 选择入口候选 `31b1ae52df57bc6fcdd1ca8d8b894685622c7d21` 已推送：
Web 509 项及 TypeScript/构建通过，后端相关定向 90 项通过，独立复核无未解决 P0/P1。
准确 SHA 的 GitHub run `33928469889` 已通过：静态 `1351 passed, 1 skipped, 1 warning`
（590.95 秒），动态 `1 passed, 1 warning`（193.17 秒）。该提交独立临时 worktree 的本地
后端/边缘全量 `2326 passed, 2 skipped`（927.66 秒），后续开发变更未混入此测试快照。
后续历史计数核验 API 提交 `a3f0483298108e4411f9d1b1167617364038ff7b` 已推送；新增 50 项
定向测试通过，精确 SHA run `33929437568` 静态 `1401 passed, 1 skipped, 1 warning`
（611.56 秒），动态失败（167.98 秒）：来源初盘已有 `requires_recount` 处置时，当前复盘轮
的空解析集合不足以重证历史复核。应用修复 `ca1a45cf1bafabd6372c1e29f7fdb8a20703b317`
已补齐同任务已预锁全集，未放宽证据校验、锁序或 ACL；后端定向 99 项通过，普通详情的
真库非变更断言同步加入。中间 run `33930872181` 已被最终客户端候选取代并取消，不计通过；
最终 `e54765a` 的 run `33931135022` 已成功：静态 `1412 passed, 1 skipped, 1 warning`
（551.04 秒），动态 `1 passed, 1 warning`（186.29 秒），包括清理步骤全部通过。
PC 基础提交 `8df5447` 与页面接线 `d553ef5a75d085cd8254584ce2e234bc6a4df590` 已推送，
Web 全套 676 项、TypeScript/构建通过；不把客户端通过等同于本次失败的 PG16 已通过。

后续目录安全候选 `75b7e42a988f1178145e91a747b43bba467dac65` 的本地相关回归 203 项通过
（58.09 秒），准确 SHA 的真库 run `33932385447` 已全绿：静态
`1549 passed, 1 skipped, 1 warning`（666.56 秒），动态 `1 passed, 1 warning`（204.55 秒），
容器清理等全部步骤成功。它包含 `addb823` 全 principal deny 保留及
`be3f615` 授权人员游标/有界扫描/末次实时身份与父链复验，新增真 API 角色及分页回归；
迁移和 ACL 不变；该候选已取得独立验收记录，但不等于一期或生产放行。
同候选本地后端/边缘全量 `2482 passed, 1 skipped`（903.41 秒），退出码 0；
前后 Git 比较确认测试源码及工作流保持冻结。Web 691 项、小程序 516 项的源码未再改变。

## 按依赖推进

### P0：异常结果恢复

`0059–0061` PG16 全量迁移/并发/API 真链验收已通过；后续数据库变更仍须独立准确 SHA 门禁。
服务器明确拒绝与网络结果未知分开处理：只允许严格、可证明未落库的业务错误解除本次意图；
不能按所有 4xx、中文报错或 `not_observed` 统一清标。

后续“未执行封存”必须增加服务端持久证据并与原 POST 共用串行化边界：
原 POST 先提交则返回已提交事实；封存先提交则所有迟到 POST（含直接 SQL 绕过）均失败。
封存不得推进需求/任务版本、库存或十个状态轴；不能用 TTL 或多查几次代替提交级排他约束。
这是待实现方案，不是已具备能力。原 lifecycle v1 哨兵缺少人员/请求等锚点，不能自动猜测回填。

小程序非期初过账页面接线已在本地候选 `3cdbde6` 完成：独立确认、持久哨兵、一次 POST 和
command-status/detail 只读恢复均已接入，未知结果仍保持阻断；该客户端候选不改变服务端永久封存
未实现、PG16 门禁未验证和生产放行边界。

服务端前向基础候选 `edd8946`、`ade2676`、`7ff69da`、`5f406db`、`c99c842` 已新增
`stocktake_posting_command_outcomes` 只追加结果表、运行时 ACL 白名单、SQLite/PG 不可变更新删除
保护、`posted`/`sealed_not_executed` 绑定约束及迁移回滚非空阻断。它允许同一任务保留多个不同请求坐标，
避免把封存误做任务级唯一状态；尚未接入封存 API、原 POST 共享锁/提交级迟到阻断、历史 completion 精确
回填和 PG16 动态门禁，因此不能视为“确认未执行并永久封存”已完成。

验收至少覆盖：响应丢失、提交前中断、重复封存、并发原 POST、身份变化、权限撤销、
旧哨兵、页面卸载及本地存储失败。未决操作永不自动生成新请求坐标。

### P0：首批建账和盘点入口

已有后端 `POST /v1/stocktakes/opening`，PC 期初页面仍需补启动入口。
当前源码复核确认入口还依赖正式控制数据链：`integrations.py` 接收 `ExternalSyncSnapshot/*`，
只有工单投影器生成正式 `SyncRun/SyncBatch/SyncInboxEvent/ExternalObjectVersion`；尚未找到
库存 `oam_inventory_control` 对应的生产投影器或 control-run 选择接口。边缘快照 ID 不能直接
冒充正式控制 run，也不能造空批次当作零库存。接下来先补授权只读准备/选项目录及隔离投影适配。
初盘人员、个人仓叶子/父仓、资产 owner/物理 owner 组合须重用启动写侧资格，不能直接复用
普通盘点目录或把两种 owner 强制相等。资产 owner 与物理 owner 分属同区域树内不同组织已有
合法服务测试；省负责人未配置时仍保持空。数据新鲜度目标按 V1.0 4.3 为不高于 45 分钟；
当前启动尚无新鲜度准入；硬门禁及计时锚点仍需明确，不能把目标自行解释成已批准的 TTL。
批次分阶段复用策略仍需明确，不得擅自给 control_sync_run_id 加全局唯一。
启动前 task_id 未产生，需独立设计 region/task_no/actor/trace 恢复锚点，不能套用 count 哨兵。
分层只读 `start-options` 后端候选已实现并提交为 `b192ccfe7186e625d7080271c3a76e2b838914ab`：
regions → asset_owners → locations → assignees，每层严格坐标、上限与授权游标，
只公开已授权组织/位置和执行人必要标识。本地相关 317 项通过；准确 SHA 的 PG16
run `33949950302` 全绿：静态 `1736 passed, 1 skipped, 1 warning`（771.99 秒），动态
`1 passed, 1 warning`（175.60 秒），于 `2026-09-05T06:46:57Z` 完成。
这只证明本切片的隔离验收，不等于发布生产。独立契约见 `OPENING_START_PREPARATION_ACCEPTANCE.md`。
本地冻结后端/边缘全量 `2669 passed, 1 skipped`（867.52 秒），退出码 0；不能替代真库结果。
PC 候选 `0bbc43894d17b8d7258fdeba3b7a1c423575920f` 已接入只读准备面板，Web 全套 745 项及
类型/构建通过，准确 SHA 的客户端 run `33950143871` 全绿；不开放启动按钮。正式控制投影的
来源、覆盖和兼容缺口见 `OPENING_CONTROL_PROJECTION_NEXT_SLICE.md`，full-only 策略尚待用户确认。
小程序后继 `762150c2d4e6ca1337b00af5b7f01409528f30d9` 已接入同一准备流程，631 项通过；
准确 SHA 的客户端 run `33950490567` 全绿（Web 745、小程序 631、类型/构建通过）。
隐藏/卸载/刷新清理与原生 picker 占位映射已回归，不扩大发布配置，不视为微信真机验收。
所有成功响应仍明确 `start_ready=false`、`control_evidence_not_evaluated`，不返回 SyncRun、
库存量或启动凭据。资格目录成功不代表控制库存就绪，也不开放启动按钮。
已从期初写侧抽取不生成 scope UUID/hash、无新锁的资格 helper；写侧保留现有锁与最终复验。
同一 grant 必须覆盖区域/资产 owner/物理 owner，同时完整 principal 的明确 deny 仍生效。
仅个人仓要求叶子及精确当前保管人工程师；区域仓可有个人子仓。读侧末次复验身份、父链和
保管绑定，写时仍重新加锁检查。普通盘点目录只可借鉴分页模式，不能直接替代期初资格规则。
`31b1ae5` 已以任务/来源轮次/范围/版本/当前身份绑定的只读人员目录替换 PC 复盘手填 UUID。
返回真实 users.id，不能用 person_id 代替；候选必须通过既有写侧资格检查并在返回前重新验证。
分页只回显已授权人员的游标；内部超过 1000 条扫描上限整体 503，不返回跨区身份或不完整页。
POST 仍会锁后重验，目录成功不等于获准写入。PostgreSQL READ ONLY 分页已通过上述准确 SHA 门禁。
盘点意图的硬刷新/进程终止保护需单独补验，不能因供给恢复已实现而推定盘点同样具备。
期初盘点/对账错误和小程序页面已移除原始幂等键，核验提示仅保留非敏感追踪坐标，原始键
仅限原请求内存。Web 已将清标限制为首次直接 POST 的精确 action/status/category/code 白名单，
未知后后续拒绝仍保留；目标级调用租约覆盖前置读取、POST 和最终核验，避免迟到双击创建新坐标。
小程序已独立核验历史响应与当前投影：旧范围 pending、未封存、错轮次、旧 approved 版本或
终态冲突均保留意图/草稿并禁止再次 POST，只有同轮合法投影才解除。
`ff9611e` 已将小程序清标限制为首次直接 POST 的严格白名单，并以调用租约覆盖确认弹窗。
截至该历史提交仍缺两端身份绑定/跨刷新持久化和受控未执行封存；后续 count 切片如下。
`a3f0483` 已新增无业务写入的历史计数
command-status API；该查询会使用既有规范锁序，不得在 SQL READ ONLY 事务调用。
`not_observed` 不证明未执行；当前轮次与历史完成独立核验。后继轮处置场景已由 `ca1a45c`
修复，并已通过上述 `e54765a` 准确候选的真库门禁。
PC count 已接持久存储/历史核验、身份重验、单次发送与同源多标签任务写屏障；默认传输不会
401 自动重发该计数。损坏记录、未知结果、离页或权限变化保留标记；初盘旧草稿不进入新轮。
小程序持久计数恢复 `d6f166c5d2e95b281ca4e548dbfa66fdc30994dd` 已完成模块、页面及独立复核，
完整 516 项测试通过；只存最小
非敏感哨兵，确认弹窗在服务上下文租约内，计数 401 不刷新重发，历史 GET 也禁用自动刷新。
进程重启后仅查询历史，`not_observed`、身份变化、损坏存储或投影漂移均不清标、不重发。
服务上下文锁不是跨设备/原生进程锁；生产微信真机生命周期与存储行为仍需隔离 UAT。
PC 数量/重复维度前置校验修复 `e54765a10b4d465c6073df69e3fa97296ef90f93` 完成后，
最终 Web 691 项、TypeScript/构建通过。具体故障矩阵及真机未验边界见
`OPENING_COUNT_RECOVERY_ACCEPTANCE.md`；不能通过清存储或重装客户端解除未知结果。
过账、关闭、复核及对账自身的跨刷新意图恢复仍须单独实现，不能因 count 屏障已接而推定完成。

日常盘点不同于以上期初恢复：`976cdba` 新增三轮回归、`ee0d0ed` 修复只读表权限，
`2691ab3` 修正事务内封轮顺序；客户端持久恢复尚未接入。
0062已实现显式历史模式与专用 owner，当前受限真库门禁通过；已释放冻结、无关旧人员变化和附件锁序
各自的准确验证范围见 `DAILY_COUNT_HISTORY_TERMINAL_ACCEPTANCE.md`。
还须补SN/截止回放、多范围、尾部身份竞争与原POST锁序矩阵，再接两端日常持久恢复。

验收：角色/区域范围、范围快照确认、重复启动、复盘负责人筛选、盲盘不可泄漏应盘数量、
差异/复核/过账/对账/关闭独立状态及跨刷新恢复。

### P1：最小待办与业务通知

先呈现待盘点、待审批、供给计划和结果待核验，不建设完整 BI。
库存过账已原子写 outbox，不等于通知已送达；仍需正式通知展开、接收人快照、投递/尝试账本、
回执核验与可恢复 worker。登录短信及已购 1000 条套餐不能作为业务通知闭环证据。
外部供应商仅使用 fake/隔离适配器验证，正式联调需另有明确授权和外部条件。
现有 API 数据库角色不能推进通知投递和 Outbox 状态，需独立最小权限 worker 及前向迁移；
通知表的不可变证据、未知发送结果和可信回执约束尚需实现。
当前需求取消约束会将需求相关 NotificationEvent/Outbox 视为阻塞，首个通知切片应限于已有
盘点事件；未经前向兼容修复，不能直接给需求审批插入通知而改变其取消语义。
派生待办应复用各领域的实时 `allowed_actions`，不另建可编辑状态；没有审批 SLA 时不得编造超时。

验收：消息重复消费不重复发送、provider 接受与实际送达分离、超时/失败可追踪、
权限过滤、正文脱敏、库存和审批事务不依赖通知服务在线。

### P1：客户端构建工具链可重复性

当前 `frontend/Dockerfile` 已使用 `pnpm install --frozen-lockfile`，依赖解析版本和 integrity
由 `pnpm-lock.yaml` 锁住；不能把清单的 `latest` 直接误报为每次生产构建都会自动更新依赖。
但 Node `22-alpine`、Caddy `2.10-alpine` 为浮动镜像，未固定 patch/digest；本地本轮验证使用
Node 24.19.0 / pnpm 11.19.0，不能据此声称 Node 22 容器工具链已经验证。
PostgreSQL 工作流不负责 Web/小程序安装、测试或构建；客户端已有独立门禁，不能互相替代。

`2e188208d892e87cd46467bf6e1a4de455eb673a` 已把 12 项直接依赖声明固定到当前锁定版本，
并声明 `packageManager: pnpm@11.19.0`；lock 只改 specifier，所有解析版本、integrity 与包图不变。
独立临时目录离线 `--frozen-lockfile --ignore-scripts` 安装成功：111 包复用、0 下载。
复制已跟踪前端源码配置后，Node 24.19.0 / pnpm 11.19.0 的全套 691 项测试（16.30 秒）、
类型检查和构建通过。未复制原 node_modules、环境凭据，未访问外网；这是当前宿主架构验证，
不是 Docker Node 22/Alpine 构建证明。
`ae263fb10a149a434c04ea5cb46c2a3bd3a906b5` 已增加独立客户端门禁，准确 SHA 的 GitHub
run `33949308200` 成功：实际 Node 24.19.0 / pnpm 11.19.0、冻结安装、Web 691 项、
类型检查/构建、小程序 578 项全部通过。新增 62 项结构和安全边界回归；workflow/action
权限及固定提交、无路径跳过的触发条件见 `CLIENT_RELEASE_GATE.md`。
后续仍需固定经过核验的容器镜像摘要；不得编造 digest、随意更换底层镜像或顺便升级依赖。
容器复现、架构兼容、供应链检查及微信真机 UAT 仍需独立验收。

### P0：隔离 UAT 和发布前置

需要用户提供/确认稳定人员映射及首管理员开通清单、期初库存来源和授权范围、隔离测试人员、
KMS/RAM/文件存储与部署参数、短信实际产品及接口兼容、备份恢复和变更审批。
真实生产访问仍禁止；没有这些条件不妨碍本地实现，但不得宣称生产可用。
一期估算仍为 10–20 个有效开发日的保守范围，不包括等待外部条件的时间。

## 复用与后置

- 省负责人已有授权/撤权 API 和配置页面，保持默认空、由管理员手工配置，不重复开发。
- 替代料比例/申请人确认的完整写链、批量打印/导入导出、全面报表列为后续受控交付，
  一期界面必须明确限制原物料计划范围。
- 分配、占用、出库、发货、物流签收、OAM 收货、RSC/个人仓履约入库属于独立后续链；
  不得以“需求已批准”“供给参考号已登记”或“通知已发送”代替这些事实。

本轮新增的分配首片已落地为迁移 `20260908_0068`、只追加的 `stock_allocations`/
`stock_allocation_serials` 事实表、严格幂等 POST 和 Web 端货源候选读取。分配命令锁定
需求/身份/货源投影，校验审批版本、期初建账、余额版本、流水游标和 SN 绑定，并只推进
需求的 `allocation_status` 轴；代码明确不创建 reservation、库存流水、出库、发货或收货事实。
本轮继续补充了 `GET /v1/material-request-allocation-command-status` 只读核验端点：客户端
用原始 `X-Request-ID` 查询当前操作者的唯一分配审计，再重读 `stock_allocations` 与需求状态，
只有审计对象、分配事实、授权版本和状态轴全部一致才返回 `confirmed`；无审计时返回
`not_observed`，证据缺失或矛盾直接返回服务不可用，禁止客户端据此自动重发。该端点只提供
服务端恢复证据，尚未接入 Web/小程序分配写页面，也不替代 PostgreSQL 16 远端门禁和隔离 UAT。
本地迁移一致性 167 项、相关后端回归 352 项通过；尚未完成 PostgreSQL 16 远端门禁、
真实授权数据、跨客户端写页面和隔离 UAT，因此仍不能宣称生产可用。
后继提交 `17ea988` 修正历史迁移断言后，全后端复跑 `3082 passed, 1 skipped`；
Web `808 passed`、TypeScript 与生产构建通过（仅保留既有大包提示）。仓库安全检查通过，
该结果仍是本地源码回归，不代替准确 SHA 的远端门禁或实际业务验收。
小程序现有正式契约回归 `716 passed`；本轮没有把未具备持久恢复和写后详情重证的分配
POST 接入小程序，避免把候选读取误扩成不安全的履约写链。

完成标准是源码、测试、准确 SHA 的门禁和 UAT 证据共同成立，不以提交数、测试总数或
历史工程量百分比代替验收。
