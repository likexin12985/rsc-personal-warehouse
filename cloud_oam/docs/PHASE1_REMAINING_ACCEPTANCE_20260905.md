# 一期剩余交付与验收边界（2026-09-05）

本清单是 V1.0 下的一期交付切分，不替代设计基线，也不授权外部系统操作。
一期重点为盘点、需求提报/逐行审批及原物料供给计划处理。
依据当前源码复核；已存在的模块不重复建设，新增候选源码未通过准确 SHA 的 PostgreSQL 16
门禁前不计入发布完成。

## 当前候选版本

`d8866496d198eb0d15a6e73cc075e97eefcdac3b` 已建立 `0059` 原物料供给计划服务、
只读历史命令核验、PC/小程序界面及数据库因果约束。
GitHub PostgreSQL 16 run `33918927666` 静态 `1222 passed, 1 skipped`，动态因旧版本摘要断言
失败，不能引用旧 `0058` 绿色门禁为其背书。`0060` 数据库安全前向修复已提交为
`6e15548cd467c4c8d1dedf633c3ed4b7710a538b`。该冻结版本本地完整后端与边缘回归
`2258 passed, 1 skipped`（824.70 秒）；本地最新 Web 为 425 项、小程序为 216 项通过。
该版本 GitHub run `33920379101` 静态步骤通过，动态在启动安全清单检查失败：
清单漏登记真实存在的 `trg_audit_events_supply_causality_0059`，已准确补齐，另修复运行时函数族
选择器遗漏；未放宽集合检查。后续两次 GitHub 候选在动态前主动取消，不计通过。
独立 PG18 辅助执行发现并修复测试新需求 ID 派生及应用多余行锁问题，随后发现历史 `0059`
供给审计键的 JSON 提取缺少括号；当前新增 `0061` 前向迁移修复并保留函数身份/权限/触发器。
准确提交与每轮证据见供给发布清单；PG18 不替代 PG16。首轮运行期间变更 fixture 所产生的
17 项失败已被上述冻结全套覆盖，该旧全套计数也不能冒充当前 `0061` 的测试结果。

## 按依赖推进

### P0：0059–0061 验收和异常结果恢复

先完成本次 PG16 全量迁移/并发/API 真链验收，修复测试或实现缺陷，再推进后续数据库变更。
服务器明确拒绝与网络结果未知分开处理：只允许严格、可证明未落库的业务错误解除本次意图；
不能按所有 4xx、中文报错或 `not_observed` 统一清标。

后续“未执行封存”必须增加服务端持久证据并与原 POST 共用串行化边界：
原 POST 先提交则返回已提交事实；封存先提交则所有迟到 POST（含直接 SQL 绕过）均失败。
封存不得推进需求/任务版本、库存或十个状态轴；不能用 TTL 或多查几次代替提交级排他约束。
这是待实现方案，不是已具备能力。原 lifecycle v1 哨兵缺少人员/请求等锚点，不能自动猜测回填。

验收至少覆盖：响应丢失、提交前中断、重复封存、并发原 POST、身份变化、权限撤销、
旧哨兵、页面卸载及本地存储失败。未决操作永不自动生成新请求坐标。

### P0：首批建账和盘点入口

已有后端 `POST /v1/stocktakes/opening`，PC 期初页面仍需补启动入口；复盘人员选择不能依赖
人工填写 UUID。复用正式、授权过滤的主数据目录和现有版本/范围封印，不新增旁路写接口。
盘点意图的硬刷新/进程终止保护需单独补验，不能因供给恢复已实现而推定盘点同样具备。
当前期初盘点及期初对账的交接错误仍把原始幂等键放入错误文本/公开属性，需移除并增加
错误序列化与页面显示不泄漏的回归；核验提示只保留非敏感追踪坐标，原始键仅限原请求内存。

验收：角色/区域范围、范围快照确认、重复启动、复盘负责人筛选、盲盘不可泄漏应盘数量、
差异/复核/过账/对账/关闭独立状态及跨刷新恢复。

### P1：最小待办与业务通知

先呈现待盘点、待审批、供给计划和结果待核验，不建设完整 BI。
库存过账已原子写 outbox，不等于通知已送达；仍需正式通知展开、接收人快照、投递/尝试账本、
回执核验与可恢复 worker。登录短信及已购 1000 条套餐不能作为业务通知闭环证据。
外部供应商仅使用 fake/隔离适配器验证，正式联调需另有明确授权和外部条件。
现有 API 数据库角色不能推进通知投递和 Outbox 状态，需独立最小权限 worker 及前向迁移；
通知表的不可变证据、未知发送结果和可信回执约束尚需实现。
当前需求取消约束会将需求相关 NotificationEvent/Outbox 视为阻塞，首个通知切片应限于已有
盘点事件；未经前向兼容修复，不能直接给需求审批插入通知而改变其取消语义。
派生待办应复用各领域的实时 `allowed_actions`，不另建可编辑状态；没有审批 SLA 时不得编造超时。

验收：消息重复消费不重复发送、provider 接受与实际送达分离、超时/失败可追踪、
权限过滤、正文脱敏、库存和审批事务不依赖通知服务在线。

### P0：隔离 UAT 和发布前置

需要用户提供/确认稳定人员映射及首管理员开通清单、期初库存来源和授权范围、隔离测试人员、
KMS/RAM/文件存储与部署参数、短信实际产品及接口兼容、备份恢复和变更审批。
真实生产访问仍禁止；没有这些条件不妨碍本地实现，但不得宣称生产可用。
一期估算仍为 10–20 个有效开发日的保守范围，不包括等待外部条件的时间。

## 复用与后置

- 省负责人已有授权/撤权 API 和配置页面，保持默认空、由管理员手工配置，不重复开发。
- 替代料比例/申请人确认的完整写链、批量打印/导入导出、全面报表列为后续受控交付，
  一期界面必须明确限制原物料计划范围。
- 分配、占用、出库、发货、物流签收、OAM 收货、RSC/个人仓履约入库属于独立后续链；
  不得以“需求已批准”“供给参考号已登记”或“通知已发送”代替这些事实。

完成标准是源码、测试、准确 SHA 的门禁和 UAT 证据共同成立，不以提交数、测试总数或
历史工程量百分比代替验收。
