# 正式拣货确认验收

本批沿用 V1.0 基线，在 `codex/production-readiness-gates` 增加正式拣货确认。
迁移 head 为 `20260911_0071`；发布过的 0069、0070 迁移保持原样。

## 已实现

- 按原占用分批拣货，数量守恒为 `原占用 = 已释放 + 已拣货 + 仍占用`。
  候选 SN 必须属于本笔原占用、未释放、未拣货，且仍处于原占用账户。
- 必须使用预置且唯一匹配的拣货账户；所有权、保管人、位置、物料、成色和批次均不变。
  候选展示批次、成色、原占用、释放、已拣货和可拣货数量，不猜测或自动创建库存账户。
- 确认时追加出库单、出库行、拣货事实与 SN 绑定，通过正式库存内核执行 `reserved → picking`。
  库存流水、余额/SN 投影、需求命令、版本、状态事件、审计及 Outbox 同事务提交或回滚。
- 原占用和分配不重写，也不重新开放已消耗分配额度。共享占用池扣除其他需求已拣货的份额，
  避免把正常移出的库存误报为不足；余额必须覆盖所有剩余原占用。
- 需求部分拣货保持 `pending_pick`；每个当前批准明细均拣齐后才进入 `picked`。
  出库单已拣货但 `outbound_at` 为空、`outbound_qty = 0`；发运及之后状态不变。
- 网页支持数量、原 SN 选择和备注；持久保存原请求后仅发送一次 POST。
  网络结果不明时保留恢复记录，暂停需求其他写入；按原追踪坐标只读核验，不自动重发。
  历史回读验证原身份和当前权限，以及原命令、出库行、库存流水、审计和状态事件。
- 0071 为新表配置最小 SELECT/INSERT 权限、始终启用的不可变保护、延迟事实关系校验、
  精确运行时函数哈希和触发器目录。空图可逐级降级再升级；存在拣货事实时拒绝降级。

## 接口

- `GET /api/v1/material-requests/{id}/reservation-pick-options?request_line_id={line_id}`
- `POST /api/v1/material-requests/{id}/reservation-picks`
- `GET /api/v1/material-request-reservation-pick-command-status`，携带原 `X-Request-ID`

POST 必须携带需求版本、原占用、数量、源余额版本与流水游标、SN、备注及独立幂等头。
所有响应禁用缓存。候选读取重新核验身份、范围、期初、投影和历史证据，并限制记录数量。

## 验证

当前代码提交：`c76ed4edbee304409bac58735cdc7a944a5a32c0`。
[Client 门禁 34205609779](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34205609779)
已通过：Web `976 passed`，小程序及门禁契约 `724 passed`，类型、构建和仓库安全检查通过。
[PG16 门禁 34205609743](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34205609743)
迁移锁竞争与历史迁移回归通过后，在正式拣货阶段发现 `outbound_status` 缺少列级更新授权。
已补齐 0071 的单列授权及空图降级撤权，并将权限加入精确运行时清单；真库同时验证缺权
和越权授权 `shipment_status` 均拒绝启动。修复提交的完整 PG16 结果待重新执行后补录。

本地迁移、数据库安全目录、API 和拣货回归曾完成 `553 passed`；
网页全量 `976 passed`（含拣货 32 项），类型和生产构建通过。
后续针对出库号、HTTP 恢复和共享池计算的检查另行执行，不与上述重叠项重复累计。

- 拣货服务、候选、历史篡改和 HTTP 幂等/恢复专项最终为 `32 passed`。
- OAM 投影目录、迁移、拣货和旧准备读取关联回归为 `95 passed, 1 skipped`；
  跳过项是普通数量物料不适用 SN 位置异常测试。
- 实际浏览器使用隔离合成数据验证了候选表格、批次/成色、SN 少选拦截和成功后的
  拣货单/出库单号展示；临时文件、标签页及本地服务均已清理，没有真实业务调用。

真实 PG16 门禁已增加普通数量与 SN 的正式拣货、先释放后拣货、拆分拣货、并发争抢、
幂等回读、审计失败整笔回滚、不可变事实保护、目录损坏启动拒绝及非空降级阻塞。
版本锁顺序修复后，本地迁移及 0071 专项 `170 passed`；真库结果按上述准确提交单独核验。
首轮 `aa0c69d` 的客户端门禁通过；PG16 在历史 0040→head 回归阶段遇到版本表死锁。
服务端日志确认另一参与者为 `autovacuum: VACUUM ANALYZE public.alembic_version`，
尚未进入新增拣货动态用例。迁移入口改为在任何版本写入前取得版本表排他锁，
并新增真库锁竞争检查，验证等待维护锁期间未持有任何写入关系锁；不关闭自动清理或重写历史迁移。
竞争检查使用实际持锁模式，不以 `backend_xid` 是否为空推断写入：PG16 会在申请排他锁时
为备库 WAL 预先分配事务号，见 [PG16 锁实现](https://github.com/postgres/postgres/blob/REL_16_STABLE/src/backend/storage/lmgr/lock.c)。
保留全部既有门禁检查，将工作流超时从 30 分钟调整至 40 分钟，容纳新增真库检查及运行波动。
既有主包体积提示仍在，当前主包约 849 kB（gzip 221 kB）。

## 后续边界

实物出库过账、发运及分包、物流签收、OAM 收货证据、分批收货与个人仓入库仍待独立实现。
拣货启动后的释放/退拣、取消及重新分配需要独立补偿命令；当前禁止直接释放已进入拣货流程
的需求。旧履约准备清单仍用于拣货前，已开始拣货的需求使用专用拣货候选和原命令核验。

下一批实物出库必须逐笔绑定本批出库行及拣货事实，限制累计出库量和 SN 不超过已拣货余量，
通过独立命令将 `picking` 转入明确绑定的在途账户并保留资产总量。发运累计不得超过已出库量，
登记运单不能再次扣减库存。0071 当前将出库单/行固定为拣货时快照；后续若增加出库投影更新，
必须由前向迁移和独立不可变出库事实证明，并同步调整旧拣货命令的历史核验，不能移除保护后直接改数。
验收必须覆盖同一出库行分批过账、重复/并发命令、跨原单 SN、失败整笔回滚，
以及后续出库发生后原拣货请求仍可只读恢复。

小程序履约入口、预生产迁移/恢复演练、真实身份与附件 UAT、备案及生产放行继续分别推进。
本批未部署生产，也未写入 OAM、RSC、Workflow 或飞书等外部业务系统。
