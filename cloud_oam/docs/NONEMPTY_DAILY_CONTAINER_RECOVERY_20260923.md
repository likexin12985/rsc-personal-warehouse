# 非空日终事实的当前容器联合恢复

2026-09-23。使用当前候选静态清单的 1,091 个输入和准确 API 镜像
`rsc-formal-api-current:20260923-daily-ops-65d43a2e4251`，在隔离环境执行。
[源库回执](../artifacts/current-container-source-20260923-nonempty-daily-attempt5/result.json)
为 `passed`：全新 PostgreSQL 16.15 实例升级至 Alembic `20261113_0134`，实际日终命令
生成 1 条不可变映射决议及 3 条截止快照。源库为 212 表、746 行；4 个可用附件对象的
字节数及 SHA-256 与目录一致，另有 1 个待上传意图。源 PG 已停止。为使新增合成证据文件
具备真实对象字节，仅在隔离测试夹具中将占位 `a` 哈希替换为该文件实际 SHA-256；
未修改冻结的业务源码或迁移。

[容器联合恢复终态](../artifacts/current-container-recovery-20260923-nonempty-daily-attempt4/result.json)
为 `passed`，真实 Compose、私有 Docker、当前 API 镜像和两个独立的新 PostgreSQL 16
实例共同参与。正常备份退出 0，实际数据库阻塞期限退出 124，取消退出 143，父 worker
终止退出 -9，损坏对象退出 1 且被拒绝。五轮完成后均无遗留数据库会话、关系锁、临时
输出或 worker，正常服务容器身份不变。对象包完成 4 个可用对象的 8 次合成 SDK
HEAD/GET 调用，备份验证后恢复至独立新库。源与恢复库逐表行数、内容 SHA-256 一致：
均为 212 表、746 行；1 条映射决议和 3 条截止快照在恢复库中保留。两库实例标识分别为
`7688613307177312271` 和 `7688614077476306958`。八角色、七个 LOGIN 的新密码、
错误/旧密码拒绝及 API 运行身份均核验；最终容器、私有引擎和 VM 全部停止，清理错误为空。
逐场景、恢复库和原始日志保存在同目录 `guest-evidence.tar`。

前三个容器尝试的失败证据均保留：首轮缺少新增合成对象的模拟元数据；第二轮模拟 ETag
与原上传完成回执不符，被正式完整性校验拒绝；第三轮五场景和恢复检查已通过，但汇总
夹具仍假定 3 个附件/6 次请求。第四轮将夹具按目录数量校验后取得终态通过。正式业务
校验、容器镜像及冻结源码始终未改。

这是**本地合成恢复证据**。六张日终表中，映射决议和截止快照两张非空；绑定、消费、
审核事件和请求封存四张仍为空。真实私有 OSS、目标 Linux、KMS、实际业务数据、生产
RPO≤5 分钟/RTO≤2 小时及连续三天对账尚未验收。完整静态第三片、GitHub PG16
runtime/Client 和真实试点门禁仍需单独放行；本批未提交、推送或部署。

后续已补[六张非空日终表的联合恢复](FULL_DAILY_CONTAINER_RECOVERY_20260923.md)，
在另一个合成源库/恢复库上逐表验证 212 表 / 788 行及全部六张非空日终表；
本页的两张非空样本仍作为独立历史证据保留。
