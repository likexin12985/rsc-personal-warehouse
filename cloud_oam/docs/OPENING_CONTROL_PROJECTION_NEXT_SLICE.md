# 期初控制库存投影：下一切片的证据与待确认边界

## 2026-09-20 / 7.71 当前接续

最新 7.71 已补原启动请求永久终结：新增 0128 不可变事实、当前授权与账本锁竞争、
COMMIT 相斥保护，以及 Web/小程序显式确认和双重只读恢复。原生 PostgreSQL **16.15**
验证真实 JWT、启动先到/终结先到、撤权等待、提交时到期、绕过应用校验仍拒绝及降级保留；
52 张受检表事实保持。后端 **442 passed**，迁移/ORM **3 passed**，最终门禁契约 **27 passed**；
Web 全量 **1,239 passed**，小程序 **1,070 passed**，类型与双构建通过，测试组有交集。
当前迁移 HEAD 为 0128；启动开关仍 false，真实公开目录仍 pending/0，正式发布检查拒绝。
当前候选完整 GitHub PG16/客户端 CI、真实来源、多角色真机、三天对账、通知供应商与回调、
PITR 和灰度验收仍未完成。待批提交例外不变，未提交、推送或部署。
详见[原请求终结与当前上线缺口](OPENING_START_SEALS_20260920.md)。

## 7.70 历史记录

最新 7.70 已接网页和小程序私有期初启动表单：选择发布批次、多范围/执行人、盲盘与冻结方式；
首次发送前持久保存非敏感请求坐标，双重只读回查及任务详情核验后才清理。页面切换、
未知结果、身份变化和跨页面竞争均保留原请求，不能换键重发；同人新授权须重新验权。
网页全量 **1,226 passed**，小程序全量 **1,067 passed**，后端接口/恢复 **14 passed**；
类型、共享协议生成校验、公开与 `/xx` 构建、静态入口和仓库安全通过。正式公开发布
仍因真实知识目录 **pending/0** 被拒绝；小程序打包排除全部新增私有工具文件。
本批 127 个迁移、后端生产服务、部署与 Edge 未改；沿用且核验 7.69 原生 PG16 证据，
未重跑完整 GitHub PG16 门禁。启动开关仍为 false，个人主体小程序仍只注册知识页。
下一批优先补未知启动结果的服务端终结证明，再做当前候选版完整门禁、真机与真实数据验收。
未提交、推送或部署；待批的提交例外保持原状，不能把本地通过视为上线完成。
详见[客户端启动与持久恢复交接](OPENING_START_CLIENT_20260920.md)。

### 7.69 历史记录

最新 7.69 已补实际第二轮复盘：真实 JWT 选发布批次启动，首轮 2 件，换人复盘 3 件，
经两级复核只过账 3 件；原轮证据保持。实际 PostgreSQL **16.15** 已验证三次同键锁竞争、
跨区域指派及非本轮人员拒绝、撤权等待后拒绝，以及独立对账不改库存后关单。
聚焦两组各 **72 passed**，后续本地隔离/拓扑/清单 **27 passed**（有交集，2 组含依赖警告）。
最终真库的 0127 空库/有数据往返、51 张受检表事实保留及 API/Edge 权限通过；三个临时
集群均已停止。127 个迁移、生产服务与客户端未改，未提交或部署。
下一批接客户端启动契约、持久保存待核验请求与只读恢复；当前准备区仍只读，完整 GitHub/
客户端门禁及真实上线验收未完成。公开知识目录仍 pending/0，星星入口保持 `/xx`。
详见[实际复盘、并发权限证据与下一步](OPENING_RECOUNT_RUNTIME_20260920.md)。

### 7.68 历史记录

7.68 当时已将 0051 历史盘点样本与当前 ORM/业务服务分离，使用可追溯的冻结合成
事实；不再临时添加后来字段。原生 PostgreSQL **16.15** 已通过 0051→0052→0127、
0051→0127 单次升级，以及策略历史被修改时拒绝升级并完整保留数据/数据库目录。
历史任务未知授权版本保持 NULL；127 个迁移、生产业务代码、部署与客户端均未改。
聚焦 **89 passed**，0052 定向迁移 **8 passed**，最后样本/CLI **20 passed**（有交集）。
生成和验证使用的 13 个自有临时集群均已停止，最终源码与日志已保存；未提交或部署。
两项已知门禁样本依赖已修复，仍须继续完整实盘/复盘/权限并发链及 GitHub 完整门禁。
页面启动保持关闭，真实目录 pending/0，当前完整客户端门禁及真实上线验收仍未完成。
详见[历史迁移样本与最终验证](OPENING_LEGACY_FIXTURE_20260920.md)。

### 7.67 历史记录

7.67 当时 已把完整 PG16 门禁的当前期初样本接入真实签名采集、授权和审核发布链，
保留一条明确零数量控制行及独立 SN 策略。真库同时发现并以 0127 前向迁移修复独立对账
事件变量与列名歧义；实盘 +1 过账、重试不重复、未对账禁止关单、独立对账后关闭均通过。
迁移/权限/ORM **334 passed**，发布与对账服务聚焦 **207 passed，1 warning**，两组有交集。
原生 PostgreSQL **16.15** 最终专项、空库和有数据迁移往返、API/Edge 权限及事实保留通过。
前 126 个迁移与客户端文件未改，8 个临时集群已停止；未提交、推送或部署。
下一批修复 0051 历史样本与当前 ORM/服务耦合，再推进完整 GitHub 门禁和页面恢复链。
页面启动仍关闭，真实目录 pending/0，完整门禁与真实上线验收仍未完成。
详见[正式发布样本、对账修复与接续缺口](OPENING_RELEASE_FIXTURES_20260920.md)。

### 7.66 历史记录

7.66 当时 已保存新期初任务的不可变操作者授权版本，0126 在任务插入及 COMMIT 用数据库
实时时钟复核当前身份、区域权限与拒绝规则。角色在事务内到期、定时 deny 生效均已
在真实 PG16 验证整批回滚；真实 JWT 的 HTTP 提交拒绝返回明确 403。旧任务不补写
未知授权版本；已有新证据时降级拒绝，API/Edge 权限边界保持。
最终聚焦 **567 passed，1 warning**；ORM/迁移 **2 passed**，迁移服务 **3 passed**，
原生 PostgreSQL **16.15** 最终专项通过。前 125 个迁移与客户端文件未改，三个集群已关闭。
下一步优先修复完整 GitHub 门禁的正式采集/发布样本及旧 0051 历史造数，再接页面启动
和持久化恢复。页面启动仍关闭，真实知识目录 pending/0，完整门禁和上线验收未完成；
未提交、推送或部署。
详见[提交时操作者权限与后续门禁](OPENING_ACTOR_COMMIT_20260920.md)。

### 7.65 历史记录

7.65 当时已新增服务端按发布批次启动、旧 raw 入口相同准入和只读结果查询。0125 对新任务
插入及 COMMIT 执行来源/映射/物料/文件与新鲜度校验，API 私表权限继续隔离，历史版本
锚点保持不变。真实 PG16 已验证非零启动、旧行篡改拒绝、提交过期回滚、零明细同请求
并发及真实 JWT 结果恢复。最终联合回归 **726 passed，1 warning，2681.63 秒**；
原生 PostgreSQL **16.15** 最终专项通过，准确源码和日志清单已随本批证据保存。
下一批优先补操作者授权在 COMMIT 实时时刻的独立复核，覆盖事务执行期间角色到期。
完整 GitHub 门禁的旧样本仍创建未发布控制快照，必须补正式采集/授权/发布样本；不能
为过门禁放松新准入。页面启动仍关闭，真实目录仍 pending/0，未提交或部署。
详见[新启动准入、只读恢复和门禁样本缺口](OPENING_PUBLICATION_ADMISSION_20260920.md)。

### 7.64 历史记录

7.64 当时已接后台按区域查看已发布控制批次，新增 0124 受限只读函数和区域游标索引。
普通 API 只取摘要，四张发布私表继续隔离；身份/授权变化、跨区域和全局 deny 均拒绝。
网页全量 **1,188 项通过**，公开与个人仓构建通过；原生 PostgreSQL 16.15 的真实角色、
分页/历史/零明细、私表边界、迁移往返及篡改拒绝已验证，最终聚焦 **390 项**和离线/迁移 **6 项**通过。
探索性联合的三项失败已定位并复验，原失败日志保留；统计有重叠，不累加。
这只是批次查询，服务端启动准入和旧入口同步约束尚未完成，页面明确不能启动。
公开真实知识目录仍 pending/0，发布检查因此拒绝。详见[本批实现与剩余启动链](OPENING_CONTROL_DIRECTORY_20260920.md)。

下一批直接接服务端发布绑定、当前准入和原结果恢复；当前函数仅供摘要查询，不扩展成 raw 表接口。

### 7.63 交接与尚未完成的启动验收


发布与新启动的交错锁问题已真实复现并修复，0123 保留退回相关验证，移除无关事件的
账本依赖；原生 PG16 实际等待和拒绝/回滚通过。详见[并发修复与下一步](CONTROL_START_CONCURRENCY_20260920.md)。
下一批接受限批次选择和服务端新启动准入，准确绑定发布 ID/摘要/控制行、采集时间及
当前授权。普通 API 不能读四张私有发布表，旧 `/opening` 入口必须同步约束；历史回读
不增加当前有效期门槛。本批联合 812 项、通知/退回补充回归 130 项通过；准确结果以
本批封存证据为准。

下一切片的验收至少包括：

- 全国管理员和准确区域管理员可选各自范围批次；越权区域、停用身份、授权版本变化均拒绝，
  不以空结果掩盖不可用或权限失败。
- 前端提交批次 ID 和摘要，服务端重建完整控制行；伪造、缺行、混批或陈旧摘要全部拒绝。
- 等待锁期间发生发布替换、授权撤销、映射撤销、附件失效或到期，新任务整批回滚。
- 有证明的零库存仍能启动；来源失败/不完整/未发布不能转成零库存。
- 请求回执未知时按原坐标只读恢复；历史已提交任务在新批次发布、原批次到期后仍准确回读。
- 新数据库能力只暴露必要字段，实际 API 角色仍不能读原始发布私有表；真实 PG16 覆盖权限、
  新旧入口一致约束、异角色发布/启动竞争及同请求并发。

## 2026-09-20 / 7.62 历史接续

已通过 0122 修复已证明零库存的实际启动，真实 API 角色并发提交只产生一个任务，后续
发布后的原任务回读通过；见[本批启动证据和锁顺序缺口](ZERO_CONTROL_OPENING_20260920.md)。
前端批次选择、服务端新启动的有效期/授权绑定仍未实现。下一批先验证并解决 publisher
与新启动的锁顺序差异，然后接受限批次选择；不得用普通空批次或 completed_at 冒充覆盖/
采集证明。以下各节按其日期保留。

## 2026-09-20 / 7.61 历史接续

独立控制 publisher 已新增 0121 私有事实、实际原子投影、版本关闭及原结果恢复，
原生 PG16 四次完整/增量/零行/重现发布与既有 opening 消费者通过。
当前 `opening_start_options` 仅有区域/归属/库位/人员选项；下一步是受限控制批次选择、
启动计划绑定和并发/恢复验证，不再重复实现发布前核查。详情及证据边界见
[独立控制发布](INVENTORY_CONTROL_PUBLICATION_20260920.md)。以下保留早期设计分析。

> 2026-09-20 / 7.60：已增加当前总部会话的发布前核查，逐行定位实际暂存记录与已审核物料版本；
> 增量保留原指针，修复映射到期未计入正规化期限的问题。772 项联合及原生 PG16 七场景通过。
> 核查仅 inspect/rollback；独立发布事实、原子写入和启动/恢复仍缺。见[本批实现与证据](INVENTORY_CONTROL_PROJECTION_PLAN_20260920.md)。

> 2026-09-20 / 7.59：非零控制整链原生 PG16、组合锁、实际角色及 API/Edge 启动校验通过，最终联合 753 项通过。
> 已修复快照原文/摘要不一致及 Edge 0119 触发器目录遗漏；head 0120，无新运行权限。
> 独立控制发布器、期初启动/恢复、完整当前候选门禁和真实验收仍缺。
> 见[本批真库证据与接续范围](INVENTORY_CONTROL_PG16_INTEGRATION_20260920.md)。以下保留历史记录。

## 7.59 后独立发布器的实施边界

本节是当前代码复核和下一切片计划，尚未实现正式控制发布。历史段落中的“来源、映射、
正规化尚缺”应按其记录时间理解；当前这三段已实现且具本地 PG16 证据。

已存在的 [0112 准备事实](../backend/app/inventory_control_models.py)分别保存来源绑定、
目录版本、采集链、逐段暂存引用和准备清单；[离线发布契约](../backend/app/inventory_control_publication.py)
仅封存四份摘要。[当前正规化](../backend/app/inventory_control_normalization.py)虽已重验
库存准入、批准映射和逐 SKU 物料证明，返回值仍是一次持锁观察，不能保存响应后直接发布。

[期初启动消费者](../backend/app/formal_services/opening_stocktake.py)要求准确
`oam_inventory_control:region:{region_org_id}` 范围、完成的 SyncRun、已应用 SyncBatch、
完整 SyncInboxEvent 集及逐行外部版本。其 `opening_control_projection_payload` 固定为
external_business_key、region_org_id、material_id、condition_code、control_qty、
mapping_status、mapping_note 七字段。新增审核与来源证据应由独立发布事实和关系保存，
不能静默改变历史控制 payload，也不能替换历史任务的 cutoff/manifest 或追加实时过期判断。

下一切片按以下依赖推进；新模型和权限必须走前向迁移，既有 120 个迁移保持：

1. 定义独立不可变发布事实及逐行来源关系，准确引用 preparation、来源/目录批准、映射决定、
   当前物料来源版本和审核摘要。传输原文摘要、覆盖/正规化摘要、正式控制 manifest 分开。
   准备和正式发布继续分离，不能仅把 `projection_published` 标志改为 true。
2. 先以 owner 当前总部会话边界实现预览/执行/原结果读取，发布事务重建并持锁核验全部证据，
   检查当前权限与最早证据截止时间。审核后变化、缺失 SKU、跨来源、撤销或过期整批拒绝。
   未来常驻发布角色须另立准确范围的 RLS/ACL，不扩大旧工单 projector 的工单范围函数。
3. 在同一原子事务写发布事实、逐行关系、正式 SyncRun/Batch/Inbox/外部版本及独立审计。
   以明确业务范围和命令幂等键约束重复执行；COMMIT 回执未知时只精确回读，禁止盲目重放。
   已有物料和库存账本保持不变，控制发布不能触发期初启动、入库或通知送达。
4. 以本批真实 PG16 合成链验证权限拒绝、并发重复发布、失败全回滚、版本历史、增量删除及
   可证明零库存。然后再接批次选择、启动计划、启动执行与持久恢复，分别记录其事实。

完整和可重建增量均已有准入，不在发布器中擅自改成只允许完整采集。当前准备准入已硬性
检查 45 分钟及当前证据有效期；未来发布/启动各自还需明确截止时间与原结果恢复规则。
同一控制批次跨范围分阶段建账的产品决策仍保留在历史待确认项，不据此改写已完成任务。
以上结构、失败路径及合成数据验证可以继续；真实来源口径、完整目录和上线验收仍独立处理。

## 7.58 及此前审计记录

> 2026-09-20 / 7.58：当前物料来源证明已接入，正规化输出升级 v2；有证据时逐 SKU 验证，缺失时整省候选阻断。
> 完整/增量/零库存 SQLite 整链及当前物料证明的原生 PG16 角色/锁专项通过，非零控制整链 PG16、publisher 和完整门禁仍待完成。
> 见[本批证据和接续边界](MATERIAL_CURRENT_SOURCE_PROOF_20260920.md)。以下保留对应历史版本行为。

> 2026-09-20 / 7.57：已补来源附件正式用途与 0120，原生 PostgreSQL 16 的物料专项及启动权限验证通过；
> 控制正规化的主数据来源证明仍 false，独立 publisher/期初启动和完整门禁待完成。
> 见[当前真库证据和剩余边界](PG16_NATIVE_SOURCE_EVIDENCE_20260920.md)。

> 2026-09-20 / 7.56：0119 已实现认证物料回执的逐行审核发布、不可变来源版本和当前物料投影；
> 旧采集/身份冲突/单位策略变化拒绝，保留历史与真实未知来源时间。真实 PG16、真实语义、
> 库存控制 publisher 和期初启动仍待完成，见[物料审核发布](MATERIAL_PUBLICATION_20260920.md)。

> 2026-09-20 / 7.55：0118、正式目录 2.0 和双端已支持真实未知来源时间，不补造时间；
> 正式物料版本/投影仍待接通，见[来源时间契约及迁移](MATERIAL_SOURCE_TIME_20260920.md)。

> 2026-09-20 / 7.54：已补物料来源总部批准/撤销、当前会话 CLI 及持锁采集准入，新增 0117。
> 真实来源版本与正式物料投影仍缺，来源准入不等于发布许可；见[物料来源授权](MATERIAL_SOURCE_AUTHORITY_20260920.md)。

> 2026-09-20 / 7.53：物料专用认证接收、传输登记、不可变回执和原结果恢复已有本地证据。
> 尚缺来源业务授权与正式版本/物料投影；回执不是发布资格，见[物料接收](MATERIAL_CAPTURE_INGRESS_20260920.md)。

> 2026-09-20 / 7.52：正式映射批准/撤销保留，另有物料两轮采集与可重验观察文件；尚无认证接收或正式主数据投影。
> 仍须主数据来源证明、真实语义、密钥生命周期和 publisher/RLS；映射批准及审核候选不是发布许可。
> 见[正式映射版本](INVENTORY_CONTROL_MAPPINGS_20260920.md)。
> 见[数量正规化审核](INVENTORY_CONTROL_NORMALIZATION_20260920.md)。
> 见[当前授权核验及限制](INVENTORY_CONTROL_ADMISSION_20260920.md)，以下保留历史审计背景。

> 2026-09-20 / 7.48：新增 0114 隔离 HMAC 声明接收、不可变凭据及逐段准备链观察，本地联合
> 902 项通过。配置渠道认证不代替有效来源/目录授权或发布；部署密钥生命周期、授权组合、
> 正规化、publisher 和真实 PG16 仍待完成。见[最新凭据验证](INVENTORY_CONTROL_ATTESTATION_20260920.md)。

> 2026-09-20 / 7.47：已有契约接入严格本地分页采集、私有证据归档、增量链与真实 HMAC 接收测试。
> 尚无可信采集认证或发布资格，见[最新采集验证及限制](INVENTORY_CONTROL_CAPTURE_20260920.md)。

> 2026-09-20 / 7.46：已补私有 PC 核查与用途受限的双向签名文件交接，API 不持有 owner 连接。
> 自动传输、真实采集及发布仍待完成，见[PC 核查与签名交接](INVENTORY_CONTROL_HANDOFF_20260920.md)。

> 2026-09-20 / 7.45：已接入当前网页登录校验的 owner 运维配置命令；PC 配置页面与受限命令传递、可信采集和发布仍未完成。
> 见[配置入口与验证](INVENTORY_CONTROL_CONFIGURATION_20260920.md)，本地 635 项通过不代表真实 PG16 或生产授权。

> 2026-09-20 / 7.44：0113 已增加独立版本授权/撤销领域服务，正式配置入口和可信采集尚未完成。
> 以[最新授权验证](INVENTORY_CONTROL_AUTHORITY_20260920.md)为当前进度；历史审计背景保留。

> 2026-09-20 / 7.43 接续：离线契约外已新增 0112 五类不可变准备事实和准确 inbox 链接；
> 来源/目录授权、可信采集与正式发布仍未完成。当前实现和证据以[最新准备验证](INVENTORY_CONTROL_PREPARATION_20260920.md)为准，下文保留原审计/设计背景。

2026-09-05 只读核查，依据 V1.0 与当前源码；不是生产接入、数据库授权或迁移实施记录。
当前只读准备目录始终不宣称控制库存就绪。正式控制投影仍未完成。

## 先证明覆盖范围，不能只看同步成功

V1.0 第 0.3 节要求 OAM 省级余额作为控制总数，第 1.5/4.3 节同时允许完整快照与增量批次。
所以省级控制集完整性是必须证明的，`SyncRun.mode=full` 却不是唯一合法实现，也不能单独证明
省级覆盖。完整的增量重建投影可能满足要求；一期主动限制为完整省级快照须由用户确认。

现有 `edge_sync/oam_edge_sync.py` 的 scope 只有 `all` 或 `warehouse:<code>`。
`--entity inventory` 虽读取仓库列表用于遍历，却只上传 inventory，不上传 warehouse 实体。
因此记录数/hash 正确只能证明该传输批次；零库存仓库不会从库存行显现，零条记录也不能
直接解释成目标省份零库存。下一切片需要明确的来源、区域与预期仓库/位置覆盖集合，
不得凭全国行政表、名称猜测或仅凭现有库存行补造完整范围。

## 来源和权限不能照搬工单投影

- `backend/app/formal_services/opening_stocktake.py` 的 `_prepare_control_evidence` 和
  `inventory_posting.py` 的 `_validate_opening_control_source` 接受既有 `code=oam` 来源。
- `oam_work_order_projection.py` 的工单发布器及配置 schema 则精确限定 `starcharge_oam`。
- `0044` 接收 scope 包含 inventory，但 projector_read/write 的 RLS 并未开放正式库存控制发布；
  不能把允许 edge 接收等同于允许发布，也不能追加任意配置字段或重命名历史来源绕过限制。
- 未发现独立 `InventoryControlSnapshot` ORM 类；已有 `StocktakeControlSnapshotLine` 绑定盘点
  任务，不能冒充新同步的独立来源账本。

正式发布需要独立的来源绑定、最小权限、RLS 和安全清单前向方案，并保留旧 `oam` 历史证据。
禁止修改历史迁移或为库存发布扩大既有工单投影权限。

## 新建准入与历史回放分开

新启动在锁前/锁后分别调用 `_prepare_control_evidence`；启动重放和盘点计数则以
`historical_at=task.control_snapshot_at` 重证冻结图。终态过账还有独立来源 validator。
数据库 `rsc_opening_start_graph_complete_0052` 的 FALSE/TRUE 调用同时覆盖新建闭包和历史验证，
启动审计、outbox 和初轮闭包也调用该函数；不能只改 task INSERT 一处。

现有 projector UPDATE 列权限不包含 `sync_runs.mode`，0044 同时绑定 staged snapshot 的 mode，
但 mode 不是任务或控制 manifest 中封存的历史字段。当前字段值不能被当作启动时 mode 的
独立不可变证明。任何新增 full-only 或新鲜度策略只作用于明确的新准入，不追溯打坏既有合法
盘点的计数、重放、复核和过账，也不能通过改写旧 mode 冒充兼容。

## 建议的最小实现顺序

1. 维持 V1.0 的全量/增量边界；分批建账复用及额外硬准入在正式启动放行前确认，
   不阻塞隔离证据校验和默认不开放权限的前向方案设计。
2. 已新增仅使用隔离假数据的来源、范围与覆盖一致性校验器，不发布正式 SyncRun，不改库存或
   开启启动。契约、反例测试和验证状态见 `INVENTORY_CONTROL_EVIDENCE_ACCEPTANCE.md`。
   它不认证来源或目录，现有 edge 也还没有提供新的显式分页证据；不能把一致性结果当成就绪。
3. 覆盖证明与来源绑定稳定后，设计前向迁移、受控发布器、控制批次选择及独立真库验收。

## 纯校验之后的最小发布依赖

不能把 `evidence_consistent` 或其四项 False 改成 True 就接到启动服务。还缺三类持久事实：

- 获授权的来源桥接与完整目录版本：将 staging 来源绑定到正式控制来源、region_org_id，
  保存目录审批/生效/撤销依据，不重命名旧 `oam`，不接受请求方自填目录作为已认证材料。
- 不可变采集/增量重建闭包：关联认证暂存 snapshot、逐仓分页、目录版本与前驱完整集。
  会被后续同步替换的 ExternalSyncCurrentRecord 不能单独作为历史证明。
- 控制发布及逐行来源关系：封存解析/映射版本、数量口径及来源行到规范化控制事件的关系。
  transport 原始摘要、目录/采集覆盖摘要、opening 控制 manifest 摘要是三种不同证据，
  不得塞成一个 `SyncRun.manifest_sha256`。固定七字段的历史控制 payload 也不能静默扩写。

建议依赖：最小前向事实模型（默认无运行权限）→独立 publisher 的精确 RLS/ACL/封存约束→
可信证据 loader 与独立数量/SKU/成色/单位/SN 解析→原子幂等发布及真库攻击/回放验收→
批次选择、启动计划及持久恢复。0044 的 formal scope 函数硬编码 `oam-work-order-scope:`，
相关 run helper 也只支持工单；只增加一行 binding 或扩大 ACL 并不能建立库存控制发布链。
任务内 `StocktakeControlSnapshotLine` 仍不是独立控制来源账本。

前四步的结构与拒绝路径可继续用假数据开发。正式启用仍需权威来源/公司/组织/仓位/区域
绑定材料、稳定审批身份和数量映射口径。负数/冻结量/单位等不可猜测；未解析项不能清零。
真实采集、实际数据接入和上线均需另行授权，不能借此绕过当前禁止访问生产的边界。

校验器至少测试错来源、错公司/组织、跨区域、缺仓、重复仓、缺位置、空库存且覆盖未知、
完整且可证明的零库存覆盖、增量重建的覆盖证明及过旧采集。输入一个“完整”布尔标记不能
替代来源清单和独立覆盖核验。失败不得产生正式投影、库存余额或启动凭据。

待用户确认：一期是否只接受完整省级快照、同一控制批次能否用于不同范围的分阶段建账。
V1.0 的不高于 45 分钟是新鲜度目标，是否作为硬准入、采用采集还是投影时点、准备计划 TTL
仍须明确；不得给历史回放追加实时过期判断。确认数据规则不等于授权连接任何生产系统。
