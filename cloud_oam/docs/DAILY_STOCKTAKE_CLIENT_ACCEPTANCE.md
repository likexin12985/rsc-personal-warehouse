# 日常盘点分页、标识录入与页面生命周期验收

日期：2026-09-05。遵循根 AGENTS.md 和 V1.0；本切片只改 Web、小程序和合成测试。
不修改后端、数据库迁移、ACL、边缘采集、部署或业务状态定义，不访问生产系统。

## 本切片交付

- 两端日常盘点任务列表支持加载更多，第 51 项不再因固定第一页而无入口。
  Web 另提供已加载任务号筛选，并明确不代表全库搜索；刷新后可继续读取后续页。
- 服务端 `next_after_id` 是下一页首个未见 UUID，后续查询包含该 UUID；不是上页末项。
  页内及跨页重复、倒退、非递增游标、空页带游标、超过 50 项均失败关闭。
  若下一页首项已不再可见，允许返回更大的 UUID，不要求首项与游标相等。
- 列表读取前后复验身份/授权，刷新、切 adapter、隐藏、卸载使过期读取失效。
  写操作与加载更多互斥；旧写回调不能污染新页面、强制跳转或清理新请求的忙状态。
- Web 支持手工/扫码枪来源，以及 SKU、物料二维码、SN 文本、SN 二维码和不确定类型。
  小程序相机扫描使用 `unknown + scan`，手填使用 SKU/SN 文本和 `manual`。
  不在客户端解释标签内容或生成 material_id/serial_id；服务端原有精确解析与范围检查不变。
- 空 SN 对应 raw/type 双 null；带 SN 时本地先要求每行数量恰为 `1.000`。
  小程序扫码回调重验身份、权限、任务版本、轮次、范围和页面生命周期，手改标签撤销扫码来源。

审批、分配、占用、出库、发货、物流签收、OAM 收货、个人仓入库、通知送达和对账同步
仍独立。本切片不推进任何履约轴，不使期初启动准备变为 `start_ready=true`。

## 2026-09-06 Web 非期初计数/复核持久恢复续验收

提交 `83f2223d8fa5031718cb5b8250c3b2796d4667fd` 已通过
[Client release gate 34025174245](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34025174245)。
Web 全套为 `37` 个测试文件、`795` 项通过，TypeScript、Vite 构建、小程序 `670` 项和仓库
安全检查均通过。

本轮把 Web `submit_initial_count` / `submit_recount_count` 以及 `review_region` /
`review_headquarters` 接入独立持久恢复模块。哨兵只保留 task、round、scope/阶段、轮次、
人员、授权版本和 trace；POST 前写后读，持久化后最多一次 POST，之后只允许只读查询对应历史命令。
历史未观察、身份/权限变化、详情责任链或版本不一致、存储故障和跨页面锁不可用均不清理哨兵、
不重放、不换键，并阻断新写。计数和复核的状态轴没有合并，过账、对账、关闭及供给履约仍不受
该模块推进。

该提交只改客户端，未新增迁移或后端逻辑；PG16 证据沿用未变更后端的
[34021817686](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34021817686)，
不能把 Client 绿灯解释为新的 PG16 运行。小程序非期初计数的持久恢复已存在基础模块但
过账/复核接线仍待下一切片；服务端“确认未执行并永久封存”、期初可信控制投影和启动前恢复
锚点仍是独立 P0。

## 已验证与复现

使用仓库现有 Node 24.19.0 工具链、冻结依赖，未升级依赖：

```sh
cd cloud_oam/frontend
pnpm test
pnpm exec tsc -b
pnpm exec vite build
```

从仓库根目录运行：

```sh
node --test cloud_oam/miniprogram/tests/*.test.js
bash cloud_oam/scripts/verify_repository_safety.sh
git diff --check
```

- 本地 Web 全套 776 项；TypeScript 通过；Vite 构建通过，仍有单包超过 500 kB 的体积提示。
- 本地小程序全套 647 项。
- 相比上一客户端基线新增 Web 31 项、小程序 16 项。
- 独立源码复核覆盖延期列表/详情/写回调、权限变化、分页乱序、扫码及 SN 数量。
- 隐藏后保留的原意图在刷新后仍显示待核实并阻断新动作；不能借新动作隐式重发原请求。
  只有专用重试入口且当前明确允许原坐标重放时才可重试；刷新本身不产生该许可。
- 功能提交 `a78d5adae54024aed9f5f8cb5724b94e4ec32de1` 的 GitHub 客户端
  [run 33956352741](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/33956352741)
  已回读全绿，含冻结安装、Web/小程序测试、类型、构建及清理。后继文档提交不改变该功能源码。

## 本切片不包含的验收

- 未运行微信真机相机/存储/前后台生命周期验收，Web 扫码枪也需真实设备验证。
- 未运行最终 Docker/Caddy 产物与 OSS、短信、微信登录联调。
- 本次未重跑 PostgreSQL 动态门禁；后端/迁移/边缘/PG 工作流与已验收基线不变。
- 页面生命周期防护不是跨进程持久化恢复（这是 `a78d5ad` 历史切片的边界）。当前
  `83f2223` 已为 Web 日常计数和两阶段复核接入独立 command-status GET 与持久哨兵；
  小程序及其他非计数动作仍需分别验收。已有期初 count 恢复接口仍限定 opening，不能直接挪用。
- 请求未知时不能因超时、刷新、`not_observed` 或换账号而认定未执行、清除屏障或换键重发。
  本切片保留现有未决意图，不声称解决所有历史未知写的恢复和人工封存。

## 下一开发切片

1. 将小程序非期初过账/区域复核/总部复核接入独立的持久恢复和 command-status 只读核验。
2. 设计并实现服务端提交级“确认未执行并永久封存”协议；封存与原 POST 共用串行化边界，
   不推进任务、库存或任何其他状态轴。
3. 检查现有 completion/audit 是否足以唯一证明原命令；不足时新增前向迁移，
   不能猜测回填 trace 或改写历史迁移。`not_observed` 仍为未知。
4. 两端发送前保存非敏感恢复哨兵；不保存幂等键、令牌、原始请求、数量、备注、
   标签原文或附件签名 URL。存储失败不发写请求；重启只先核验原命令。
5. 创建、下发、复核、过账、关闭分别补证明，不把 scope count 恢复泛化为全部动作完成。
6. OSS 精确 CSP/CORS、审批转派、通知兼容、可信控制投影、发布产物与恢复演练仍独立推进。
