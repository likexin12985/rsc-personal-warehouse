# 7.98 完整静态结果与配置 CLI 迁移版本修复

2026-09-21。完整静态会话 **9496 已退出 1**：210 个文件，**4,960 passed / 1 failed /
3 skipped / 1 warning**，pytest 用时 13,354.32 秒。1,364 个非 Markdown 输入在整个运行
期间保持原摘要，监督器确认无修改、新增或删除；源码冻结已经结束。

唯一失败为 `test_cli_head_pin_tracks_alembic_graph`。正式迁移已到 `20261108_0129`，
[配置 CLI](../scripts/configure_inventory_control.py)仍固定 `20261107_0128`，会拒绝正确升级的
数据库。本批只把 REQUIRED_HEAD 更新为 0129；没有自动迁移、扩大权限或改动业务写入。

[固定证据](../artifacts/control-cli-head-20260921/verification.json) SHA256：
`f18173c3b3e13c129ecfbf71e36b45e3b2c8cf7469ea310df822b6934d4131a9`。
[原完整结果](../artifacts/static-release-candidate-20260921-target-guard-fix/result.json)、
[准确一行修改](../artifacts/control-cli-head-20260921/change.diff)、
[聚焦原日志](../artifacts/control-cli-head-20260921/focused.log)均保留。

## 当前修复的验证

- `83939` 退出 0：直接针对正式 CLI 的现有测试 **13 passed / 45 deselected / 1 warning**，
  26.30 秒。包含迁移图匹配、凭证不回显、显式数据库配置、预览/执行/原请求恢复及拒绝错误目标。
- `69302` 退出 0：一个全新自有 PG16 实例，从已核验的合成完整备份恢复后执行 **6 个场景**。
  当前 owner、数据库和 0129 接受；0128、多 HEAD、无 HEAD、错误库名和 API 角色均拒绝。
  版本异常在测试事务中模拟并回滚，不代表实际执行了降级迁移。
- 实际核对锁等待 5 秒、语句及事务空闲期限 30 秒。206 张表前后摘要一致，实例已停止。
- 原生检查先验证准确的一行修正候选，完整静态结束后才写入正式源码；最终确认候选与正式
  CLI 逐字节相同，当前相对完整运行的源码差异只有这一文件。

证据校验脚本首次错误预期 52 项 deselected；按真实终端的 45 项更正，保留原校验失败记录。
没有为这个计数错误重跑已经通过的测试。

## 下一步与证据边界

当前没有活跃完整静态进程；不得继续等待或重启旧会话 9496。B3 进程期限及之前的候选
证据保留其原受检版本；本批差异不涉及它们的生产依赖文件。

继续按[备份正式接入清单](OSS_BACKUP_COMPOSE_20260921.md)整合入口、共享纯附件校验、
worker 与 Compose，再接日终映射/截止的正式迁移、安全目录、状态投影和区域权限。
整合后的准确候选仍需重新运行完整静态及 GitHub PG16/Client 门禁。

原完整测试仍是失败记录，本批聚焦和局部原生 PG16 不能替代当前候选完整发布门禁。
未提交、推送或部署。公开 CI 提交例外、原 Edge 恢复答复及真实数据验收边界保持，
见[续开发交接](CONTINUE_DEVELOPMENT.md)。
