# 历史静态门禁记录（7.73—7.75）

**本文件仅保留历史。2026-09-22 当前运行已转到[8.21 完整目录门禁](STATIC_RELEASE_GATE_20260922.md)：会话 6070 / PID 68895 正在验证修复后候选，收集 6,710 项、冻结 1,472 输入；旧 52984 已受控结束，不得复用。以下 58287、7406、26717 等旧进程均已结束，不得继续等待或按历史指令重复启动。**

## 7.75 当前接续：修复后完整静态回归运行中

备份角色并发修复已落地，当前聚焦 **32 passed**；全新 PG16 恢复 **207 表 / 546 行**
完整摘要一致，五种拒绝场景通过。原生反例与候选的三个临时实例确认停止；
[修复、证据与边界](BACKUP_ROLE_CONCURRENCY_20260921.md)已记录。

**当前完整回归会话 `58287` / 测试 PID `74295` / 监督 PID `74102`**，受检 209 个文件，
先前清单 4,944 项；尚无完整通过结论。目录为
`cloud_oam/artifacts/static-release-candidate-20260921-role-fix/`，核对 `process.json`、
`static.log` 和最终 `result.json`。运行期间冻结 1,361 个非 Markdown 文件，保留本会话，
不要重复启动。旧会话 `7406` 和 `26717` 均已结束，历史“运行中”段落不再适用。

最新周额度核验剩余 **39%**；用户要求剩余 10% 时停止、汇报整体进度并完成交接。
完整 GitHub 门禁、真实 Docker/OSS、真实来源/通知/设备、持续对账和生产灰度仍缺。
公开目录 pending/0、星星入口 `/xx`、私有小程序排除和关闭的功能开关保持；公开 CI
提交例外和 Edge 恢复答复仍待处理，未提交、推送或部署。

以下均为历史记录；进程状态以本节和最新状态 JSON/原始终端结果为准。

## 7.75 最新状态：角色并发备份修复已落地

已复现并修复角色成员变更导致“备份返回成功但 2 行变 0 行”的窗口；正常完整应用备份
恢复 **207 表 / 546 行**一致，五种新增拒绝路径通过，三个全新 PG16 实例均已停止。
详见[备份角色并发修复与证据](BACKUP_ROLE_CONCURRENCY_20260921.md)。

**会话 `7406` / 测试 PID `56890` / 监督 PID `56870` 已确认结束，退出 2。**
595 passed、1 skipped、1 warning，源码稳定；因已确认的新备份缺陷主动停止以落地修复，
不能算作全量通过。旧 `26717` 也已结束。修复后的新完整回归尚待启动，接手须先核验
最新状态 JSON 和终端结果，不能按历史段落重复启动。

额度剩余 10% 时停止、汇报并完成交接；最近周额度核验剩余 40%。完整 GitHub 门禁、
真实 Docker/OSS、真实来源/通知/设备、持续对账和生产验收仍缺，未提交、推送或部署。

以下段落保留发现时的历史状态；运行中句柄若与本节冲突，以本节及最新状态 JSON 为准。

## 7.75 当前接续：新候选完整静态回归运行中

备份相关改动现在会在仅变更 `scripts/backup.sh` 或 `deployment/backup/**` 时触发
PG16 门禁；pull_request 和已有 push 范围均已补齐，拓扑聚焦 **2 passed**。
当前完整静态回归已启动：**会话 `7406`、测试 PID `56890`、监督 PID `56870`**，
209 个文件，前次收集为 4,944 项。当前还没有完整通过结论；保留这一会话，运行期间
冻结 1,361 个非 Markdown 源文件，不能重复启动或继续等待已退出的旧会话 `26717`。

准确命令、源码摘要与原始日志位于 `cloud_oam/artifacts/static-release-candidate-20260921/`，
终端状态以 `result.json` 和原会话返回值为准。本次只改 CI 路径及其契约断言，7.74 的
备份运行代码、数据库迁移及客户端保持一致；[备份修复证据](BACKUP_PRODUCTION_ENTRY_20260921.md)
仍可按其明确范围使用，不能替代当前全量或 GitHub 门禁。

用户要求额度剩余 10% 时停止、汇报整体进度并完成交接；最近核验周额度剩余 **40%**。
公开目录 pending/0、正式 OSS、真实通知/设备、三天对账及生产灰度仍未验收；公开 CI
提交例外和 Edge 恢复答复仍待用户处理。未提交、推送或部署。

## 7.74 当前状态

原会话 `26717` / PID `85216` 已确认退出 2，3,332 passed、2 failed、3 skipped，
原 1,352 个源码摘要保持。因两项已复现失败和待落地修复而主动中断，未全量通过。
0109/0127 历史断言现已修正；备份入口及部署契约同步修复，当前聚焦 **34 passed**。
新 CI 清单 **209 文件 / 4,944 项收集成功**，尚未完整执行。没有另一套长回归在后台运行。
实际 PG16 和容器客户端范围见[7.74 交接](BACKUP_PRODUCTION_ENTRY_20260921.md)。

以下内容保留 7.73 原始证据及中断前的信息，旧句柄不再是当前运行指令。

工作树和分支不变，保留全部未提交改动；本批没有修改生产服务、数据库迁移、权限、
客户端或部署配置，没有提交、推送、部署或外部业务写入。

## 已处理问题

按 GitHub `static_safety` 相同清单运行本地回归，发现两项过时的清单断言：当前函数清单
已经扩展到 23 个，旧测试仍要求 16 个；当前 23 个触发器也不再只有 0044 的原始 3 个。

现在保留 0044 原始函数/触发器和 0082 收货扩展的历史校验，明确列举新增函数；触发器
从 0114、0116、0119、0121 实际迁移推导属性，并固定允许的表和函数范围。仍要求完整
集合相等且没有重复，没有改成“至少若干个”或放宽生产运行时校验。

静态 CI 另补入 6 个遗漏的测试文件：0037 取消迁移、退回入账的 HTTP/小程序契约、完整
事实链、0111 证明迁移、按验收单查询入账状态，以及盘点安全过账服务。拓扑测试还要求
`test_stock_return_inbound*.py` 全组进入静态 CI，避免以后新增文件再次遗漏。

## 已取得的结果

- 7.72 的 1,428 个源码摘要在开始时全部复核一致。
- 第一套 202 文件回归发现上述 2 项失败，在 572 项通过后主动中断进行修复，退出码 2；
  它不是完整通过。原日志与准确终端结果保留。
- 首次聚焦修复又发现历史常量应对应 0082，而非 0081；该错误已改，失败日志保留。
- 最终守卫清单及 CI 拓扑聚焦：**16 passed，24.81 秒**。
- 扩大的迁移聚焦前次为 39 passed / 1 failed，失败就是上述历史常量；不得报告为全绿。

证据目录：`cloud_oam/artifacts/static-release-candidate-20260920/`。

### 2026-09-21 当前候选客户端完整回归

已按当前客户端 CI 清单在本机 Node 24.19.0 / pnpm 11.19.0 运行：冻结依赖安装、
Web 全量 **75 文件 / 1,241 项通过**、类型检查、公开及 `/xx` 双构建、静态入口检查、
小程序全量 **1,070 项通过（0 失败、0 跳过）**、共享启动协议生成检查。8 个步骤均退出 0，
监督进程已终止并记录结果。安装未启用生命周期脚本，未部署。

正式目录发布检查另行执行，准确以 `PUBLIC_CATALOG_NOT_READY` 拒绝；这是当前真实目录
pending/0 的预期结果，不能称为正式发布门禁通过。本次在 macOS ARM64 本地执行，
不替代 Ubuntu GitHub Actions 的远端运行。受检的 1,352 个非 Markdown 文件集合及摘要
均保持不变；重新构建的 21 个文件与已通过 Caddy 容器检查的输入逐字节相同。

证据目录：`cloud_oam/artifacts/client-release-candidate-20260921/`，包括准确命令、各步骤
日志、`client-result.json` 和 `build-manifest.json`。共享协议检查出现 Node 的
`stripTypeScriptTypes` 实验特性警告，私有构建也有超过 500 kB 的分块大小提示；两者均
退出 0。分块大小留待性能验收核对，不能以本次构建通过替代真实设备加载或 500 用户压测。

## 7.73 中断前的整套验证记录

2026-09-21 新发现 2 项历史清单断言失败，均已单独复现：
`test_notification_person_targets.py` 的 0109 用例，以及
`test_reconciliation_event_binding_migration.py` 的 0127 用例，均把旧迁移哈希与当前 0128
清单比较。各自历史清单匹配，候选补丁已保存并通过 `git apply --check`；受检源码冻结，
未应用补丁，也没有将单独复现结果算成通过。原运行仍继续收集剩余结果。

第二次运行选择修改后 CI 中的全部 **208 个文件**，收集到 **4,930 个测试，1 个模块跳过**。
跳过原因是发布仓库故意不包含本地 `work/inventory_query_portal/oam_read_client.py`，
不是跳过 PG16 后把结果算作实际数据库验证。

- 工具会话句柄：`26717`；pytest PID：`85216`。两者都必须实时复核，不能只看本文件。
- 命令：`.venv/bin/python -m pytest -v --tb=short --maxfail=5 --durations=20`，后接
  当前静态 CI 的 208 个准确路径；完整参数在 `static-second-command.json`。
- 工作目录：`cloud_oam`，`PYTHONPATH=backend`。本地 Python 3.12.14、pytest 9.1.1、
  pglast 7.18、httpx 0.28.1；GitHub 固定 Python 3.12.13，因此本地结果不冒充远端结果。
- 日志：`static-second.log`。本次交接时正在历史迁移测试，尚未取得终端结果。
- 监督进程完成后写 `static-result-second.json`，包含退出码、终端确认、源码漂移检查。
  `static-second-source-manifest.json` 保存开始时所有非 Markdown 候选源码摘要。

接手时先等待原句柄或核对原 PID 和命令。观察超时不代表停止，禁止因此重跑；只有取得
明确终端结果或确认句柄/进程已消失后才处理后续动作。运行期间不要改动受检代码。
若发现失败，按日志修复后再取完整结果；若运行通过，仍需记录全部失败数、跳过原因和
源码稳定性，才能更新本文件及当前状态，不能以日志末尾若干 PASSED 推断全套通过。

## 仍未关闭的上线门槛

2026-09-21 实际发现当前 `backup.sh` 的只读角色预检通过，但默认 `pg_dump` 因 RLS
拒绝导出 `audit_logs`。完整读取策略预检＋RLS 参数的本地候选已完成联合恢复，生产修复
仍待原回归结束后落地；不得因本地候选通过把现行备份脚本标为通过。正式 OSS 也不在
现有上传目录归档中，见[新阻断及联合恢复证据](DATABASE_OBJECTS_RESTORE_20260921.md)。

后续[策略并发检查](BACKUP_RLS_CONCURRENCY_20260921.md)已通过：实际证明两种缺行反例，
并验证保护锁后新建快照的候选路径、DDL 排队超时拒绝、普通 DML 不混入旧快照，以及
206 张应用表加 2 张合成测试表的 547 行完整恢复。生产备份脚本仍未改，不能据此关闭 B5。

2026-09-21 补充：官方 Caddy 2.10.2 Linux ARM64 容器的 **16 项入口路由检查通过**，
镜像层及平台清单摘要已核对，23 个测试输入保持一致，临时虚拟机已停止。
生产配置与源码未变，公开目录仍 pending/0；该结果不等于正式镜像、生产 TLS 或公网验收。
详细范围、失败记录和证据见 [Caddy 容器入口验证](CADDY_CONTAINER_ACCEPTANCE_20260921.md)。

2026-09-21 另完成一次全新隔离 PG16.15 的本地物理备份与 WAL 指定点恢复：206 张表、
544 行事实摘要一致，恢复后 API/Edge 权限通过，两个临时实例停止。该演练未运行完整
GitHub runtime gate，也未覆盖生产规模、非空 SN 流转、附件或生产 RPO/RTO；详见
[本地恢复演练](PITR_LOCAL_DRILL_20260921.md)。

随后用另一套全新集群补入非空 SN 流转：206 张表/560 行一致，恢复点后的释放被排除，
余额/SN 重建、59 条审计事件哈希链和恢复后原幂等键重试通过；损坏备份以退出 1 拒绝。
实例均已停止，[增强验证](PITR_SERIAL_RESTORE_20260921.md)仍不替代生产 RPO/RTO 与附件恢复。

当前整套回归是本地静态/SQLite/语法/契约测试，没有执行受保护的完整 PG16 runtime gate，
没有伪造 GitHub 环境或连接现有数据库。128 个迁移及生产运行时保持，7.71 的本地实际
PG16.15 证据保留。当前候选远端 PG16/客户端完整 CI、真实数据与设备/角色 UAT、生产
迁移/PITR、500 用户负载、连续三天对账、通知真实供应商与回调仍未齐。

公开知识目录仍 pending/0，首页星星按钮仍跳转 `/xx`；原 Edge 单次恢复授权仍未收到
答复。本批没有执行恢复或重新判定认证。原待批候选提交例外继续保留。
