# 当前镜像日终运维容器实跑

2026-09-23，范围是冻结输入摘要 `b2f07f16c03bde985b83727c37126f2a4aa6057d8b49a6c7c035bf7209368592`
对应的本地候选镜像 `rsc-formal-api-current:20260923-daily-ops-65d43a2e4251`。
最终证据在 `artifacts/current-container-recovery-20260923-daily-cli-attempt4/result.json`、
`input-manifest.json` 和 `guest-evidence.tar`。这是合成数据与隔离 Linux VM 的实际
Docker/Compose/PG16 执行，不是目标机或真实业务环境验收。

实际运维容器以 UID 65532、无网络、只读根目录、全部 capability 丢弃运行。映射状态通过
继承的私有 FD 3 与隐藏 TTY 输入各精确回读同一条决议；截止恢复通过私有 FD 精确回读
原截止。错误审核摘要和所有人连接文件权限过宽均返回 `daily_ops_rejected`，未改变事实。
短期合成 access token 没有进入命令行、环境、持久产物或命令日志，证据包不含运维临时
目录及运行时密码文件。此次只读回读没有生成新的决议、截止、审计或库存流水。

同一次完整链路还验证正常备份退出 0、数据库期限退出 124、取消退出 143、父 worker
强制终止退出 -9、损坏对象拒绝退出 1；每场景后剩余会话和关系锁均为 0。第二个新建
PG16 实例恢复了 212 表 / 788 行，六张日终表分别有 1/3/2/7/7/1 行，逐表事实 hash
一致；两实例均停止、私有引擎与 VM 均停止。镜像输入和冻结源码在运行前后保持一致。

失败尝试原样保留：`...-attempt1/` 的非特权容器无法穿过合成 PG socket 的 0700
权限；`...-attempt2/` 仅把隔离 socket 调整为可穿越，FD 正反向与完整恢复通过；
`...-attempt3/` 增加隐藏 TTY 后通过 CLI，但父 worker 故障夹具的 20 秒注入屏障超时，
整轮失败且完成资源清理；`...-attempt4/` 将夹具屏障改为 45 秒后完整通过。
上述调整只涉及忽略目录中的合成夹具，未改变产品源码或数据库授权规则。

剩余边界：目标 Linux 的真实 Compose、KMS/私有 OSS、短信与通知渠道、真实人员/OAM 来源、
期初和连续三天对账尚未验证；完整静态第三片与 GitHub PG16 runtime/Client 门禁未终端通过。
这些未完成项仍阻止提交和上线。
