# RSC 个人仓开发交接

**交接日期：** 2026-09-19；最新接续更新 2026-09-21（8.14，额度停止）

**工作树：** `/Users/replace-with-local-user/.codex/worktrees/06f6/oam`

**分支：** `codex/notification-delivery-worker`

**当前提交基线：** `f713999`；后续源码仍在本工作树，未提交。

**交接证据版本：** 以本文件最新接续、当前工作树和对应 artifacts 源码清单为准；历史提交不代表这些未提交改动已通过发布门禁。

**工作树状态：** `f713999` 之后新增公开知识首页、`/xx/` 个人仓入口及库存事实通知覆盖，当前未提交；实时状态以 `git status --short --branch` 为准。

**产品基线：** [`RSC个人仓与物资运营扩展系统_正式生产版需求与架构设计_V1.0.md`](../../docs/RSC个人仓与物资运营扩展系统_正式生产版需求与架构设计_V1.0.md)

## 当前接续任务

唯一当前工作树、活跃回归、额度停止条件和上线缺口统一维护于[续开发入口](CONTINUE_DEVELOPMENT.md)。
7.75 已修复备份角色并发缺行，32 项聚焦通过，实际 PG16 恢复 207 表 / 546 行一致；
五种新增拒绝路径和三实例停止均已验证，见[备份角色并发交接](BACKUP_ROLE_CONCURRENCY_20260921.md)。
7.77 已落地 0129 通知展开最小列权限、受控恢复与取消竞争修复，见[专项交接](NOTIFICATION_EXPANSION_STATUS_20260921.md)。
8.14 已新增可复用日终 PG16 夹具与审核门禁模块。停止线前启动的 59233 已退出 0：8 个实际场景、7 条审核事件和 1 份终结证明核验，既有库存数量保持，自有实例已停止。模块尚未接入 GitHub runtime，CI 空库路径及完整门禁仍缺；额度已到剩余 10%，停止新增开发，见[整体进度与停止交接](PRODUCTION_HANDOFF_QUOTA_STOP_20260921.md)。

8.13 已完成当前网页、真实 API 与全新本地 PG16 的六项局部联验：正常开启、提示按档案隔离、提交后断线恢复、未送达请求保留、明确终结及外区隔离。数据库为 2 条开启事件、1 份终结证明，库存与截止不变；修复提示串档案问题。59 项聚焦、完整前端 1,346 项、类型和私有构建通过，两轮实例及服务器均停止。附件到总部审核的浏览器全流程、完整门禁和真实资料仍缺，见[浏览器联验证据](DAILY_REVIEW_BROWSER_20260921.md)。

8.12 已接入私有日终开启/逐项解释/专用证据/总部审核与退回页面，并补浏览器跨刷新恢复、对象锁和明确终结确认。110 项聚焦、80 文件共 1,345 项完整前端、TypeScript 和双构建通过；公开产物隔离及星星 `/xx` 路径保持。后端与 0134 逐字节保持 8.11，没有新增 PG 实例。真实浏览器/API 联合验收、当前完整门禁及真实资料仍缺，见[网页操作证据](DAILY_REVIEW_CLIENT_20260921.md)。

8.11 已完成日终原请求安全坐标恢复与服务端永久终结证明，数据库 HEAD 升为 0134。462 项后端聚焦、13 项 CLI、83 个本地 PG16 场景和六类期初完整流程通过，三个自有实例均停止。并发原提交/终结、迟到 SQL 拒绝、到期回滚、缺失/伪造/重复审计及丢失回执恢复已验证；库存和截止事实保持。前端本批未改，浏览器持久恢复与写操作页面仍缺，见[本批证据](DAILY_REVIEW_RECOVERY_20260921.md)及[页面接续](DAILY_REVIEW_UI_NEXT_SLICE.md)。

8.10 已接入私有日终查询页面与受同源保护的 Cookie 命令入口，数据库 HEAD 保持 0133。前端完整 1,293 项、后端聚焦 77 项、公开/私有双构建及 61 个本地 PG16 场景通过；两次自有实例均停止，原失败保留。查询与审核状态分开；跨刷新恢复及写操作页面仍缺，见[网页接入证据](DAILY_REVIEW_WEB_20260921.md)和[下一批依赖](DAILY_REVIEW_UI_NEXT_SLICE.md)。

8.09 已接入日终审核 HTTP/原请求恢复、专用附件和真实审核状态/历史查询，HEAD 升为 0133。518 项当前聚焦、13 项 CLI 检查、61 个本地 PG16 场景、六个期初完整流程及旧附件历史升级保护通过；5 个自有实例均停止。该批尚缺的只读页面已由 8.10 补齐；写操作页面、当前完整门禁和真实来源/OSS 验收仍缺，见[接口接入证据](DAILY_REVIEW_HTTP_20260921.md)及[页面接续](DAILY_REVIEW_UI_NEXT_SLICE.md)。

8.08 正式日终审核数据库与服务层已集成，Alembic HEAD 升为 0132。407 项聚焦、13 项 CLI 检查、53 个本地 PG16 场景及六个实际期初完整流程通过；权限种子、目录漂移、非空回退拒绝、12 条审计回执均核验，8 个自有实例均停止。该批尚缺的 HTTP、专用附件和查询状态已由 8.09 补齐，页面仍待实现，见[正式接入证据](DAILY_REVIEW_FORMAL_20260921.md)。

8.07 日终审核持久化候选已通过 47 个本地 PG16 场景（23 个新增专项）。4 份合成对账提交 12 条命令，完整审计前缀、独立审核、并发、到期回滚及非空回退拒绝已核验；库存与截止档案保持，六个实例均停止。该批尚未接入正式 0132；8.08 已完成数据库与服务层，8.09 已完成 HTTP/附件/查询，页面仍待完成，见[持久化候选证据](DAILY_REVIEW_PERSISTENCE_20260921.md)。

8.06 已补日终逐项解释、定向退回补证、独立审核及原请求回执恢复核心。132 项聚焦和 35 个本地 PG16 场景通过，其中 11 项为新增核心验证；两个自有实例均停止。正式写端、权限种子与 0132 迁移尚未接入，内存审核结果不代表数据库已审核；该批 HEAD 为 0131，见[审核核心与接入约束](DAILY_RECONCILIATION_REVIEW_20260921.md)。

8.05 已补总部/区域日终查询与分页明细，0131 只读视图不暴露私有档案。513 项聚焦、13 项 CLI 检查和 24 个原生 PG16 场景通过；207 张既有业务表在迁移往返后内容一致，4 个自有实例均停止。日终解释/审核、页面入口、当前容器工件及完整门禁仍缺，见[日终查询证据](DAILY_RECONCILIATION_QUERIES_20260921.md)。

8.04 已补新增采集账号的恢复前准备、阶段检查与新凭证启用；420 项聚焦和 87 个实际 PG16 场景通过，其中 16 项恢复场景。当前数据库备份在独立新库恢复后 208 表 / 934 行内容一致，七个登录账号的新密码通过、旧密码拒绝，API/边缘/投影/采集权限通过，四个实例停止。当前容器联合附件恢复与真实云端仍缺，见[新增账号恢复证据](DAILY_CAPTURE_RESTORE_20260921.md)。

8.03 两个只读角色的配置与权限共存证据保留，见[角色配置与验证](DAILY_CAPTURE_ROLES_20260921.md)。日终区域查询已在 8.05 落地，审核接口已在 8.09 落地；下一步页面及进程部署，并补当前容器工件与完整门禁。

8.02 日终后端/0130 迁移及 39 个原生场景的历史证据保留，见[正式集成证据](DAILY_FORMAL_INTEGRATION_20260921.md)。旧 API 镜像不包含当前日终后端、角色恢复及查询改动。

8.01 已通过正式备份入口的实际 Docker/Compose 全链路：5 个场景通过，新包在第二个全新 PG16 实例恢复后 206 表 / 572 行一致，3 个可用对象和 1 个待上传意图核验。API 实际就绪且以 star_oam_api 连接；超时、取消、主进程终止、内容损坏后无会话/锁/临时容器残留，服务身份保持。4 次失败及数据目录保留，所有实例、引擎和 VM 已停止。API 为测试配置、OSS 为合成传输；真实云端验收和当前完整门禁仍缺，见[容器全链路与恢复交接](BACKUP_CONTAINER_E2E_20260921.md)。

7.99 已将 B5 入口、worker、导出和 Compose 接入正式目录，API 共用 22 个纯附件校验定义。首轮聚焦 263 通过、1 处 Compose 层级失败；修正后受影响 20 项通过。正式入口 13 场景、实际 PG16 5 场景及实际 Compose 解析通过，206 表 / 572 行恢复一致，两个实例及 VM 停止。该批尚缺的容器全链路已由 8.01 补齐，真实 OSS 仍待验收，见[正式集成专项](BACKUP_FORMAL_INTEGRATION_20260921.md)。
B3 原映射/截止及事务期限证据保持。7.97 独立进程入口通过 12 个 PG16 场景，建连停顿、SIGSTOP、半截回执及父进程丢失均核验；三份截止/审计、库存六表保持，三个实例停止。见[最新进程专项](DAILY_CUTOFF_PROCESS_20260921.md)。正式迁移/安全目录已完成；当前八角色容器联合恢复、真实网络/进程管理与真实映射仍缺；日终解释/审核及页面已实现，8.13 完成浏览器联验子集，完整链路仍待验收。完整 9496 已结束：4960 通过、1 处 CLI 版本失败；正式一行修复已过 13 聚焦和 6 PG 场景，见[CLI 修复](CONTROL_CLI_HEAD_20260921.md)。冻结已结束，下一步正式集成后重跑完整门禁。周额度剩余 10%，已触发停止条件；暂停新增开发，待用户恢复。
当前候选完整回归尚未通过；旧 `26717`、`7406` 和 `58287` 已结束。未提交、推送或部署。
以下为历史阶段证据，不能把旧工作树、旧门禁或旧运行中的 PID 当成当前指令。

### 7.72 历史验证

7.72 已将公开知识目录导入改为原始 XLSX + 精确审核计划，核对完整工作表集合、表头、
列、行范围及预期条数，保留准确来源坐标。旧任意 JSON 入口关闭；双端写入失败恢复及
并发编辑保留已验证。后端聚焦 **70 passed**、网页 **29 passed**、小程序 **67 passed**，
类型与双构建通过；这些是合成夹具结果。独立导入依赖恢复，CI 静态任务已接入但未远端执行。
真实公开目录仍 **pending/0**，正式发布拒绝；原 Edge 连接 blocked，单次恢复授权尚待答复，
未重连或判定飞书登录失效。452 个后端生产/迁移/部署/Edge 文件与 7.71 摘要一致，迁移 HEAD
仍 0128，本批没有重跑 PG16。当前候选完整 GitHub 门禁、真实资料/通知/设备、三天对账、
PITR 与生产灰度仍缺；未提交、推送或部署，原待批提交例外保持。
详见[原表导入、验证与下一步](PUBLIC_KNOWLEDGE_IMPORT_20260920.md)。

### 7.71 历史验证

最新 7.71 已补原启动请求永久终结：新增 0128 不可变事实、当前授权与账本锁竞争、
COMMIT 相斥保护，以及 Web/小程序显式确认和双重只读恢复。原生 PostgreSQL **16.15**
验证真实 JWT、启动先到/终结先到、撤权等待、提交时到期、绕过应用校验仍拒绝及降级保留；
52 张受检表事实保持。后端 **442 passed**，迁移/ORM **3 passed**，最终门禁契约 **27 passed**；
Web 全量 **1,239 passed**，小程序 **1,070 passed**，类型与双构建通过，测试组有交集。
当前迁移 HEAD 为 0128；启动开关仍 false，真实公开目录仍 pending/0，正式发布检查拒绝。
当前候选完整 GitHub PG16/客户端 CI、真实来源、多角色真机、三天对账、通知供应商与回调、
PITR 和灰度验收仍未完成。待批提交例外不变，未提交、推送或部署。
详见[原请求终结与当前上线缺口](OPENING_START_SEALS_20260920.md)。

### 7.70 历史记录

最新 7.70 已接网页和小程序私有期初启动表单：选择发布批次、多范围/执行人、盲盘与冻结方式；
首次发送前持久保存非敏感请求坐标，双重只读回查及任务详情核验后才清理。页面切换、
未知结果、身份变化和跨页面竞争均保留原请求，不能换键重发；同人新授权须重新验权。
网页全量 **1,226 passed**，小程序全量 **1,067 passed**，后端接口/恢复 **14 passed**；
类型、共享协议生成校验、公开与 `/xx` 构建、静态入口和仓库安全通过。正式公开发布
仍因真实知识目录 **pending/0** 被拒绝；小程序打包排除全部新增私有工具文件。
本批 127 个迁移、后端生产服务、部署与 Edge 未改；沿用且核验 7.69 原生 PG16 证据，
未重跑完整 GitHub PG16 门禁。启动开关仍为 false，个人主体小程序仍只注册知识页。
下一批优先补未知启动结果的服务端终结证明，再做当前候选版完整门禁、真机与真实数据验收。
未提交、推送或部署；待批的提交例外保持原状，不能把本地通过视为上线完成。
详见[客户端启动与持久恢复交接](OPENING_START_CLIENT_20260920.md)。

### 7.69 历史记录

最新 7.69 已补实际第二轮复盘：真实 JWT 选发布批次启动，首轮 2 件，换人复盘 3 件，
经两级复核只过账 3 件；原轮证据保持。实际 PostgreSQL **16.15** 已验证三次同键锁竞争、
跨区域指派及非本轮人员拒绝、撤权等待后拒绝，以及独立对账不改库存后关单。
聚焦两组各 **72 passed**，后续本地隔离/拓扑/清单 **27 passed**（有交集，2 组含依赖警告）。
最终真库的 0127 空库/有数据往返、51 张受检表事实保留及 API/Edge 权限通过；三个临时
集群均已停止。127 个迁移、生产服务与客户端未改，未提交或部署。
下一批接客户端启动契约、持久保存待核验请求与只读恢复；当前准备区仍只读，完整 GitHub/
客户端门禁及真实上线验收未完成。公开知识目录仍 pending/0，星星入口保持 `/xx`。
详见[实际复盘、并发权限证据与下一步](OPENING_RECOUNT_RUNTIME_20260920.md)。

### 7.68 历史记录

7.68 当时已将 0051 历史盘点样本与当前 ORM/业务服务分离，使用可追溯的冻结合成
事实；不再临时添加后来字段。原生 PostgreSQL **16.15** 已通过 0051→0052→0127、
0051→0127 单次升级，以及策略历史被修改时拒绝升级并完整保留数据/数据库目录。
历史任务未知授权版本保持 NULL；127 个迁移、生产业务代码、部署与客户端均未改。
聚焦 **89 passed**，0052 定向迁移 **8 passed**，最后样本/CLI **20 passed**（有交集）。
生成和验证使用的 13 个自有临时集群均已停止，最终源码与日志已保存；未提交或部署。
两项已知门禁样本依赖已修复，仍须继续完整实盘/复盘/权限并发链及 GitHub 完整门禁。
页面启动保持关闭，真实目录 pending/0，当前完整客户端门禁及真实上线验收仍未完成。
详见[历史迁移样本与最终验证](OPENING_LEGACY_FIXTURE_20260920.md)。

### 7.67 历史记录

7.67 当时 已把完整 PG16 门禁的当前期初样本接入真实签名采集、授权和审核发布链，
保留一条明确零数量控制行及独立 SN 策略。真库同时发现并以 0127 前向迁移修复独立对账
事件变量与列名歧义；实盘 +1 过账、重试不重复、未对账禁止关单、独立对账后关闭均通过。
迁移/权限/ORM **334 passed**，发布与对账服务聚焦 **207 passed，1 warning**，两组有交集。
原生 PostgreSQL **16.15** 最终专项、空库和有数据迁移往返、API/Edge 权限及事实保留通过。
前 126 个迁移与客户端文件未改，8 个临时集群已停止；未提交、推送或部署。
下一批修复 0051 历史样本与当前 ORM/服务耦合，再推进完整 GitHub 门禁和页面恢复链。
页面启动仍关闭，真实目录 pending/0，完整门禁与真实上线验收仍未完成。
详见[正式发布样本、对账修复与接续缺口](OPENING_RELEASE_FIXTURES_20260920.md)。

### 7.66 历史记录

7.66 当时 已保存新期初任务的不可变操作者授权版本，0126 在任务插入及 COMMIT 用数据库
实时时钟复核当前身份、区域权限与拒绝规则。角色在事务内到期、定时 deny 生效均已
在真实 PG16 验证整批回滚；真实 JWT 的 HTTP 提交拒绝返回明确 403。旧任务不补写
未知授权版本；已有新证据时降级拒绝，API/Edge 权限边界保持。
最终聚焦 **567 passed，1 warning**；ORM/迁移 **2 passed**，迁移服务 **3 passed**，
原生 PostgreSQL **16.15** 最终专项通过。前 125 个迁移与客户端文件未改，三个集群已关闭。
下一步优先修复完整 GitHub 门禁的正式采集/发布样本及旧 0051 历史造数，再接页面启动
和持久化恢复。页面启动仍关闭，真实知识目录 pending/0，完整门禁和上线验收未完成；
未提交、推送或部署。
详见[提交时操作者权限与后续门禁](OPENING_ACTOR_COMMIT_20260920.md)。

### 7.65 历史记录

7.65 当时已新增服务端按发布批次启动、旧 raw 入口相同准入和只读结果查询。0125 对新任务
插入及 COMMIT 执行来源/映射/物料/文件与新鲜度校验，API 私表权限继续隔离，历史版本
锚点保持不变。真实 PG16 已验证非零启动、旧行篡改拒绝、提交过期回滚、零明细同请求
并发及真实 JWT 结果恢复。最终联合回归 **726 passed，1 warning，2681.63 秒**；
原生 PostgreSQL **16.15** 最终专项通过，准确源码和日志清单已随本批证据保存。
下一批优先补操作者授权在 COMMIT 实时时刻的独立复核，覆盖事务执行期间角色到期。
完整 GitHub 门禁的旧样本仍创建未发布控制快照，必须补正式采集/授权/发布样本；不能
为过门禁放松新准入。页面启动仍关闭，真实目录仍 pending/0，未提交或部署。
详见[新启动准入、只读恢复和门禁样本缺口](OPENING_PUBLICATION_ADMISSION_20260920.md)。

### 7.64 历史记录

7.64 当时已接后台按区域查看已发布控制批次，新增 0124 受限只读函数和区域游标索引。
普通 API 只取摘要，四张发布私表继续隔离；身份/授权变化、跨区域和全局 deny 均拒绝。
网页全量 **1,188 项通过**，公开与个人仓构建通过；原生 PostgreSQL 16.15 的真实角色、
分页/历史/零明细、私表边界、迁移往返及篡改拒绝已验证，最终聚焦 **390 项**和离线/迁移 **6 项**通过。
探索性联合的三项失败已定位并复验，原失败日志保留；统计有重叠，不累加。
这只是批次查询，服务端启动准入和旧入口同步约束尚未完成，页面明确不能启动。
公开真实知识目录仍 pending/0，发布检查因此拒绝。详见[本批实现与剩余启动链](OPENING_CONTROL_DIRECTORY_20260920.md)。

### 7.63 历史记录

最新 7.63 修复控制发布与新期初启动交错时的实际 PostgreSQL 死锁。启动统一按账本、
人员权限图、控制来源持锁，并在等待后重验；0123 前向迁移让三项退回校验先忽略无关
事件，再对相关事实取账本锁，未扩大权限。原生 PostgreSQL 16.15 已通过真实锁等待、
旧批次拒绝、相关业务约束保留、九种迁移目录篡改拒绝、迁移往返及历史保护。
联合回归 **812 passed，1 warning，1918.67 秒**；另有通知/退回回归 **130 passed**。
聚焦 54 项包含在联合内。受限批次选择、服务端准入绑定及完整
GitHub 门禁、真实数据和上线验收仍未完成；未提交、推送或部署。
详见[发布与启动并发修复](CONTROL_START_CONCURRENCY_20260920.md)。

### 7.62 历史记录

7.62 当时修复已发布零库存批次无法启动的问题：新增 0122 前向迁移，仅允许具正式发布
证明的零行批次通过控制锁；普通空批次仍拒绝，运行权限不扩大。原生 PostgreSQL 16.15
已验证真实 API 角色并发启动只提交一个任务、后续发布替换后的原任务回读、库存交易/
余额不变、迁移往返/历史保留及 API/Edge 启动边界。三个新集群均已停止。
联合回归 **718 passed，1 warning，893.00 秒**。批次选择页面、新启动有效期/授权绑定和发布与启动交错锁验证
仍待完成；完整 GitHub 门禁及真实上线验收未完成，未提交、推送或部署。
详见[零控制批次实际启动](ZERO_CONTROL_OPENING_20260920.md)。

### 7.61 历史记录

7.61 当时已实现独立控制库存发布：0121 新增发布、控制行、原始行关系和版本关闭事实，
原子写入正式控制 SyncRun/Batch/Inbox/外部版本，支持准确重试回读、零库存、删除后重现。
真实 PostgreSQL 16.15 已验证四次发布、并发原请求、opening 消费者、历史保留、过期提交
回滚、角色/SQL 拒绝、迁移往返和 API/Edge 启动。最终联合 **1183 passed，1 warning**；
全迁移 166 项通过，2 处过时 head 断言修正后复验通过（已包含在联合内）。
普通 API/edge/projector 权限不扩大；没有生成库存账、盘点任务或通知事实。
详见[独立控制发布与原结果恢复](INVENTORY_CONTROL_PUBLICATION_20260920.md)。下一步补正式控制批次选择、启动绑定及恢复；
完整 GitHub 门禁、真实来源与上线验收仍待完成，未提交、推送或部署。

### 7.60 历史记录

7.60 新增控制发布前核查：每个汇总来源关联准确采集链/暂存行及审核物料版本，
增量未变行保留原行指针，删除行移出计划；任一目标物料缺证据整批拒绝。修复了正规化
有效期漏计映射到期的问题。当前总部会话 owner CLI 仅 inspect，始终回滚、不发布。
最终联合 **772 passed，1 warning，96.66 秒**；原生 PostgreSQL **16.15** 七场景及角色/
组合锁、API/Edge 启动、0120 往返和有数据降级保留通过。没有新增迁移或运行角色权限。
详见[控制发布前核查及准确来源关系](INVENTORY_CONTROL_PROJECTION_PLAN_20260920.md)。独立发布事实的前向迁移、正式控制批次
原子写入、版本关闭和原结果恢复仍未实现，随后再接期初启动。完整门禁与真实验收未完成，
未提交、推送或部署。以下保留各批历史状态。

### 7.59 历史记录

7.59 当时已完成非零控制整链的原生 PostgreSQL **16.15** 验证：完整/增量删除、缺失 SKU、
目标零库存、全量零库存及新完整采集六种场景通过，组合锁、实际角色、API/Edge 启动校验
及有数据降级保留通过。最终联合 **753 passed，1 warning，86.82 秒**。
真库暴露的签名快照原文与摘要不一致、0119 Edge 触发器目录遗漏已修复并复验。
沿用 **0120**，无新增迁移或运行角色权限；详见[控制整链真库证据](INVENTORY_CONTROL_PG16_INTEGRATION_20260920.md)。
下一步推进独立控制发布器的不可变事实、原子投影和期初启动/恢复。完整当前候选门禁、
真实数据/通知供应商和上线验收仍未完成；未提交、推送或部署。以下保留各批历史记录。

本批最终联合 **658 passed，1 warning，93.75 秒**；门禁清单最终专项 **2 passed**，与联合有交集不累加。

7.58 当时已把当前物料来源证明接入控制库存正规化：逐 SKU 核验准确来源/采集器、签名回执、
审核发布、当前版本、来源批准及附件；缺证据时整省候选余额阻断。完整/增量/零库存跨服务
测试已覆盖，新原生 PostgreSQL **16.15** 的当前来源证明、实际角色与同 SKU 发布锁竞争通过。
沿用 **0120**，没有新增数据库权限；详见[当前物料来源证明](MATERIAL_CURRENT_SOURCE_PROOF_20260920.md)。
下一步先补非零控制整链的 PG16 组合验证，再推进独立控制 publisher。完整门禁、真实来源、
通知供应商与上线验收仍未完成，未提交或部署。以下保留各批历史记录。

7.57 已补齐来源配置附件的正式上传用途及 **0120** 前向约束。真实 PostgreSQL **16.15**
已通过空库迁移/0120 往返、受限 API 上传完成下载、来源批准撤销、并发物料发布和 A→B→A
历史、数据库图/角色/锁检查、backup 快照及生产 API 启动权限校验；有数据时降级拒绝且
发布记录保留。详见[真库证据与来源附件](PG16_NATIVE_SOURCE_EVIDENCE_20260920.md)。
这是本地专项真库验证，完整 GitHub PG16 gate、真实数据/通知供应商、真机和上线验收仍待完成。
未提交或部署，后续优先接入控制库存正规化的当前物料来源证明。以下为各批历史记录。

7.56 已实现物料审核发布：以准确认证回执和逐行原始 hash 审核状态、单位及追踪规则，
原子保存正式物料、初始策略、不可变来源版本与发布审计；后续版本保留身份和历史，A→B→A
不覆盖旧版。旧采集、已有无证据 SKU、单位/策略改变整批拒绝，缺行不删除，来源时间保持 null。
新增 **0119** 两表、15 个 ALWAYS 触发器及当前会话 owner CLI；API/edge/projector 无新权限。
详见[物料审核发布](MATERIAL_PUBLICATION_20260920.md)。真实 PG16 尚未运行，未提交或部署。
本批最终后端联合 **600 passed，1 warning，67.71 秒**；SQLite 全 head 往返、语法/依赖和安全检查通过。

7.55 打通真实未知来源时间：**0118** 仅放宽 materials.source_updated_at 可空，保留
已知值、引用及触发器；有未知值时拒绝降级。正式目录 API 升为 **2.0**，字段显式 null，
双端同时读取严格 1.0 与 2.0，并显示“来源更新时间：未知”。不使用采集/同步时间补造。
部署顺序为先兼容客户端，再迁移与 API；不代表正式物料来源版本或投影已接通。
详见[未知物料来源时间](MATERIAL_SOURCE_TIME_20260920.md)。

后端最终联合 **649 passed，1 warning，73.16 秒**，包括完整 SQLite head 往返及实际 HTTP
到小程序解析；Web 全量 **70 文件 / 1168 passed**，小程序最终全量 **1058 passed**。
真实 PG16 helper 已准备但未运行。未提交、部署或真实来源操作，7.54 原证据保留。

7.54 增加总部物料来源业务授权、显式撤销及当前会话 CLI，新增 **0117** 不可变决定表与
独立 material_source.authorize 权限，API/edge/projector 不增权。采集准入锁定准确传输来源、
授权和审核文件，覆盖采集至接收完整区间及当前时点；撤销、到期、授权空隙、篡改与陈旧采集拒绝。
COMMIT 确认丢失只查询原结果。来源授权已实现，正式物料版本/投影及发布/启动仍未完成。
详见[物料来源授权与准入](MATERIAL_SOURCE_AUTHORITY_20260920.md)。

本批最终大范围联合 **1244 passed，1 warning，67.63 秒**，包括完整 SQLite head 往返。
后续补权限目录漂移时的降级保护，最终专项 **68 passed，1 warning，19.33 秒**，也含完整
SQLite head 往返；与大范围结果有交集，不累加。真实 PG16 helper 已接并发/锁/ACL/保留检查
但未执行；未提交、部署或真实来源操作。7.53 原证据保留。

7.53 补物料专用 HMAC 接收、准确传输登记、不可变回执、显式上传与原结果查询。
新增 **0116** 前向迁移和 2 表，edge 仅追加/读取受控暂存回执，API/projector 无新权限；登记支持
到期/撤销，默认关闭。COMMIT 回执未知后只查询原 capture_id，轮换后的当前凭据可回读旧回执。
传输认证不等于来源业务授权、正式物料投影或发布，所有发布/启动标志仍 false。
详见[物料接收与恢复](MATERIAL_CAPTURE_INGRESS_20260920.md)，7.52 及此前原证据保留。

采集/库存控制/迁移/权限/部署联合 **1189 passed，1 warning，71.60 秒**，包含完整 SQLite
head 往返。追加生产路由/配置及部署 SQL 检查后，最终边界联合 **116 passed，13.44 秒**。
真实 PG16 helper 已接入但未执行，本批没有真实来源读取、上传、提交、推送、部署
或调度启用。Web/小程序与之前 115 个迁移未改；当前候选 PG16 的提交例外仍待原答复。

7.52 增加 OAM 备件主数据实际采集入口与独立重验契约：两轮完整分页、准确字段/类型、
内容版本、未知来源时间和私有不可覆盖归档。观察范围仅为当前会话可见的备件列表，尚无物料专用
认证接收、授权与正式投影；所有可信主数据/发布标志仍为 false。既有默认同步、DB 权限及迁移
保持，见[物料主数据采集](MATERIAL_MASTER_CAPTURE_20260920.md)。7.51 原证据保留。

本批首轮 **101 passed，0.45 秒**，补齐原 Edge 归属检查后采集/正规化/映射/边缘安全最终联合
**323 passed，17.33 秒**；此前 315 项联合结果保留。
依赖检查、编译及 CLI help 通过；没有新迁移、真实来源读取、上传、提交或部署。
真实 PG16 仍无当前候选结果，原未获回复的 CI 候选提交例外不视为授权。

7.51 新增正式映射版本批准/撤销：当前总部网页会话通过 `--mapping-file` 预览、执行和
精确回读，决定与审计不可变；规则绑定准确来源/目录和有效区间，版本不得复用或重叠。
正规化检查可使用 `mapping_decision_id` 取当前批准版本，重验撤销与审核文件；原独立规则仍
只能预览。映射批准与主数据来源证明分开，发布/启动保持 false。
详见[正式映射版本](INVENTORY_CONTROL_MAPPINGS_20260920.md)。

7.51 最终联合 **1029 passed，1 warning，82.70 秒**，包括完整 SQLite head 往返；保护 PG16 与
未托管本地客户端共 **2 skipped，0.39 秒**。新增一表与 `20261025_0115` 前向迁移，复用总部
配置权限，普通 API/edge/projector 不获权限。真实 PG16 尚未运行；Web/小程序和之前 114 个
迁移未改。该批没有真实批准、来源调用、调度启用、提交、推送或部署。7.50 原证据保留。

用户新增要求：网站、小程序首页为“交流备件知识大全”，不展示登录；随后指定个人仓地址
`rscwz.cn/xx`。本轮按该二级路径实现，不再按二级域名方案推进。
随后按用户要求在网站页首保留“星星后台管理”按钮，点击进入 `/xx`；首页不嵌入登录表单。

- Web 公开/个人仓分别构建；Caddy 按 `/` 与 `/xx/` 分发，业务 API 和鉴权保留。
- 小程序只注册公开知识页，原业务页保留源码且排除出公开上传包。
- 导入工具、关键词/分类/分页、来源版本、旧首页缓存退休及新增门禁已实现。
- 最新 Web 全量 70 文件/1166 项通过；此前目录导入与部署安全 32 项、小程序全量 1043 项与聚焦 65 项通过。
- 双构建、开发产物检查与 Caddy 2.10.2 本地 16 路径检查通过；静态资源缺失返回 404，私有缓存不再向 JS 返回 HTML。
- Docker 增加目录发布硬检查，另有双端 `--release` 检查；空目录、无效溯源或构建过期会阻断。当前 pending 的发布检查按预期失败。
- 真实飞书表格尚未完整导出，目录保持 0 条 pending；本轮未上线、未提交，未新增 PG16 结果。

详细说明、原表读取边界、导入命令和剩余验证见
[公开备件查询与个人仓入口变更](PUBLIC_KNOWLEDGE_ENTRY_20260919.md)。
真实数据与目标部署仍是公开发布的阻塞项；等待期间继续下述独立的通知/控制库存开发。
不得用当前网页预览替代生产验收。

7.33 批次已接入库存 Outbox 到通知的有界投影，补齐非期初盘点逐笔库存 Outbox，
按实际保管人和已有业务通知去重。最新 85 项通知/盘点/历史权限联合聚焦、201 项库存联合回归、7 项前端通知测试通过；
PG16 专项已接入既有三轮复盘的非零盘盈事实，但新代码尚未取得真库运行结果。详见
[库存事实通知覆盖与验收](INVENTORY_NOTIFICATION_COVERAGE_20260919.md)。本批仍未提交或部署。
后端全量首轮（4773 项）已结束：4769 passed、3 skipped、1 failed，耗时 4172.24 秒、退出码 1。
唯一失败是已修正的 0037 历史 ACL 比较；上述 85 项复测覆盖该修正及新增盘点 Outbox 故障注入。
3 个跳过为 2 个无 SN 数量夹具和本机未配置的真实 PG16，定向范围复核为 3 passed、3 skipped。
不能称为单次全量通过，也不覆盖其后 7.34—7.49 的新增功能。

7.34 来源异常改为按对象保存点回滚及持久审计隔离，其他来源和既有发运通知继续
扩展；联合 79 项与单独 worker 集成 1 项通过。PG16 已加入隔离/重启/锁保持专项但未实跑。

7.35 已补总部来源异常查看和显式复核：校验当前权限、原内容摘要、最新审计版本与
独立幂等键，原失败审计保持不可变。后端联合 66 项、Web 全量 68 文件/1123 项通过；
个人仓构建和公开产物开发检查通过。PG16 追加真实 API role 的复核权限/回滚/幂等专项，
尚未实跑，详见[库存通知来源异常复核](INVENTORY_NOTIFICATION_OPERATIONS_20260920.md)。
身份恢复、历史事件与性能验证仍待完成。旧本地源码快照不含 7.34/7.35，获得 CI 提交确认后
仍须重新冻结准确候选。

7.36 新增原始通知目标与接收项绑定事实，迁移 head 为 `20261019_0109`。缺少账号或渠道
不再丢失原人员目标，历史事件不补猜目标；同 key 和库存消费者均保持首次目标清单。已有目标
封存时拒绝降级。最终迁移/权限/目标专项 **493 passed、1 skipped**（保护 PG16 未运行），
通知及发运/收货/工单/退回/验收/入账联合 **390 项通过**。该切片当时尚未实现身份恢复。
本地证据目录为 `cloud_oam/artifacts/notification-target-retention-20260920/`；均非全量或实发证据。详见
[通知原始目标留存](NOTIFICATION_TARGET_RETENTION_20260920.md)。

7.37 已实现总部原目标列表、历史目标未知列表及显式恢复 API/私有页面。恢复要求当前正式
身份、精确 provider/app 域 HMAC 和原观察摘要，只追加一条合法接收项及不可变审计，再由独立
扩展进入 queued。已有失败/unknown 接收项不建立替代渠道；超时结果必须刷新后核查。
Web 全量 **69 文件 / 1144 项通过**；后端最终通知/身份/运维/迁移联合 **188 passed、1 skipped**，
包含真实 SQLite 迁移后的已种权限、绑定、审计、回滚及身份变化防护。跳过为保护 PG16，
专用运行入口已准备但未实跑；本轮未重跑后端全量，迁移 head 仍为 0109。
本地证据目录为 `cloud_oam/artifacts/notification-target-recovery-20260920/`，完整范围见
[通知原目标查看与显式恢复](NOTIFICATION_TARGET_RECOVERY_20260920.md)。原始渠道解析、历史数据与性能缺口继续保留。

7.38 已统一首次创建和显式恢复的 provider/app 域正式身份解析，旧手机号/OpenID 不能独立创建
接收项；无渠道保留原目标，旧事件不自动补发。最终通知联合 **210 passed、1 skipped**（保护 PG16），
业务调用方聚焦 **64 项通过**；Web/小程序/迁移源码与 7.37 哈希一致，未重跑客户端或后端全量。
仍需不可变渠道验证事实、发送前复核及实际 provider 证据，不能称真实渠道已经上线。
详见[首次通知身份核验](NOTIFICATION_CHANNEL_IDENTITIES_20260920.md)。

7.39 已补退回独立入账的实际服务和 HTTP 回归：修复预览 reason、整数错误状态、精确
receipt/request/key 重放、当前接收权限及已完成结果封存。数量/SN、同包裹分批验收分别入账、
事务回滚和小程序原结果解析均纳入验证。另补齐此前遗漏的 0106 两个守卫函数的运行时安全清单，
数据库安全/0106 迁移联合 **345 项通过**；源码 head 仍为 0109，没有编辑历史迁移。
最终业务/HTTP/验收/通知身份联合 **95 项通过，679.02 秒**；完整结果与范围见
[退回独立入账验证](STOCK_RETURN_INBOUND_VALIDATION_20260920.md)。

7.40 已新增准确 receipt 绑定的独立入账封存表与 **0110** 前向迁移，修复小程序嵌套 seal
契约，验证旧验收封存不被改写、迟到提交拒绝、精确回查和同包裹分次验收隔离；入账/封存不再
直接对 API 只读验收表加行锁。业务/HTTP/验收联合 **77 项通过**；安全/迁移联合 **358 项通过**，含完整 SQLite head 升降级；
另有迁移历史保留/冲突预检 1 项、共享退回请求 43 项及小程序契约 4 项通过。
最后追加的孤立旧审计保护已补验：封存/迁移 20 项、孤立审计拒绝和已完成入账回读各 1 项。
详见[独立入账封存](STOCK_RETURN_INBOUND_SEALS_20260920.md)。PG16 双连接用例已接入但未实跑。

7.41 已新增完整入账事实重证与 0111 前向守卫，迁移/安全 342 项、证据专项 26 项、
入账/封存业务及实际 HTTP 30 项通过。
真实 PG16 尚未运行；完整范围见[库存事实验证](STOCK_RETURN_INBOUND_FACTS_20260920.md)。
7.42 已补按验收单的入账状态 API、小程序刷新/迟到回复防护、确认前方案比对及原请求恢复。
新状态 API 10 项、小程序全量 1056 项、实际小程序封存 payload 到 HTTP 回读 1 项、
部署/门禁合同 32 项及入账/封存/库存命令联合 34 项通过。真实 PG16 未运行，业务页没有加入公开小程序上传包。
详见[按验收单回读与恢复验证](STOCK_RETURN_INBOUND_STATE_20260920.md)。
7.43 新增 B1 发布准备的五类不可变事实和 **0112** 前向迁移，准确关联完整 inbox 快照，
全量/增量/空库存均可保存和回读。同版本冲突拒绝、重放复用、最终重证失败整组回滚，
API/edge/现有 projector 不增权。迁移/安全/准备联合 **551 项通过，70.45 秒**，
部署/门禁合同 **31 项通过**；保护 PG16 入口仍为 **1 skipped**，未执行真库。
Web 197、小程序 299、既有迁移 111 文件与 7.42 哈希一致，未重跑客户端或全量后端。
详见[控制库存发布准备事实与验证](INVENTORY_CONTROL_PREPARATION_20260920.md)。
7.44 新增来源/目录准确版本授权、显式撤销与 0113，绑定有效总部权限、证据文件及不可变审计；
来源撤销使其目录失效，明确重新授权不覆盖历史。授权/准备/迁移/安全联合 **577 项通过**，
部署/门禁合同 **31 项通过**；保护 PG16 **1 skipped**，动态门禁未运行。
7.44 时领域服务尚未接入配置入口，授权也不代表采集认证或发布。详见
[来源与目录授权验证](INVENTORY_CONTROL_AUTHORITY_20260920.md)。
7.45 已补带当前网页登录校验的 owner 运维命令入口，支持准确预览、执行与原结果查询；决策及会话审计原子保存，未知提交结果不自动重试。联合 **635 项通过，42.50 秒**，部署合同 **31 项通过**，真实 PG16 仍未执行。详见[配置入口与验证](INVENTORY_CONTROL_CONFIGURATION_20260920.md)。
下一本地开发为 PC 配置页面及受限命令传递、可信采集证明和专用发布链；B8 真库/真机验收继续保留。
不能把本地通过说成控制库存就绪、退回入账或正式上线已闭合。

7.46 已增加私有 PC 核查页面和用途受限的双向签名文件交接。后端联合 **694 项通过，60.40 秒**，
部署合同 **31 项通过**；Web 全量 **70 文件 / 1166 项通过**。真实 PG16 未执行，自动传输、
审核原文件在线验收与真实配置验收仍待完成。详见[PC 核查与签名交接](INVENTORY_CONTROL_HANDOFF_20260920.md)。

## 当前结论

通知投递运维切片已经完成后端依赖恢复、管理员查询、失败重试、worker lease、通知扩展/投递边界、迁移权限、前端运维入口和聚焦回归。通知事件、排队、发送、送达、已读、收货、个人仓入账和对账仍是独立事实；通知成功不代表库存或 OAM 业务完成。

控制库存现有离线四摘要契约；7.43 又增加 owner 管理的不可变准备事实和准确暂存链接。7.44 又增加独立的版本授权/撤销领域服务和审计，不改原准备接口语义；7.45 已接入认证运维命令，7.46 又补 PC 核查与签名文件交接，自动传输仍待完成。准备保存不赋予授权，配置授权不等于采集认证、正式发布或启动资格；运行角色写权限未扩大。`projection_published=false` 与 `start_ready=false` 必须继续保持。

因此当前状态是“代码和静态/服务级门禁较完整，生产环境验收未完成”，不是正式上线状态。

## 已完成范围

### 通知投递运维

- `backend/app/formal_services/notification_delivery_operations.py`：总部管理员查询、渠道/状态筛选、失败重试、全国范围和权限校验。
- `backend/app/routers/formal_notifications.py`：读取与显式重试 API；重试请求使用 `apiNoReplay`，认证恢复不会自动重放写请求。
- `backend/app/notification_expander.py`：将待处理通知事实扩展为投递事实。
- `backend/app/notification_dispatcher.py`：显式注入供应商 adapter、单 worker lease、发送结果和未知结果隔离。
- `backend/app/formal_services/notification_delivery.py`：provider message id/channel 精确匹配，`sent → delivered → read` 单调推进，时序倒退和未知对象失败关闭。
- `20261018_0108_notification_delivery_operations.py`：为 `star_oam_api` 授予必要的通知读取/追加和投递状态列更新权限；不授予 attempt UPDATE/DELETE；管理员运维权限单独种子化。
- Compose 的通知 worker 为 opt-in profile，使用 API 运行身份；基础部署不携带微信、短信、飞书凭据或默认外呼 adapter。

重试只接受有明确 429/5xx 失败证据、尝试次数未超限且幂等键/请求标识一致的对象。超时、断连、缺少 provider 响应等结果保持 unknown，不得盲目重发。

### 控制库存前置事实

- `backend/app/inventory_control_evidence.py`：隔离的来源、目录、分页、全量/增量链和覆盖一致性校验器。
- `backend/app/inventory_control_publication.py`：在上述校验成功后计算四个独立 SHA256：
  `source_binding_sha256`、`catalog_sha256`、`capture_chain_sha256`、`control_manifest_sha256`。
- 该模块不读取数据库、网络、文件、会话或锁，不创建 SyncRun、库存余额、审计、Outbox 或启动凭据。
- `backend/app/inventory_control_preparation.py` 与 0112 另行保存来源/区域绑定、目录版本、采集链、准确快照链接和准备 manifest。原 HTTP 字节摘要与规范化证据摘要分别保留；只读回查重证完整图。
- 五张新表为 owner 写、backup 读，原运行角色无权限；有数据拒绝降级。记录一致性仍不代表来源/目录获批或控制库存发布。
- `opening_start_option_schemas.py` 和现有准备目录继续返回 `start_ready=false`。

### 审计与交接材料

- [通知投递运维验证与正式基线缺口审计](NOTIFICATION_OPERATIONS_BASELINE_AUDIT_20260919.md)
- [继续开发记录](CONTINUE_DEVELOPMENT.md)，最新追加 7.46
- [控制库存发布准备事实与验证](INVENTORY_CONTROL_PREPARATION_20260920.md)
- [来源与目录授权验证](INVENTORY_CONTROL_AUTHORITY_20260920.md)
- [退回独立入账验证](STOCK_RETURN_INBOUND_VALIDATION_20260920.md)
- [首次通知身份核验](NOTIFICATION_CHANNEL_IDENTITIES_20260920.md)
- [退回独立入账缺口审计](STOCK_RETURN_INBOUND_GAP_AUDIT_20260920.md)
- [通知原目标查看与显式恢复](NOTIFICATION_TARGET_RECOVERY_20260920.md)
- [库存通知来源异常复核](INVENTORY_NOTIFICATION_OPERATIONS_20260920.md)
- [控制库存证据验收边界](INVENTORY_CONTROL_EVIDENCE_ACCEPTANCE.md)
- [期初控制投影下一切片](OPENING_CONTROL_PROJECTION_NEXT_SLICE.md)
- 本交接文档：`RSC个人仓开发交接_20260919.md`

## 已验证证据

以下结果来自当前工作树或本分支前一提交链；重复计数不相加：

| 范围 | 结果 | 说明 |
|---|---:|---|
| Python 依赖 | `pip check` 通过 | 隔离环境为 `cloud_oam/.venv`，未入库 |
| 7.45 配置/会话/授权/准备/迁移/安全 | `635 passed` | 42.50 秒；含真实 SQLite 服务、JWT 会话、脚本恢复和完整 head，保持 0113 |
| 7.46 PC 签名交接/配置/授权/准备/迁移/安全/启动 | `694 passed` | 60.40 秒；真实 JWT/Ed25519/SQLite 和 HTTP 正式依赖，head 0113；动态 PG16 未执行 |
| 7.46 Web 全量及部署合同 | `1166 passed` / `31 passed` | 分别为客户端与部署检查；不累加到后端组合，双构建通过 |
| 7.45 部署/门禁合同 | `31 passed` | 1.09 秒；PG16 动态未运行，本地保护入口 1 skipped |
| 7.44 版本授权/撤销/准备/迁移/安全 | `577 passed` | 34.30 秒；完整 SQLite head 往返和权限种子，head 0113 |
| 7.44 部署/门禁合同 | `31 passed` | 1.13 秒；PG16 动态门禁未运行，保护入口 1 skipped |
| 7.43 控制准备/迁移/数据库安全 | `551 passed` | 70.45 秒；含完整 SQLite head 往返与模型对齐，迁移 head 0112 |
| 7.43 部署/门禁合同 | `31 passed` | 1.27 秒；PG16 保护入口为 1 skipped，不能计作真库通过 |
| 7.42 状态 API / 实际封存请求 | `10 / 1 passed` | 108.37 / 14.04 秒；实际验收、过账和小程序请求契约 |
| 7.42 小程序全量 | `1056 passed` | 2.96 秒；包含内部业务页面源码测试，公开包仍只有知识查询 |
| 7.42 部署与门禁合同 | `32 passed` | 3.26 秒；PG16 保护入口另为 1 skipped，不计通过 |
| 7.42 入账/封存/库存命令 | `34 passed` | 340.74 秒；完整 Python/Node 运行时的联合回归 |
| 7.41 入账/封存业务与 HTTP | `30 passed` | 325.11 秒；已使用完整 Node/Python 运行时重跑 |
| 7.41 入账事实证据 | `26 passed` | 333.19 秒；含篡改拒绝、合法后续移动和只读恢复 |
| 7.41 迁移/数据库安全 | `342 passed` | 148.12 秒；完整 SQLite head 往返，非 PG16 |
| 7.40 独立入账封存/入账/验收联合 | `77 passed` | 516.86 秒；后续孤立旧审计保护已追加最新源码聚焦补验，详见 7.40 验证记录 |
| 7.40 迁移/数据库安全联合 | `358 passed` | 67.63 秒；包含完整 SQLite head 升降级和 ORM 对齐，非 PG16 |
| 7.39 退回入账业务/HTTP 联合 | `95 passed` | 679.02 秒；含验收、正式通知身份、实际小程序原结果解析和门禁拓扑；不是全量或 PG16 |
| 7.39 0106 迁移/数据库安全联合 | `345 passed` | 38.62 秒；0106 实际 SQLite 建表后的数量/SN 入账与留存，PG SQL/ACL 静态清单；不是完整迁移链或真实 PG16 |
| 7.39 既有冲销与退回前链 | `6 passed` | 40.05 秒；通用合法冲销与业务退回防绕过 |
| 7.38 首次通知身份最终联合 | `210 passed, 1 skipped` | 132.36 秒；保护 PG16 跳过 |
| 7.38 业务调用方聚焦 | `64 passed` | 174.77 秒；未覆盖退回独立入账完整命令，已单列缺口 |
| 7.37 通知目标恢复最终联合 | `188 passed, 1 skipped` | 122.84 秒；含真实 SQLite 迁移、身份、运维和旧通知链，保护 PG16 跳过 |
| 7.37 Web 全量 | `69 files / 1144 passed` | 个人仓及公开构建、开发产物检查通过；真实目录发布仍阻断 |
| 控制证据与发布前契约 | `198 passed` | 含四摘要独立性、篡改变化和失败关闭 |
| 通知 delivery/expansion/operations | `20 passed` | provider stub/服务级边界，不是真实供应商发送 |
| 数据库安全与部署权限 | `316 passed` | 含 ACL、部署安全和运行边界 |
| Alembic 全量（历史） | `168 passed` | 0108 时的结果；当前源码 head 已为 `20261021_0111`，本批完整 SQLite 升降级见 7.41 |
| PG16 诊断与 workflow 拓扑 | `15 passed` | 静态门禁合同 |
| 通知运维/投影安全等后端联合集 | `367 passed` | 之前最终通知运维回归记录 |
| 迁移/权限/PG16 静态联合集 | `508 passed` | 之前最终通知运维回归记录 |
| 前端 | `64 files / 1077 passed` | 之前最终 Client gate 前的本地证据 |
| TypeScript/Vite | 通过 | 保留既有 bundle 体积提示 |
| 本地 PostgreSQL 16 动态门禁 | `1 skipped` | 本机为 PostgreSQL 18，跳过不计通过 |
| 远端 PostgreSQL 16 gate | success | [run 35432056789](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/35432056789)，针对通知运行时代码等价提交 `7266ae2`；不是当前 `260ed78` 的新运行 |

重新验证常用命令：

```bash
cd /Users/replace-with-local-user/.codex/worktrees/06f6/oam/cloud_oam/backend
../.venv/bin/python -m pytest tests/test_inventory_control_evidence.py -q
../.venv/bin/python -m pytest tests/test_notification_delivery.py tests/test_notification_expansion.py tests/test_notification_delivery_operations.py -q
../.venv/bin/python -m pytest tests/test_database_security.py tests/test_database_deployment_security.py -q
../.venv/bin/python -m pytest tests/test_alembic_migrations.py -q
../.venv/bin/python -m pytest tests/test_pg16_release_gate_diagnostics.py tests/test_pg16_workflow_topology.py -q
../.venv/bin/python -m pytest tests/test_postgresql16_release_gate.py -q
../.venv/bin/python -m compileall -q app
cd /Users/replace-with-local-user/.codex/worktrees/06f6/oam
git diff --check
```

最后一条 PG16 release gate 在本机预期会因没有明确的 PostgreSQL 16 disposable 环境而 skip。不能把 PostgreSQL 18 结果写成 PG16 通过；需要真实 PG16 时使用 CI 隔离数据库并记录准确提交 SHA。

## 未关闭的正式基线缺口

### 通知缺口

- **N1 真实 adapter**：微信、短信、飞书真实发送、凭据/KMS 边界、provider 错误语义和测试环境发送证据尚未完成。
- **N2 provider callback**：没有可上线的 HTTP 回调入口、各 provider 验签、时间窗、nonce、密钥来源、provider event 去重和安全审计。不要先写一个通用 callback。
- **N3 飞书双向机器人**：没有查询、跳转、审批/库存命令的正式接口链；机器人不能直接改库存。
- **N4 通知覆盖矩阵**：库存 Outbox 投影、非期初盘点逐笔事件、来源隔离及总部显式复核已通过本地聚焦；7.36—7.38 已补原目标留存、受控恢复及首次渠道正式身份解析。真实 PG16、历史缺失 Outbox/原目标、发送前身份与配置复核、扫描性能和异常汇总仍待完成。
- **N5 通知专用真库并发**：仍需 PostgreSQL 16 下的同键/异键重试、worker 竞争、审计失败回滚和 provider timeout unknown 测试。

### 非通知缺口

- **B1 控制投影**：已有准备、版本授权、PC 签名交接、严格采集、HMAC/有效授权检查、正规化及正式映射批准/撤销；7.52—7.54 补物料两轮采集、传输登记、认证接收/恢复、总部来源批准/撤销及持锁准入；7.55 已支持数据库/目录/双端的真实未知来源时间。仍缺不可变正式来源版本与物料投影、单位/批次真实语义、完整部署密钥治理、自动配置/检查/映射交接、原文件验收、独立 publisher/RLS 和发布事务内复核、原子发布、批次选择与启动恢复。映射批准、传输登记和回执均不是发布许可，分页一致性不等于来源事务快照。
- **B2 数据迁移**：组织、人员、SKU、历史单据、附件的数量/关键字段/附件校验和多轮演练未闭合。
- **B3 日终对账**：尚无连续至少 3 天的省级 OAM 控制数与本地明细差异解释证据。
- **B4 真实环境身份与渠道**：首管理员、真实人员映射、微信/短信/KMS/私有附件链缺完整验收记录。
- **B5 恢复能力**：缺 RPO≤5 分钟、RTO≤2 小时的 PITR/WAL 或等效恢复演练，以及库存/SN/审计核对。
- **B6 UAT/真机**：缺真实角色、设备、附件和关键 V1.0 场景的 UAT/真机证据。
- **B7 压测**：缺 500 用户规模的延迟、错误率、锁等待、队列积压、限流和告警报告。
- **B8 退回独立入账**：7.39—7.41 已补服务、独立封存、完整图重证与 0111；7.42 已补按验收单状态回读、小程序刷新和封存请求契约。仍缺当前候选 PG16 动态证据及真机/正式角色验收。见[最新专项验证](STOCK_RETURN_INBOUND_STATE_20260920.md)，不得宣布闭合。

## 下一位开发者的推荐顺序

1. 继续 B1 不可变正式来源版本与物料投影。7.55 已完成未知来源时间可空迁移、目录 2.0 和双端契约，不再重复开发；生产启用按客户端先行顺序。0112—0118、PC 签名交接、7.49 授权检查、7.50 正规化、7.51 映射批准/撤销及 7.52—7.54 两轮物料采集/认证接收、总部业务授权/撤销和持锁准入已有本地证据；显式传输登记准确关联 oam/starcharge_oam，业务授权单独保存。不能重复当作未开发，也不能缓存回执 JSON 直接发布或启动。后续补单位/批次真实语义、部署密钥治理、原文件验收及独立 publisher/RLS，在发布事务内重验授权、映射、登记、凭据、时效和主数据并发范围。自动配置/检查/映射交接仍待补齐，维持全量/增量及显式空库存语义。
2. 补当前候选 PG16 和 B8 真机/正式角色验收。状态 API、页面刷新/恢复、完整图重证和 0111 已完成本地验证，不重复开发或改旧迁移。并接续通知的不可变渠道证明、发送前复核、历史 Outbox/原目标与扫描性能。
3. 在 provider 合同冻结后，按 provider 分别实现 callback ingress、验签、重放防护和 `RobotInboundEvent` 最小权限迁移；不要新增泛化内部协议。
4. 为通知 worker 和 callback 补真 PostgreSQL 16 并发/中断测试，再接预生产 adapter。
5. 继续处理迁移、对账、恢复、UAT、压测和灰度上线证据；每一项都要记录准确提交、环境、结果和边界。

## 不可违反的交接约束

- 继续使用本工作树和分支时，禁止 `reset`、`revert`、强制丢弃或覆盖未提交改动；先读正式基线再改代码。
- 不向 OAM、RSC、Workflow、飞书或真实供应商发送写请求，除非当前任务另行明确授权并完成对应健康检查/回读。
- 不把 provider 送达、飞书消息已读、物流签收、OAM 收货或通知排队当作个人仓入账。
- 不把 `evidence_consistent`、摘要 hash、迁移成功、Client gate 或全局 PG16 gate 单独当作控制库存就绪。
- 不给 `robot_inbound_events` 直接追加宽泛运行时 ACL；先冻结 provider 合同和字段映射。
- 不在未知发送结果上自动重试；中断后先精确回读，再决定是否允许显式重试。
- 不打印、提交或复制 OAM token、Cookie、验证码、provider secret 或原始敏感响应。

## Git 交接注意事项

2026-09-19 实时 `gh repo view` 显示目标 `likexin12985/rsc-personal-warehouse` 为 **PUBLIC**。
临时 CI 分支也会公开候选源码。当前已请求用户确认“先做验证候选提交、现有分支仍等门禁通过后
提交”的例外，并补充告知当前仓库可见性；尚无答复，不得因继续开发或等待超时而视作授权。
Git HTTPS 的只读分支探测本次在 75001 ms 后连接超时；GitHub API 可用，并确认拟议分支
`codex/notification-launch-candidate-20260919` 尚不存在。这不是凭据失效证据，也未创建分支。

代码实现基线为 `260ed78`。本地分支与远端存在不同的等价提交历史，具体 ahead/behind 以交接时的 `git status --short --branch` 为准；不要用 reset/rebase 强行对齐。需要发布当前交接提交时，先比较树和远端状态，再采用不会丢失本地提交的正常发布方案，并记录准确远端 SHA。

当前已知远端 `origin/codex/notification-delivery-worker` 为 `68573f16ce74aeeb6cc0479ffb076cd2b7c9eac2`，树为
`70ad779de5df06f79c23169224c862010ede28b7`；本地 `260ed78` 的树为
`aae417b2a02013a0cc2975d72e38cc9bf08dbf0b`。远端尚未包含本轮控制库存四摘要契约、对应测试和交接文档，不能把远端 68573f1 当作当前完整代码。

本批最终联合 **712 passed，1 warning，106.82 秒**；包括完整 SQLite head 往返。
