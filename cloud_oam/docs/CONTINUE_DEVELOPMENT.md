# 切换账号后的续开发入口

## 2026-09-15 统一扫码直达 SN 精确查询

扫码结果进入本人 SN 明细时，现在携带经过契约校验的 SN，仅执行一次服务端精确
`serial_no` 只读查询；页面不会先展示整个库存账户，也不会把二维码原文写入页面或存储。
用户主动选择“显示全部”后才回到账户分页列表。SN、人员、库存账户和账本游标仍由服务端
完整回读校验。小程序全量回归 **1038 项通过**；本批仍未推送，等待现有 PG16/static
门禁终态以避免并发取消。

## 2026-09-15 统一扫码直达 SN 明细

统一扫码页在完成正式只读解析后，新增本人 SN 的直达入口：只有服务端返回的 `serial`
对象且带有 `view_personal_serials` 能力时，客户端才按返回的本人库存账户标识打开
`formal-personal-warehouse-serials`；物料、批次和库位仍只能打开个人仓只读页。页面不保存
二维码原文，也不新增任何库存、收货或盘点写入。新增行为回归已通过；完整小程序回归
**1037 项通过**。当前工作树改动尚未推送，需等待现有 PG16 云端门禁完成后再提交远端，
避免 workflow concurrency 取消正在运行的准确 SHA 检查。

## 2026-09-15 统一二维码只读扫码入口

在个人仓 SN 逐件详情之后，补齐小程序统一扫码页 `pages/formal-scan/index` 和正式只读接口
`GET /api/v1/scan/qr`。服务端只接受当前正式库存权限下的精确活动二维码：物料/批次仅返回
目录身份，个人仓库位和 SN 必须严格属于当前人员的有效个人仓；已作废、不存在、跨人员或
投影不一致的对象失败关闭。响应不返回二维码原文、库存凭据或其他人员数据，也不触发收货、
盘点、库存过账或任何写入。

客户端在相机回调的函数局部变量内使用扫描值，既不写入 `Page.data`、本地存储和恢复标记，
也不把二维码放进响应契约；页面隐藏、卸载、会话轮换或权限变化会丢弃迟到结果。工作台已增加
统一扫码入口，旧 v0.9 页面仍保持隔离。

本批新增后端统一扫码专项验证、小程序契约/页面验证，并通过小程序全量回归 **1037 项**、
库存读取组合回归 **44 项**。本批仍未部署、未写入生产数据库、未操作 OAM/RSC/飞书；真机
相机、HTTPS 合法域名、发布配置、PG16 准确 SHA 门禁和上线验收仍需继续完成。

## 2026-09-15 个人仓 SN 逐件详情与精确扫码查询

上一批准确提交 `5825ca06e07edb18bef93a06e85159eb70ffd72a` 的 Client workflow
`34856837732` 和 PostgreSQL 16 workflow `34856837759` 均已完整成功。本批在该已验证
基线上新增只读接口
`GET /api/v1/inventory/personal/me/accounts/{stock_account_id}/serials`：服务端复用本人
个人仓、期初建立、当前保管责任、完整账本重建和投影一致性门禁，只接受本人库位内准确
库存账户。数量管理物料、外部账户、期初未建立、SN 数量与账户余额不一致、SN 物料/批次/
生命周期不一致均失败关闭。

小程序个人仓的 SN 管理物料现可进入逐件页，按固定账本游标分页，显示当前仍在该账户内的
SN，并可用相机扫描 SN 条码执行精确只读查询。扫描原文只存在于当前函数内存，不写入
Page.data、Storage 或恢复记录；服务端响应不包含二维码。页面离开、登录令牌或授权版本变化、
跨页账本变化均清空结果。统一二维码对象扫码、真机相机/UAT 和发布配置仍需后续完成。
本批后端库存读取专项 **43 项通过**，小程序完整回归 **1034 项通过**，Python/JavaScript
语法和差异格式检查通过。本批未部署、未迁移生产数据库、未写 OAM/RSC/飞书或其他
外部业务系统。

## 2026-09-14 个人仓不可变流水入口

本批在既有个人仓只读页新增“个人仓流水”入口及
`GET /api/v1/inventory/personal/me/transactions`。服务端先复用完整个人仓期初、保管责任、
权限和投影重建门禁，只返回触及当前本人个人仓库位的已过账交易；来源库存账户及其他人员
坐标不返回。数量按每个库存账户维度分别保留 SKU、单位、成色、库存状态、增减方向和 SN
数量，禁止把不同物料或单位相加。小程序读取前后复核身份与授权，跨页账本游标变化、页面
离开和会话变化均清空本次展示。

当前备案顺序按用户最新确认执行：微信小程序管局驳回原因为代码尚未发布，开发、真机/UAT、
试用/发布完成后再重新提交备案；该驳回不作为代码失败。此前已确认的准确 HEAD Client 与
PG16 云端门禁均成功，本批新增后端读取和客户端页面仍须以提交后的新 SHA 重跑两项门禁。
本批未部署、未迁移生产数据库、未写入 OAM/RSC/飞书或其他外部业务系统。

## 2026-09-13 账号切换交接快照（优先于下方历史记录）

用户本次要求因账号额度用完而切换账号继续开发。本节为交接，不新增业务功能。
继续使用用户提供的原项目目录及 `codex/production-readiness-gates` 分支；桌面旧
worktree 的 `codex/mini-post-recovery` 不是本次续开发入口，不要在那里实现或提交。

交接核验时，最新功能提交为 `94fa487f411db9f3c66852fe9d6b3d4d66403fcc`，
本地与远端同 SHA，ahead/behind 为 `0/0`，功能代码均已提交并推送。
本交接说明另作本地文档提交；新账号应核验实际 HEAD、提交父节点及差异，不能
把下方历史 SHA 当作当前 HEAD，也不要为了回到上述功能 SHA 而 reset。
既存根目录 `AGENTS.md` 修改和未跟踪的
`cloud_oam/deployment/{offhost_backup,operations,preproduction,recovery}/`
均保留，不属于本批，不要一并提交或清理。

同一功能 SHA 的 GitHub Actions 在交接检查时：

- Client workflow `34747724933` 已完成，结果 **success**。
- PG16 workflow `34747724937` 仍为 **in_progress**：`pg16_runtime` 正在执行真实
  PostgreSQL 16 迁移与并发检查，`static_safety` 正在执行投影与边缘静态检查。
  当时尚未观察到失败，但不能据此宣布整个 gate 通过。

新账号先准确读取以上 run 的最新状态。**同分支 PG16 检查运行时不得推送，文档
推送也会通过 workflow concurrency 取消原检查。** 因此本交接文档先仅本地提交；
当前门禁结束后再决定下一批推送。每次 commit 和每次 push 前均须单独运行
`bash cloud_oam/scripts/verify_repository_safety.sh` 并等待退出 0。
旧的账单阻塞和本机缺 PG16 记录已不代表本次状态：本次真实本地 PG16 已验证，
云端 runner 已启动；旧迁移链遗漏问题已修复并包含在当前功能提交。

## 2026-09-13 本地继续开发进度（b3d38a6 之后）

在上述交接快照之后，已完成并本地提交 `b3d38a6`：小程序退回包裹验收页现已提供
整组数量/SN 验收、短少与破损记录、异常凭证、三码扫描（二维码只留内存）、预检、
单次正式提交，以及按 `work_order_id + shipment_id` 精确范围的超时恢复与互斥封存。
验收仍只登记独立验收事实，不增加个人仓库存、不解除原保管责任，也不替代 OAM 收货。

预检响应已逐行绑定本次提交的物料、数量、SN、异常类型和凭证，返回字段改变或跨包裹
时会拒绝继续提交。小程序全量回归 **1017 项通过**；新增验收专项 4 项通过；原生
WXML/WXSS 编译、语法检查和 `git diff --check` 均通过。该提交仍只在本地，PG16
云端门禁 `34747724937` 运行期间暂不推送，以免触发同分支并发取消。

下一步仍是新增前向迁移，把已确认的退回验收独立接入库存入账、个人仓归属和保管责任
交接；必须沿用统一原子过账入口，并保持验收、入账、通知、OAM 只读收货分别记账。

## 2026-09-13 0106 独立退回入账边界

### 0106 小程序入账入口与恢复摘要（2026-09-14）

已将独立退回入账接入 `formal-stock-return-receiving` 小程序页：从已确认的分批
验收记录生成只读入账预览，确认后只调用 0106 入账接口；结果未知时按
`receipt_id + request_id` 回查，只有服务端明确确认未执行才允许封存。客户端恢复标记
使用独立 `stock_return_inbound` 类型，和退回验收标记分开，仍共享同一包裹互斥锁。

服务端请求摘要已固定为 `receipt_id + request_id + plan_hash`，不再包含事务内随机入账
ID 或动态检查时间，因此提交前即可持久化准确恢复摘要。普通需求收货、OAM 收货和
验收事实均未复用或合并。新增客户端契约专项 3 项、后端摘要专项 1 项；退回验收、
入账与恢复组合回归及小程序全量回归均通过。当前仍需最终 HEAD 的 PG16 云端门禁、
真机/UAT 和生产部署验收。

已在 `codex/production-readiness-gates` 本地完成 0106 前向迁移与正式写入边界：
新增独立退回入账、明细、SN 和过账关联四类 append-only 事实，readiness 函数从
0105 精确推进到 0106；预览接口保持只读，提交接口在统一库存过账事务中写入本地
在途到账户到区域/个人可用账户的转移，并以 `receipt_id`、请求键和幂等键分别做
精确绑定。该链不创建旧 `InboundOrder`，也不调用 OAM 收货接口。

本地迁移/ORM 对照 168 项、数据库安全 300 项、入账契约与组合回归 304 项通过，
仓库安全检查通过；真实 PostgreSQL 16 workflow `34752453341` 仍在运行，必须等
其结束后再推送并重跑本批准确 SHA 的静态与动态门禁。

0106 后续又补上事务证明和不确定结果恢复：`return_inbound_postings` 必须指向入账
头部绑定的同一库存事务；入账请求可按 `receipt_id + request_id` 精确回查，只有在
没有观察到库存过账审计时才允许创建 `receive_return` 封存。对应本地提交为
`05979a0` 与 `321228e`，尚未随正在运行的旧 SHA 门禁推送。

后端已先本地提交 `a0d37c8`，新增独立入账合同层和 3 项合同测试：命令只能由已接受
数量和已核验 SN 组成，只允许在途账户到区域仓账户的转移，且不携带普通需求收货或
OAM 收货字段。使用隔离 Python 3.12 环境执行该专项为 **3 项通过**。当前尚未创建
0106 数据库事实、正式路由或生产过账；这些仍是下一批实现内容。

已在 `11d1507` 增加只读入账计划：它从已验证的 `stock_operation_receipts` 读取已接受
数量和 SN，重新核验当前区域仓保管责任及目标账户，再生成统一库存转移命令；计划本身
不写库存、不创建普通 `inbound_orders`，也不把 OAM 收货当作入账依据。0106 事实、
幂等提交和过账回读仍待实现。

另外修正了迁移门禁基线：`test_alembic_migrations.py` 原仍停在 0104，未包含 0105
验收表和 `receive_return` 权限。修复后该文件全量 **168 项通过**；修复提交为
`92c3ff9`，待当前旧 PG16 run 终止后与其余本地提交一起推送。

下一批范围明确为：

1. 在已有小程序接收查询页实现退回包裹整组验收表单，接正式异常凭证上传、
   三码扫描、预览及单次提交。扫描证明仅留内存，不把二维码或凭据持久化。
2. 为 `receive_return` 补原请求标记、准确回查和互斥封存恢复入口，携带准确的
   `shipment_id`；超时结果未知，未查到不等于未执行，禁止盲目重发。
3. 随后新增前向迁移，实现退回验收后的独立库存入账及保管责任交接，复用统一
   原子过账入口；验收、库存入账、个人责任、通知和 OAM 只读收货继续分别记账。

目前后端独立退回验收及小程序查询已经实现，**小程序验收提交和独立退回入账
尚未完成**。可从下列代码接续：
`miniprogram/pages/formal-stock-return-receiving/`、
`miniprogram/utils/stock-return-receiving-contract.js`、
`miniprogram/utils/work-order-recovery-store.js`、
`miniprogram/utils/work-order-recovery.js`，以及后端
`app/stock_return_receipt_schemas.py` 和 `app/formal_services/stock_return_receipt_*.py`。
上述路径分别相对 `cloud_oam` 或 `cloud_oam/backend`。

本批本地测试进程均已结束；准确功能 SHA 的小程序全量为 **1013 项通过**。
下节保留各项日志和合成库坐标。并发验证已写入持久验收及封存事实，禁止重置、
重复执行同一并发场景或强制降级；不要完整运行旧的合成库初始化脚本。
用户已确认小程序备案及阿里云域名 `rscwz.cn` 备案通过，但尚无本批真机验收、
生产部署或上线放行。历史工期估算不是实测剩余时长，也不是交付承诺。

## 2026-09-13 退回包裹独立验收与区域仓小程序查询

本批从 `b31b91f629936a1c88ca3a140dc3b7216a574f7e` 接续，使用原目录的
`codex/production-readiness-gates`。此前未推送的 `7cf8d0e` 是接收方查询，
`b31b91f` 是旧 PG16 回滚测试补齐当前完整迁移链。远端 `06e85c3` 的
`34743613777` 已完整结束：静态检查成功，真实 PG16 失败于旧迁移链遗漏，
修复已包含在 b31；该结果不能记为新版门禁通过。后续以实际 HEAD 和 CI 为准。

新增前向迁移 `20261015_0105` 和退回包裹专用验收头、行、SN、异常证据。
支持数量件/SN、正常、短少、破损、错料、错 SN、拒收、分批验收、原请求回查和
互斥封存。短少仅记录观察量，之后仍能补收；接受及拒收消耗累计确认量，破损是
已接受量的一部分。仅当前总部/区域负责人、原包裹指定接收人、当前有效保管责任
和范围权限同时成立才允许验收。验收不改库存、不解除个人责任、不伪造 OAM 收货。

正式接口已挂载，前缀
`/api/v1/stock-returns/my-receiving/{shipment_id}/receipts`：GET 历史、POST 提交、
POST `/preview`、GET `/by-request/{request_id}`、POST 原请求 `/seal`。
响应丢失只能准确回读或互斥封存，未观察到原请求不证明未执行。普通收货行、
直接入账、库存过账、跨操作请求键及伪造证据继续由 SQL 守卫拒绝。
新表只追加，禁止修改、删除及清空；存在验收或验收封存历史时禁止降级。

小程序新增 `pages/formal-stock-return-receiving/index`，从总部/区域工作台进入，
按本人当前接收仓分页查看原包裹、累计确认量、剩余 SN 和逐次异常说明；无法核验的
包裹单独提示。数据按完整响应契约和累计数量校验，页面不显示 QR、库存账户或恢复键。
离开页面、切换账号、查询期间权限变化和读取失败均清除业务展示，迟到响应不能恢复它。
**本页目前是查询页，尚未提供验收提交表单；独立退回入账适配器也尚未实现。**

本批验证均已完成，日志位于本机 `/tmp`：

- 后端组合回归 **148 项通过**：`oam-return-receipt-domain-final.log`。
  正式接口及迁移/workflow 组合 **13 项通过**：`oam-return-receipt-http-integration.log`。
- 数据库权限及新迁移 **305 项通过**：`oam-return-receipt-manifest-final.log`；
  完整退回迁移专项 **23 项通过**：`oam-return-receipt-migrations-final.log`；
  最终迁移/门禁拓扑/诊断 **21 项通过**：`oam-return-receipt-final-gate-static.log`。
- 真实 PG16 数量/SN 正常、五类异常、短少后补收与小数分批验收通过并完整回滚：
  `oam-return-receipt-pg-extended-v4.log`；直接 SQL 伪造/超收/错码/跨键/直接入账
  拒绝及不可变约束通过：`oam-return-receipt-pg-sql.log`。
- 两个真实连接的五组提交/封存竞争通过，仅产生一个有效结果，持久原请求/HTTP
  回读及降级阻断通过：`oam-return-receipt-pg-concurrency.log`。
- 真实接收身份与 HTTP、PG 强制 READ ONLY、Node 小程序目录/验收响应契约通过，
  仅执行 SELECT、原库存及验收历史未变化：`oam-return-receipt-pg-mini-read.log`。
  此检查已接入完整云端 PG16 gate，不能用纯 SQLite 或模拟响应代替。
- 准确功能提交 `94fa487` 的小程序全量 **1013 项通过**：
  `oam-return-receiving-mini-94fa487-full.log`；异常说明换行/Unicode 长度校验等
  专项 **25 项通过**：`oam-return-receiving-mini-tests-final.log`。
  新查询页及工作台原生 WXML/WXSS 编译均退出 0，尚无真机验收或发布。
- 私有文件签名校验改用签名返回后的数据库当前时间，避免长事务起点导致错误过期。
  文件事实仍使用原时间规则，过期和超长链接仍拒绝；最终文件专项 **44 项通过**：
  `oam-file-signing-final.log`。

验证库仅为本机合成隔离库：`/tmp/oam-return-0105-trial.json` 为回滚副本，
`/tmp/oam-return-0105-sql-trial.json` 已有持久验收/封存历史；准确结果坐标在
`/tmp/oam-return-receipt-durable.json`。原 0104 历史保持冻结。0105 开发过程中的
空草稿版本经精确源校验后事务替换，原文保留于
`/tmp/oam-return-0105-installed-v{1,2,3,4}.txt`。
**禁止重置上述持久历史、重跑同一并发场景或强制降级。** 空迁移往返另在独立
PG16 集群验证，原函数/权限/表/触发器完整恢复：
`/tmp/oam-ci-reversal-pg16-isolated/receipt-migration-final.log`。

下一步接小程序整组验收表单、异常凭证、三码扫描、单次提交及 receive_return
恢复入口，再新增前向迁移实现独立库存入账和保管责任交接。当前仍需新版完整云端
门禁、真机/UAT 及生产验收放行；备案通过不代表这些门槛已完成。本批未部署、
未写生产库、未调用外部业务写入或发送通知。既存 AGENTS.md 修改及部署证据目录保留。

## 2026-09-13 PG16 旧回滚测试遗漏 0104 的修复

当前提交基线 `7cf8d0ea39877bf21b20822670d4f5a0f82bced2`。远端 `06e85c3`
的 PG16 `34743613777` 中，真实数据库任务 `103687372396` 已失败，静态任务
仍在运行。实际失败发生于旧 0097 逆向库存历史守卫的测试准备阶段：测试手写的
后续迁移列表从 0103 开始，漏掉 0104，导致 0103 的函数完整性校验拒绝继续。
完整原日志 `/tmp/oam-ci-06e85c-runtime.log`；不是账单或 runner 启动问题。

测试现在从 Alembic 当前 HEAD 自动取得到 0097 之前的完整逆序链，保留每个迁移的
原始降级前置检查、函数摘要、ACL 和历史数据保护。增加连续父版本顺序回归，
并纳入静态 workflow；没有修改任何已冻结迁移。

迁移顺序、workflow 拓扑及诊断共 16 项通过，日志
`/tmp/oam-ci-06e85c-fix-tests.log`。为避免改变旧合成库的跨库权限，使用全新独立 PG16 集群
`/tmp/oam-ci-reversal-pg16-isolated`（仅本机 55497）验证：完整升级至 0104，
事务内依次回滚 0104–0098，并成功进入旧 0097 降级；最终事务回滚后全部原函数
正文、所有者和 ACL 完整恢复。日志
`/tmp/oam-ci-reversal-pg16-isolated/check-final.log`。这验证了本次函数版本错误的
修复；旧历史注入场景和整个云端门禁仍需下一次完整运行确认。

接收验收功能仍在未提交开发：新增专用验收头/行/SN/异常、预览、原子提交和
原请求回查/互斥封存，复用 receipts 单头，保持库存不变。短少不消耗累计确认量，
破损为已接受量的子集；异常证据使用正式上传及精确原验收读取权限。
数量/SN 正常与重放专项 2 项通过；异常、短少后补收、原请求恢复和损坏历史专项
15 项通过，日志 `/tmp/oam-return-receipt-exceptions-final.log`。扩展组合回归运行中。
**尚无 0105 数据迁移或公开验收写路由，不能部署当前未提交改动。** 新 ORM 已包含
0105 计划字段，不能用它运行冻结 0104 业务库；后续必须新增完整迁移，并在独立克隆
验证 SQL 数量/SN/权限/审计守卫及真实两连接竞争，再接页面和独立入账。

## 2026-09-13 接收仓退回包裹查询与最新云端状态

从 `06e85c355696ce55dda4c5c5d82b4bbaddfaadfb` 接续，分支仍为
`codex/production-readiness-gates`。前批小程序分包功能及静态修复已经推送，远端
同 SHA。初次直连 GitHub 443 超时，随后只对推送命令使用本机系统已经启用的
HTTP 代理 `127.0.0.1:7890` 成功；未修改全局代理或业务 Edge 路由。

云端父提交 `5231c90` 的 PG16 `34738764566` 已完整结束：真实 PG16 子任务
**success，1 passed，6220.25 秒**，原日志 `/tmp/oam-ci-5231-runtime.log`。
其静态任务仍为 3568 通过、2 失败、3 跳过，所以整条旧 workflow 为失败。
两项旧迁移测试修复已随 `06e85c3` 推送。新提交的 Client `34743613798`
**success**；PG16 `34743613777` 的静态与真实数据库子任务均已启动，结果待定。
不在它运行期间再推送同分支，以免取消正在进行的验证。

新增接收方专用后端只读入口：

- `GET /api/v1/stock-returns/my-receiving`，按准确包裹 ID 分页。
- `GET /api/v1/stock-returns/my-receiving/{shipment_id}`，核验准确原包裹详情。

仅当前总部/区域负责人、运单指定接收人、当前有效区域仓保管人以及组织范围内的
库存/库存作业读取权限同时匹配时可读。原接收责任已结束或更换时阻断该包裹；
其他可核验包裹仍可展示。读取结束复核当前权限、仓库、责任分配、包裹范围、
库存与审计游标，拒绝读取期间发生变化的结果。

将退回运单的不可变事实核验与本人恢复接口的当前授权分开。内部历史核验只使用
原记录的用户/人员/版本坐标计算摘要，没有授权能力，也不加载原发出人的当前
登录身份。接收方响应只包含准确运单和明细；不公开发出人的库存账户、余额、
QR 原文、请求摘要或恢复键。原发出方的恢复接口仍要求本人当前权限。

本批未新增验收/入账写接口，也未登记任何真实签收、验收或库存交接。0104 迁移、
已有合成运单和封存事实保持冻结。验证记录：

- 原分包提交/恢复与接收查询组合 **58 项通过**：
  `/tmp/oam-return-receiving-regression.log`。
- 最终接收查询专项 **20 项通过**：`/tmp/oam-return-receiving-final.log`；原过账证据
  异常仅阻断对应包裹的补充专项 **1 项通过**：`/tmp/oam-return-receiving-isolation.log`。
- 真实接收人 API 身份、真实 HTTP、数量件和 SN 件原包裹，在 PostgreSQL
  `SET TRANSACTION READ ONLY` 下只执行 SELECT，所有库存及原单据保持不变：
  `/tmp/oam-return-receiving-pg-final.log`。本轮未创建或修改合成业务事实。
- 云端门禁拓扑和诊断 **15 项通过**：`/tmp/oam-return-receiving-gate.log`。
  新只读场景已接在永久分包及降级保护之后；所有原门禁保持。

下一步新增前向迁移实现接收仓的独立验收事实，准确关联本包裹的专用退回分包行，
再实现入账和保管责任交接。应支持正常/异常、数量/SN、分批及原请求恢复；不能
伪造需求或货源分配来调用正向需求收货接口。0104 当前仍主动拒绝普通收货/物流
记录直接挂到退回运单，后续仅通过完整新守卫开放已核验的专用关联。库存变化
继续走统一过账入口；接收查询通过不能视为区域仓已入账或原个人责任已解除。

## 2026-09-13 小程序退回交运分包与真实 PG16 SDK 验证

从 `cbfc10d1477b3651821834be299d885f9909293e` 接续，仍在原项目目录的
`codex/production-readiness-gates`。0104 迁移、既有合成分包和封存历史保持冻结。
本批没有部署、外部业务写入或通知发送。

新增 `pages/formal-stock-return-shipments/index`，从准确原退回或实物发出记录进入。
按原发出行选择数量或尚未装包的 SN，填写承运商、运单号和北京时间的实际交运
时刻（支持秒），核验整组明细后确认一次提交。原退回、实物发出和承运分包状态
分别展示；装完当前部分发出批次不能将整个原退回标为全部发运。接收仓验收、
入账与保管责任交接仍是后续独立动作。

新增严格的目录、历史、预览与恢复契约：校验原发出绑定、同一在途账户整组余量、
SN 所属及重复、物料精度、时间先后、身份权限以及 Python/JS 一致的原请求摘要。
提交前冻结完整确认内容；本机仅持久化最小 `stock_return/ship_return` 恢复标记，
不保存完整业务正文或幂等密钥。结果未知不重发，重启后只回查原请求或互斥封存。
离开页面、身份权限变化及不确定提交后清除草稿，保留原请求恢复记录。

本批验证全部结束并退出 0：

- 小程序全量 **988 项通过**：`/tmp/oam-return-shipment-mini-full-final.log`。
  新增契约与页面专项 **24 项通过**：`/tmp/oam-return-shipment-mini-final.log`。
- 本机微信开发者工具的原生 WXML/WXSS 编译通过，含新分包页及原退回、发出入口：
  `/tmp/oam-return-shipment-wxml-final.log`、`/tmp/oam-return-shipment-wxss.log`。
  这是原生编译证据，尚未进行真机页面验收或上传发布。
- 小程序真实 API 身份、真实 HTTP 与 PG16 共 **6 组场景通过**：
  `/tmp/oam-return-shipment-mini-pg.log`。数量件和 SN 件分别验证正常提交、
  服务端成功后响应丢失、到达前丢失并封存；每组仅一次业务 POST，目录/预览/回查
  只有 SELECT，原 GET 确认事实，事务完整回滚且原库存历史保持不变。
- 云端门禁接入上述六组 SDK 场景，排在永久发出竞争之前，避免使用已耗尽的原 SN；
  原竞争、降级保护及分包门禁均保留。拓扑与诊断 **15 项通过**：
  `/tmp/oam-return-shipment-mini-gate.log`。

父提交 `5231c90efae6df7f8695726752e11d5d70663289` 的 PG16 `34738764566`
复查仍在运行；其静态任务失败的两项旧迁移测试已经由 `cbfc10d` 修正，需在新提交
重新验证。该任务结束前不推送同分支，以免自动取消正在运行的真实数据库任务。
下一步继续接收仓验收、入账与保管责任交接；完整云端门禁、真机/UAT 和生产放行
仍需各自取得证据。

## 2026-09-13 退回分包目录、独立历史与云端静态门禁修复

从 `1a4ce5d17abec040de9f3563e343c2cf6affb4a2` 接续，分支仍为
`codex/production-readiness-gates`，开发位于原项目目录。0104 迁移、现有合成
运单和封存历史保持冻结；本批没有部署、外部业务写入或通知发送。

新增 `GET /api/v1/work-orders/{work_order_id}/returns/{operation_id}/shipments/options`
与同路径的 `GET .../shipments`。目录按准确原发出行返回已装包、剩余、在途持有、
未绑定运单及可选数量；SN 候选排除已装包项，历史仍保留原 SN。共用当前库存、
接收责任、物料策略、冻结及分包承诺校验，读取过程中权限或审计游标变化时拒绝
旧结果。尚未发出的退回返回空目录，不以空账户范围检查无关仓库的盘点冻结。

发运历史嵌入独立的原退回、取消及实物发出历史。完成状态按原退回的每条总数量
判断；只装完当前一小批、但原退回尚未全部发出时，仍为部分发运。分包登记和
查询均不产生库存入账，也不解除原保管人的责任。历史读取与新建分包使用不同权限。

已取得验证证据：

- 查询专项最终回归：`/tmp/oam-return-shipment-queries-final.log`。组合回归
  `/tmp/oam-return-shipment-queries-unit.log` 中原分包提交/恢复 39 项通过；初次
  新查询测试的两处身份模拟错误已修正，并在范围专项
  `/tmp/oam-return-shipment-query-scope.log`（2 项）重新通过。查询期间权限撤销
  专项 `/tmp/oam-return-shipment-query-revocation.log`（2 项）通过。
- 正式路由隐私、PG16 门禁拓扑和诊断 20 项通过：
  `/tmp/oam-return-shipment-query-routes.log`。
- 0104 已有六条合成发出候选，经真实 API 身份、真实 HTTP 和 PostgreSQL
  `SET TRANSACTION READ ONLY` 核验完整目录及历史，执行语句只有 SELECT，
  原始数量、SN 和库存快照保持不变：`/tmp/oam-return-shipment-queries-pg.log`。
- 另一条未装包的准确原发出行，在单事务内实际登记部分运单、读取目录/历史、
  验证非法物流绑定/超量/缺少审计均被原 SQL 守卫拒绝，最后完整回滚，原已提交
  数据保持不变：`/tmp/oam-return-shipment-query-partial-pg.log`。
- 一笔保留的尚未发出退回，在 PG16 强制只读事务下返回空候选及未发运历史：
  `/tmp/oam-return-shipment-empty-directory-pg.log`。未重置或降级测试库。

云端父提交 `5231c90efae6df7f8695726752e11d5d70663289` 的 PG16 workflow
`34738764566`：静态任务结束为 3568 通过、2 失败、3 跳过，耗时 48 分 40 秒；
真实数据库任务仍在运行。这次失败来自两个旧测试遗漏后续迁移的函数变化：
0027 原函数经 0102 精确替换，以及 0093 期初账户守卫经 0100、0103 演进。
测试现核验完整旧源、精确替换和当前摘要链，未改动迁移或放宽运行时校验。
修复专项 4 项通过：`/tmp/oam-ci-5231-static-fixes.log`；原云端完整日志
`/tmp/oam-ci-5231-static.log`。静态修复需随新提交重新跑云端，不能将父任务的
失败记为已通过。已有同分支真实 PG16 任务结束前不推送，以免并发策略取消它。

下一步实现小程序退回分包页面：从准确原退回进入、选择原发出行及 SN、登记
承运商/运单号/实际交运时间、整组预览确认、单次提交及 `ship_return` 原请求
恢复。随后继续接收仓验收、入账和保管责任交接。当前仍缺小程序本批真机验收、
完整云端门禁及生产放行证据。

## 2026-09-13 退回承运运单与分包（0104）

从 `5231c90efae6df7f8695726752e11d5d70663289` 接续，仍使用
`codex/production-readiness-gates`。本批开发位于原项目目录，应用中的旧
`codex/mini-post-recovery` worktree 未修改。备案已由用户确认通过；本批没有部署
或写入外部业务系统。父提交的 Client `34738764565` 已通过，PG16 `34738764566`
已确认真实数据库和静态回归两个 runner 同时运行，尚未取得完整结束结果。

新增 `20261014_0104`，复用不可变 `shipments` 运单头，以三个专用表关联准确的
退回发出行和 SN，不伪造需求申请或货源分配。分包累计不超过原发出量；同一在途
账户的整组选择还受尚未绑定运单的实际持有量约束。同一原发出行的 SN 不能重复
装包。实际交运时间不能早于发出及接收责任生效时间，也不能晚于当前时间。
运单登记不产生库存流水，不解除发出人的保管责任；签收、接收仓验收和后续入账
需要各自的正式事实。现阶段拒绝把原正向需求的物流或收货接口绑定到退回运单。

新增正式接口：
`POST /api/v1/work-orders/{work_order_id}/returns/{operation_id}/shipments/preview`、
同路径的 `POST .../shipments`、`GET .../shipments/by-request/{request_id}` 和
`POST .../shipments/by-request/{request_id}/seal`。新增 `stock_operation/ship_return`
最小权限。请求标识与原提交、取消、发出共用操作者命名空间；响应丢失只回查原键，
不存在的原请求可互斥封存，不自动重放。所有响应禁用缓存，不返回二维码原文。

分包不推进库存流水，因此以独立审计顺序固定每次分包之前的承诺量，历史回读同时
使用原库存游标，避免同一库存游标下多包混淆先后。0104 前向替换三个原完整摘要
已核验的守卫，新增两个私有函数、18 个触发器；既有 0100–0103 迁移保持原样。
运行时 ACL、函数和触发器清单、HEAD、权限预期及 CI 清单同步扩展。

已核验：

- 31 项服务/输入契约通过：`/tmp/oam-return-shipment-unit.log`（23 项）及
  `/tmp/oam-return-shipment-contract.log`（8 项）；HTTP 恢复与坐标 8 项在回归中通过。
- 最终数据库安全、退回恢复、迁移与门禁配置回归 366 项通过：
  `/tmp/oam-return-shipment-final-regression.log`（退出 0）。正式路由的框架错误隐私
  专项 5 项通过：`/tmp/oam-return-shipment-production-route.log`。
- 5 项迁移边界与 SQL 检查通过；全量 SQLite 升级、ORM、权限及降级和 HEAD 图
  检查通过，`/tmp/oam-return-shipment-schema.log` 与
  `/tmp/oam-return-shipment-schema-final.log`。前一日志中的旧 HEAD/权限排序断言
  失败已经修正并由后一日志通过，不作为尚未解决的代码错误。
- 独立 PG16 克隆升级及运行时安全检查通过：`/tmp/oam-return-0104-upgrade.log`。
  元数据 `/tmp/oam-return-0104-trial.json`；原 0103 数据库未修改。
- 数量/SN 真实 API 身份提交、SQL 守卫、精确重放和恢复通过：
  `/tmp/oam-return-0104-smoke.log`（退出 0）。真实 HTTP、SELECT-only 回查、非法
  物流绑定/超量/缺少审计拒绝及完整回滚通过：
  `/tmp/oam-return-0104-rollback-gate.log`（退出 0）。
- 真实两连接数量/SN 重复提交、封存先到和提交先到均通过，以
  `pg_blocking_pids` 确认实际锁竞争。`0.375 + 0.375 + 0.250` 完成一条原发出行，
  没有新增库存事务。空 0104 升降级保留旧事实；已有永久分包/封存后降级被拒绝，
  历史和运行时权限不变：`/tmp/oam-return-0104-commit-gate.log`（退出 0）。

0104 测试库现有永久运单及封存历史，迁移及已提交合成事实保持冻结，不得重置、
修改旧迁移或重复执行整批提交竞争。后续迁移继续复制新逻辑库。下一步补退回分包
目录、独立历史列表及小程序页面，然后实现接收仓验收、入账和保管责任交接。
当前后端专项通过不等于真机/UAT、完整云端门禁或生产放行通过。

## 2026-09-13 云端门禁串行超时修复

本地 `2cbdc35f6b607d9d1d099beb2ff1d28366ad73ca` 已完成退料实物发出页面。
旧基线 `6b30e917972fdae0deec83ec04a8f8bc0b82de0c` 的 PG16 `34733606663`
现已结束，GitHub 明确标注超过 `2h0m0s` 总时限。真实数据库阶段 **1 passed**，
耗时 4386.65 秒；静态回归到 63% 时被取消，不能声称静态回归通过。
原完整日志 `/tmp/oam-ci-6b30-pg.log`。这次不是账单或 runner 启动故障。

将真实 PG16 迁移/并发和原静态安全回归拆为两个独立 runner，同时运行。保留原
`postgresql16-release-gate` 汇总检查名，只有两项均为 success 才成功；失败、取消、
跳过、超时或结果缺失均拒绝通过。原静态测试文件与运行参数全部保留，两个任务
使用相同已固定的 Python、Node 和依赖版本，静态任务不依赖临时 PG 库。
真实门禁保留 180 分钟上限，静态门禁单独 120 分钟，汇总 5 分钟。

后续退回运单/分包已开始准备 ORM 和输入输出结构，目前属于未提交开发，尚无
0104 迁移或可发布入口。计划复用 `shipments` 通用单头，用专用原发出行关联，
保留数量/SN 分包边界，不伪造需求分配。现有 0103 迁移与已应用合成历史继续冻结。

## 2026-09-13 退料发出目录、并发验证与小程序页面

从本地 `6a25d2218b76b8056eec0c73d8b96a25adb3a44c` 接续，分支仍为
`codex/production-readiness-gates`。0103 迁移已经应用并产生永久发出事实，保持冻结，
本批不修改旧迁移。备案已通过；本批开发和验证均未部署或写入外部业务系统。
父基线 0102 的 PG16 `34733606663` 已运行超过 110 分钟且仍在静态回归；本批
新增真实发出/封存竞争及六组 SDK 场景，故将后续 job 总时限从 120 提高到
180 分钟，保持全部检查不变。这不代表该运行或本批完整云端门禁已经成功。

新增本人原退回的发出目录和独立历史接口：
`GET /api/v1/work-orders/{work_order_id}/returns/{operation_id}/outbounds/options`
与同路径的 `GET .../outbounds`。按准确原行展示已发出、剩余、当前持有和可选数量；
已发出 SN 从候选移除但保留在原批次历史。取消事实独立，原退回仍保持已提交状态。
目录核验当前库存、期初、追踪策略、冻结和接收责任；权限和游标变化时拒绝旧结果。
目录沿用既有期初证据的库存/任务/审计锁协议，不写业务事实；不能将它称为无锁查询。
发出历史另在 PostgreSQL 强制 READ ONLY 事务中通过核验。

小程序新增 `pages/formal-stock-return-outbounds/index`，从原退回记录进入。
支持按准确原行分批选量、重新扫描实物三码、选择实际发出日期及北京时间、服务器
预览、整组确认、单次提交和每批历史。已取消或全部发出时不再提供新发出草稿。
库存进入在途位置与承运、签收、接收仓验收、入账仍分别处理，保管责任不自动解除。

发出恢复标记使用 `stock_return/outbound_return`，保存原工单/退回 ID、操作者、
请求摘要及计划摘要，不保存 QR、库存正文、实际发出时间或幂等密钥。与原提交、
取消共用工单级恢复锁；写入超时不重放，只读取原请求或经服务端互斥封存。
身份/权限变化、离开页面、刷新和不确定提交后均清除页面内存草稿。时间摘要保留
完整六位微秒，数量使用整数精度计算，不跨物料累计数量判断完成。

已完成验证：

- 后端目录、发出及恢复 47 项通过，`/tmp/oam-return-outbound-queries-unit.log`。
- 真实 PG16 六组多连接提交竞争通过：数量/SN 重复发出，封存与发出两种先后，
  取消与发出两种先后。通过 `pg_blocking_pids` 证明实际锁竞争及唯一持久结果，
  `/tmp/oam-return-0103-concurrency-pg.log`（退出 0）。
- 真实 HTTP 读取七笔永久退回，验证完整发出、已取消、已封存及
  `0.375 + 0.625` 分批转全部发出，所有读请求只发 SELECT 且不改库存事实。
  失败降级后全部历史、函数与权限保留，`/tmp/oam-return-outbound-queries-pg.log`。
- 历史接口的 PostgreSQL 强制只读事务验证通过，
  `/tmp/oam-return-outbound-history-readonly-pg.log`（退出 0）。
- 小程序全量 964 项通过，`/tmp/oam-return-outbound-mini-full-final.log`；新增契约与
  页面专项 22 项通过，`/tmp/oam-return-outbound-mini-final.log`。涵盖过量、重复 SN、
  汇总同一账户、时间/摘要篡改、取消、响应丢失、权限变化、隐藏页面及扫码迟到。
- 小程序真实 API/PG16 跨语言六组验证全部通过，
  `/tmp/oam-return-outbound-mini-pg.log`（退出 0）。数量/SN 分别走正常提交、
  成功后响应丢失、到达前丢失并封存；真实 API 读取目录和历史，Python/JS 摘要
  一致，只发送一次库存 POST，以原 GET 确认，整笔回滚后库存及历史保持原样。
  验证使用新建合成工单，原旧工单过期时不改写其历史。

永久并发/分批场景的合成世界见 `/tmp/oam-return-0103-concurrency-worlds.json`，
其中准备的库存已经用完，不得重新执行整批种子或重置。原先回滚场景的合成世界
`/tmp/oam-return-0103-worlds.json` 仍可用于逐事务回滚测试。0102 和 0103 原库均
保持冻结，不能为测试清空或降级。下一步在本批真实接口验证及云端门禁通过后，
继续承运运单/分包、区域仓验收、入账与保管责任交接；小程序仍需真机验收。

## 2026-09-13 退料实物发出后端（0103）

从 `6b30e917972fdae0deec83ec04a8f8bc0b82de0c` 接续，仍在
`codex/production-readiness-gates`。该 SHA 的 Client `34733606656` 已成功；
PG16 `34733606663` 仍运行中。后续推送须先核验该运行已结束，避免取消前一轮门禁。
本批没有部署或外部业务写入，备案通过不替代发布验收。

新增 `20261013_0103`：独立的退料发出单、明细、SN 核验事实和
`stock_operation/outbound_return` 最小权限。实物从本人原退料占用账户移到已选定、
已真实建账的在途位置，保留资产归属及发出人的保管责任；支持按原明细分批发出。
发出、物流、区域仓验收与后续入账仍是独立事实。原退料已经取消、数量超出原明细、
SN 不属原退料、范围/权限失效或存在库存冻结时拒绝发出；已有发出不能整单取消。

正式 API 增加发出预览、提交、原请求查询和封存四个入口，均绑定原工单与原退料。
提交、取消、发出共用操作者/请求编号命名空间，封存和迟到写入互斥。查询只读，
原响应丢失后回查原请求，不能自动重放。原退料的提交状态没有被改成收货或入账状态。

迁移保留既有不可变退料/封存事实，按原完整摘要前向替换四个守卫/账户准入函数，
新增两个私有 SECURITY DEFINER 函数和 16 个触发器；运行时清单、权限、审计触发器、
迁移 HEAD 与 CI 清单同步更新。库存摘要沿用统一服务的数量格式（整数 `1`，小数
`0.375`），业务预览中的数量仍保留三位小数。已有发出或发出请求封存时拒绝降级。

验证记录：

- 基础发出与既有请求恢复：35 项通过，`/tmp/oam-return-0103-unit.log`。
- 新发出请求的服务/HTTP 恢复、只读回查、权限及跨操作封存：16 项通过，
  `/tmp/oam-return-0103-recovery.log`。
- 原接口、目录、数据库安全及迁移回归：344 项通过，
  `/tmp/oam-return-0103-regression.log`；最终函数摘要修正后安全专项 311 项通过，
  `/tmp/oam-return-0103-final-security.log`。
- 全量 SQLite 升级/ORM/权限/降级和 HEAD：3 项通过，
  `/tmp/oam-return-0103-final-schema.log`；新迁移 SQL、旧封存保留及新历史禁止降级：
  5 项通过，`/tmp/oam-return-0103-history-migration.log`。
- 真实 PG16 从冻结 0102 独立复制，元数据 `/tmp/oam-return-0103-trial.json`。
  前向迁移、运行时权限/函数/触发器检查通过；无发出历史时降级/升级可保留旧记录。
  原有区域未关闭盘点保持不变；新建合成区域真实完成个人仓和在途位置零实盘、两级
  复核、过账和关账，再通过统一库存服务准备数量/SN 库存。
- SN 实际发出、精确回放、SELECT-only 查询、SQL 封存互斥和审计缺失拒绝，以及整笔
  回滚通过，`/tmp/oam-return-0103-serial-pg.log`（退出 0）。数量分批最终复测也通过，
  `/tmp/oam-return-0103-quantity-pg.log`（退出 0）。

下一步：先补真实 PG16 多连接的发出/封存/取消竞争与永久发出历史降级证明，再补
退料发出目录和历史读模型，接小程序操作页面。小程序当前恢复标记只支持提交/取消，
发出标记需要自己的严格计划摘要和返回契约；不能伪装成取消请求。后续实现运单/分包、
区域仓验收及入账。当前后端专项通过不表示 UI、整个履约链或完整云端 release gate 已通过。

0102 原库保持冻结；0103 合成库已含真实期初与库存准备记录，不得重置。新迁移继续
复制独立逻辑库。每次 commit 和 push 前仍须运行仓库安全检查并等待退出 0；保持根
`AGENTS.md` 修改及四个 deployment 目录原状。

## 2026-09-13 在途位置正式期初及门禁断言修复（0102）

本批提交 `8223a69b9a959b0923bd0dd3f2c108a76fcce293` 已推送，核验远端同 SHA、
ahead/behind 0/0。Client `34733126633` 已成功；PG16 `34733126626` 在旧期初回填
目录检查处失败：HEAD 上仍期待 0101 的终态函数摘要。旧日志
`/tmp/oam-ci-8223-pg-failed.log`。后续修复让 HEAD 目录使用当前已审阅的完整函数
清单，同时保留历史迁移的原摘要；期初分支片段也按 0102 变更校验。已在冻结的
0102 合成库只读验证回填目录、全部期初函数/权限及范围守卫，退出码 0：
`/tmp/oam-transit-0102-head-gate-check.log`。完整云端门禁仍须再次运行。
退料发出 9 个开发文件已按原 SHA256 完整恢复，当前仍未提交。

从本地 `35bd714c0cfca49295c196a681a7bddf4ac191be` 接续，仍使用
`codex/production-readiness-gates`。备案已通过；本批没有生产部署或外部业务写入。
原项目路径和保留目录规则沿用下一节。

退料实际发出需要把实物移到所选在途位置，但既有期初只支持区域仓和个人仓。
本批增加 `20261012_0102`，前向扩展期初的启动、实盘、复盘、复核及过账核验。
在途位置必须直接属于同一归属的启用区域仓，执行人仍需要区域管理权限；不能用
工程师个人盘点权限代替。网页和小程序期初准备目录支持在途位置，目录选择不代表
已经建账。普通日常盘点目录本批未扩展。

迁移逐一固定 12 个既有库存函数和 readiness 的前后完整摘要、替换次数，并保留
函数归属、权限及配置。新增位置保护触发器阻止已建账在途位置及直属父仓的结构
被改写。已有在途盘点历史时拒绝降级；旧迁移文件未修改。新 readiness 为
`5fa738c93218aa8f0de434935f7c6a8f94ec5e71c99a9a283df3b64b142d0de9`。

已核验最终结果：

- 新迁移、位置权限、原迁移摘要衔接、HEAD 图共 16 项通过：
  `/tmp/oam-transit-opening-targeted.log`。
- 数据库安全和期初准备目录回归 468 项通过：
  `/tmp/oam-transit-opening-regression.log`。
- 全量 SQLite 升级与 ORM 结构、权限集合及降级一致性专项 1 项通过：
  `/tmp/oam-transit-opening-schema-match.log`。
- 小程序准备目录与状态 54 项、网页契约 29 项通过；TypeScript 与 Vite 构建通过：
  `/tmp/oam-transit-opening-mini.log`、`web.log`、`tsc.log`、`vite.log`
  （后三者同一 `oam-transit-opening-` 前缀）。
- 从 0101 合成库复制出独立 0102 逻辑库，旧库只读；元数据
  `/tmp/oam-transit-0102-trial.json`。真实 PG16 前向迁移及运行时安全验证通过；
  没有在途历史时 0102→0101→0102 成功并保留旧封存事实。
- 在新建合成区域走完零确认、区域复核、总部复核、过账和关账，库存流水、余额
  不增加；已建账在途位置/父仓改写被拒绝。已有历史后降级被拒绝，函数及权限
  完整保留，退出码 0：`/tmp/oam-transit-0102-lifecycle-fresh.log`。
  旧测试库里原有未完成盘点保持原状。该逻辑库现在含永久在途期初历史，后续
  不得重置或降级；新迁移试验继续使用独立复制库。

远端旧基线 `23bf1abbbd38d2e5431ba6dd04748a3c0268908a` 的 PG workflow
`34727525967` 已结束：真实 PG16 迁移/并发阶段成功；后续回归 3460 通过、3 失败、
3 跳过。失败是旧 HEAD、旧表/权限集合、旧账户准入函数摘要断言未随 0100 更新，
不是付款或 runner 启动失败。本批已修复并通过上述对应测试；仍须以本批提交的
完整云端门禁作结论。失败原日志 `/tmp/oam-ci-23-pg-failed.log`。

退料实物发出的服务代码已完成 19 项专项验证，包含部分数量、SN、准确幂等回查、
取消拦截及失败回滚：`/tmp/oam-return-outbound-unit-transit.log`。这部分尚未形成
可发布接口和数据库约束，作为未提交开发保留，不纳入本次独立期初提交。
为核验期初提交的 ORM，曾完整暂存该部分到
`/tmp/oam-return-outbound-wip-20260913-0102/manifest.json`（逐文件 SHA256）；
提交后恢复工作区，不得丢弃。下一步在新 0103 迁移完成退料发出事实表、SQL 校验、
原请求查询/封存及接口，再连接承运、接收、入账和保管责任解除。严禁伪造需求
分配来套用现有 shipment 外键；原退回请求也不能代表实物已发出或责任已解除。


## 2026-09-13 小程序工单退回、接收仓选择与独立历史

从本地 `5df7917546356e55092d5e5eb3762d1b37562591` 接续，分支仍为
`codex/production-readiness-gates`。所有本批编辑在原项目
`/Users/replace-with-local-user/Documents/Codex/2026-06-11/oam`（替换为本机用户）；
不要在旧的应用工作树复制开发。
本批没有新的 SQL 迁移，仍以不可改写的 `20261011_0101` 为数据库前提。

新增 `GET /api/v1/work-orders/{id}/returns/options` 与 `GET .../returns`。
前者提供准确原回收行以及所属区域仓的有效在途选择，核验唯一当前接收保管人，
来源和位置关系发生变化时拒绝旧结果，最后再核验库存游标。后者逐笔读取本人不可变提交和独立取消，
核验完整库存及审计证据；关闭或转派不抹去原历史。两者只读且禁止缓存。

小程序新增 `pages/formal-stock-returns/index`，从本人工单及待恢复记录进入。
已接通数量选择、SN 实物三码、目标选择、服务端预检、整组确认、提交、取消、
原请求 GET 及永久封存。确认明细在可滚动弹层中展示。来源不可用时仍可读取
已有历史；提交和取消各自使用当前独立权限，不把发出或接收当作已完成。
扫码草稿只在页面内存，离开、刷新、身份权限变化或写入结果未知时清除。
恢复标记复用原工单锁和持久化存储，只增加提交方案摘要/取消原单 ID，不保存
二维码、请求密钥或重发正文。写入一次后必须用原 GET 核验，404 不允许重发。

已完成验证：

- 新后端选择与历史 12 项通过：`/tmp/oam-return-options-history-final-cursor.log`。
  最后读取目标仓期间库存游标变化的数量/SN 两种情况均拒绝旧选项。
- 正式退回 HTTP 全部 21 项回归通过：`/tmp/oam-return-expanded-routes.log`。
- 小程序全量 941 项通过：`/tmp/oam-return-mini-full.log`。确认弹层完善后，页面
  7 项另行复测通过：`/tmp/oam-return-review-page-final.log`，不重复累加。
- 微信开发者工具自带编译器对新增页面和工单入口 WXML、新页面 WXSS 编译成功：
  `/tmp/oam-return-wxml-compile.log`、`/tmp/oam-return-wxss-compile.log`。
  这不等于真机扫码、视觉验收或微信发布。
- 真实 PG16 小程序 SDK 联调 12 种路径全部通过，已读取最终退出码 0：
  `/tmp/oam-return-mini-pg-complete.log`。
  测试用的是合成事务并在每例结束完整回滚；输入管道未关闭导致的测试进程
  退出超时已修复，旧失败日志不能当成最终验证记录。
- 最后补入库存游标检查后，数量/SN 的当前选择服务又分别在 PG16 API 角色下
  验证只执行 SELECT、准确来源/目标和完整快照回滚，退出码 0：
  `/tmp/oam-return-options-current-pg.log`。

真实 SDK 联调已纳入完整 PG16 workflow；新增 GET、预检必须证明只执行 SELECT，
所有提交/取消最多一次 POST，响应丢失用准确 GET 恢复，未送达通过封存结束。
更早的 0101 双连接提交与永久历史保护证据见下一节，不重置那些测试库。

远端已核验的基线仍为 `23bf1abbbd38d2e5431ba6dd04748a3c0268908a`：Client
`34727525941` 成功；PG16 `34727525967` 的真实迁移与并发阶段已成功，后续
回归仍运行。完整结论须实时核验，结束前不要推送而取消该次门禁。0101 与本批
准确提交 SHA 的完整云端门禁尚不能宣称通过。

下一步实现独立的退回实物发出事实，准确关联原退回行及 SN；增加发运能力时，
须同时阻止已发出的单据整单取消及原回收冲销。随后实现逐发运行的接收、入账
和个人保管责任解除，保留物流签收及异常的独立状态。现有 `shipment_lines` 强绑定
需求链的 `outbound_postings`/`outbound_lines`；扩展退回时须明确前向迁移，不能伪造
需求分配来套用该链。备案已通过；尚未部署或
迁移生产、写入 OAM/RSC/飞书。既存 AGENTS.md 修改和四个部署目录继续保留。

## 2026-09-13 正式退回接口、原请求恢复与封存（0101）

从已推送的 `23bf1abbbd38d2e5431ba6dd04748a3c0268908a` 接续，分支仍为
`codex/production-readiness-gates`。新增正式退回预检、提交、取消、两类原请求
GET 与封存接口，已注册到正式应用；接入契约见 [STOCK_RETURN_API.md](STOCK_RETURN_API.md)。
本批核对时，上述 SHA 的 Client `34727525941` 成功，PG16 `34727525967` 仍运行。
不要在旧 PG 门禁结束前推送而取消它；本批准确提交与云端结论仍需实时核验。

原请求查询只读取不可变退回、取消、库存、审计及封存证据。404 仅表示未观察到，
孤立或矛盾证据按 503 拒绝，不能视为未执行。封存入口在账本、身份和工单锁下
核验原请求：已执行则返回原成功事实；未执行才追加永久封存与审计。请求标识
按本人跨提交/取消唯一封存，迟到请求即使改幂等键也不能执行。历史回查不把工单
关闭或转派视为原记录消失。提交、取消、读取仍使用各自独立权限。

迁移 `20261011_0101` 增加封存事实表和数据库双向互斥，禁止更新、删除和截断。
审计、身份、原工单/退回单及原请求摘要必须完整匹配。运行时清单现为该域 198 个
触发器、77 个函数，readiness 摘要
`61bff09975f03d0c8049253fc733692d6d3f5b4a633bb537e9418a96b582544a`。
0100 已有 SQL 未修改，0101 保留其账户创建准入。新迁移及 HTTP/并发检查已接入
完整 PG workflow，永久 0101 封存放在旧迁移降级证明之后，避免遮蔽旧约束。

接口复测修复了 `InventoryReadError` 错误属性名造成的 500，改为使用其正式错误
结构。路径工单、当前操作人和请求头须匹配正文；封存拒绝幂等键请求头。正式应用
的 401/404/405/422 等错误同样禁止缓存，输入校验不回显扫码内容。实测数据库
成功提交后故意丢失响应，返回 503 后可用原 GET 恢复，未重复执行库存动作。

已完成并读取最终退出状态的验证：

- 服务/权限/安全清单关联回归 330 项通过，迁移专项另 4 项通过。
- HTTP 专项分批合计 21 项通过；退回核心及既有冲销接口回归 38 项通过；原需求单
  输入隐私回归 1 项通过。对应日志为 `/tmp/oam-return-0101-routes-unit-fixed.log`、
  `framework-fixed.log`、`headers.log`、`preview.log`（后三者同一 0101 前缀），
  `/tmp/oam-return-0101-command-regression.log`、`/tmp/oam-return-0101-validation-regression.log`。
- 真实 PG16 数量/SN 的提交及取消均通过原请求只读回查、服务与 SQL 双向互斥、
  全业务快照回滚，日志 `/tmp/oam-return-0101-recovery-pg.log`。
- 真实 API 角色 HTTP 八种场景全部通过：数量/SN × 提交/取消 × 执行/封存，原 GET
  只执行 SELECT，核对准确结果且业务快照不变，日志 `/tmp/oam-return-0101-http-pg.log`。
- 真实双连接八种竞争全部通过：数量/SN × 提交/取消 × 封存先完成/执行先完成。
  检测到锁等待后才提交首事务，新连接回查一致，测试结束无净退回占用。空封存表
  0101→0100→0101 保留全部旧历史；新增永久封存后降级被拒绝，历史、函数、权限
  完整保留，日志 `/tmp/oam-return-0101-concurrency-pg.log`。

上述专项不替代本批准确 SHA 的完整云端门禁。另一次试图筛选 PG 门禁文件的静态
测试只得到 `1 deselected`，没有执行用例，不计入通过数。HTTP 门禁随后补入预检
路径断言；该预检接口已通过数量/SN 单元验证，新增真库预检断言待下一轮完整门禁。

本轮从只读 0100 合成库复制出独立 0101 逻辑库，元数据
`/tmp/oam-return-0101-trial.json`，仍使用本机 55487。原 0099 和 0100 库未改写。
**0101 独立库现已存在永久合成封存，禁止降级、清空或回改已应用的 0101 SQL。**
后续 SQL 修改须走前向迁移；只读或整笔回滚专项可继续，新全量门禁须另建隔离库。

下一步补齐退回区域仓/在途位置选择接口并接入小程序退回页及恢复记录，然后实现
实物发出、准确发运行的接收和保管责任解除。提交或取消不代表这些后续事实。
备案已通过；未部署、迁移生产或写入外部业务系统。继续保留既存 AGENTS.md 修改
及四个部署目录。

## 2026-09-13 退回核心提交与 CI 执行时限修复

正式退回核心已提交 `cb3c6df9c00fbd42f17f14d6af3fc4a7247e004b`。上一批
`43ede46` 的 PG16 workflow `34724454861` 现已结束：真实迁移与并发阶段成功
（23:05:51–23:32:32 UTC），随后服务回归于 00:05:04 UTC 被取消。GitHub
check-run `103636022057` 明确注释超过 `1h0m0s` 最大执行时间，并非本次已观察到
代码断言失败，也不能算全门禁通过。将完整任务时限从 60 调至 120 分钟，保留
全部动态与静态检查；Client `34724454860` 先前成功。

0100 的完整本地验证见下一节。工作树另有开发中的 0101 退回请求回查、封存及
新迁移：服务/权限关联测试 330 项通过，SQL 语法已解析，但尚未完成真实数据库
联调，不能纳入本次已验证核心的推送。推送只传输明确已提交的 Git 对象。

## 2026-09-13 正式退回单核心与库存占用（0100）

从已推送的 `43ede461e4001e4480a7d952d4d6405dba0277fd` 接续。退回来源校验
`e4ce410` 与旧门禁断言修复已一并推送，43ede46 的 Client 成功，PG16 仍需核对
最终 workflow 结果；不能用本轮隔离专项替代云端整轮结论。

新增正式 `stock_operation_orders`、`stock_operation_lines`、SN 证明与取消事实。
提交把准确原回收行的可用库存转为同一位置、所有组织、个人保管人、成色和批次的
`return_pending`；取消只释放本单尚未发出的整笔占用。原应退责任不会因提交或取消
消失。多笔部分退回按原回收行累计占用，同时按实际来源账户合计控制数量；SN
绑定原回收记录和实物三码。个人仓所属区域仓、在途位置与当前接收保管人分别验证。
已关闭工单可按本人独立退回权限处理；原记录回查不把关闭当成库存或责任完成。

迁移 `20261010_0100` 增加不可变事实、完整单据/流水/审计/状态/Outbox 联合校验、
原行历史数量预算、独立权限，以及准确 `return_pending` 账户创建准入。SQL 拒绝
绕过原退回单的普通移动和通用库存冲销；有活动退回时，原回收也不可直接冲销。
服务端预检和过账时重读来源、账本及保管人；不一致时整体拒绝。运行时清单现为
192 个该域触发器、76 个该域函数，readiness 摘要
`ffe5a9a8416226cd3bb3a2ac4d8bfcd6d3ed0c176fa66ed29e2c86cc5e74ca85`。

**本批是退回后端核心，尚未开放新的退回 POST、接入小程序、创建实物发出或接收
事实。** 返回的 `submitted` 是原提交状态，取消保留独立记录。接收前不能解除个人
保管责任；后续发运增加后，必须先阻止已发出单据走本批整单取消，再实现剩余数量
处置。新接口须完成准确原请求 GET、未送达永久封存与迟到写入互斥后再开放。

验证记录：退回/迁移/来源安全复测 40 项通过；部分数量专项 1 项通过；安全及迁移
304 项通过（含与前者重叠的迁移测试，不累加）；既有库存冲销回归 16 项通过。
前期扩大回归 122 项通过、3 项失败，失败夹具及旧清单问题已在上述复测修复。
日志 `/tmp/oam-return-0100-foundation-final.log`、`/tmp/oam-return-0100-quantities.log`、
`/tmp/oam-return-0100-security-complete.log`、`/tmp/oam-return-0100-reversal-regression.log`。
真实 PG16 数量/SN 场景均验证了提交、取消、精确重放、SQL 绕过拒绝、完整证据缺失
拒绝和全业务快照回滚，日志 `/tmp/oam-return-0100-business-final.log`。实际生产 API
角色的函数源码、触发器和最小权限检查通过，日志 `/tmp/oam-return-0100-runtime.log`。
真实双连接对数量/SN 分别验证相同键重放、不同键来源竞争和并发取消，四组均通过，
新会话回查一致；永久合成历史存在后降级被拒绝，全部历史及运行时约束保留。日志
`/tmp/oam-return-0100-concurrency.log`。以上结果均已读取完成状态，不是仅启动测试。

本轮从原 55487 合成测试库只读备份建立独立逻辑库
`rsc_pg16_release_gate_0052_backfill_return01005314026e9c`，连接参数仍是本机 55487。
元数据 `/tmp/oam-return-0100-trial.json`。已修正克隆库新建时的数据库 CONNECT/TEMP
权限，使其与门禁一致。0100 在该独立库已有永久合成退回/取消记录，**不得回改本版
SQL、降级或清空此库**；后续 SQL 修正必须另起前向迁移。原 55487 的 0099 库和
55492 未升级或改写。首次空 0100→0099 演练保留旧历史已通过；已有新事实后必须拒绝
该降级。新全量门禁需再建隔离库，不可对上述已留历史库运行旧全量包装脚本。

备案已通过；未部署、迁移生产或写外部业务系统。本批提交、推送 SHA 与云端结果
以实时核验为准。继续保留既存 AGENTS.md 修改和四个部署目录。

## 2026-09-13 修复当前迁移安全清单的旧门禁断言

`7360a38` 的 PG16 workflow `34721561587` 已结束：真实数据库迁移与并发阶段
通过；后续测试 **3377 通过、3 跳过、1 失败**。唯一失败是
`test_forward_readiness_manifests_match_head_hashes` 仍用对象身份断言当前安全
清单等于历史 0097；实际头版本与清单已经是 0099。改为读取 Alembic 实际单一
HEAD，对比该迁移的正式 readiness 摘要，保留各历史版本摘要验证。未改变生产
安全检查、SQL 守卫或已有迁移。云端失败日志 `/tmp/oam-736-ci-failure.log`。

退回来源校验已本地提交 `e4ce410c083b8af8ba925fe94ac7dc578fb7e4f7`，等待此修复
一并推送。工作树另有开发中的正式退回单提交/取消代码，尚未完成数据库约束和
整体测试，不纳入这次门禁修复提交；推送只发送明确已提交的 Git 对象。

## 2026-09-13 正式退回来源与整批选择预检

从已推送的 `7360a38b131ef3a1979e11ce07358f783d40d444` 接续。新增
`GET /v1/work-orders/{id}/return-sources` 与
`POST /v1/work-orders/{id}/return-sources/preview`。完整核验原回收/成对更换、
冲销、期初、当前库存、SN 流水、保管关系及本人来源权限后，逐原回收行返回应退
数量、同一账户可用数量与本行可选数量。原实物转为其他工单占用后，原应退责任
仍保留，可选数量归零；不能按同物料换账户、成色、批次或其他原行 SN 代替。

整批选择逐行绑定准确原回收行及来源账户，按账户合计校验共享数量，整批 SN
去重并验证实物三码，同时检查物料策略及来源/未来 `return_pending` 维度硬冻结。
读取期间来源、账本、保管关系、审计或权限变化时丢弃结果；预检末尾重新核验
完整来源、策略及冻结。已关闭工单仍可按本人读取权限准备独立退回；来源停用、
过期、期初未建立、历史责任不明或冲销待核验会阻断选择。响应只含 SN 展示信息，
不回传二维码；成功和业务错误均为 private/no-store。

`planning_status=source_selection_only`，`selection_hash` 仅绑定来源选择，
`basis_hash` 绑定当前来源、权限、库存及准确物料策略，不是未来退回命令的
幂等摘要或写入授权。时间证据统一 UTC，避免同一时刻因数据库会话时区改变摘要。
本轮没有创建退回单、移动库存、产生接收或解除责任，没有接入旧 Transfer 原型。
数据库仍为 0099，未新增/修改迁移与运行时源码清单，尚未接入小程序退回提交页。

验证：退回专项 **58 项通过**，关联工单/结束检查/原操作查询及接口回归 **129 项
通过**；UTC 与策略摘要修正后 **7 项定向复测通过**（与专项重叠，不累加）。日志
`/tmp/oam-return-sources-unit.log`、`/tmp/oam-return-sources-regression.log`、
`/tmp/oam-return-sources-basis-regression.log`。隔离 PG16 55487 数量/SN 两种场景
的最终联调均通过，包含跨会话时区及已有库存夹具，日志
`/tmp/oam-return-sources-pg16-final.log`。真实 API 角色验证
HTTP 查询和预检只执行 SELECT、业务快照不变，合成库存/身份/操作变化事务回滚。
新专项与动态联调均加入 PG workflow；不能在已有永久历史的 55487/55492 上重跑
全量或降级，后续专项继续使用事务回滚。

上游 `7360a38` 的 Client `34721561584` 成功；PG16 `34721561587` 的真实迁移与
并发阶段成功，后续测试步骤仍在运行，尚不能宣称整个 workflow 通过。本批实际
提交、推送及云端结果须以实时核验为准；不得为推送本批而取消上一轮 PG 门禁。

下一步实现正式 `stock_operation_orders` / `stock_operation_lines` 退回命令与
不可变行来源，重新核验收货仓、在途仓和有效保管人绑定。退回占用、实物发出、
对方接受及责任解除分别记账；接收必须关联准确发运行、原回收行、数量/SN 与
统一过账流水，未接受的差异继续保留在途责任。新写权限及数据库约束须独立建立，
不能沿用本轮读取权限作为退回提交授权。源行已有活动退回后，还须阻止工单回收
冲销绕过其责任链。备案已通过，尚未部署或进行外部业务写入。

## 2026-09-13 小程序整笔冲销确认与原请求恢复

从已推送的 `91571df619ef077a5418a1c35ce08ae566b44263` 接续。新增本人原操作
`GET /v1/work-orders/{id}/material-reversals/originals`：完整核验原记录与反向证据，
普通操作逐笔列出，成对更换只列一个整体，已完整冲销的原记录标记为不可选择。
列表是原始历史，不代表当前库存允许冲销；过大或证据不一致时拒绝返回部分候选。

小程序可从核验后的原记录选择整笔冲销，填写原因，重新预检并确认所有反向明细、
数量、批次、SN 状态变化及新旧件配对。占用、消耗、释放与成对回收的逆向效果分别
展示；实物退回、接收与保管责任仍须走后续独立链路。提交前持久化本人、工单、
请求 ID、权限版本、请求摘要及方案摘要；不保存幂等键、原因、完整方案或扫码内容。
一次发送后只接受原请求 GET 完整证据。断网、404、部分结果或权限变化保留恢复
记录；工单不在当前列表也可在本人待确认请求中回查或确认永久封存。页面离开、
刷新、切换身份及不确定提交均清理当前草稿；旧三类恢复记录保持兼容。

本轮同时修复三个门禁/恢复问题：

- 上游 PG16 `34719220933` 实际失败在 SN 越权拒绝的旧错误文本断言，数据库由
  0098 正确拒绝。断言已匹配新守卫；Client `34719220957` 成功。
- PG 会话为东八区时，登记提交与回查返回同一时刻的不同 JSON 表示。登记、封存
  及冲销结果统一序列化为 UTC，增加偏移时区原证据回查回归。
- 0097 历史污染迁移测试须在回滚事务内按 0099→0098→0097 还原函数前置版本，
  再验证拒绝旧单边逆向；不能直接在 0099 的函数上执行 0097 降级。未改已应用 SQL。

验证：小程序全量 **922 项通过**；扩大后端回归 **164 项通过**，时区与冲销 HTTP
专项另 **20 项通过**。日志 `/tmp/oam-reversal-mini-backend.log`、
`/tmp/oam-reversal-timezone-routes.log`、`/tmp/oam-mini-reversal-full.log`。
隔离 PG16 55487 上，真实生产 SDK 与 API 角色完成数量/SN 各六种联调用例，共
**12 种通过**：占用、消耗、释放、整组成对冲销，已执行但响应丢失后的恢复，以及
未送达后封存并拒绝迟到提交。查询/预检只执行 SELECT，整组结果及本地记录核验
通过，所有合成业务变化完整回滚。日志 `/tmp/oam-reversal-mini-pg16-final.log`。

55492 的前部全量验证从失败点分段续验后已走完所有剩余工单、登记、恢复、封存、
并发、降级拒绝及最终运行时源码/ACL 校验。分段日志为
`/tmp/oam-pg16-0099-full/local-result.log`、`/tmp/oam-pg16-0099-tail.log`、
`/tmp/oam-pg16-0099-preview-tail.log`、`/tmp/oam-pg16-0099-registration-tail.log`、
`/tmp/oam-pg16-0099-boundary-tail.log`。早期日志保留当时的失败，修复后未重跑已通过
的永久样本；这不是新 HEAD 的一次完整云端通过。新 SDK 联调已加入完整 PG workflow。

**55487、55492 均为 0099，均已有永久合成冲销及封存历史。禁止降级、清空或重跑
全量/旧续跑包装脚本。** 后续专项使用事务回滚；需要全量门禁时另建隔离库。
运行时保持 174 个触发器、74 个函数，readiness 摘要不变。
备案已通过，尚未部署、迁移生产或进行外部业务写入，尚未完成微信真机验收。
下一步继续拆回情况处置、正式退回单与接收解除保管责任，并跟踪本批实际 SHA 的
Client/PG16 门禁。继续保留既存 AGENTS.md 修改与四个部署目录；提交推送以实测为准。

## 2026-09-13 冲销原请求恢复、永久封存与正式接口（0099）

从已推送的 `13e37d1db671926baa1dfc92f97d87dd5dd4663b` 接续。
新增 `GET /v1/work-orders/{id}/material-reversals/by-request/{request_id}`、
同坐标 `/seal` 及 `POST /v1/work-orders/{id}/material-reversals`。查询核验
原父命令、整组反向流水与关联审计/状态证据；本人历史在工单转派、关闭、权限版本
变化后仍可按当前读取权限核验。孤立或矛盾证据返回待核验错误；404 仅表示本次
未观察到结果，不授权重发。结果含原操作/原配对、原因和全部反向操作/交易 ID。
封存绑定本人、准确工单、请求 ID 与内容摘要，不创建库存事实；同一原请求永久
禁止迟到写入，即使换用幂等键。已成功请求返回原整组结果。

迁移 `20261009_0099` 扩展封存类型 `reverse`，保留不可变封存表及 0094 审计证明，
新增两个延迟触发器，在账本锁内双向排斥冲销与封存。通用库存冲销入口继续关闭。
正式 POST 在完整预检摘要及权限锁校验后执行原子冲销，提交完成后才返回结果；
提交不确定必须按原请求回查。**小程序确认、持久恢复及封存 UI 尚待实现**，
后端接口开放不等于端到端验收或生产上线。

上游 Client `34717845419` 成功；PG16 `34717845446` 失败于测试仍匹配 `0090`
的跨工单预留错误文本，实际数据库由 `0098` 正确拒绝。已更新数量与 SN 的精确
错误断言；将新永久冲销/封存并发样本移到旧版历史降级证明之后，避免新历史先行
拒绝掩盖旧门禁。另修正本地 PG 非 UTC 时 0051 合成控制夹具的时间标准化。

验证：恢复/封存与历史回归 **89 项通过**；扩大数据库权限和迁移回归 **466 项通过、
1 项动态 PG 跳过**，两处版本清单旧断言修正后 **2 项复测通过**。隔离 PG16 API
角色通过数量/SN 的 SQL 双向封存互斥、单次 HTTP 提交/封存、准确 GET 回查、
GET 仅 SELECT 和完整业务快照回滚。真实双连接分别验证封存先提交、冲销先提交，
再由新会话完整回查。0098→0099→0098→0099 保留原历史验证通过；新永久封存
存在后禁止降回 0098 已验证。完整门禁另在新隔离库继续检查。

日志：`/tmp/oam-reversal-seal-regression.log`、`/tmp/oam-reversal-0099-security-regression.log`、
`/tmp/oam-reversal-seal-pg16.log`、`/tmp/oam-reversal-seal-commit-pg16.log`、
`/tmp/oam-reversal-0099-transition.log`。完整门禁当前日志为
`/tmp/oam-pg16-0099-full/local-result.log`，须回读最终结果，不得由专项通过推断全量通过。
运行时清单为 **174 个触发器、74 个函数**；readiness 摘要
`b243cded179bdc3db917929adedc477a32f34eef1d1988319d77529058f0e393`。

**55487 当前为 0099，已有永久合成冲销封存历史，禁止降级、清空或跑全量门禁。**
旧 0098/0099 transition 包装脚本仅作历史证据，不能再次使用。55492 是本轮独立
全量门禁库；55491 首次全量尝试因合成夹具时区失败已停止进程并保留文件。
备案已通过，尚未部署、未迁移生产或写外部业务。继续保留既存 AGENTS.md 和四个
部署目录。本批提交和推送状态须以实际 HEAD/工作树核对。

## 2026-09-13 工单完整冲销后端与 PG16 证明（0098）

从 `f6103ac68c82e6ea99d4584d617a1d5bf84709ef` 接续。新增不可变
`work_order_reversals` 父命令与 `work_order_reversal_items` 原操作/反向操作关联。
专用服务支持普通占用、释放、消耗、回收，以及整个成对更换的补偿；成对按先回收
反向、后消耗反向处理。统一过账入口在账本与引用锁内再次校验完整预检摘要，逐行
原样反向账户、数量、批次和 SN，写独立 `reverse` 工单事实、审计及 Outbox。
不会修改原流水或原配对；通用冲销仍禁止处理工单来源。

新增迁移 `20261008_0098`，数据库强制检查父命令、所有子操作、原始工单、准确
反向行及 SN 集合、方案和请求摘要、审计、状态事件和 Outbox。工单占用按包含反向
交易的每个账本前缀核算，不能借用其他工单的预留余额。SN 从原交易前的历史状态
恢复；曾消耗的拆回件恢复 consumed，首次登记拆回件恢复库外 active。身份登记
继续保留，采用有效首次入库标记约束再次回收的原工单/依据范围，不能因有历史
反向流水而跨工单逃逸。结束前检查核验完整冲销后撤销对应原回收待退回项，同时
把尚未重新回收的登记 SN 列为待回收。

**尚未开放面向客户端的冲销提交接口**。当前提交能力仅是后端服务和数据库约束，
预检接口继续返回 `preview_only`。下一步需补 `reverse` 原请求 GET 查询、永久
未执行封存及数据库竞争约束，再添加正式写路由、小程序确认和不确定结果恢复。
不能先把新服务直接接到页面按钮，也不能把内部服务返回值当成已完成 HTTP 提交。
之后继续拆回情况处置、正式退回单及接收解除保管责任链。

验证证据：

- 工单与库存扩大回归 **616 项通过**，一处新 SQLite 迁移夹具缺基础表；修正后
  连同 4 处权限清单旧断言、迁移和冲销写入专项 **19 项复测通过**。
- 迁移与数据库权限扩大回归 **464 项通过**，4 处上述旧断言在专项中修正通过。
  本次未宣称两个扩大全量在修正后全部重新执行。早期服务回归 73 项及扩大写入/
  结束检查 37 项通过，与上述回归有重合。
- 隔离 PG16 真实 API 角色已验证数量/SN 占用、消耗、释放逆向，首次登记拆回
  SN 整组逆向，以及后续跨工单更换逆向恢复此前 consumed 状态。缺父审计、伪造
  父内容、缺子操作、通用入口绕过与直接 SQL 单边逆向均拒绝，业务快照完整回滚。
  日志 `/tmp/oam-reversal-write-pg16-final.log`。
- 真实双连接并发已分别验证同键重放与不同键竞争原操作：均仅一笔提交成功，且
  新会话可完整回读。为证明真实提交，隔离库永久保留了两组**合成数量件**占用与
  冲销事实，实物库存和占用净变化为零。日志 `/tmp/oam-reversal-concurrent-pg16.log`
  前三行；该脚本末尾因预期降级拒绝的异常类型断言过窄失败，修正后单独重验
  `/tmp/oam-reversal-history-pg16.log` 已通过，未重复提交并发样本。
- 0097→0098→0097→0098 在提交新冲销历史前已验证保留既有库存、登记、封存和
  配对历史，运行时完整函数源码与 ACL 校验通过。当前清单为 **172 个触发器、
  73 个函数**；readiness 摘要为
  `c3b1e7dbd15f5e98e0e73820fce6d57a14fa7c4be2bcc7029e861f9b825bd61c`。

**本地 55487 当前实际为 0098，且已有永久冲销历史，禁止再运行降级或空库全量
迁移脚本。** 旧 `/tmp/oam-reversal-0098-transition.py` 和 migration 包装脚本仅是
历史证据，不能再次降级使用；新增专项应使用事务回滚，保留历史以验证后续升级。

本轮上游 `946bb1b` 的 PG16 `34713947142` 和 Client `34713947132` 均已成功。
本批完成后再推送并核验新 SHA，不能把上游成功视为本批云端门禁通过。备案已通过，
未部署、未迁移生产库、未进行外部业务写入。本批无前端修改，继续保留既存
AGENTS.md 修改及四个部署目录。

## 2026-09-13 专用冲销原操作与整批反向方案预检

从已推送的 `946bb1b8efa20df1e10f9750ee7ccac557bc8114` 接续。新增本人正式只读
`POST /v1/work-orders/{id}/material-reversals/preview`。请求必须准确选择一个原操作
或一个原成对更换，并填写原因。返回 `planning_status=preview_only`，包含原工单、
来源版本、权限版本、账本游标、原交易/原行、精确反向账户、SKU/成色/批次、数量、
占用增减、SN 原历史状态及配对。请求摘要与完整方案摘要分别绑定；不返回二维码。

占用冲销只可使用本工单仍持有的完整原占用，不能从共享预留账户借用其他工单库存。
释放和消耗的反向方案恢复原预留账户，回收的反向方案不改变占用。成对更换以父记录
为整体，按先回收反向、后消耗反向的顺序返回两笔明细；单独选择一个子操作被拒绝。
所有原操作均重新验证不可变明细、原请求证据、审计和 Outbox；已有反向交易、外部
经手人、后续 SN 流转、库存不足、冻结、停用、来源过期或权限变化均不返回部分方案。

SN 恢复目标从原交易前的完整流水计算。消耗反向恢复 active 及原预留账户；回收反向
对曾消耗的拆回件恢复 consumed，对首次登记拆回件恢复库外 active 并保留身份登记。
这两类状态不能简单统一重置为 active。纯数量、SN、不同 SKU 和批次+SN 均有验证。
预检前后比较库存、原始证明、主数据、策略、冻结与物料审计游标；公共审计快照读数
已抽出，避免未来结束检查与冲销证明互相导入。

实际写入仍未开放，0097 的数据库约束继续生效。预检没有创建库存账户、修改余额、
更新 SN、写审计或解除保管责任；方案摘要不是可重复使用的入账许可。下一步应复用
本批完整原对象解析，增加不可变冲销父命令/子关联和 `reverse` 工单操作，在账本与
原引用锁下重新计算，再由统一过账入口原子追加一笔或完整两笔反向交易、原操作关联、
审计及 Outbox。同步扩展 PostgreSQL 原交易证明、工单占用累计前缀、SN 生命周期重建、
原请求读取/未执行封存和结束责任检查；最后接入小程序确认与不确定结果恢复。

后续特别注意：首次登记 SN 的回收入库被冲销后，身份登记仍在，原工单待回收责任
不能被清掉。现有首次入库作用域只检查 `last_movement_id is None`；加入反向流水后
必须按“有效首次入库是否仍存在”判断，不能因为存在一条已冲销的历史入库就允许
跨工单逃逸原登记绑定。库存恢复、业务补偿、物理退回与保管责任解除仍分别取证。

隔离 PG16 55487（0097）已通过真实 API 角色 HTTP 预检与 SELECT-only 监听：普通
数量/SN 占用和消耗反向、首次登记 SN 成对更换、登记 SN 后续跨工单投入与回收另一个
曾消耗 SN，均返回准确方案，期间没有业务写入，最后完整回滚。日志
`/tmp/oam-reversal-plan-pg16-final.log`，该轮还使用完整原库快照核验每次 HTTP 预检
前后均无变化。扩大后端回归 **215 项通过**；共享审计读取抽出及新增批次案例后的
预检/结束检查 **69 项通过**（与扩大回归部分重合）。日志
`/tmp/oam-reversal-plan-final-backend.log`、`/tmp/oam-reversal-plan-shared-final.log`。
新测试已加入 PG16 workflow，本批无前端修改。

远端已恢复推送并精确回读为 `946bb1b`，本批开发尚未推送，避免取消正在进行的云端
PG16 `34713947142`。Client `34713947132` 已成功。未新增迁移、未部署或进行外部
业务写入；保留既存 AGENTS.md 和四个部署目录，55487 仍有不可变合成历史。

## 2026-09-13 工单冲销入口约束修复（0097）

从本地 `c7796c69a89081d4eea6a0268bc91c60e6b8e80d` 接续。检查专用冲销时复现：
通用 `reverse_inventory_transaction` 可以把原工单占用流水挂到另一个来源类型下
做反向交易，原工单占用与成对操作事实却没有相应补偿。SQLite 服务测试证明可写；
隔离 PG16 也证明原数据库约束可接受完整反向交易。PG 复现仅在测试中把账户权限
校验切换到合成人员既有的本人 `work_order_material.operate` 范围；正式工程师并
没有因此获得通用冲销权限。全部库存、审计、投影及 SQL 约束仍真实执行，最后回滚。

新增 `20261007_0097`，在现有延迟工单完整性证明中同时检查原交易与反向交易。
即使反向交易改用别的来源类型，原交易的工单来源或操作绑定仍会被识别并阻断。
服务层在原请求重放之前也检查相同边界。升级前遇到此类历史记录要求审查补偿，
不删除或改写历史。原函数 OID、所有者、ACL 和触发器保留；更新函数源码摘要、
运行时 readiness 与迁移图到 0097，不增加数据库权限。

当前这是阻止错误冲销的修复，**没有开放专用冲销提交功能**。下一步必须完整实现
普通原操作引用、整笔反向明细与工单占用归属；成对更换必须以父命令一次追加两笔
逆向交易、保留原新旧件对应关系，并按原账本状态恢复已消耗 SN。不能只删除本次
门禁、重置可变生命周期或直接调用通用反向接口来完成工单冲销。

隔离 PG16 55487 已从 0096→0097→0096→0097，既有库存、登记、封存及成对历史
保持不变，运行时完整源码与 ACL 校验通过。API 角色的数量/SN 占用、消耗、释放
直接 SQL 反向交易均被拒绝；成对更换的两个子交易分别单独反向也被拒绝。完整
统一过账入口绕过应用限制后仍被数据库拒绝。另在同一回滚事务中临时恢复旧函数、
插入旧漏洞数据，再验证升级因历史预检被拒绝，最终函数/ACL/数据全部恢复。
日志：`/tmp/oam-reversal-repro-pg16-final.log`、`/tmp/oam-reversal-migration-pg16.log`、
`/tmp/oam-reversal-boundary-pg16-final.log`。这些验证已加入全量 PG16 workflow。

同时补验证了登记 SN 的后续跨工单生命周期：原工单首次成对回收→另一个工单占用
并消耗→新成对回收，通过后仍可读取原身份登记证明。原代码已将登记上下文限制
在首次入库，不需要放松规则；此项新增服务回归覆盖，尚不代表真机 UAT。

验证状态：工单相关后端 **123 项通过**，通用冲销相关 **12 项通过**（与前述有
重合），迁移和权限静态专项 **42 项通过**。扩大迁移/数据库权限回归 **467 项通过**、
一处旧 down_revision 断言失败；修正到 0096 后，该项与迁移/诊断专项 **21 项复测
通过**。未重复运行已通过的 467 项。日志 `/tmp/oam-reversal-workorder-regression.log`、
`/tmp/oam-reversal-app-regression.log`、`/tmp/oam-reversal-static.log`、
`/tmp/oam-reversal-schema-regression.log`、`/tmp/oam-reversal-schema-followup.log`。

远端 `11bb356` 的 Client `34710289593`、PG16 `34710289604` 已全部成功。先前
推送 `c7796c6` 因 github.com:443 连接超时失败；之后精确远端读取确认仍是
`11bb356`，没有将未知结果当作推送成功。本次修复通过验证后统一提交并推送。
小程序和域名备案已通过；尚未部署、验收 UAT 或进行生产迁移。保留 AGENTS.md
既存修改及四个部署目录。55487 当前实际 **0097**，旧临时包装脚本中的 0096
断言已过期；仍禁止运行空历史全量迁移/低于 0096 的降级脚本。

## 2026-09-13 工单结束前物料责任检查

从本地 `8f71544b4b9f788cf067d343ae9889b3dcf4bd84` 接续。新增本人只读
`GET /v1/work-orders/{id}/material-completion-check` 及小程序“结束前物料检查”。
结果分别列出本工单未释放占用、身份登记后待回收 SN、已回收但尚无退回解除证明
的原明细，以及未记录配对的 SN 消耗所需的拆回情况核对。数量按原行及单位保留，
不跨物料求和，不把共享预留账户余额当作本工单剩余占用。

检查从库存交易、工单操作、原明细、成对父命令、身份登记及审计重证；历史交易与
操作集合须完整一致。占用扫描包含已无正余额的原账户，避免漏掉历史责任；外部
经手人范围、尚未适配的冲销历史、来源过期/停用、期初未建立均明确阻断。登记和
库存分开取证，既比较账本游标，也比较物料审计游标和当前工单/身份。证据缺失、
中途变化或历史超限返回失败，不使用部分列表判断完成。抽出普通操作历史证明
校验供本检查复用，原请求恢复仍使用原始请求摘要，不伪造原 request_id。

小程序按原物料、批次、成色、数量和 SN 展示待办；错误、离开页面、切换身份、
权限变化或后续提交会清除旧检查结果。本机有待确认/不可读取请求时，即使服务端
当前无未完成项，也先要求原结果核验。OAM 状态与物料责任分开，已关闭工单仍
显示未完成项；这个 GET 不关闭 OAM 工单，也不改变库存或个人保管责任。

验证：后端扩大回归 **133 项通过**（包括结束检查 28 项），小程序全量 **900 项
通过**（页面专项 61 项包含其中）；工单 WXML 编译通过。隔离 PG16 55487 的真实
SDK/HTTP 链路已验证“未释放占用→登记后待回收→成对入账后待退回”，包括响应
丢失恢复、未执行封存与完整回滚。额外 SQL 监听验证无历史工单的检查仅发 SELECT，
完整快照不变。新测试已加入全量 workflow。

验证中发现：现有个人仓期初重证会依次锁账本、盘点证据、身份及审计图，因此
严格 `SET TRANSACTION READ ONLY` 会因 SELECT FOR UPDATE 被拒绝。这是既有完整
证明路径，当前保留；未删锁或改为只看期初标志。业务读取没有数据写入，但不能
宣称读取不持锁。减少此类读取持锁、完整 MVCC 证据验证及 500 用户压测仍属于上线
前性能工作。日志 `/tmp/oam-completion-pg16-final.log` 先证明三条链路通过，末尾记录
严格只读事务拒绝；独立 `/tmp/oam-completion-read-pg16.log` 证明保留锁机制的
SELECT-only 检查通过。其他日志为 `/tmp/oam-completion-backend-final.log`、
`/tmp/oam-completion-mini-full.log`、`/tmp/oam-completion-wxml-compile.log`。

尚未完成：工单专用冲销（含已消耗 SN 生命周期及成对两笔逆向交易）、拆回情况
明确处置记录、正式退回单及接收确认。目前没有正式退回解除凭证生产链，故每条
已回收明细仍按待退回列示；后续必须按准确原行解除，不能用当前库存变少代替。
新登记 SN 在后续工单重用时的原登记上下文限制也需继续核验，不能只看首次回收。
本批是检查能力，不是整个工单闭环或上线完成。未新增迁移，未部署或操作外部业务。

上个已推送 SHA `11bb356` 的 Client `34710289593` 成功，PG16 `34710289604`
数据库阶段成功、静态回归仍在运行；其终态前继续保留本地提交，避免取消该运行。
保留既存 AGENTS.md 与四个部署目录。55487 实际仍为 0096，有永久合成历史，
继续使用回滚专项验证，禁止重跑空历史迁移脚本。

## 2026-09-13 小程序未知拆回 SN 登记与原请求恢复

从已推送 `11bb356e1dfcd58ddcdc14b8224b134c4cefa3da` 接续，小程序工单成对更换页
已接入独立的拆回 SN 登记。仅服务端明确返回 `412 removed_serial_not_found` 时
提供入口；先核对物理标签，再使用完整 SKU/SN/二维码、准确已存在批次和原投入
依据预检。确认页展示工单、投入 SKU/成色/批次、拆回件和 SN，明确只登记身份，
库存不增加。二维码只在内存中使用，不进入页面数据或恢复存储。

登记、普通物料操作与成对回收共享每工单恢复锁。确认前刷新本人权限和可操作占用，
先持久化并回读最小原请求记录，再发一次不自动重放的登记 POST。仅匹配原请求的
GET 登记证明或独立 `register_removed` 永久封存证明可以清理记录。网络超时、401、
精确未见结果 404、摘要不符、错误命令类型或存储异常均保留恢复边界。全局本人待
确认入口支持原工单关闭或不在列表时回读；未执行封存仍须明确确认。

正常登记后保留当前内存草稿，用户须再次识别拆回件、选择投入 SN 并独立确认整批
回收，不能把登记当作入库。结果不确定时清空物理草稿并停止后续新命令；离开页面、
切换身份、权限变化和迟到响应均有阻断。登记确认还显示投入成色及批次，以区分
同一 SKU 的不同库存依据。

验证：小程序全量 **893 项通过**，相关后端 **89 项通过**。本机微信开发者工具的
WXML 编译器已成功编译工单页面；这不代替真机交互验收。隔离 PG16 55487（实际
0096）通过真实小程序 API 适配器与正式 HTTP 验证普通 SN 正常登记、批次+SN
服务端执行后模拟响应丢失及恢复、未到服务端请求的精确 404 与永久封存三条链路。
Node 与 Python 的中文/emoji 请求摘要一致；登记只增加身份及审计，没有库存变化；
随后独立成对确认才产生一条父替换、两条操作和两笔库存交易。各场景均在强制检查
数据库延迟约束的事务内执行并完整回滚，最终快照相同。新增 SDK 联调加入 PG16
全量 workflow，在后续恢复测试耗用合成库存之前运行。

日志：`/tmp/oam-registration-mini-final.log`、`/tmp/oam-registration-client-backend-final.log`、
`/tmp/oam-registration-submit-pg16-final.log`、`/tmp/oam-registration-wxml-compile.log`。
上个 SHA 的 Client `34710289593` 已完整成功，PG16 `34710289604` 当前仍在数据库
阶段运行，尚不能宣称全量通过；其终态前不推送新 SHA，避免 workflow 自动取消。
本批未新增迁移，55487 仍有此前的不可变合成历史，禁止重跑空历史迁移脚本。

下一步按 V1.0 继续工单物料冲销、结束前未释放占用/待回收/待退回检查，以及退回单
解除个人保管责任。当前工单服务尚无完整结束检查，登记/消耗成功不能表示工单闭环。
小程序和域名备案已通过；生产部署、真机 UAT、迁移、对账及恢复演练分别验收。
保留既存 AGENTS.md 修改和四个部署目录，未操作生产或外部业务数据。

## 2026-09-13 拆回未知 SN 的受控身份登记后端

从已推送 `1174dd891210ff41821d174725e380bdd4433f1c` 接续。新增
`20261006_0096`：仅对当前本人有效、新鲜工单和仍有占用的投入依据，允许登记
已存在 SKU 的拆回 SN、二维码及已存在的准确批次。身份登记追加一条不可变登记、
一个 SN、一个二维码绑定和一条审计，不生成库存账户、余额、流水或通知。
登记后的首次库存入账必须属于原工单、原投入依据和本人个人仓的完整成对回收；
旧件/坏件成色以实际回收命令确认，之后退回解除保管责任仍是独立流程。

正式 API 保持无权直接 INSERT 序列号或二维码主表。受审计的登记表 BEFORE
触发器创建准确身份，内部函数禁止 API 直接调用。复用有界账户、保管关系、物料、
策略和批次锁，取得锁后重新预检；原请求沿用账本→当前身份→原工单锁顺序。
重复 SN/二维码、跨工单首次入库、普通非成对入库、缺审计及与永久封存共存均阻断。
原请求 GET 返回本人原登记或独立 `register_removed` 封存证明，不返回二维码。
工单改派/关闭不抹掉合法本人历史；提交结果不确定时只按原请求回读。

新增路径为 `/v1/work-orders/{id}/material-replacements/removed-registrations`，
包含只读 `/preview`、登记 POST、`/by-request/{trace}` GET 及其 `/seal`。
业务预检和库存投影错误返回稳定错误；数据库提交失败返回未确认，不宣称登记成功。
小程序的未知 SN 登记入口、单次提交、共享工单恢复锁和对应确认页尚未接入，下一步
优先实现。现有成对页面仍只接受已登记的拆回 SN，不能把后端完成称为整条功能已上线。

验证记录：迁移 SQL/PLpgSQL 解析、旧封存保留和新历史禁止降级均通过；SQLite
head 与 ORM 完整一致且可全降级。后端扩大回归 **454 项通过**；投影清单、迁移图
和登记迁移专项 **21 项通过**（与扩大回归部分重合，不相加）；部署与 PG16 诊断
**27 项通过**。未带 PG16 环境的主门禁用例跳过，真实数据库证据来自下述独立实例。
隔离真实 PG16 55487 已完成 0095→0096→0095→0096 往返，旧库存与封存保持一致；
运行时 ACL、函数源码和触发器清单检查通过。API 角色的 SN 及批次+SN 登记、原结果
HTTP 回读、不同 SKU 成对回收均通过；库存测试完整回滚。绕过应用检查后，数据库
仍拒绝重复标识、缺审计、普通首次入库和两个顺序的登记/封存冲突。并发 SKU、位置
和批次修改被锁住。另已提交合成登记/封存，验证真实迟到请求、独立只读会话 GET、
API/owner 不可修改/删除/清空及非空降级阻断，测试占用均用正式释放归还。

日志：`/tmp/oam-removed-registration-broad-final.log`、
`/tmp/oam-removed-registration-static-final.log`、`/tmp/oam-removed-registration-schema.log`、
`/tmp/oam-registration-migration-pg16.log`、`/tmp/oam-registration-runtime-pg16.log`、
`/tmp/oam-registration-atomic-pg16.log`、`/tmp/oam-registration-lot-pg16.log`、
`/tmp/oam-registration-commit-pg16-final.log`。迁移往返日志末尾的旧 `function_set`
失败已定位为运行时函数查询遗漏 `_0096`，补入后由独立 runtime 日志证明通过。

已核验上一 SHA 的 Client `34706862113` 成功；PG16 `34706862106` 数据库阶段成功，
静态阶段因四处测试仍指向 0094 清单/旧数量而失败。本批已修正历史与当前版本映射、
准确函数和触发器数量，并把新静态及真实数据库测试加入 workflow；持久登记测试放在
0093/0094/0095 历史降级证据之后，防止新阻断掩盖旧检查。新 SHA 空库全量 CI 尚待运行。

55487 当前实际为 **0096**，目录仍叫 `/tmp/oam-pg16-0093-suite`，已有不可变拆回登记
及封存，禁止再运行旧迁移往返或空历史全量脚本。后续沿用只读查询和回滚专项测试；
空库串联须另建隔离实例。备案已通过，生产部署、真机 UAT、迁移、对账和恢复演练仍
分别验收。本批未操作生产或外部业务系统，保留既存 AGENTS.md 和四个部署目录。

## 2026-09-13 小程序成对更换整批确认提交

从本地 `8f612beb01cc47ae407dc8a2f63e08e513c63ea3` 接续。我的工单物料页新增
“成对消耗与回收”，逐条投入登记拆回件，明确旧件/坏件、扫描 SKU、按策略填写
批次或扫描 SN/二维码，并逐件选择对应投入 SN。每个拆回行明确绑定投入行，
不按名称或数量推断配对；同一目标的多个 SN 合并成一条回收明细，重复数量目标
须先合并。所有投入 SN 与拆回 SN 必须完整、唯一且对应同一投入依据。

识别和整批预检均只读。确认前刷新本人工单和可用占用，逐行重新识别拆回件，
核验原身份、物料/批次/SN 绑定及管理策略，再核验完整父命令摘要。确认页逐行
展示投入消耗、拆回入库及全部 SN 配对；二维码只留在内存，不进入页面数据、
确认内容或恢复存储。改成色、批次或扫码使识别失效；删除投入件同时移除对应
拆回草稿，移除已选 SN 清除失效配对；离开、切换身份或操作类型均清空扫码草稿。

确认后，父命令沿用普通操作的每工单恢复锁，先持久化并回读最小原请求记录，
再只发一次不自动刷新/重放的库存 POST。仅父原请求 GET 中两笔库存事实的完整
证明或永久封存证明允许清理记录。超时、401、精确未见原结果 404、子操作证明、
摘要不符或存储异常均不能当作成功，也不能再发库存 POST。本人待确认入口继续
支持原父请求回读及明确确认后的未执行封存。

验证：小程序全量 **879 项通过**（`/tmp/oam-paired-submit-mini-final.log`）；页面专项
46 项通过（包含在全量中），覆盖数量、批次、多 SN 显式逆序配对、多投入明细、
后续行失败导致整批阻断、候选/策略/库存变化、重复点击、取消、权限撤销、退出、
迟到扫码及不确定写结果。隔离 PG16 55487（实际 0095）使用真实小程序 API 适配器
经正式 HTTP 分别完成数量和 SN 成对确认：识别/预检无写入，确认后一次父 POST
追加一条父替换、两条物料操作和两笔库存交易，父 GET 摘要与原客户端一致，最后
全部回滚、快照一致。日志 `/tmp/oam-paired-submit-pg16.log`，本次未消耗合成库存。
新 `pg16_work_order_replacement_submit_gate` 已加入全量 PG16 workflow 调用链。

已核验远端 `804b03a` 的 Client `34703680346` 与 PG16 `34703680337` 均完整成功。
本批应与此前两个本地提交一起正常推送，新提交 SHA 的全量云端结果需独立跟踪；
已有历史实例的本地专项验证不能替代新 0095 空库全量串联。

下一步继续未知 SN 受控登记、冲销与工单结束检查、退回解除保管责任及完整 V1
其余范围。小程序和域名备案已通过；真机 UAT、生产部署、数据迁移、对账和恢复
演练仍独立验收。保留既存 AGENTS.md 修改及四个未跟踪部署目录，未操作生产或
外部业务数据。55487 已有永久父封存，不能按空库降级；后续测试继续复用只读
查询和回滚事务，不能重放旧补库存或旧迁移往返脚本。

## 2026-09-13 成对替换原请求封存与迟到过账互斥

从本地 `7dc529a39d33fe8fc42e6cc0a2cac81ead61fe30` 接续。新增迁移
`20261005_0095`，在原不可变封存表中增加父命令 `replace`，原 0094 身份、审计、
不可修改/删除/清空保护保持原 SQL。新增两处延迟约束分别检查父替换与封存的
两个插入顺序，保证同一人、工单、原请求不能既过账又封存；更换幂等键也不能
绕过已封存原请求。运行时清单新增 2 个触发器和 1 个内部函数，并固定新源码 hash。
SQLite 约束变更保留原行、重装原不可变触发器；已存在父封存时禁止降级丢失语义。

新增成对原请求 `/seal`，先以同一账本→当前身份→原工单锁顺序读取两笔原交易。
已执行则返回完整原证明；未执行才追加封存及一条审计，绝不生成库存或通知事实。
普通三类命令与父替换各用独立操作类型，仍共享同一客户端工单恢复锁。原工单
改派、关闭或授权版本变化不抹掉合法本人历史；缺少权限、摘要不符或证据矛盾时
保留记录。父 GET 兼容此前已过账结构及 404，新增独立封存响应，POST 库存结构不变。

小程序本人待确认入口已支持父请求“结束未执行请求”：精确 404 只表示目前未见
原结果，需用户确认后发一次不自动重放的父封存，再 GET 核验才清理；普通 404、
超时、401、子操作证明或不匹配结果均不能清理，也不能转成再次扣库。后台已执行
原请求会直接返回两笔库存证明。退出页面、切换身份或权限变化继续阻断后续动作。
完整成对库存确认提交页面仍待实现，未知 SN 受控登记和后续 V1 范围继续推进。

验证：后端扩大回归 **198 项通过**；小程序全量 **868 项通过**；最终迁移图、
SQLite head/ORM 完整一致性及全降级、PG16 诊断 **16 项通过**。独立 0095 迁移与
旧记录保留专项 5 项为补充范围，其中 4 项与其他回归重合，不合并计算总数。
真实隔离 PG16 55487：0094→0095→0094→0095 保留旧库存及普通封存；数量/SN
完整父证明、绕过应用检查后的两个插入顺序、真实有库存占用的并发迟到 POST、
提交后新会话及 HTTP 回读、API/owner 不可变保护和有父封存禁止降级均通过。
并发测试先用正式占用准备，再用正式释放归还；封存本身仅增加一条审计，不改变
库存。另主动消耗合成测试库存并经 API 入账补回，覆盖全套 CI 后库存耗尽的准备
路径；重复准备不增加库存。实际封存响应由小程序校验器及共享存储恢复通过。

日志：`/tmp/oam-replacement-seal-broad.log`、`/tmp/oam-replacement-seal-mini-final.log`、
`/tmp/oam-replacement-seal-final-migration.log`、`/tmp/oam-replacement-seal-schema-final.log`、
`/tmp/oam-replacement-seals-pg16.log`、`/tmp/oam-replacement-seals-pg16-integration.log`。
55487 现在确实为 **0095**，目录名称仍 `/tmp/oam-pg16-0093-suite`，已有永久父封存。
禁止重复运行 `/tmp/oam-replacement-seals-pg16.py` 的原 0094 升降级前置步骤，也不能
再把它作为空历史数据库；合成库存已通过流水恢复到后续测试可用状态。

全量 PG16 workflow 已加入新静态和真实并发门禁：父封存原子测试在普通恢复耗库
之前，永久父封存放在旧 0093/0094 降级拒绝证据之后，避免覆盖历史断言。新 SHA
完整空库串联尚未完成，不能用这批有历史实例的专项结果替代。远端 `804b03a` 的
Client `34703680346` 成功；PG16 `34703680337` 数据库阶段成功、静态阶段仍运行。
继续等待其完整终态后再推送后续本地提交，避免 cancel-in-progress 取消旧证据。

下一步接成对替换整批确认提交页面、逐行拆回草稿和新旧 SN 配对，再继续未知 SN
受控登记、冲销、工单结束检查、退回解除保管责任及完整 V1。备案已通过，生产
部署、真机 UAT、数据迁移、对账和恢复演练仍独立验收。保留既存 AGENTS.md 与四个
未跟踪部署目录，本批未部署或操作生产/外部业务数据。

## 2026-09-13 小程序成对命令契约与共享原请求恢复

从已推送 `804b03a857531936ff6747d73d3d9b8673673856` 接续。小程序新增成对
消耗/旧坏件回收的完整命令摘要、拆回件扫描响应及预检/原结果校验。保留三位
精确小数，完整检查每条投入都有回收依据、新旧 SN 唯一且逐一对应同一投入行，
SKU/SN/二维码均进入原命令摘要。批次、成色、数量、配对或任一码改变都不能沿用
旧预检。二维码仅来自本次实物扫描并留在内存，服务器候选响应不可带二维码。
数量、批次、SN、批次+SN 四种策略均覆盖；未知 SN 仍需后续受控登记，不自动入账。

成对请求沿用普通命令的每工单唯一恢复键、持久化回读和互斥锁，独立使用
`work_order_replacement/replace` 类型；未扩大普通占用/消耗/释放枚举。已存在
成对待确认请求时普通提交在发送前阻断，反向覆盖也禁止。本人待确认请求入口
可读取列表外原替换，只有完整父请求 GET 证明及当前身份再次核验才清理原记录；
404、超时、子操作证明、仅 POST 结果和存储异常都保留。成对封存尚未实现，页面
不显示其封存按钮，直接调用也不会误向普通子接口发送封存。正式成对库存提交
页面尚未开放，下一步先完成独立数据库封存证明，再接整批确认提交与恢复流程。

验证：小程序全量 **862 项通过**。新增契约专项 **12 项通过**，为全量中的子集，
不可相加；另补 Python 独立生成的 Unicode/SN 父命令摘要固定向量并复核通过。
真实隔离 PG16 55487 使用实际小程序模块生成数量/SN 成对请求，正式 HTTP 预检、
过账及原 GET 结果均由客户端校验器验证，原证明可经共享恢复存储精确清理；
预检无账户/库存写入，全部业务测试事务回滚，库存、SN、审计及 outbox 快照一致。
测试 JSON 只含合成业务数据，Node 子进程未获得数据库环境凭据。新客户端契约
校验已接入既有 PG16 preview gate，小程序测试由 Client workflow 全量执行。
日志：`/tmp/oam-work-order-replacement-contract-mini-final.log`、
`/tmp/oam-work-order-replacement-canonical-final.log`、
`/tmp/oam-work-order-replacement-contract-pg16-final.log`。

远端 `804b03a` 的 Client `34703680346` 已成功；PG16 `34703680337` 仍在运行。
待该 PG16 完整终态后再推送本批，避免 cancel-in-progress 取消旧证据。不要重复
启动已通过的本地 PG16 事务测试或误对 55487 执行空历史降级，它已有 0094 封存。
后续继续成对封存、最终提交页面、未知 SN 登记、冲销、工单结束检查及退回解除
保管责任；完整 V1、真机 UAT、上线部署、对账和恢复演练仍分别验收。
保留既存 AGENTS.md 与四个未跟踪部署目录，本批未部署或操作生产业务数据。

## 2026-09-12 成对回收整批预检与准确拆回件识别

从 `62fb6707c098539555ac4a657cf2edcd6189ae6f` 接续。新增成对替换的只读整批
预检与拆回物料识别接口：先核验本人工单消耗明细，再按准确 SKU、批次及 SN
三码核验拆回件和原占用保管维度。预检沿用实际过账规则，覆盖策略、库存游标、
工单来源、当前授权及源/目标冻结；回收目标账户尚不存在时也不创建账户或库存。
未知 SN、已在库 SN、错误批次及不匹配维度均阻断，未知 SN 需后续受控登记。
返回完整成对命令摘要及安全候选信息，不返回原始二维码；两接口均禁止缓存。

验证：工单、成对回收、预检、来源与部署回归 **213 项通过**。真实隔离 PG16
55487 用 API 角色经正式 HTTP 分别验证数量件和 SN：扫码与预检不改变库存、
账户、审计等快照；随后实际成对过账与原请求 GET 的摘要一致；全事务结束后
库存、SN、流水、审计、配对与 outbox 完整回滚。新检查已纳入 PG16 workflow。
日志：`/tmp/oam-work-order-replacement-preview-regression.log`、
`/tmp/oam-work-order-replacement-preview-pg16.log`。本批未改前端，844 项仍是前批
确认提交的小程序结果，不能作为新客户端功能验证。

已核验远端 `c6fcf99b574e0a9c1cdf9e42fe3629c76d2d93fb` 的 Client
`34700216953`、PG16 `34700216972` 均完整成功，可推送后续本地批次启动新 SHA CI。
新 SHA 的云端结果需独立等待，不用本地回滚测试替代全量门禁。

接续：成对命令客户端摘要、扫码和预检契约；按同一工单共享普通命令与替换的
恢复互斥；替换原请求永久封存及确认提交页面。当前不支持多条回收明细落入同一
目标账户，须先合并；若多个投料来源需共享目标，必须同时设计来源/配对追溯和
数据库证明，不能只让预检放行。未知 SN 受控登记、冲销、工单结束检查、退回、
库存作业及完整 V1 剩余链路继续推进，生产部署与真机 UAT 仍单独验收。
保留既存 AGENTS.md 修改及四个未跟踪部署目录，未操作生产或外部业务系统。

## 2026-09-12 成对回收的本人历史恢复与完整证据核验

从 `08576d495859c0032ca4b5cd2f76cdf882477734`（小程序普通命令确认提交）接续。
成对替换原请求 GET 改用本人历史操作坐标及当前有效读取权限，不再依赖原 OAM
工单仍分配给本人或仍处于活动状态。原工单改派/关闭、授权版本更新后，当前合法
本人仍能查询已执行原结果；其他人员即使拥有全国权限也不能读取。读取期间身份
或权限变化时丢弃结果。GET 在原字段之外新增人员、原请求和内容摘要，供后续
成对回收页面核验恢复记录；POST 返回结构保持不变，404 仍不证明未执行。

原结果校验进一步检查两笔交易的编号、账本游标、原请求、历史占用归属、库存
审计/业务审计完整链、两组 outbox 与状态事实；缺少任一侧证据不能确认。JSON
内容在精确选取唯一事件后比较，避免 SQLite 的 JSON null 和字段顺序差异，也
防止用另一个同坐标事件凑出证据。读取不产生库存、审计或通知事实。

验证：后端工单、原请求、成对回收、来源、封存、HTTP 与部署扩大回归
**240 项通过**（含成对回收专项 42 项，不相加）。真实隔离 PG16 55487 在 API
角色、`READ ONLY` 事务中核验数量和 SN 原替换及实际 HTTP 返回，库存、SN、
流水、审计、配对与 outbox 快照未变。新只读 PG16 检查和静态用例已接入 workflow。
日志：`/tmp/oam-work-order-replacement-read-regression.log`、
`/tmp/oam-work-order-replacement-read-pg16.log`。
本批前端未变，844 项为前一确认提交批次的全量结果；完整 0094 空库结果也仍属
前批，不能替代新 SHA 的 CI。推送前继续等 `34700216972` 完整终态，避免取消。

接下来完成成对替换的客户端摘要/恢复存储、整批预检与回收候选、确认提交页面。
占用/消耗/释放与替换的未确认请求必须按同一工单互斥；不能用暂无原结果开放
再次扣库。替换未执行原请求的封存也需扩展独立数据库证明，0094 当前仅覆盖
普通三类命令。随后继续未知拆回 SN 受控登记、冲销、工单结束检查、退回解除
保管责任及完整 V1 剩余链路；生产部署、真机 UAT、对账和恢复演练仍独立验收。
保留既存 AGENTS.md 修改与四个未跟踪部署目录，未操作生产或外部业务系统。

## 2026-09-12 工单占用、消耗与释放确认提交

从本地 `63b72e2c8ac8347c0402e03aba96dc85308f2d51`（0094 原请求封存）接续。
小程序工单页已接入实际占用、消耗和释放按钮：先刷新本人来源及库存候选，重新
构造整批草稿并预检，再在页面展示工单号、全部物料、成色、批次、数量及已扫描
SN，由用户明确确认。任一行不足或不匹配则整批停止；扫描二维码只在内存中。

确认后生成安全原请求标识与独立幂等键，保存并回读恢复记录后才发一次库存 POST。
请求头与正文使用相同原请求和幂等键，禁用自动刷新重放；POST 返回成功本身不
显示库存完成。只有原 GET 返回与本次摘要、工单、人员和操作相符的完整原证明，
且当前身份权限仍一致，才精确清除恢复记录。超时、401、普通 4xx、无结果、摘要
不符或存储异常均保留恢复边界。退出页面、切换账号或取消确认时停止后续请求。
操作权限在页面打开后单独撤销，也在确认提交/封存之前阻断，不依赖最终接口拒绝。

验证：小程序全量 **844 项通过**，CI 配置/诊断 **27 项通过**。真实隔离 PG16
55487 分别用数量和 SN 运行小程序实际 API 适配器、草稿提交及恢复存储，经正式
HTTP 路由完成占用→释放→再次占用→消耗；核验每步一次 POST、正文/请求头标识、
持久化先于写入及原 GET 证明。仅替换 wx 传输为测试 JSON 管道，测试主机名不联网。
各接口在回滚事务内强制执行全部延迟约束，结束后库存、流水、SN、审计和 outbox
快照一致；真正提交后新会话恢复仍由独立原请求 PG16 门禁覆盖。新集成检查已接入
PG16 workflow，并固定与 Client 相同的 Node 版本；小程序变更也触发该门禁。

日志：`/tmp/oam-work-order-submit-mini-final.log`、
`/tmp/oam-work-order-submit-ci-config.log`、`/tmp/oam-work-order-submit-pg16.log`。
首轮集成夹具在替换测试配置前导入了 API，触发空服务地址保护；已调整测试加载
顺序后通过，未修改正式发布配置或放松保护。

前批完整空库 PG16 0094 已在 **55489** 正常结束：
`/tmp/oam-pg16-0094-full-suite/local-result.log` 最终为
`Full local isolated PG16 0094 full suite PASS`，原进程 `57848` 退出码 0。
该结果覆盖 0094 及此前完整串联，不冒充本批新增小程序集成的全量重跑。
55489 已有封存与已消耗测试库存，不可重跑空历史降级或直接重复耗库命令。
远端 `c6fcf99` 的 Client `34700216953` 成功；PG16 `34700216972` 数据库阶段
成功、静态阶段尚运行。待它终态后推送后续提交，避免取消正在执行的门禁。

下一步继续成对消耗/旧坏件回收页面及原结果恢复、未知拆回 SN 受控登记、冲销、
工单结束检查、退回解除保管责任、库存作业和完整 V1 剩余链路。备案通过已记录；
本批未部署、生产写入或提审，真机 UAT、迁移、对账、送达与恢复演练仍分别验收。
保留既存 AGENTS.md 修改和四个未跟踪部署目录。

## 2026-09-12 原请求永久封存与迟到提交互斥

从已推送 `c6fcf99b574e0a9c1cdf9e42fe3629c76d2d93fb` 接续。其 Client
`34700216953` 已成功；PG16 `34700216972` 仍运行，待终态再推送后续提交。
更早 `9e99349` 的 Client `34698214935`、PG16 `34698214968` 均完整成功。

新增 `20261004_0094`、不可变 `work_order_command_seals` 和独立封存审计。
原工单、当前用户、操作类型、原请求和内容摘要构成恢复坐标。封存与实际库存
命令按相同顺序持有账本、当前身份和原工单锁：已执行则返回原证明，未执行才
封存。普通占用/消耗/释放的迟到请求即使用新幂等键，也不能绕过原请求封存；
成对替换子操作仍用独立命令边界。数据库延迟约束分别检查两个写入顺序，不能
让封存与过账共存。封存只增加一条审计，不产生库存、出入库或通知事实。
API 仅可读和插入封存记录，不能改、删或清空；有封存事实时禁止降级丢弃。
运行时清单更新到 158 个受检触发器、65 个审批/工单内部函数。

新增原请求 `/seal` 接口；原 GET 兼容返回已提交、暂未观察到或已封存未执行。
读取仍核验本人和历史审计，不因后来工单改派/关闭或授权版本更新抹除原证明。
小程序“本人待确认请求”新增“结束未执行请求”：先 GET 原结果，必要时由用户
确认后发一次不自动重放的封存请求，再通过原 GET 核验后精确清理恢复标识。
一般 4xx/409、超时、暂无记录或内容不符均不清理；切换账号、权限改变、离开
确认框期间退出页面均停止后续写入。只读权限不能显示封存操作。

本地验证：后端扩大回归 **614 项通过**；来源函数清单 **14 项通过**；迁移图、
SQLite head 与 ORM 一致性/升降级及新迁移专项 **6 项通过**；小程序全量
**835 项通过**。数量/SN 的真实 PG16 验证覆盖两种写入顺序、明确已提交原结果、
持锁封存与并发迟到 POST、重启会话读取、重复封存、API/owner 不可变约束及
有历史禁止降级。实际 PG16 HTTP GET 与重复封存通过，两个真实响应经小程序
校验器解析通过。测试库存未变，封存按预期只追加审计。

日志：`/tmp/oam-work-order-seals-regression.log`、
`/tmp/oam-work-order-seals-mini-final.log`、`/tmp/oam-work-order-seals-schema-final.log`、
`/tmp/oam-work-order-seals-source-pins-final.log`、
`/tmp/oam-work-order-seals-pg16-complete.log`、`/tmp/oam-work-order-seals-http-pg16.log`。
55487 现为 0094 且已有不可变封存；禁止重跑空历史升降级或降到 0093。
首轮真实 SQL 发现变量与表别名重名，已修复；测试断言同步采用原不可变触发器
的 55000 错误，并严格核验恰好一条审计增量，其余库存快照不变。

完整空库串联已另在 **55489** 启动，目录 `/tmp/oam-pg16-0094-full-suite`，
日志 `local-result.log`，当前进程句柄 `57848`。沿用同一进程检查，不重复启动。
首次 55488 因启动时遗漏 `-c timezone=UTC`，旧期初测试夹具 hash 在 Asia/Shanghai
时区不匹配而停止；该测试实例已停止并保留日志。55489 使用与历史 55487/CI
一致的 UTC。完整空库门禁尚不能记为通过，需接续确认终态；新静态与 PG16
封存检查已接入 workflow，永久封存测试放在旧迁移阻断检查之后，不覆盖旧证据。

下一步接占用/消耗/释放的最终确认提交：刷新物料和整批预检，用户确认，先保存
并回读恢复摘要，再发一次 POST，并按原 GET 核验；未知结果保留到回读或封存。
现有页面仍未开放实际库存提交按钮。后续继续成对回收页面、未知拆回 SN 受控
登记、冲销、工单结束检查、退回解除保管责任和完整 V1 剩余链路。
本批未生产写入、部署或提审；备案、UAT、送达、对账和恢复演练分别验收。
保留既存 AGENTS.md 修改与四个未跟踪部署目录。

## 2026-09-12 本人待确认请求独立恢复入口

从 `4e2694d`（新命令正式来源检查）接续。小程序工单页现在独立枚举本机保存的
本人待确认请求，不依赖原工单仍在本人列表、当前搜索页或物料候选可以读取。
工单转派、关闭，或列表/候选请求失败时，当前身份与读取权限核验通过后，仍可
按原工单、操作类型和原请求读取结果。隐藏、切换账号或权限撤销时清空页面内容，
保留恢复记录。后台原请求读取本就核验本人历史操作，不依赖当前工单归属。

恢复存储只返回当前人员的合法记录；存储目录及记录读取前后变化不能冒充空列表。
损坏记录保留并提示，不覆盖、不删除，也不阻止其他可核验请求单独恢复。
同一工单的异常存储别名及已有故障继续阻断该对象；其他人员记录不展示、不发送。
页面只读 GET，禁用自动刷新重放。暂无记录、超时、摘要不一致均保留原请求；
只有完整原结果和当前身份再次核验成功后才精确清除。恢复入口仍不代表库存提交
按钮已开放，也不把通知送达、生产部署或正式 UAT 算作完成。

验证：小程序全量 **829 项通过**，包括列表缺失/失败、原工单转派后的恢复、
切换账号/损坏身份、权限撤销、退出时迟到响应、重复点击、存储故障与内容漂移。
日志 `/tmp/oam-work-order-global-recovery-mini-final.log`；这是本地验证。
远端 `9e99349` 的 Client 已成功，PG16 数据库阶段已成功、静态阶段仍运行，
待其终态后再推送本地提交，避免取消。保留既存 AGENTS.md 和四个部署目录。

下一步继续不可变原请求封存和迟到 POST 互斥，再接一次确认提交、原请求回读及
成对回收页面；后续未知拆回 SN、冲销、工单结束检查、库存作业和上线验收不变。

## 2026-09-12 新工单命令的正式来源边界

从 `b6c145b195f155709d6a9f03bc20e7958444407c` 接续。普通占用、消耗、释放、
回收的新命令现在复用本人查询的正式来源证据，核验 OAM 来源类型、当前版本、
内容哈希、人员归属、启用状态和 45 分钟同步时效。校验位于库存账本、当前身份
及精确工单锁之后、过账之前。成对替换也在创建回收目标维度前执行该校验。
操作本身仍使用 operate 权限，不额外依赖 read 授权。原键已提交命令先使用原
交易时间和历史证据；后来来源过期、停用、版本不可用，不误删或重复执行原操作。

测试夹具统一提供带正式来源版本的合成工单，不再使用仅 active 的无版本壳记录。
PG16 的首个 reserved 账户、普通工单、序列生命周期及成对替换夹具同步更新。
真实 PG16 另用并发连接验证工单更新在命令持有原 OAM 行共享锁时确实超时，
释放命令锁后才可继续；所有测试更新及库存事务回滚，不改变生产或外部 OAM。

验证：后端工单、来源、查询、预检、恢复和成对替换扩大回归 **273 项通过**。
实际隔离 PG16 55487 的数量/SN 占用、预检、消耗查询与回滚通过，包含上述
并发来源锁检查。日志：`/tmp/oam-work-order-source-regression.log`、
`/tmp/oam-work-order-current-source.log`、`/tmp/oam-work-order-source-pg16.log`。
前端未变，上一批 818 项结果不冒充本批重新执行。

下一步完成未执行原请求的永久封存，再接确认提交界面。封存须与普通 POST
共用并发边界：若原操作已提交则返回原证明；只有原请求未执行才形成不可变封存，
迟到的同一原请求不得再过账。禁止用一般 4xx/409 或暂无记录清空恢复标识。
恢复入口还须覆盖工单已转派、已不在本人列表的原请求，不能依赖新物料候选读取。
当前仍未开放最终库存提交按钮或生产部署；继续推进完整 V1 与上线验证。

## 2026-09-12 工单物料扫码草稿与整批预检

本批从本地提交 `76a9935c818fbe37caea75c0f8dce17738fc5583`（原请求恢复）
接续；其父提交 `9e99349c2482006a061c267e9524874261131f33` 已推送且远端
同 SHA。该 SHA 的 Client `34698214935` 已成功，PG16 `34698214968` 尚在
运行；原请求恢复和本批先保留本地提交，须等它终态再推送，避免自动取消。

新增只读 `POST /v1/work-orders/{id}/material-operations/{occupy|consume|release}/preview`。
整批使用最新本人 OAM 来源、个人仓期初/保管责任、余额及原工单剩余占用，校验
每条数量、库存策略、SN 当前归属、实体 SKU/SN/二维码和释放目标；任一条失败
不产生库存或操作事实。复用实际过账的追踪规则和三码比较器，返回本次内容摘要、
来源版本、库存游标、身份版本和核验时间，不返回扫码原文。读取期间来源、库存、
权限或物料策略变化则丢弃整批。另复用过账的硬冻结检查，读取前后核验来源和
目标维度；首次占用尚未创建 reserved 账户时也检查其精确维度，不创建占位账户。
非匹配物料冻结不扩大阻断。预检通过不等于已占用、消耗或释放。

物料候选新增 `release_target_stock_account_id`，按原账户资产组织、位置、保管人、
SKU、批次、成色精确查找 available 维度。即使该可用账户余额为零、未出现在
正库存候选中，仍能正确释放；不让工程师手填 UUID，不按 SKU 猜账户。

小程序工单页新增占用/消耗/释放草稿：选择物料、定点数量、分开扫描 SKU/SN/
二维码，受控物料按实际采集的 SN 计数。候选不会预填物理扫码证据；二维码仅留
页面内存，隐藏、退出或切换账号时清空，迟到的相机/网络回调不恢复旧草稿。
整批预检前刷新物料并重新构造相同意图，响应须匹配人员、来源、游标和内容摘要。
编辑草稿后清除旧预检结果。只有正式 operate 权限且没有待确认原请求时可准备
新草稿；读取权限不展示草稿操作。当前只开放预检，尚未接最终库存提交按钮。

验证：后端工单、预检、原请求恢复、成对替换、审计扩大回归 **261 项通过**；
小程序全量 **818 项通过**；随后补冻结检查后的预检专项 **59 项通过**（与扩大
回归有重叠，不相加）。真实隔离 PG16 55487 的数量/SN 预检、错误二维码、
超量阻断、占用后消耗/释放候选及回滚通过，前后库存快照一致；实际 PG16 DTO
通过小程序候选及预检校验器，两端内容摘要一致。首轮失败来自测试夹具时间绑定
不完整和修改历史策略，已改用完整过期来源及原生 SN 策略，未放松产品校验。
日志：`/tmp/oam-work-order-preview-regression.log`、
`/tmp/oam-work-order-preview-mini-final.log`、`/tmp/oam-work-order-preview-freeze.log`、
`/tmp/oam-work-order-preview-pg16-final.log`、
`/tmp/oam-work-order-preview-contract-pg16.log`。新静态用例与真实 PG16 预检已
加入 workflow。55487 本批只增加合成工单引用，库存命令均回滚；仍保留可用旧件。

下一批接实际占用/消耗/释放的确认提交与原请求回读。开放提交前，补普通命令
`_command_effective_at` 对**新命令**的正式 OAM 来源/新鲜度验证（当前该处只
检查 active），并保留原键历史回放；同时核验同步和命令的锁边界。前端提交前
必须持久化并回读恢复摘要，POST 不自动重放，成功后按原请求 GET 核验再清除；
明确拒绝的处理需与原操作、请求和内容绑定，不得把一般 4xx/409、未查到记录
或超时当成可重新扣库的许可。之后继续成对替换、未知拆回 SN、冲销和工单结束检查。
本批未部署、生产写入或提审；备案通过已记录，正式 UAT、恢复演练、通知送达、
对账及完整 V1 剩余链路仍需逐项完成。保留既存 AGENTS.md 与四个未跟踪部署目录。

## 2026-09-12 普通工单命令原请求恢复

从本地 `9e99349c2482006a061c267e9524874261131f33` 接续。其上一提交
`9d506c448d7fbac95218a12ee7346fb0b017c112` 的 Client `34696752934`、
PG16 `34696752930` 已全部成功；这只证明该准确 SHA，不代替本批 CI。

新增只读 `GET /v1/work-orders/{id}/material-operations/{occupy|consume|release}/by-request/{request_id}`。
从原请求散列关联库存审计和状态事实，交叉核验原工单命令、明细、SN、库存流水、
原占用归属、两条审计链和 outbox。只返回当前用户本人原操作；成对替换子操作
不混入普通命令。同一请求对应多笔同类操作时明确报冲突，不选择第一笔。
原 SN 后续消耗、工单关闭或当前授权版本变更，不抹除历史证明；当前身份仍须有效。
没有观察到记录返回 `not_observed`，不表示提交失败，也不授权自动重发。
普通命令同时拒绝请求头和正文原请求标识不一致。

小程序保存的恢复记录只有工单、人员、授权版本、操作类型、原请求标识和内容摘要，
不存令牌、幂等键、数量、物料明细或扫码原文。按工单加本地锁并核验写后记录，
禁止未确认操作被覆盖。查看页重新进入后可读取原请求；只有原请求、内容摘要、
人员和原操作证据全部一致且读取前后身份未变化，才清除该记录。隐藏页面后的
响应丢弃，超时、未观察到结果、身份变化、存储异常均保留恢复边界，不自动 POST。
跨 Python/JavaScript 的请求摘要已用包含中文和 emoji 的三码样例核验。

验证：后端工单、查询、占用、成对替换及审计扩大回归 **216 项通过**；小程序
全量 **809 项通过**。隔离真实 PG16 **55486** 在 API 角色下分别完成数量与 SN
的占用、释放、再次占用、消耗提交，并在新会话和实际 HTTP 入口回读全部原操作；
运行时权限检查前后通过。新静态用例及真实 PG16 原请求回读已加入正式门禁。
日志：`/tmp/oam-work-order-recovery-regression.log`、
`/tmp/oam-work-order-recovery-mini-all.log`、`/tmp/oam-work-order-recovery-pg16.log`。
55486 的剩余测试库存已被本批终态消耗，不得直接重跑这些命令或降级历史表；
55487 保留上一批可用旧件库存，可用于后续独立事务验证。

下一步接整批提交前预检、真实扫码和占用/消耗/释放页面，再接成对替换与未知拆回
SN 受控登记。当前页面只具备工单、物料查询与原结果恢复，尚未开放新的物料提交。
小程序和域名备案通过仍不等于已上线；本批未生产写入、部署或提审，真机 UAT、
恢复演练、通知送达、对账和完整 V1 剩余链路继续推进。保留既存 AGENTS.md 和
四个未跟踪部署目录。推送前先检查上一条 PG16 CI 已终态，避免自动取消它。

## 2026-09-12 本人工单与原工单剩余占用查询

0093 成对替换已提交并推送为 `9d506c448d7fbac95218a12ee7346fb0b017c112`，
远端同 SHA。Client `34696752934` 已成功；PG16 `34696752930` 仍运行，须保留
到完整终态后再推送下一批，避免被新 push 自动取消。本批开发从该提交接续。

新增并注册 `GET /v1/work-orders/mine` 与
`GET /v1/work-orders/{work_order_id}/material-options`。本人列表使用正式 OAM
投影 UUID，校验来源版本、内容哈希、当前归属和时间；支持分页、字面搜索和所有
状态。旧快照显示原同步时间并标记过期，不返回可操作能力。即使拥有全国读取权限，
本人入口也不返回其他工程师工单。读取前后核验当前身份和授权版本。

物料候选复用正式个人仓建账、保管责任、余额和 SN 流水证明；同一游标下读取
本人 available 库存与本工单原 reserved 流水。提取共享剩余占用计算器，写入预检
与页面使用同一算法，不用合并账户余额推断工单数量。`selectable_quantity` 为
本工单剩余数量，原账户 `quantity` 仍独立保留；预留 SN 仅包含本工单未消耗/释放
的准确集合。没有期初时不展示数量；中途库存、来源或权限变化则丢弃整份候选。
只返回 SN 编号，不预填二维码或制造三码校验事实。

小程序新增 `pages/formal-work-orders`，从工作台和“我的”按正式权限进入：搜索、
翻页、选择本人工单，查看来源更新时间/同步时间、可用物料和本工单剩余占用。
数量保持十进制定点字符串，展示本工单数量而非混合 reserved 账户总量。
请求只读且不缓存，不自动刷新重放；隐藏页面、切换账号、权限变化后的旧响应不显示。
旧原型工单物料路由仍隔离。本批页面为查看入口，尚未接扫码投料、消耗、释放、
配对回收提交；不能把查询候选当作占用或消耗完成。

本地验证：工单、成对替换、库存查询扩大回归 166 项通过；小程序全量 799 项
通过，随后“我的”入口及相关专项 28 项通过（与全量有重叠，不相加）。应用 OpenAPI
已核验两条正式 GET 路由。实际隔离 PG16 55487 通过数量及 SN 原工单查询、
占用归属、消耗后刷新和整笔回滚；包括 0093 回收旧件重新占用/消耗的查询。
小程序响应校验器直接解析了实际 PG16 返回的数量/SN 响应，两者通过。
测试库只有回收旧件剩余，门禁复用真实回收库存，不虚构新件余额；测试新增原工单
引用，库存命令与查询后回滚，前后库存快照一致。两条新静态测试和真实 PG16
查询串联已加入 workflow，准确新 SHA 的 CI 仍须推送后核验。

日志：`/tmp/oam-work-order-query-regression.log`、`/tmp/oam-work-order-mini-all.log`、
`/tmp/oam-work-order-mini-final.log` 和 `/tmp/oam-work-order-query-pg16-fixed.log`。
55487 已有完整替换事实，禁止重跑空库升降级；新增查询门禁可在独立事务中验证并
回滚库存。不要把历史 0093 的完整空库验证改写为新查询批次的全量 CI。

接下来先补普通工单命令按原请求精确回读与本地恢复，再接工程师扫码投料/消耗/
释放、成对回收。随后继续未知拆回 SN 受控登记、冲销、工单结束检查、退回解除
保管责任和完整 V1 剩余链路。备案通过已记录；本批未生产写入、部署或提审，
仍需真机 UAT、恢复演练、通知送达和 OAM 对账等独立上线证据。
继续保留既存 AGENTS.md 与四个未跟踪部署目录。

## 2026-09-12 工单成对替换与旧坏件回收

从已推送 `28511fef9fc2db06a5f69289614facf2c07c0ee6` 接续，规范工作区和
`codex/production-readiness-gates` 分支不变。该 SHA 的 Client `34694396537`
成功；PG16 `34694396401` 已完整成功，两项云端门禁均为该准确 SHA。

新增 `20261003_0093` 和不可变 `work_order_replacements`：一个命令绑定新件
消耗、拆回旧坏件入仓两笔相邻库存交易。子操作通过延迟外键反向绑定原命令，
提交约束验证工单、工程师、账户、数量、SN 配对、完整命令、审计和 outbox。
缺任一笔均不能提交，不合并消耗、回收和通知送达的业务状态。

回收必须明确关联本次投料账户，继承其资产组织、个人仓位置和保管人，另明确
拆回 SKU、批次及旧/坏成色。仅创建空维度，真实回收过账才产生数量；指定目标
UUID 只能断言既有精确账户。三码须匹配，新旧 SN 完整配对且不能相同。
SN 状态由完整流水重建；已消耗 SN 只有绑定本次配对回收、资产组织相同，才能
恢复 active 并进入旧坏件账户。原消耗流水保留，身份、删除和任意改状态仍受保护。

新增 `POST /v1/work-orders/{id}/material-replacements` 和
`GET /v1/work-orders/{id}/material-replacements/by-request/{request_id}`。
原键重试核验原命令及两笔历史交易，不再扣库；同键修改内容拒绝。原请求查询只读，
校验明细、SN、配对、审计和 outbox，不使用 SN 后来位置假定原结果。
提交异常返回未确认，保留原请求查询；暂无记录不能当作新建命令重试的依据。

本批专项验证：运行时/权限与旧工单接口 354 项通过；新增输入、迁移及 HTTP 专项
19 项通过（范围有重叠，不相加）；SQLite head/ORM 一致性及升降级 3 项通过。
隔离 PG16 55486 已通过 0093 升降级、后期 CAS 失败完整回滚、函数源/ACL 漂移
拒绝、备份只读权限和 API 运行时校验。数量及 SN 成对过账、原请求证明、同键改内容
拒绝、缺回收/配对/审计/outbox 整笔回滚、不同键并发仅一笔成功、同键并发重试、
真实 HTTP 提交/回读及库存只读查询均通过。55486 现有替换事实，禁止降到 0092。
扩大库存、SN 查询、工单及锁回归 276 项通过。新隔离空库 55487 的完整历史迁移
及履约串联已全部通过，进程退出 0；覆盖空库、权限与升降级、期初建账、审批、
分配释放/拣货、发运、本人验收/入账、工单占用、SN 消耗及两种成对替换。
同 CI 的 91 个静态文件检查完成：2902 项通过、2 项跳过，进程退出 0。日志分别为
`/tmp/oam-pg16-0093-suite/local-result.log` 与 `/tmp/oam-0093-ci-static.log`。
55486 专项完整结果位于 `/tmp/oam-0093-commit-gate.log`；不得在有替换事实的
55486 或 55487 重新运行空历史升降级或复用原库存的门禁。
以上为本地验证；本批提交后仍须核验准确新 SHA 的 CI，不能视为生产上线证据。

下一步提交本批并保留新 CI 运行到终态，同时继续工程师工单页面、未知拆回 SN 的受控登记、
受控冲销、工单结束的占用/回收/待退检查、退回解除保管责任及后续库存作业。
当前回收 SN 必须已存在于正式 SN 主档；未开放任意 SN 注册、SN 报废或无关联
激活。outbox 已建立，实际发送/送达、OAM 对账、恢复演练、真机 UAT 和正式部署
仍分别验收。备案通过不替代这些门槛；本批未生产写入、部署或提审。
保留既存 AGENTS.md 和四个未跟踪部署目录。

页面接线核对：小程序 `pages/work-materials` 和 `work-material-create` 仍使用被
production guard 隔离的 `/work-order-materials` 原型接口；PC 的物料履约历史入口
从旧 `/work-orders` 详情取 summary.id，并非正式本人工单候选合同。
下一批先补 `/v1` 正式本人 OAM 工单列表与映射、按原工单计算剩余占用的候选明细，
再让页面选择物料/扫码，不让用户填写内部 UUID，也不放开旧原型路由。
`work_order_query.py`、对应 DTO/独立 router 和测试已经起草，本人工单查询 10 项
本地测试通过；来源版本校验、本人范围、分页、字面搜索、请求前/读取中权限变化、
HTTP 合同和旧同步时间均有覆盖。旧快照仍展示并标为过期，不冒充刚同步，禁止新操作选择。
后续正在起草本人精确工单详情及物料候选查询，须验证后接线。该 router 尚未注册到 main，
也未接页面；查询草稿应独立于 0093 提交。

## 2026-09-12 工单 SN 消耗与生命周期流水证明

从已推送的 `2e8c48262b392ce9b529575caf24bdfc9f3de75d` 接续，仍在规范工作区
`~/Documents/Codex/2026-06-11/oam`、`codex/production-readiness-gates` 分支开发。
该 SHA 的 Client `34692840817` 成功；PG16 `34692840815` 真实数据库阶段成功，
静态阶段仍运行。继续保留这次运行到终态，再推送新提交，避免自动取消旧门禁。

统一库存服务现在允许本人有效工单消耗原占用的 SN。消耗流水、SN 的 consumed
生命周期、空当前位置、余额、工单事实、三码证明、审计和 outbox 同事务提交。
写入和库存查询共用完整 SN 流水重建：初始 active，逐笔验证来源与上一目标连续，
有效工单消耗后成为 consumed；不能仅修改可变状态把已消耗 SN 恢复为可用。
原请求重试按历史事实核验，不再次扣库，也不因当前 SN 已消耗而否定原成功结果。
同时修复 PC 工单历史响应合同：后端包含顶层 schema_version，旧前端把它当成
额外字段拒绝；现按 1.0 版本精确解析，缺失或未知版本仍拒绝。

新增 `20261002_0092`：数据库延迟提交约束重建 SN 的完整流水、SKU/批次、
当时有效的跟踪政策、数量、当前位置与生命周期。只授予 API 对 lifecycle_status
和 updated_at 两列的 UPDATE；SN 身份字段、删除和清空仍受保护。时间必须对应
原消耗流水，缺任何投影均不能提交。迁移保留原 readiness 函数 OID/所有者/ACL，
后期源代码校验失败会回滚函数、触发器及新列权限；已有终结 SN 事实阻止降级。

验证：库存、查询和权限专项 453 项通过；工单、锁及迁移专项 141 项通过。
追加生命周期查询专项 9 项、HTTP 合同 18 项通过（与前述范围有重叠，不相加）。
前端工单专项 9 项、TypeScript 和 Vite 构建通过；构建仍有既有大包体提示。
实际隔离 PG16 55485 通过 0092 升降级、后期校验失败原子回滚、函数源/权限漂移
拒绝；通过 SN 真实消耗、跨工单 SN 拒绝、只改状态/时间/身份拒绝、遗漏生命周期
或位置拒绝、审计/outbox 异常时包括 SN 状态在内的完整回滚，以及不同键并发只成功
一次、原键重试和消耗后的库存只读查询。已有消耗事实后降级被阻止，运行时目录
仍准确。仓库安全检查通过。新增实例 55486 已通过完整 0092 履约链：空库迁移、
权限/历史升级回滚、正式建账、审批、分配、释放/拣货、出库发运、本人验收入账、
拒收不入账、首次工单预留账户、数量/SN 占用归属以及 SN 消耗和重试均通过。
55485 和 55486 现均含终结 SN 消耗事实，不得降到 0091；后续空历史降级演练
使用独立实例。完整静态清单首次执行 2880 项通过、2 项失败、2 项跳过；两项失败
仅来自本地受保护 OAM 客户端的旧 curl 测试，与已迁移的共享 Edge 适配器合同不符。
已更新测试为真实共享 Python 适配器的进程边界模拟：凭证和请求体只经 stdin 传入
指定 Edge 入口，不出现在进程参数或环境参数；业务失败及超时均只调用一次，不盲重试。
全程模拟进程返回，不启动业务浏览器或发送 OAM 请求，也不改受保护客户端。
修正后两个边缘测试文件共 37 项通过，包含原两项失败和新增超时回归；未重复已通过
的整套静态检查，不能将补测与 2880 相加。上述本机结果不能替代本批准确 SHA 的
云端门禁、真机 UAT 或正式上线验收。

下一步继续完整 V1：以换代修新件消耗与旧坏件回收的原子配对、旧坏件账户受控
创建、受控冲销、工程师工单页面及原请求恢复。SN 报废、已消耗 SN 无关联回收/
冲销仍明确阻止；不能因本批 SN 消耗完成就开放这些尚缺事实证明的操作。随后
继续工单结束检查、退回解除保管责任、库存作业、通知/对账、恢复演练和真机 UAT。
备案已通过，尚未部署、提审或声明上线。保留既存 AGENTS.md 和四个未跟踪部署目录。

## 2026-09-12 云端入账合同测试修复

上一批 `33d2019a2a2d4518639d3f81ea59786705cf7d1e` 的 PG16 运行
`34690840247` 已完整结束：真实迁移/权限/并发阶段成功，静态阶段 2746 项通过、
2 项失败、3 项跳过。两项失败均为旧入账合同测试的 FakeDb 缺少事务锁接口
`get_transaction`；不是账单或 runner 未启动，也不是实际 PG16 失败。

两项合同测试现在直接验证选择合格收货明细的命令构造层：拒收明细不解析或创建
账户、不生成库存移动；全拒收必须失败。采用带时区的真实入账时间，并精确断言
原验收、明细、目标和 create 参数。保留统一库存服务的事务锁、审计及 outbox；
完整过账与回滚由现有 Session 回归和实际 PG16 串联继续验证。

本地业务提交 `01b71e18c02b348e65e0bf42803ae81ef9786ff6` 已包含 0091 账户
准入、PC 调用适配及完整本地 PG16 验证。云端上次完整运行耗时约 38.5 分钟，
加入真实工单用例后将 CI 超时上限从 40 调至 60 分钟，保留全部检查项目。
修正后入账合同、过账、本人入账及数据库边界回归 93 项通过、1 项数量场景不适用
的 SN 用例跳过。完成本批提交并推送后，继续核验准确 SHA 的新云端门禁。下一段仍为 SN 消耗
生命周期、配对回收、冲销及工单页面；完整 V1 上线目标继续有效。

## 2026-09-12 首次工单占用自动建立预留账户

接续本地 `10f3ebf9844a7c76d7d9ed2bfe7dfa4b4e7c50ff`，分支仍为
`codex/production-readiness-gates`。该未推送提交曾为 `4f66f29`，仅因交接文档
个人绝对路径改为可移植路径而 amend，旧 SHA 不再是 HEAD。远端仍为
`33d2019a2a2d4518639d3f81ea59786705cf7d1e`，Client `34690840289` 成功；
PG16 `34690840247` 的真实数据库阶段成功，完整运行终态仍待核验。

工程师占用个人仓物料时可以不传 reserved 目标账户。服务从本人可用账户复制
资产组织、保管人、位置、SKU、成色及批次，仅把状态改为 reserved；已有精确维度
直接复用。调用方指定目标只能作为既有账户断言，不能任意新建 UUID 或改变维度。
重复原请求不创建账户；新账户没有初始数量，只有同事务真实占用过账才产生余额。
同一 SKU 来自不同成色或批次账户可以同批投入，重复来源账户及重复 SN 仍拒绝。
PC 端工单命令调用已同步允许省略占用目标，释放/回收的目标仍必填；未知字段和
无效目标仍拒绝，不由客户端生成账户。

新增迁移 `20261001_0091`，保留期初盘点和验收入账两条账户准入规则，增加绑定
本人有效工单占用的窄范围分支。新账户必须匹配正式建账位置、原来源维度、占用
明细和首次库存交易，初始余额版本为 1、游标和数量来自该交易；不允许独立空账户、
虚构余额或先有历史移动。占用事实、审计及 outbox 继续由 0090 提交校验约束。
函数体替换校验完整哈希，保持 OID、所有者和 ACL；有新账户过账事实即阻止降级。

验证：后端扩大回归 755 项通过；迁移专项 15 项通过（与前者有重叠，不相加）。
追加后端接口专项 48 项通过（有重叠）；前端工单专项 8 项、TypeScript 和 Vite
构建通过。实际隔离 PG16 已验证首次数量/SN 占用、同键并发只生成一个账户和一笔库存交易、
缺 outbox 回滚、孤立账户拒绝、额外假余额拒绝、后续消耗/释放及已过账降级阻止；
独立空历史实例通过升降级、后期 readiness 漂移时整次迁移回滚及 API 运行时校验。
仓库安全检查通过。完整 CI 串联测试所需库存由隔离环境的统一 API 流水补齐，
保留原 0.25 数量收货和 SN 拒收证据；不覆盖测试余额、不把拒收改为验收。
完整本地 PG16 串联已在独立实例通过：从空库迁移、目录/权限及失败回滚，到真实
期初建账、审批、分配、占用、拣货、分批出库/发运、本人验收/入账，再到首次工单
占用账户创建、数量/SN 跨工单隔离、原键恢复和并发校验。新 SHA 云端门禁仍须
推送后核验；本地结果不替代云端完整终态。

本机 55484 和 55482 均为 0091：55484 有实际合成过账事实，禁止直接降级；
55482 用于空历史迁移演练。新增 55485 为完整串联测试专用实例，时区显式 UTC，
避免旧测试夹具按数据库当前时区生成时间字符串造成控制批 hash 不一致。
这些均为隔离测试环境，不是生产验收证据。保留既存 AGENTS.md 和未跟踪部署目录。

下一步推进 SN 消耗的流水生命周期投影与冲销，再做以换代修新旧件配对、旧坏件
账户受控创建、工程师工单页面及原请求恢复。现阶段 SN 消耗/报废仍显式阻断，
只有占用/释放可用；后续还须完成工单结束检查、库存作业、通知、对账、恢复演练和
真机 UAT。保持完整 V1 上线目标，备案通过不替代生产验收；本批未部署或提审。

## 2026-09-12 工单占用归属与数据库提交校验

已核验 `7ce78e496591008e871447f5c2dac17374507a5c` 的两个云端门禁完整成功：
Client `34689238762`，PG16 `34689238778`。随后已推送工程师个人仓入账后端及小程序
提交，远端为 `33d2019a2a2d4518639d3f81ea59786705cf7d1e`；该 SHA 的 Client
`34690840289` 成功，PG16 `34690840247` 仍在运行。先取得这次完整终态再推送本批，
避免新 push 取消正在运行的验证；不能把上一 SHA 的绿灯当作本批 CI 结果。

在隔离 PG16 实际复现了缺口：A 工单占用个人仓物料后，B 工单能够消耗同一 reserved
账户中的数量。复现整笔回滚。现在按“原工单 + 原 reserved 账户 + 流水游标”从不可变
库存流水计算剩余占用，消耗/释放前整批校验数量和 SN，不采用合并账户余额判断工单
是否可领用；原请求重试按原交易之前的游标校验，不会重复扣减。四个库存命令及事实
记录器统一先锁库存账本，再锁当前身份与工单。

新增迁移 `20260930_0090`：延迟提交约束要求每笔工单库存事务有且仅有一笔支持的
工单操作，其操作者、个人仓、账户维度、实际流水、SN、完整命令指纹、审计和 outbox
必须一致。按流水顺序检查各工单占用的数量及 SN，任何历史前缀不得透支，后补占用
不能修饰更早的消耗。补齐过账唯一索引、事实 TRUNCATE 防护和运行时函数/触发器/
权限/索引校验；已有工单操作或孤立工单流水时，升级和降级均阻止并要求核验历史，
不改写旧记录。SQLite 只覆盖通用迁移和服务逻辑，提交级校验以实际 PG16 为准。

验证：扩大后端回归 740 项通过；迁移/图结构专项 28 项通过（有重叠，不相加）；仓库
安全检查通过（899 个候选文件）。实际隔离 PG16 API 角色验证数量占用/消耗、SN
占用/释放、跨工单数量拒绝、同账户跨工单 SN 拒绝、绕过 Python 后数据库拒绝、孤立
流水拒绝、审计或 outbox 不一致的完整回滚、原键重试及不同键并发只成功一次。空历史
升降级、后期 readiness 函数漂移导致完整迁移回滚、运行时目录，以及有已过账工单
事实时阻止降级均通过。新增工单回归与实际 PG16 流程已经接入 CI；本批尚待推送及
准确 SHA 的完整云端门禁。

下一步继续完整 V1：先补齐新个人仓 reserved 库存维度的受控创建，再实现 SN 消耗
生命周期、以换代修配对、旧坏件回收及冲销的完整事实链，并接工程师工单页面和原请求
恢复。当前 SN 消耗/报废仍由统一账本显式阻止，不能把 SN 占用/释放通过记为 SN 消耗
已完成。工单结束前的未释放、待回收和待退回核验也仍须实现。随后继续库存作业、
通知、对账、恢复演练及真机 UAT，保持“持续开发到上线版本”目标，不能缩减为单批
交付或单个门禁通过。备案已通过；本批未进行生产写入、部署或提审。

保留既存 AGENTS.md 修改及四个未跟踪部署目录。仅编辑规范工作区
`~/Documents/Codex/2026-06-11/oam`，分支
`codex/production-readiness-gates`。本机隔离 PG16 55484 已升级至 0090，包含合成
工单并发验证事实；该实例不能降级到 0089。后续迁移升降级演练使用独立空历史实例。

## 2026-09-12 小程序个人仓入账与恢复

上一批工程师后端已本地提交 `0106c06`。远端仍为
`7ce78e496591008e871447f5c2dac17374507a5c`：Client `34689238762` 成功；
PG16 `34689238778` 的实际迁移/权限/并发阶段成功，静态检查仍在运行。
先保留这次运行取得完整终态，再推送后续提交，避免新 push 自动取消旧门禁。
不能把旧 SHA 阶段结果记为本批完整 CI 成功。

新增 `my-inbounds/candidates` 只读分页，每页最多 20 条（小程序每页 5 条）。每次
本人验收分别显示待入账、已入账、无合格数量或待核验；错误证据只阻断对应记录。
重新核验本人、原发运/验收、保管关系和入账流水；总部代入账同样显示已入账。
仅返回验收、物料、合格/拒收数量及合格 SN，不暴露来源账户和余额，不创建账户。
SN 按每条验收明细批量查询。页面退出、权限变化和迟到响应不会恢复旧数据。

小程序已从“本人收货”及验收完成页接入“个人仓入账”。确认前后重新读取验收内容
和需求版本，只提交原验收 ID、指纹与期望版本，不能在页面改写入账数量或账户。
写前保存并回读最小恢复记录；不保存写键、令牌、数量和 SN。超时/退出后只使用
原请求标识查询，not_observed 保留原记录，精确匹配后才展示完成并清理。
已入账、全拒收、无权限或异常记录均不能再次提交。通知/OAM 收货继续独立。

验证：后端扩大回归 233 项通过，1 项数量场景不适用的 SN 用例跳过；最后查询专项
19 项通过（与扩大回归重叠，不相加）。小程序全量 791 项通过；仓库安全检查通过。
实际隔离 PG16 以 API 角色跑通待入账查询、原子过账及只读已入账查询，包含并发防重、
晚期失败回滚、SN 精确迁移及全拒收不入账。最终只读验证覆盖 4 笔已入账和 2 笔全拒收
的合成记录，含总部及工程师原入账，未接触生产或 OAM 写入。尚未进行微信开发者工具
或真机视觉/UAT 验收；本批没有部署、提审或声明正式上线。

下一步继续完整 V1 范围：先核验已有工单物料服务与真实 PG16 的差距，重点复现
两个本人有效工单共用 reserved 账户时，消耗/释放是否精确归属于各自原占用。
当前输入未显式带原占用操作关联，需核对数据库约束后补齐，不能仅凭账户余额认定
某工单可消耗。并核对新个人仓 reserved/旧坏件维度创建、统一锁顺序和原请求恢复，
再接工程师工单物料页面；随后继续库存作业、通知、对账、演练和真机验收。
保留既存 AGENTS.md 修改与未跟踪部署目录。

## 2026-09-12 工程师本人验收入账接口

从已推送准确 SHA `7ce78e496591008e871447f5c2dac17374507a5c` 接续；该 SHA
Client `34689238762` 成功，PG16 `34689238778` 仍运行，须继续核验终态。
该提交已修复上一轮静态盘点前置断言，不再将旧失败当作账单阻塞。

新增工程师 `my-inbounds`：仅确认本人原验收、验收指纹和需求版本，不能指定
来源/目标账户、数量或其他保管人。本人原包裹授权在统一库存服务取得引用锁后再次
校验；工程师仍没有通用库存过账或来源仓查询权限。只移动本次合格数量及合格 SN，
全拒收不入账。原入账单保持不可变，状态从过账绑定推导；入账、库存、连续版本、
审计及 outbox 同事务。新增按原幂等键和原请求标识的只读恢复接口，写入开关关闭
仍可核验，当前读取权限和原证据重新检查，权限版本变化后可读取原结果。

迁移 `20260929_0089` 保持既有总部接口的数据库约束，扩展本人入账的精确连续版本
证明：原验收人、原验收命令、本人入账审计、版本和库存事务必须一致。升级/降级
锁定历史并校验函数体，保留 OID/所有者/权限；存在本人入账事实即阻止降级。

验证：后端扩大回归先完成 536 项，通过修正两处新增 HTTP 测试断言并重新加载测试
源码后，涉及失败的全部专项 74 项通过（范围有重叠，不相加）。真实本机隔离 PG16
以 API 角色跑通工程师数量及 SN 入账：通用过账被拒绝，晚期审计失败整笔回滚，
双键并发只成功一次，同键不重复，只读恢复无写入。0088/0089 升降级、后期函数漂移
导致完整回滚、运行时目录以及已有本人入账事实阻止降级均已实际通过。

下一步实现本人待入账查询及小程序确认/恢复页面。上述为后端阶段结果，尚未完成
该页面、完整新 SHA 云端门禁、真机 UAT、恢复演练和正式发布。备案通过与生产验收
分别核验，继续沿完整 V1.0 范围开发。

## 2026-09-12 首次收料自动建立个人库存维度

从准确 SHA `7e65a8a1e5bdac158c050ea4d9475543dd80c2ea` 接续。该提交 Client
`34687701447` 成功，PG16 `34687701464` 真实迁移、权限与并发阶段通过；完整终态为
失败：静态阶段 2701 项通过、6 项失败、3 项跳过。6 项均为盘点前置流程静态断言仍
要求 `zero_confirmed=False`，未适配已加入的零库存个人仓。现改为 AST 核验：无物理
观察行，且仅当实际快照行数为零时确认空仓，普通日常差异前置仍默认一行；保留真实
初盘、两级复核、过账及关闭调用检查。相关 54 项通过。完整新 SHA 门禁须推送后核验。

补齐首次收料缺口：原入账要求管理员预建个人 SKU/成色/批次账户，而 API 的账户新增
只允许正式期初复盘。现在由已验收包裹的原在途事实解析目标维度，以 INSERT-only
方式建立缺失账户；相同维度冲突复用既有账户，禁止 UPDATE 账户维度。新账户创建、
统一库存过账、入账绑定、连续版本命令、审计及 outbox 在同一事务提交。创建及更新
时间显式使用同一个值，避免两个默认时间相差微秒使数据库不可变证据校验失败。

迁移 `20260928_0088` 保留原期初账户门禁，仅允许精确绑定已验收入账的新个人账户。
要求个人仓已正式建账、本人和目标仓与原发运一致，保持资产所有组织、物料、成色及
批次；初始余额必须等于本次合格入账量，版本为 1，流水编号等于本次过账。不能独立
建立空账户或追加假余额。升级/降级用完整函数体哈希校验替换，保持函数 OID、所有者、
执行权限和安全配置；后续函数漂移导致整次迁移回滚，存在入账事实则阻止降级。
SQLite 仅验证通用迁移与服务边界，API 角色及延迟账户约束以真实 PG16 为依据。
创建入账单及提交入账两个旧接口同时补上正式需求写入开关，关闭时不会调用写服务。

验证：最新后端专项 380 项通过、1 项数量场景不适用的 SN 用例跳过；迁移头与 PG16
诊断专项 15 项通过、1 项云环境入口跳过。两组范围不同，不代表完整项目验收。
本机一次性 PG16 用真实 API 角色完成数量和 SN 全链：目标账户事先不存在，实际
入账创建账户；审计失败、版本命令完成后的失败及伪造初始余额均整笔回滚；并发只
成功一次，同键重放不新增事实，只读投影不更新原单；全拒收不入账，合格 SN 精确迁移。
已在实际 PG16 跑通写入仓库的 0088 升降级、函数漂移导致的事务回滚及权限保留回归。

下一步继续工程师本人专用入账权限、API 与小程序确认/恢复页面。当前自动开户仍走
既有总部履约接口，未完成工程师自助入账界面。工程师必须限定本人原包裹，以统一库存
内核处理原在途转入，不能通过授予通用库存过账权限或伪造总部身份实现。继续落实
真机/UAT、恢复演练与部署；小程序及 `rscwz.cn` 备案通过与生产验收分别核验。

## 2026-09-12 个人仓入账事实与连续版本边界

从准确 SHA `0d0835963d347f6aedb3693a810086da1d30716e` 接续。该提交 Client
`34686297645` 成功；PG16 `34686297700` 的真实迁移、权限和并发阶段已通过，
本节编写时剩余静态检查尚未结束，须继续核对完整终态，不能把阶段成功记为整条门禁成功。

修复旧入账服务对不可变 `inbound_orders` 加行锁并修改状态的冲突：入账单保留原始
待入账事实，展示状态由唯一 `inbound_postings` 与已过账库存事务推导。父需求锁和
收货单唯一索引共同阻止重复生成入账；实际库存仍经统一过账服务，从原在途账户转入
该包裹绑定的个人仓，只移动合格数量及合格 SN，保留资产所有组织、物料、成色和批次。
全拒收验收不能生成入账单。已入账重放仍重新核验身份、权限、原事务和版本命令。

新增迁移 `20260927_0087`：入账事实只追加，数据库精确比对验收数量、目标账户和 SN，
拦截不匹配流水以及 UPDATE/DELETE/TRUNCATE；运行时核验函数、ALWAYS 触发器和唯一
索引。每次成功入账追加独立 `personal_inbound` 连续版本命令和审计，库存、绑定、
需求投影及 outbox 同事务提交。旧入账事实或重复入账单会阻止升级；存在新入账事实
会阻止降级，须评审迁移，不能删除或悄悄重新标记历史记录。SQLite 失败时完整回滚 DDL，
PostgreSQL 离线 SQL 同样保留数据预检。

本地后端扩大回归 398 项、入账专项 71 项通过，专项有 1 个数量场景不适用的 SN 用例跳过；
两组有重叠，不能相加作为总数；迁移及门禁诊断专项 16 项通过，仓库安全检查通过。
本机一次性 PG16 的真实 API 角色已完成数量、SN 入账链，回滚、并发重复入账、
不可变原单、连续版本命令及 READ ONLY 查询均通过；全拒收包裹无入账，另一包裹
仅移动合格 SN。测试先完成本人零库存确认、区域及总部复核、正式建账和关闭，
并验证资产所有组织使用人员所属区域公司，不能误用其部门作为库位资产所有组织。
未完成期初资格时，统一库存服务正确拒绝过账；不能绕过此条件。
入账后实际 PG16 运行时安全检查通过，降级被 `0087` 拦截后版本与安全边界保持完整。
完整新 SHA 云端门禁仍须提交后核验。

下一步继续工程师专用入账 API、最小权限和小程序确认/恢复入口；本批仍使用已有总部
履约入口，未给工程师开放通用库存过账权限。之后推进真机/UAT、恢复演练及发布配置。
小程序及 `rscwz.cn` 备案已由用户确认通过；备案与生产验收、上传提审、部署分别核验。

## 2026-09-12 小程序本人验收表单与只读恢复

从准确 SHA `29c4666523392c92798af231af314f20073bd2b8` 接续。该提交 Client
`34685311085` 成功，PG16 `34685311077` 失败于真实只读候选查询：原出库历史校验
复用审计链头行锁，触发 SQLSTATE `25006`。这是代码问题，不能记为账单或 runner 阻塞。
本批新增审计链头坐标快照读取，继续验证完整历史链至 genesis；并发追加不改变已捕获的
不可变前缀。本人包裹列表、候选及恢复使用无锁校验，原写入调用保留默认锁定校验。
SQLite 回归同时检查 ORM 的 FOR UPDATE 标志，避免方言隐藏行锁。隔离本机 PG16 的
实际 API 角色已复现旧错误，并确认新校验在 READ ONLY 事务通过；完整云端门禁须按新 SHA 核验。

小程序包裹验收页接入数量和逐件 SN 表单。扫码仅标记核对，SN 合格需显式选择；拒收、
短少、破损、错料和错 SN 使用本行专用正式异常凭证，上传完成确认后才能提交。按物料精度、
未验收余量和当前包裹 SN 校验；数量使用精确十进制，拒收不计为合格。提交前展示 SKU、
合格/拒收数量与情况，确认前后重新核验包裹和身份权限。验收成功仍明确提示个人仓独立入账。

发送前在本地持久保存人员/需求/包裹、授权与需求版本、原请求追踪标识、时间及请求摘要，
不保存写入幂等键、令牌、原始请求、数量、SN 或文件内容。按包裹加本地操作锁、存储写后
回读；损坏或覆盖风险时停止发送。超时、隐藏、重启和账号切换后保留原坐标。新增只读
`GET /v1/material-requests/{request_id}/my-receipts/trace-status`，以
`X-Original-Request-ID` 精确读取当前本人原验收，校验原命令、事实和摘要；只具读取权限时
仍可恢复，不依赖写开关或幂等密钥。查无结果不等于未执行，禁止清除或自动重放。
只有严格匹配的提交前业务拒绝响应可解除待核验记录，并要求刷新重新核对；其他错误保留记录。

本地相关后端回归 277 项、最终追踪恢复专项 9 项通过；小程序完整回归 780 项通过。
微信 WXML/WXSS 编译及实际编译后 SN、数量、凭证、待核验、成功和错误分支执行通过。
仓库安全检查通过。此处是本地验证记录，不能代替本批准确提交的 Client/PG16 终态。

下一步实现工程师专用个人仓入账动作、不可变库存事务和连续版本命令，禁止借用通用库存
过账权限。之后继续真机/UAT、恢复演练和发布配置。小程序及 rscwz.cn 备案已由用户确认
通过；本批未上传、提审、部署或写入生产业务数据。

## 2026-09-12 验收异常凭证与旧收货入口修正

从本地 `a6316b153ab77c51a83a8fc867a36f8c0ccfecbd` 接续。前一基线
`c50976b3956828725f449894240fbef442e08bfc` 的 Client `34683525819` 和
PG16 `34683525839` 均已完整成功；不再是账单、runner 或此前连续版本命令阻塞。
本批及 a6316b1 的候选查询仍须取得新提交准确 SHA 的云端门禁，不能沿用 c50976b 的结果。

新增 `receipt_exception_evidence` 正式私有文件用途。本人 receive 权限只允许创建专用上传
意图，经 OSS HEAD 完成确认后才能绑定验收；可用状态、完整文件元数据、原上传人、人员绑定、
授权版本及已绑定情况均重新校验。同一文件不得复用于另一笔验收。已绑定凭证下载要求当前
需求可见范围及本人包裹/保管责任，或总部/区域的目的仓履约与库存读取范围；撤销 receive
写权限不影响仍具读取权限者查询历史凭证。上传权限本身不能用于下载他人的未绑定文件。
PC 与小程序上传公共组件同步支持新用途，并校验上传、完成确认的用途一致。

新增前向迁移 `20260926_0086`，扩展既有文件守卫，不新增数据库角色或通用库存过账权限。
验收异常绑定守卫为 ALWAYS INSERT 触发器，核对收货人、包裹、收货明细、异常类型及专用
可用文件；文件行锁保证跨验收复用检查的并发边界。保留原函数 OID、所有者及 ACL，精确函数
体 SHA 与 readiness 清单一并推进。存在既有异常/附件时停止升级，不能默认为合规证据；存在
新用途上传意图或异常凭证时停止降级。SQLite 对应守卫和迁移失败回滚同时覆盖。

旧总部/区域收货入口同步执行异常凭证及收件人预检，缺少文件返回明确业务错误。修正旧代码
将拒收 SN 标为 accepted 的缺陷：全拒收的逐件结果均为 false；混合 SN 验收必须使用能分别
指定合格/拒收 SN 的本人入口。破损、错料、错 SN、拒收不得计为合格；正常验收不得带异常
文件，避免未绑定附件被静默丢弃。此次只记录验收事实，库存流水、OAM 收货和通知送达保持独立。

本机隔离 PostgreSQL 16 已完成 0086 升级、空新事实降级、再升级及完整运行时安全校验。
本地相关后端回归 705 项通过，最终边界及旧入口专项 58 项通过，覆盖专用上传/下载、跨人和
跨单拒绝、旧入口、迁移失败回滚、验收原请求恢复及入账；小程序 763 项、PC 文件上传 7 项、
TypeScript 与 Vite 构建通过。云端 PG16 新增实际 API 角色
上传、异常验收、并发与下载路径，最终结果必须另按本批提交查询。

下一步仍是小程序包裹验收表单：合格/拒收数量和逐件 SN、异常文件选择、原请求持久保存、
提交前最终复核、超时后原幂等键只读恢复。当前扫码页尚不提交验收。之后实现工程师专用个人仓
入账动作、不可变库存事务及连续版本链，再完成真机/UAT、恢复演练及发布配置。用户已确认
小程序和 rscwz.cn 备案通过；本批未上传、提审、部署或写入生产业务数据。

## 2026-09-12 本人包裹待验收明细与扫码核对

从准确 SHA `c50976b3956828725f449894240fbef442e08bfc` 接续。该提交 Client
`34683525819` 已成功；PG16 `34683525839` 的真实隔离数据库阶段已成功，后续静态安全
检查仍在运行，尚不能记作整个 workflow 成功。此前连续版本命令缺失导致的发运提交失败
已通过该真实阶段复核；本批新增候选查询也纳入下一提交的实际 API 角色只读事务检查。

新增 `GET /v1/material-requests/{request_id}/my-receiving/{shipment_id}/candidates`。
只读取明确绑定本人、唯一有效个人仓及当前保管责任的包裹；内部校验原出库事实、累计数量、
物料精度、批次和 SN，返回合格、拒收、未验收数量及尚未验收的逐件 SN/二维码，不返回来源
账户或库存余额。已验收 SN 不再进入候选；数量与逐件结果不符、跨包裹重复 SN、错误物料/
批次或权限/需求版本变化时停止返回。读前读后核验身份、需求及当前保管责任，禁止自动 flush，
读接口 no-store，写开关关闭时仍可查询。是否有 receive 权限、未批准、未交运和全部验收完毕
分别提示；候选结果不是写入授权快照，实际 POST 仍须重新校验。

小程序“本人收货”可进入“包裹验收核对”，展示批次、精确数量、剩余 SN；每行初次展示 20 个
SN，按需增加。扫描只按当前包裹内完整二维码或唯一 SN 精确匹配，不解析陌生 URL、猜测 SKU
或选取同号物料的第一条；重复扫码不增加计数。隐藏、卸载、刷新、切换身份、权限失效时丢弃
候选/扫码临时结果，迟到回调不能恢复旧数据。扫码标记仅为本次核对，绝不增加合格验收量。

本地后端本人收货/验收/候选回归 121 项、小程序完整回归 762 项通过；微信 WXML/WXSS 编译、
编译后 SN/批次/数量/错误分支及仓库安全检查通过。最终提交的 Client/PG16 仍须另取准确 SHA
的云端结果，不能沿用上一提交的成功阶段。

本批不新增数据库迁移，不上传小程序或部署生产；备案通过不替代验收。验收 POST 与恢复 API
已在上一批实现，但小程序当前页仍不提交验收。下一步接验收表单、原请求持久化及精确回读、
异常附件专用用途和本人文件读取，再接工程师个人仓入账专用权限、不可变库存事务及连续版本链。
不能直接借用 request_attachment/stocktake_evidence 上传用途，或对工程师开放通用库存过账。

## 2026-09-12 工程师本人验收写入与原请求恢复

从 `5550f97024dfce27bef5d32aeebc082c086d17da` 接续。旧 0076 权限种子实际也向工程师授予了
`material_request/fulfill`，但来源库存范围使其无法完成本人包裹验收；此前只读批次没有解决写入。
新增前向迁移 `20260923_0083`，把工程师的既有默认 `fulfill` 种子改为专用 `receive`，保留显式
deny 并推进相关人员的授权版本；不修改总部/区域履约权限，不授予工程师通用库存过账权限。
自定义种子不符则停止迁移。readiness marker 和精确函数体 SHA 同步推进，0082 历史清单保留。

新增 `POST /v1/material-requests/{request_id}/my-receipts`，只接受当前登录人及其唯一有效个人仓
绑定的包裹，调用方不能指定收货人或库存账户。按当前正式身份、独立 receive 权限、需求版本、
原出库与包裹、当前保管责任、物料精度、累计数量及未验收 SN 整体校验。合格/拒收 SN 分别保存；
破损、错料、错 SN、拒收不允许混入合格数量，异常文件必须由本人上传且状态 available。
收货事实、异常、原命令审计、Outbox 和需求版本在一个事务中写入；不生成库存流水、不调用 OAM。

独立 `GET /v1/material-requests/{request_id}/my-receipts/command-status` 使用本人原幂等键回读，
验证完整命令哈希、审计、时间、逐行数量/SN、收件人与当前权限；写开关关闭时仍可只读查询。
未找到结果返回 `not_observed`，不证明未执行，不自动重发。原键改内容冲突，授权失效不可重放。
验收时间统一按 UTC 保存；每次新增验收均推进需求版本，即使仍处于部分验收状态。

本地专项 56 项通过；收货/入账相关回归 201 项、迁移回归 168 项、数据库安全 345 项通过。
一次性本机 PostgreSQL 16 完成 0083 升级、空新事实降级、再升级；完整 PG16 gate 限定 GitHub
托管 runner，本机跳过不计通过。新 API 角色验收/并发/回滚用例已接入云端真库门禁，须以本批
准确 SHA 的结果验收。无生产迁移、业务写入或部署。

首轮 `558bd87` Client 通过；PG16 `34677242480` 在旧迁移回退检查失败，因为测试辅助函数仍引用
0082 readiness hash。已推进到 0083 并在本地 head 检查中比对动态门禁与运行时清单；另统一权限
迁移的用户→权限行锁顺序。必须随修正提交重新取得完整门禁，不能把首轮失败记作通过。
第二轮 `c250a20` 已通过此前迁移/出库检查，进入新增本人验收用例；测试个人仓漏设父级，被
`ck_stock_locations_personal_binding` 拒绝。已为构造数据明确建立区域父仓，不放宽数据库约束。
第三轮 `3fa4608` Client 通过，PG16 `34678067768` 暴露真实运行缺口：API 没有
`personal_inbound_status` 列更新权，旧身份触发器也冻结了该轴。新增前向迁移 `20260924_0084`，
仅授予该列更新，并以当前版本的逐物料发运、验收及对应已过账库存事务核对投影；变化时必须
推进版本和更新时间。原供货命令继续保留当时的中性个人仓状态，后续供货写入的既有边界不变。
SQLite 更新守卫同步；旧函数的 OID/所有者/ACL 保留，函数体 hash 与 readiness 推进到 0084。
存在非中性个人仓投影时阻止降级。PG16 新增直接伪造 accepted/posted 的拒绝测试，普通 API
没有整表 UPDATE 权；不开放发运、物流签收、OAM 收货等其他状态轴。

`3fa4608` 本地完整后端回归 3623 通过、1 跳过（不含云端专用 PG16 主门禁）。0084 的独立
本机 PG16 升级/空新事实降级/再升级及运行时完整安全边界验证通过；新 SQL 与应用状态推导
的边界回归及安全测试 322 项、完整迁移回归 168 项通过。本机验证仍不能代替准确提交 SHA
的完整云端门禁。

第四轮 `f4e872b` Client 通过；PG16 `34678995344` 通过了前置迁移、权限和出库验证，
在发运提交时被审批投影函数拒绝：原发运/验收代码推进需求版本却没有追加不可变版本命令，
违反 `command_count = request.version + 1`。新增前向迁移 `20260925_0085`，保留连续版本计数、
最小/最大版本及最后更新时间校验，允许显式 shipment/receipt 命令并核对原事实、幂等键、
人员、时间、请求/结果内容与版本审计。不能用放宽计数或伪造空命令修复。

发运、旧履约收货和本人验收统一记录独立事实及其版本命令；每次新事实推进一个版本，同一键
重放不增加版本。本人验收恢复同时校验原版本命令和独立审计，缺失或篡改停止返回成功。
本机隔离 PG16 已完成 0085 升级、空新命令降级、再升级和运行时完整安全边界验证；扩展专项
158 项、完整迁移与安全回归 482 项通过，云端必须重跑本批准确 SHA。个人仓入账过账仍需在下一阶段接专用权限、
原库存事实和版本链，不代表此次已经完成入账链或上线验收。

本批交付后端。小程序仍是本人包裹查询；下一步补验收候选（含逐件 SN）、表单/扫码和异常附件，
提交前持久保存原请求并精确回读后清理，再接工程师专用个人仓入账动作。不能把已登记验收当作
个人仓已入账，也不能直接开放旧 fulfill/通用库存过账入口替代专用动作。

## 2026-09-12 小程序本人收货查询

从 `545a8bd94f29458e656ae47f6a476ed95a1b1672` 接续；该提交 Client `34674654464` 已通过。
发现现有收货 POST 要求 `material_request/fulfill`，既有发运/收货历史还校验来源库存权限，
不能直接作为工程师本人收货入口。因此先新增只读
`GET /v1/material-requests/{request_id}/my-receiving` 和小程序“本人收货”页，从需求详情进入。

查询先重读正式身份及需求可见范围、本人库存读取权限，只选择显式绑定当前人员的包裹；
要求目的地为唯一有效个人仓、叶子位置及当前唯一保管责任。内部核对需求出库证据、物料、
累计发运及累计验收数量，对客户端只返回包裹、运单、目的地和逐物料数量，绝不授予来源库存
读取权限或返回来源账户。读取前后核对授权及需求版本，所有数据查询为 SELECT；写开关关闭
时仍可查询。其他收件人/未绑定收件人的包裹不返回，错误绑定或不完整证据停止返回当前页。

小程序每页 5 个包裹，明确区分已发运、合格验收、拒收和未验收；数量使用精确小数字符串，
校验数量守恒、身份、分页坐标和需求版本。GET 使用 no-store/noRefresh，页面隐藏、切换账号
或权限变化后清除数据，迟到响应不恢复旧页面。微信编译另发现需求详情三个既存条件分支错误，
已修复附件、审批步骤和空取消明细的条件/循环结构。

本地相关后端 105 项、小程序 750 项通过；微信 WXML/WXSS 编译及编译后空/非空分支验证通过。
本人收货查询回归已加入 PG16 云端门禁的固定测试清单。
准确提交 SHA 的云端门禁仍需随提交核验。本批无迁移、无权限扩大、无真实业务写入。
当前页仅查询，不登记验收、不创建入账、不读取 SN 明细或把验收当作已入账。
下一步需增加工程师专用收货动作的最小权限、精确收件绑定、异常/SN 校验与独立原请求恢复，
再接小程序验收提交及个人仓入账；不能把现有 `fulfill` 或库存只读权限直接扩成工程师写权限。

## 2026-09-12 小程序独立个人仓入口与备案进度

从准确 HEAD `8cdff840fd886465a61a5b398fe7318acca4cb1e`、分支
`codex/production-readiness-gates` 接续。该基线的 Client `34667110894` 和 PG16
`34667110976` 均已成功；下文“本批待取得门禁”为提交时历史记录，不代表当前基线仍失败。

用户已明确确认小程序备案和阿里云 `rscwz.cn` 备案均通过。2026-09-12 本地公网只读核验：
主域名 DNS 可解析、HTTPS HEAD 返回 200，www 返回 301 到主域名。备案状态来源于用户确认，
未登录控制台复核；域名连通不证明当前正式业务 API、身份配置或微信合法域名配置完成。

新增 `pages/formal-personal-warehouse/index`，底部“个人仓”、工作台及“我的”可进入。
页面仅查询正式 `/v1/inventory/personal/me`，按物料编码/名称/批次搜索，并独立筛选成色和库存
状态；每次最多增加 40 条展示明细，保持三位小数字符串、原单位、资产所属及批次维度，不计算
跨物料总量。未配置个人仓、期初未建立、已建账无明细、筛选无结果和读取失败分别处理。
读取前后重新核验身份和权限，个人仓请求使用 no-store；隐藏、卸载和账号/授权变化清除旧数据，
迟到响应不能覆盖新请求或恢复隐藏页面。工作台已有需求 tab 入口同步修正为 `switchTab`。

本地小程序回归 739 项通过，微信开发者工具 WXML/WXSS 编译通过。提交后以准确 SHA 的
Client 门禁为准。本批不改后端或数据库；个人仓目前仅展示本人库位账户，不能当作全部待收包裹、
SN 明细或库存流水。下步继续工程师端收货/入账与扫码链路，并补真实登录、附件及真机/UAT。
发布 API/PC 地址仍为空，须在目标服务和权限验收后配置；未执行上传、提审、部署或生产业务写入。

## 2026-09-12 收货投影权限修正与 PG16 实测

本批从 `bc22f050535182b29b70da3f5025764d4a40ee51` 接续。该提交的 Client 门禁通过，
PG16 run `34655204094` 已启动真实 runner，但因运行时 RLS/函数清单未推进到 0082 失败；
不是 GitHub 账单阻塞。0082 尚未作为生产迁移执行，本批在该待发布迁移上修正权限边界。

- 同步运行的来源、内部快照 ID、范围、模式、水位和 manifest hash 必须逐项匹配；新增受限
  UPDATE 策略，允许运行完成和失败后显式重试。没有对应收货证据不能伪造 completed。
- 所有收货读取及证据插入均经过同一完整范围：只读来源、来源实例、收货范围、公司、组织、
  完成快照和当前记录。无绑定、其他公司/实例或未完成快照默认不可见；唯一确认的发运映射及
  来源状态、时间、版本和 payload hash 决定证据能否插入。
- 收回 `star_oam_api` 的收货证据 INSERT；API 仅查询证据，投影器按列追加证据。工单/收货
  projector、edge 和 API 的运行时函数清单均精确校验各自允许的调用角色。
- 共享校验不再按名称排除收货策略：完整比较策略集合、表达式、角色，校验函数体 SHA256、
  owner、search_path、EXECUTE ACL、绑定表约束、FORCE RLS 和不可变触发器。
- 新增表保留备份读取；空数据降级恢复 0081 的 RLS 和列权限。已有范围绑定、收货运行或证据时
  阻止降级。收货 worker 必须有同坐标的 enabled 读/写绑定才能启动。

本机现已建立独立的一次性 PostgreSQL 16.15 集群，全部数据为构造测试数据。实际角色测试通过：
跨范围/未完成快照不可见、伪造证据与完成状态拒绝、正常发布及幂等、失败隔离和原快照重试、
API 写入拒绝、备份读取、策略/函数/约束/触发器篡改拒绝、空库降级再升级与 API 启动权限校验。
这些用例已纳入 GitHub PG16 gate；本地专项不等于完整云端发布门禁。本批还需以最终准确 SHA
获取 Client/PG16 门禁结果。

本批仅实现云端**已完成镜像 → OAM 收货证据**的数据库边界。收货采集的生产适配器、edge
收货入镜权限、部署范围配置/人工映射流程、真实数据联调和 UAT 尚须继续，不应把构造快照
验证当作真实 OAM 端到端同步或生产上线。物流签收、本地验收、个人仓入账、通知送达和对账
继续分别验收；备案推进不替代这些门禁。

## 2026-09-12 OAM 收货快照运行账本与失败隔离

OAM 收货只读投影现在为每个已完成快照建立独立 `SyncRun` 运行坐标，绑定来源、范围、模式、快照水位和完成清单哈希；相同快照重复调用只返回幂等结果，不重复创建证据。投影校验或精确业务映射失败后，可记录不含载荷的失败码进入隔离状态，空闲队列会跳过该快照，避免坏快照反复重试阻塞后续快照。修复精确映射等可纠正原因后，必须带原快照 ID 显式重试，成功后才清除失败状态并完成投影；完成清单本身损坏时应重新同步新快照，不修改既有完成证据。

这仍是云端已完成镜像到不可变 `oam_receipt_evidence` 的只读边界，不调用 OAM，不推进本地收货、个人仓入账、通知或对账；独立收货投影 worker 的专用 PostgreSQL 16/RLS 门禁随后由 `20260922_0082` 迁移承接。

## 2026-09-12 OAM 收货证据投影 worker 骨架

新增独立 `app.oam_receipt_projector` 进程入口，沿用正式工单投影器的范围会话锁、Repeatable Read
事务、一次处理/轮询模式和失败隔离输出。它只消费已完成的云端收货镜像，成功后写不可变
`oam_receipt_evidence` 与 `SyncRun`，不会调用 OAM，也不会创建本地 `receipts`、个人仓入账或库存流水。
坏快照返回可追踪的失败运行 ID；带原快照 ID 的显式重试才会再次进入投影。

生产环境入口现在要求既有工单 ACL 证明和收货专用 `20260922_0082` RLS/ACL 证明同时通过；迁移或
范围绑定未安装时仍 fail-closed。SQLite/开发环境只用于契约回归，不代表生产 worker 已可运行。

## 2026-09-12 工单物料履约历史明细

工单详情的“物料履约”页现在在只读历史中返回并展示不可变物料行：物料、库存账户、数量、操作前后成色和
SN 证据坐标，并显示每个 SN 的 SKU/二维码核验结果。历史查询仍先通过正式工单读取授权，只执行 GET，不创建
操作、不调用库存过账，也不把展示结果当作新的履约事实；没有物料行的旧记录会明确显示“无明细”。后端增加
批量行/SN 查询，避免逐条读取造成隐性 N+1；前端对历史行、SN 坐标和核验状态执行严格字段校验。

本批定向验证：工单物料路由 `17 passed`，前端物料操作契约 `6 passed`，TypeScript 检查通过。完整 Web
回归和本批准确 SHA 的 Client/PG16 门禁仍需在提交后取得；OAM 收货证据、真实库存过账、通知送达和对账状态
继续保持独立。

## 2026-09-12 OAM 收货快照投影契约

本批把 OAM 收货纳入边缘只读快照协议：专用范围前缀为 `oam-receipts:`，实体为
`oam_receipt`，载荷严格限定为外部编号、`synced/exception` 状态、带时区来源时间和来源版本。
正式投影只消费已经完成的云端镜像，不访问 OAM；必须先存在唯一的
`ExternalObject(entity_type=oam_receipt)` 和唯一 `approved` 的
`ExternalObjectMapping(local_object_type=shipment)`，且目标发运单真实存在，否则整条记录阻断。
重复的相同载荷幂等回读，冲突不覆盖既有证据。该边界只写不可变
`oam_receipt_evidence` 证据，不推进本地收货、个人仓入账、通知或对账状态。

核验日期：2026-09-08。此文件是当前交接摘要；旧聊天和旧日期段落只作追溯。
产品规则仍以根目录 `AGENTS.md` 和 V1.0 正式需求全文为准。

## 2026-09-11 OAM 收货证据页面只读接线

在后端 `GET /api/v1/material-requests/{request_id}/oam-receipt-evidence` 基础上，正式需求详情页新增 OAM 收货证据面板。前端严格校验证据 ID、来源时间、来源版本、状态和 SHA256，只通过 no-store GET 展示来源时间、版本、发运绑定和载荷指纹；没有证据时明确显示暂无记录，查询失败保持错误提示。

该面板不提供 OAM 写入、不修改本地收货、不创建个人仓入账，也不把 OAM 状态合并到本地履约状态。前端本地回归 `62 files / 1065 passed`，TypeScript 与 Vite production build 通过；本批待提交后重新取得 Client/PG16 门禁结果。

## 2026-09-11 OAM 收货只读证据边界

本批在发运、物流签收和本地收货事实之外增加 OAM 收货的只读证据边界：新增不可变
`oam_receipt_evidence` 表、前向迁移 `20260921_0081` 和
`GET /api/v1/material-requests/{request_id}/oam-receipt-evidence`。证据必须绑定已存在的
`ExternalObject(entity_type=oam_receipt)` 与本地 `Shipment`，以来源版本、来源时间和
payload SHA256 做幂等坐标；重复回读只接受完全相同的绑定，冲突即阻断。

该接口只读取当前需求可见范围内、且有库存读取授权的发运证据，不创建本地收货、个人仓
入账、通知或对账事实。内部 `record_oam_receipt_evidence` 仅作为受控投影边界，未接入
真实 OAM 写入。迁移头已同步为 `20260921_0081`。本地完整后端回归 `3512 passed,
2 skipped`，其中 Alembic 回归 168 项通过；前端 `60 files / 1062 passed`，TypeScript
与 Vite production build、仓库安全检查均通过。本批提交后的 Client/PG16 云端门禁仍需
以准确 SHA 的新运行结果为准。

## 2026-09-11 收货履约查询可见性与身份门禁

本批继续沿收货、异常证据和个人仓入账链路收紧读写边界：收货历史、个人仓待入账历史和物流事件历史在读取前均重新加载正式身份、授权版本及需求可见范围，不能仅凭通用路由权限读取其他需求的履约事实。收货登记、待入账创建和个人仓过账若缺少身份读取通道则直接停止，不发送 POST；有身份通道时先保存原请求恢复哨兵，结果未知仍只允许原坐标只读核验。

本批本地完整后端回归 `3511 passed, 2 skipped`，前端正式 Web 回归 `60 files / 1062 passed`，TypeScript 与 Vite production build 通过。未执行真实 OAM、物流平台或生产写入；异常证据上传、OAM 收货只读证据、通知送达和对账仍是独立后续事实。

发运历史和发运候选查询随后也接入同一需求可见性校验，保证发运、物流、收货和个人仓历史不会因不同查询入口产生权限差异。本次专项回归覆盖发运/物流/收货/入账查询 `33 passed`；前一提交 `528934a` 的 Client 与 PostgreSQL 16 门禁均已通过。

## 2026-09-11 物流签收与异常证据恢复

本批从 `f2d7c216` 接续，仍在 `codex/production-readiness-gates` 分支，目标是把物流事件作为独立事实接入原请求恢复链。

- 新增只读 `GET /api/v1/material-requests/{request_id}/shipments/{shipment_id}/logistics-command-status`。查询按当前需求可见性、包裹与原出库事实、库存读取权限、当前身份和原 `Idempotency-Key` 绑定；`not_observed` 不代表未执行，也不允许自动重发。
- 物流事件页面提交前持久保存原包裹、事件类型、带时区时间、来源、证据文件 ID、外部引用和授权版本。结果未知时刷新继续阻断，只通过原键回读；只有事件字段、包裹、身份和页面上下文全部一致才清除哨兵。
- 页面补充证据文件 ID 与外部引用输入，签收、运输、揽收、异常仍保持独立事件，不推进收货或个人仓入账状态。
- 本地验证：Web `60 files / 1062 passed`，物流/发运/收货/入账/正式 API 后端定向 `121 passed`，TypeScript 与 Vite production build 通过。提交后需再取得准确 SHA 的 Client/PG16 门禁。

本批未执行真实 OAM、物流平台或生产写入；证据文件上传本身、通知送达、OAM 收货证据、对账和真机/UAT仍是后续独立交付项。

## 2026-09-11 收货与个人仓入账持久恢复

本批从 `e26039c` 接续，继续保持 `codex/production-readiness-gates` 分支；本批提交后以实际 SHA 和云端门禁结果为准。

- 收货新增只读 `GET /api/v1/material-requests/{request_id}/receipt-command-status`，按当前身份、需求可见性、原出库事实、库存读取授权和收货内容哈希核验；缺失结果返回 `not_observed`，不自动重发，也不依赖写入开关。
- 收货页面提交前保存幂等键、请求内容和身份授权版本；结果未知时刷新仍保持阻断，只能通过原键 GET 核验后清除哨兵。客户端复现后端收货 canonical SHA256，并检查需求版本和身份上下文。
- 个人仓入账创建与过账使用独立持久哨兵：创建结果未知按原收货单只读回读，过账结果未知按入账单只读回读；两个事实不合并，均禁止自动重复 POST。
- 本地验证：Web `59 files / 1057 passed`，收货恢复后端定向 `83 passed`，TypeScript 与 Vite production build 通过。GitHub Client/PG16 尚待本批准确提交 SHA 的门禁结果。

本批仍未接入真实 OAM/生产写入；物流签收、异常附件完整业务链、通知送达和真机/UAT 继续保持独立验收项。哨兵损坏或本地存储不可用时保持 fail-closed，不能清理后重发。

## 2026-09-11 发运分包与原请求恢复

主开发目录及分支不变，本批从实际 `b2aad06` 接续；该基线的 Client `34430303773` 与 PG16
`34430303689` 已通过，历史账单阻塞及下文旧日期的“PG 待验”不再作为当前基线阻塞。

- Web 可填写本包数量并选择原出库的 SN，发运完成后重新查询剩余数量及包裹历史。
- 提交前重读身份、权限、需求版本、出库余量和 SN，保存原始请求并回读本地记录后只发送一次 POST。
- 新增 `GET /api/v1/material-requests/{request_id}/shipment-command-status`，以原 `Idempotency-Key`
  查不可变发运记录，复核当前身份/授权版本、需求可见性、原出库事实及来源库存范围。
- 恢复 GET 不受全局业务写入开关关闭影响；查询原结果不创建任何业务事实。`not_observed`
  不证明未执行，不允许换键或自动重发。客户端校验原内容 SHA256、目标、承运商、运单、数量、SN，
  再次验证身份及页面上下文后才清理原请求。刷新或切换单据期间的迟到响应继续保留原坐标。
- 分包及恢复候选本地验证：Web `57 files / 1051 passed`，后端发运/HTTP/出库/收货/入账
  定向回归 `147 passed`，TypeScript/Vite 构建通过。新候选的云端门禁以实际提交 SHA 的结果为准。

本批不改库存过账和数据库迁移，不代表物流签收、收货、个人仓入账或消息送达已经完成。
目前发运目标仍使用显式位置/人员 ID；正式目的地选择器、收货/入账各自持久恢复、异常附件完整链路、
工单操作入口、真机及业务 UAT 仍需后续交付。发运恢复记录损坏或存储不可用时保持阻断，
不得直接清除记录后重发；需要先恢复原记录或由有权限人员核对服务器事实。

## 1. 从哪里接续

| 项目 | 已核验值 |
|---|---|
| 本机主工作目录 | `~/Documents/Codex/2026-06-11/oam`（`~` 为本机用户主目录） |
| 开发分支 | `codex/production-readiness-gates` |
| 私有远端 | `https://github.com/likexin12985/rsc-personal-warehouse.git` |
| 已验收功能代码 | `4a6ec83b27f76f84d1d8fe2a9ded699c9b166349` |
| 本次交接准备前，本地与远端 HEAD | `0b426d088d4998fddb9c53e17c2998c93155ce46` |
| Alembic 唯一 head | `20260912_0072` |

本文件随上述 HEAD 的纯文档后继提交发布；接手时重新读取实际 HEAD 和远端，
不要把本文件中的交接前 SHA 当成回退目标，也不要从落后的 `main` 重新开发。
同一台电脑直接使用主工作目录。其他机器通过有权访问上述私有仓库的 GitHub 身份取得
该开发分支，再按 CI 的固定版本重建隔离开发依赖；不搬运原目录中的凭据或生产材料。

本次另查到两个干净的旧工作树，均无主开发分支之外的独立提交，无待合并工作：

- `~/.codex/worktrees/06f6/oam`：`codex/mini-post-recovery`，`6349d8d`。
- `~/.codex/worktrees/8403/oam`：detached `0bb7091`。

这些旧工作树保持原样。主目录的四个未跟踪目录为受保护本地材料：
`cloud_oam/deployment/offhost_backup/`、`operations/`、`preproduction/`、`recovery/`。
不清理、不暂存、不打包上传；其存在不表示有未提交的本批业务代码。

## 2. 哪些已经完成

- 核心库存账户、不可变流水、余额/SN 投影、权限、审计和事务 Outbox 已建立。
- 期初与日常盘点主要服务、页面、复核、过账和历史恢复已实现；正式控制库存接入及
  受控期初启动仍有缺口，不能把这些服务的测试通过当成真实业务建账已就绪。
- 需求提报、三级逐行审批、供给计划、Web 分配、占用、释放、拣货和实物出库主要路径已接入。
- 0072 按原拣货及原出库行建立独立出库事实，支持数量/SN 分批，库存从拣货中转入在途。
  原出库单/行仍是不可变拣货快照；当前出库数量必须从 `outbound_postings` 读取。
- 实物出库使用唯一预置且明确绑定的在途账户，保持原所有权、保管人、位置及物料维度；
  不自动创建账户或猜测交接责任。网页提交结果未知时保留原请求，只用 GET 核验，不重发 POST。

准确范围、接口和出库回归见[实物出库验收](OUTBOUND_POSTING_ACCEPTANCE.md)。
旧迁移 0071 及更早版本不可为新功能直接改写；下一批使用前向迁移并同步安全目录。
0072 首次空图降级失败已在 `4a6ec83` 修复并通过真库，不再当作当前阻塞。

## 3. 可复核的测试证据

| 工作流 | 对应提交 | 结果 |
|---|---|---|
| [PG16 34215778501](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34215778501) | `4a6ec83` | success；真库迁移/并发 1 passed，后端/边缘门禁 2416 passed、2 skipped |
| [Client 34215778415](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34215778415) | `4a6ec83` | success；Web 1009、小程序及门禁契约 724 项通过，类型/构建/安全检查通过 |
| [Client 34218828264](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/34218828264) | `0b426d0`，仅文档 | success；未改变上述后端/迁移基线 |

PG16 使用 GitHub Actions 的一次性隔离数据库，本机没有可用 Docker/PostgreSQL 16 真库。
本地环境跳过真库测试不计通过；不把门禁指向共享或生产数据库。
后续仅文档提交通常只触发 Client；任何新后端/迁移代码必须取得其准确 SHA 的完整 PG16 结果。
既有 Web 主包约 872 kB（gzip 223 kB）的体积提示仍在，未作为失败隐藏。

## 4. 下一批交付：发运与分包

按 V1.0 第 1.8、2.3、3.7 节及[出库验收的后续范围](OUTBOUND_POSTING_ACCEPTANCE.md#后续开发)继续：

1. 建立正式 `shipments`、`shipment_lines`、`shipment_serials`，逐笔绑定原出库事实和
   出库行，支持同一出库事实分为多个包裹；不能按旧快照的 `outbound_qty=0` 计算剩余。
2. 冻结来源、目标位置、人员和需求修订关系；人工填写承运商、运单、交运时间。
   首期不接承运平台、不自动下单，地址文字或同名人员不得作为身份绑定。
3. 实现只读候选、独立幂等交运命令、原请求状态查询和 Web 页面。
   累计交运不超过原出库数量，SN 归属唯一且不可重复发运；一包完成不代表整单完成。
4. 发运事实、需求版本/状态事件、审计及 Outbox 同事务提交，登记交运不得再次移动或扣库存。
   结果未知时只读恢复原命令，后续状态变化后仍能核验原出库和拣货命令。
5. 补数量/SN 守恒、越权、竞争、幂等、整笔回滚、历史恢复及发运前后库存完全不变的测试；
   新迁移核验精确函数/触发器、空图降级再升级及非空事实降级阻断，再通过 Client/PG16 门禁。

优先参考这些代码：

- `backend/app/formal_services/material_request_outbound.py`、`material_request_outbound_options.py`。
- `backend/app/inventory_models.py`、`demand_models.py`、`database_security.py`。
- `backend/app/routers/formal_material_requests.py`、`backend/app/material_request_outbound_schemas.py`。
- `backend/alembic/versions/20260912_0072_outbound_postings.py`。
- `backend/tests/test_material_request_outbound.py`、`test_outbound_postings_migration.py`、`pg16_outbound_gate.py`。
- `frontend/src/FormalMaterialRequestOutboundPanel.tsx`、`materialRequestOutbound.ts`、`materialRequestOutboundRecovery.ts`。

上述路径相对 `cloud_oam/`。发运完成后继续人工物流/签收、工程师分批及异常验收、
独立个人仓入库流水；通知、OAM 收货证据及对账不能代替任何一步。

## 5.1 本次继续开发进度：0073 发运与分包

已在主工作目录实现 0073 前向迁移、`shipments`/`shipment_lines`/`shipment_serials`、人工承运商/运单登记 API、只读发运列表、Web 发运面板，以及数量/SN 守恒和“登记发运不再次扣库存”的回归测试。当前实现仍把物流签收、分批验收、OAM 收货证据和个人仓入账作为独立后续事实；不能把发运登记当作收货或入账完成。

本地专项结果：后端 0072 出库与 0073 发运相关 46 项通过；前端发运契约 2 项通过；TypeScript 类型检查通过。新迁移和后端代码仍需取得准确提交 SHA 的完整 PostgreSQL 16 门禁后，才能进入下一批收货/个人仓入账。

## 5. 完整 V1.0 和上线仍缺什么

- 上述发运收货链；审批代理、替代料以及已分配/拣货后的完整取消补偿。
- 正式工单投料、消耗、旧坏件回收；人员调拨、退回、报损报废和离职交接。
  被 production 隔离的旧原型页面/路由不能计入正式完成。
- 小程序履约、统一扫码等入口；正式报表、批量导入导出、打印和通知送达链。
- OAM 可信控制库存发布、组织/人员正式只读发布及日终对账、受控建账启动准备。
- 首管理员与真实身份/短信/微信/附件联调、预生产迁移及备份恢复、压测、真机/UAT、灰度和发布放行。

开发与备案并行推进。当前未部署生产、未执行外部业务写入，也未因本交接新增此类授权。
此前“6–10 周”已经明确更正为缺乏工时测量的粗估，不作为交付承诺。
下一批记录实际开发、CI 等待、返工和可获取的额度变化；账号共享用量不能直接归因于本项目。
不在交接文档保存账号标识、余额、验证码、令牌或登录材料。

## 6. 0074/0075 收货与个人仓入账进度

当前分支已完成从发运到个人仓入账的后端主链：0074 新增物流事件、收货、收货异常、待入账事实；0075 新增不可变 `inbound_postings` 事务绑定；0076 新增独立 `material_request.fulfill` 写权限。收货接口执行数量/SN累计校验，入账确认调用统一 `post_inventory_transaction`，不直接更新库存表。详情页已提供创建待入账单和确认过账入口。

最近提交：`4ade78e`（fulfillment 权限回归）。本地验证：前端正式 Web 测试 53 个文件/1013 个用例通过，TypeScript 与 Vite production build 通过；迁移与 ORM 对照通过，数据库安全/PG16 相关测试 303 passed、1 skipped；仓库安全脚本通过。Vite 仅提示主 chunk 体积偏大，未影响构建。GitHub 门禁仍需以账号额度恢复后的真实运行结果为准。

## 7. 接手续检及运行环境

同机先在主目录执行以下只读检查，确认后按第 4 节直接开发：

```sh
cd "$HOME/Documents/Codex/2026-06-11/oam"
git status --short
git branch --show-current
git rev-parse HEAD origin/codex/production-readiness-gates
git worktree list
git diff --name-only 4a6ec83b27f76f84d1d8fe2a9ded699c9b166349 HEAD
```

读取实际远端 ref 与工作流结果；缓存的 `origin/` 引用不能代替一次实时远端读取。
每次 commit 和 push 前，从仓库根运行 `bash cloud_oam/scripts/verify_repository_safety.sh`。
只暂存本批明确文件；不使用 `git add .`、`git clean`、强制推送或重置旧工作树。

本机后端解释器为 `cloud_oam/.venv/bin/python`；Node 为
`~/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`。
接手先验证路径仍存在；新机器以 CI 配置的 Python/Node/锁文件为准，不依赖本机绝对运行时路径。
相关专项完成后跑所需门禁；文档更改不需要重新跑本地全套业务测试。

本会话建议的开发配置是 GPT-5.6 Sol / High / Standard（Fast 关闭）；这是建议，
没有代替用户更改选择器。轻量页面可用 Medium，疑难事务可专项复核；不常驻 Max/Ultra，
不以切换模型证明服务容量、代码状态或业务验收已恢复。

## 7.1 物流事件 Web 接入（当前增量）

最新实际 HEAD：`c512ecd`（`test: cover logistics timeline ordering`）。
0074 已建的 `logistics_events` 现在具备正式写入 API、按发运单读取 API、Web 适配器严格响应/输入校验，
以及发运面板中的事件历史展示和 `pickup / transit / signed / exception` 登记入口；
收货验收面板已接入收货人、发运明细、合格/拒收数量、验收条件和 SN 输入；
异常事实已写入 `receipt_exceptions` 并在收货响应、历史页面展示。
异常收货明细可绑定证据文件 ID，并纳入收货幂等请求指纹；前端合法证据 ID 契约已覆盖。
个人仓入账历史根据不可变 `inbound_postings` 绑定派生 `posted`，不改写原始入账单事实。
物流事件登记只追加签收/运输事实，不改变收货验收状态、库存或个人仓入账状态。

待入账单创建现在在同一事务写入幂等 `personal_inbound_order_created` Outbox 事实，供待办与通知处理；库存过账仍由统一库存过账服务产生独立库存事件。

物流事件写入与历史读取均复核发运事实的来源库存账户范围；本批履约后端定向回归 17 项、Web 发运/收货/入账契约 10 项通过。
随后扩展履约与数据库安全回归至 317 项，全部通过。
收货与物流时间线现均拒绝早于交运时间的事实；修复后物料需求全套仍为 513 passed、1 skipped。
物流时间线专门契约回归新增 2 项，履约相关定向回归合计 18 passed。

本批本地验证：正式 Web 测试 53 个文件/1019 个用例通过，TypeScript 检查和 Vite production build 通过，后端完整测试 3353 passed、2 skipped，仓库安全检查通过；
已推送 `origin/codex/production-readiness-gates`。下一步仍需补正式收货验收录入页面、异常证据处理、
收货/入账历史读取和端到端 PostgreSQL 16 门禁；云端门禁结果仍受账号额度与网络可用性影响。

## 7.3 收货异常与个人仓入账边界修正

提交 `3e7a786` 修正了收货异常与个人仓入账之间的数量边界：混合收货中的拒收/破损行继续只保留为收货异常事实，个人仓过账只读取合格数量；整张收货单没有任何合格数量时明确阻断，不生成空库存交易。新增两项服务回归，收货/入账仍保持独立状态轴，未改变既有库存过账入口或迁移。

本次定向验证：`tests/test_material_request_inbound_contract.py` 为 6 passed；提交前后仓库安全检查均通过，准确 SHA 已推送到既有开发分支。该修正尚未取得新的 PostgreSQL 16 真库门禁，不替代端到端收货、SN 和并发验收。

## 7.2 远端门禁现状

截至本次核验，最新 Client 工作流 `34276684676` 对应 HEAD `4a23049`，job 在启动约 3 秒后失败，
`steps: []`，没有执行任何测试步骤；更早的 PostgreSQL 16 工作流 `34275459633` 也呈现同样的快速失败形态。
这属于 GitHub 账号额度/运行环境阻断，不能作为代码失败证据，也不重复触发无效重跑。

## 8. 新账号可直接粘贴的提示词

> 在 ~/Documents/Codex/2026-06-11/oam 主仓库继续 RSC 个人仓开发。
> 先完整阅读 AGENTS.md、docs/RSC个人仓与物资运营扩展系统_正式生产版需求与架构设计_V1.0.md，
> 再读 cloud_oam/docs/CONTINUE_DEVELOPMENT.md 和 cloud_oam/docs/OUTBOUND_POSTING_ACCEPTANCE.md。
> 核对 codex/production-readiness-gates 的实际 HEAD、远端、工作树及准确 SHA 的门禁。
> 当前主链已到 0072 实物出库；直接实施下一批“发运与分包”的后端、Web、断线恢复、迁移和测试，
> 发运必须绑定原出库事实，累计数量/SN 不超量，不能再次扣库存。完成后继续收货与个人仓入账。
> 维持 V1.0 独立状态、正式权限及 PG16 门禁，安全检查通过后提交推送到既有私有开发分支。
> 保护本地部署目录，开发与备案并行，不操作外部生产系统，不重新开发已验收功能。
> 每批报告实际开发耗时、测试等待、完成内容和可归因的额度变化，不再给未经实测的总工期承诺。

## 7.4 收货与入账页面事实绑定修正

最新实际 HEAD：`d8dd7c3`。收货验收页改为加载当前需求的已登记发运事实，并用真实
`shipment_line_id` 下拉选择；同时修复前端发运响应校验丢失明细主键的问题。个人仓入账页改为
从当前需求的收货历史选择收货单，避免手工输入任意 UUID。两项改动均只改变入口校验和展示，
不改变后端事实、库存过账或状态轴。

本地前端全量回归：53 个测试文件、1020 个用例通过；TypeScript 构建通过。提交前后仓库安全
检查均通过，已推送 `origin/codex/production-readiness-gates`。后续仍需继续补正式工单履约、
通知送达、OAM 可信控制库存和 PG16 真库门禁等 V1.0 缺口。

## 7.5 个人仓入账真实服务契约与重试修复

本批修正了此前替代过账服务的测试未发现的实际阻断：个人仓入账从原在途账户转入本账
个人仓，应使用 `transfer`；`inbound` 仅用于外部进入受管账。已验收合格数量现在解析到
预置 `available` 账户。重试不再绕过所属需求、发运目标和统一库存过账鉴权；错误通过
正式 HTTP 状态返回并回滚。新增数量/SN 场景，收货/入账专项合计 24 passed；
扩大物料需求与库存过账回归为 617 passed、1 skipped（130.45 秒）。

详见 [个人仓入账服务验收记录](PERSONAL_INBOUND_ACCEPTANCE.md)。此前“后端主链完成”的描述
应理解为已有接口和模型，不能作为真实首笔过账或 PG16 联调完成的证据；本批无生产写入。

## 7.6 个人仓入账页面恢复

基于实际 HEAD `93cd4e4` 的本批改动：个人仓入账页现在能从服务端历史继续处理待入账单；
写请求结果未知时暂停重提交，只有只读 GET 核验按钮可解除状态。前端新增 2 项恢复契约，
全量回归为 54 个文件、1023 个用例，TypeScript 构建通过。该页面恢复不改变后端事实，
也不把 GET 回读当成库存过账完成。

## 7.7 正式工单物料事实表基础

基于 `7f21ed8` 新增迁移 `20260917_0077`：`work_order_material_operations`、
`work_order_material_lines`、`work_order_material_serials` 和
`work_order_replacement_pairs`。四张表均为只追加事实，分别保存 OAM 工单绑定、操作类型、
个人仓库存账户、数量/成色、SN 的 SKU/二维码校验，以及以换代修新旧件配对；没有复用旧
`work_order_materials` 原型表，也没有自动猜测 OAM 工单或人员。

本批只建立正式数据边界和迁移/ACL 基础，尚未宣称投料、消耗、释放、回收服务完成；下一批
 将实现逐工单权限预检、批量整批校验和统一库存过账命令。0077 的 PostgreSQL 16 真库门禁
仍未取得。

## 7.8 正式工单物料只读预检

新增 `formal_services/work_order_material.py` 作为正式工单物料操作的只读预检边界：只接受
活动状态的 OAM 工单、精确匹配的工单工程师和操作人、启用中的正式物料，以及属于该操作人的
`available` 库存账户；物料、成色和库存账户必须逐项一致。整批明细会先校验数量、状态、重复
物料和重复 SN，全部通过后才允许后续命令进入写入阶段。本批没有创建操作事实，也没有库存过账；
对应后续的占用/消耗命令必须继续调用统一库存过账服务。

预检模块同时提供操作请求指纹生成器，固定工单、操作人、操作类型和整批明细的规范化哈希，
供后续幂等写入使用；不接受未定义的操作类型。

本批继续增加 `record_posted_operation`：它要求调用方先取得状态为 `posted` 的统一库存事务，
再追加工单操作、明细和 SN 校验事实；重复幂等键且请求指纹一致时只返回原事实，指纹或库存事务
不一致时阻断。该函数本身不改库存余额。

新增正式路由 `POST /api/v1/work-orders/{work_order_id}/material-preflight`，只提供当前登录人员
的只读预检结果；路由要求 `work_order_material:operate` 权限，并拒绝伪造操作人。它不会创建操作
事实或改变库存，后续写入命令仍须在统一库存过账成功后调用事实写入服务。

本批新增 `POST /api/v1/work-orders/{work_order_id}/material-operations`，接收操作类型、幂等键、
整批明细和统一库存事务 ID，只接受已经 `posted` 的库存事务，再追加正式工单操作事实；同一幂等
键可安全重放，绑定不同请求或不同库存事务时返回冲突。生产 App 导入仍由配置层要求真实
PostgreSQL URL，未用占位数据库绕过门禁。

操作 DTO 现支持声明新旧 SN 配对并纳入请求指纹；实际登记命令暂拒绝只有 UUID 的配对，必须由
新件消耗和拆回件入账的关联命令生成 `work_order_replacement_pairs`。

预检现在还会逐 SN 读取正式库存序列、生命周期和当前位置，要求 SN 的物料与明细一致、状态为
`active`，且当前位置正是提交的库存账户；只提交 SN UUID 但无法证明库存坐标时会阻断。

带 SN 的明细现在要求 SN 数量与物料数量逐一相等；数量为 2 却只提交 1 个 SN 等情况在预检阶段
直接拒绝，避免库存数量与序列号事实分叉。

新增只读历史接口 `GET /api/v1/work-orders/{work_order_id}/material-operations`，按正式工单返回
不可变操作事实，供履约页面刷新和断线恢复；该接口不推断库存状态，也不修改任何事实。
历史接口现在先核验正式工单存在，不会把不存在的工单伪装成空历史，缺失对象明确返回 404。

事实写入还会校验操作类型与库存事务类型严格对应：占用/释放/消耗/回收/冲销分别只能绑定
`reserve`/`release`/`consume`/`inbound`/`reversal` 事务，跨类型绑定直接冲突（回收类型修正见 7.15）。

操作写入 DTO 现在在 HTTP 边界使用枚举约束，只允许 `occupy`、`release`、`consume`、`recover`、
`reverse` 五类正式操作，未知类型在进入服务前即被拒绝。

## 7.9 工单操作证据修正与真实剩余范围

本节修正 7.8 的完成口径。此前的 9 项测试主要覆盖纯函数/DTO，没有验证数据库写入、
HTTP 数据范围或原库存事务绑定。`record_posted_operation` 仍是已有库存流水的证据登记器，
不能代替“批量占用、消耗、回收及操作事实同事务提交”的正式业务命令。

本批修正：

- 登记必须重新校验当前权限版本、工单工程师和对象范围；写入通过现有 owner-lock 函数锁定
  OAM 工单，避免对 API 只读投影直接申请 UPDATE 权限。
- 只接受 `source_document_type=work_order_material` 且来源 ID、过账用户精确匹配的流水；
  行数、个人库位、账户、物料、成色、数量和原流水 SN 集合逐项一致。
- 不再按 SN 的当前可变位置验证已发生的消耗事实；历史回读使用不可变流水，工单关闭后
  仍可按原键读取既有事实，换键不能重复登记同一事务。
- 每个受控 SN 必须提供并验证实际 SKU、SN 和二维码值，禁止收到 UUID 就自动写
  `sku_verified=true` / `qr_verified=true`。错误或缺失三码证据不产生操作事实。
- 任意新旧 SN UUID 配对不构成以换代修证据，当前明确阻断；后续必须由新件消耗与拆回件
  入账的关联命令建立配对。此前“已实现配对”仅是 DTO/表结构，不能计为正式回收完成。
- 历史 HTTP 查询验证具体工单范围并禁用缓存。预检返回 `coordinates_validated`，不再使用
  容易被误读为库存可过账保证的 `ready_for_posting`。数量限制为有限的 numeric(18,3)，输入
  拒绝未定义字段；同值 `1` / `1.000` 使用相同请求指纹。

新增隔离 SQLite 数据库及 HTTP 契约覆盖跨工单、跨人员、数量不符、伪造扫码、缺失扫码、
历史重放、重复绑定和数据范围。测试模拟历史流水和当前身份，不等于真实扫码验收、
完整生产鉴权种子、统一库存命令联调或 PostgreSQL 16 并发验收。

本批新增 `execute_consume_operation` 的事务组合：调用统一 `post_inventory_transaction` 后在同一
未提交会话追加工单事实；路由提交或回滚整个会话。当前专项测试验证了调用顺序和失败边界，
但尚未取得真实 PostgreSQL 16 的余额投影/并发结果。下一步仍须实现占用、释放、回收命令、
对应权限迁移、业务审计及 Outbox、以换代修双向流水配对、完整回滚/并发测试和准确 SHA 的
PG16 门禁。保留上述目标，
不要把当前证据登记接口当作已完成的工单物料功能上线。

本批验证：扩展库存/锁约定/数据库安全回归 422 passed（28.77 秒）；随后补充 HTTP 成功扫码重放、
数据库晚期异常回滚及权限版本失效，工单专项合计 34 passed（2.63 秒）。
旧 HEAD `abd1df1` 的 PG16 run `34289777177` 在 2 秒内终止，`steps: []`，未执行测试。
本批仍没有真实 PostgreSQL 16 并发通过证据。

## 7.10 原子消耗命令

新增 `POST /api/v1/work-orders/{work_order_id}/material-operations/consume`。它只生成 `consume`
库存流水：每条明细从本人个人仓账户扣减，统一过账返回成功后才追加工单操作事实；路由最后才提交
数据库事务，异常时回滚两类事实。相同幂等键由库存过账和工单事实同时回读，工单后来关闭也不
会把已完成的幂等重放误判成新操作。

本批服务专项 35 passed，覆盖 evidence/DTO/HTTP 路径和原子调用编排；由于本机无 PostgreSQL 16，
尚未把真实余额投影、并发锁和期初证据图当作通过。占用、释放、回收仍需各自的账户方向、
权限、审计和 PG16 门禁。

## 7.11 释放占用命令

新增 `POST /api/v1/work-orders/{work_order_id}/material-operations/release`。释放必须同时提供
占用来源账户和本人可用个人仓目标账户；服务逐项核验物料、人员、个人库位和目标状态，调用统一
`release` 库存流水后再登记工单事实。目标账户纳入幂等指纹，不能用同一幂等键改投另一目标。
真实余额投影和 PG16 并发仍待门禁验证。

## 7.12 工单占用顺序约束

新增 `POST /api/v1/work-orders/{work_order_id}/material-operations/occupy`。占用只允许从本人
`available` 账户移动到本人 `reserved` 账户；消耗和释放的来源账户现在必须是 `reserved`，从而
把“先占用、再消耗或释放”的 V1.0 顺序写入服务边界。三类命令共用统一库存过账和幂等键，
当前专项回归 36 passed；实际余额投影、SN 位置并发和 PG16 门禁仍待验证。

## 7.13 操作事实审计与待办事件

工单物料操作事实现在与 `material_request` 审计链、Outbox 待办事件在同一数据库事务中追加。
缺少审计链头时写入会 fail-closed；同一操作重放只返回既有事实，不重复产生审计事件或 Outbox
事件。专项回归新增审计链和 Outbox 幂等断言，当前工单物料证据测试为 21 passed。

这只证明 SQLite 事务边界和事件幂等契约；正式 PostgreSQL 16 门禁、事件消费者、权限迁移、
余额投影并发和收货/个人仓入账链仍需继续实现与验证。

## 7.14 工单物料权限迁移

新增 Alembic `20260918_0078`，为正式工单物料历史读取和占用/释放/消耗/回收操作建立独立的
`work_order_material/read` 与 `work_order_material/operate` 权限，并向 admin、区域经理和
技师角色写入最小授权。迁移快照、权限集合和回滚检查已通过；真实 PostgreSQL 16 门禁当前
仍因环境未提供数据库而跳过，不能视为 PG16 通过。

迁移全量回归随后通过 `168 passed`（约 149 秒），覆盖升级到 `20260918_0078`、ORM 快照、
权限集合、角色授权和可回滚性；该结果仍不替代真实 PostgreSQL 16 并发门禁。

个人仓入账成功后，入账单状态与需求单 `personal_inbound_status` 现在在同一事务中更新为
`posted`；重复回放只复用既有库存事务和绑定，不重复增加库存事实。该状态是需求侧读模型，
库存事务仍是入账事实来源。收货审计/通知事件和真实 PostgreSQL 16 余额并发验证仍待完成。

## 7.15 回收请求与实际验证范围修正（2026-09-09）

复核 `519de8d` 发现，所谓独立 recover DTO 实际误绑到了 release 接口，recover 仍引用 release
DTO；仅检查路径存在的测试没有覆盖该错误。本批修正两个接口的 OpenAPI 类型，并通过真实 HTTP
请求验证调用的命令、返回结果和异常回滚。四个执行接口均拒绝请求操作人与当前登录人员不一致。

按 V1.0 第 1.9 节，回收明细现在只接受明确的 `used` 或 `damaged` 成色和一个
`target_stock_account_id`，不再接收无用途的来源账户 `stock_account_id`。目标账户必须属于当前
人员、位于有效个人仓、处于可用状态且成色一致。通用事实登记入口也不能绕过旧/坏件条件。
调用方须使用新的目标账户字段契约；释放接口继续要求来源和目标两个账户。

真实库存校验另发现旧 recover 命令把外部拆回件写成 `return`，而该类型只允许受管账户间移动。
本批改用外部入库 `inbound`，业务事实仍为 `recover`；未放宽统一库存服务对内部退回的限制。

四个命令原来每次重新生成 `effective_at`，与库存幂等指纹冲突。现在持有工单锁时，只复用同一
业务过账键下的原始时间，仍把当前操作者和当前输入交给统一库存服务重新计算指纹、核验权限。
没有原交易且工单已关闭时，在调用库存服务前拒绝新操作。四类命令的真实库存服务回放测试覆盖
原时间恢复、关闭工单后的原请求、修改数量冲突和撤销库存权限；首笔交易使用隔离预置证据。

另有旧件/坏件两条测试从完整已复核的期初图开始，使用真实 SQLite 库存服务验证余额 0→1、
重复调用仍为 1，以及库存过账后业务审计异常时余额、流水、工单事实和 Outbox 整笔回滚。
HTTP 输入、OpenAPI、目标账户、事实/审计/Outbox 与上述余额测试分别提供证据，均不等于
PostgreSQL 16 锁竞争、真机扫码、以换代修配对或生产放行通过。

本批验证：工单 HTTP/证据/预检与统一库存过账回归合计 `165 passed`（31.71 秒）。下一步仍须
验证迁移后真实技师角色能否完成所需库存过账授权，以及准确 SHA 的 PostgreSQL 16 门禁。
## 7.16 工单过账的最小权限边界

工单物料的四个原子命令现在向统一库存过账服务传递明确的权限资源
`work_order_material`/`operate`。库存账户范围仍由统一过账服务锁定并逐账户核验，但不再把
技师提升为可直接调用任意库存过账能力的 `inventory_transaction/post` 角色。真实回放测试
移除广泛库存过账权限后仍能重放原工单事实；移除工单操作权限则在进入库存服务前拒绝。

这只证明当前正式权限模型和隔离测试一致；迁移后的真实技师身份、RLS/数据库 ACL 与
PostgreSQL 16 并发门禁仍需在准确 SHA 上验证。

## 7.17 工单详情物料履约回读入口（2026-09-09）

Web 工单详情新增“物料履约”页签，使用正式工单物料历史接口读取占用、消耗、释放和收回事实。
页面只读展示操作号、操作类型、库存过账事务和状态；接口响应执行严格 UUID、字段集合和版本校验，
不接受旧 `/work-order-materials` 原型数据，也不会把读取失败当作写入成功。该入口为后续正式操作面板、
收货和个人仓入账联动提供事实回读位置。

本批前端全量回归为 `55 files / 1028 tests passed`，生产构建通过；结果仍不替代真实 PostgreSQL 16
门禁、RLS/ACL 和收货/入账端到端验证。

## 7.18 履约历史操作类型契约收紧（2026-09-09）

工单物料原子操作历史的前后端响应契约现在使用 Literal 集合校验：`occupy`、`release`、`consume`、
`recover` 和通用事实登记入口保留的 `reverse` 均为合法操作类型，终态为 `posted`。未知操作、未知状态
或额外字段会失败关闭；Web 页签把 `posted` 显示为“已过账”，把 `reverse` 显示为“冲销”。这保持了
原子命令边界，同时不丢失通用冲销历史。

本批前端页面回归为 `55 files / 1029 tests passed`，后端工单路由与预检回归为 `31 passed`，生产构建和
仓库安全检查通过；仍不替代 PostgreSQL 16 真库门禁。

## 7.19 PG16 readiness head 修复（2026-09-09）

GitHub 付款方式更新后，Client gate 已在真实 runner 通过；PG16 gate 首次实际执行暴露出
`rsc_oam_runtime_binding_ready_0044()` 仍固定检查 `20260912_0072`，而 Alembic head 已到
`20260918_0078`，导致 `edge_inbox` 与 `star_oam_projector` 的绑定就绪检查错误失败。
新增 `20260919_0079` 只推进该 readiness marker，保留函数 OID、owner、ACL、SECURITY DEFINER、
search_path 和精确 prosrc SHA 校验；降级直接恢复稳定的 0072 marker，以保证继续回退到历史
0072 migration 时仍可验证。运行时安全 manifest 和 PG16/Alembic head 常量已同步到 0079。

本批本地验证：readiness 专项 `3 passed、1 skipped`；迁移与 OAM 投影安全回归 `182 passed`。
真实 PostgreSQL 16 并发 gate 已具备重跑条件；首次真实执行继续暴露出 `inbound_postings`
缺少 `star_oam_api` 的 `SELECT/INSERT` ACL，已由后续 0080 迁移修复；0080 同时把 readiness
marker 原子推进到当前 head，尚未把云端重跑结果写成通过证据。

## 7.20 分批收货与个人仓入账状态聚合（2026-09-09）

个人仓状态现在按当前修订的每条最终批准明细分别聚合：`pending_acceptance` 表示已经发运但还没有
合格验收，`partially_accepted` 表示仅有部分合格数量，`accepted` 表示全部合格数量已收货但仍有
数量未过账，`posted` 只有在每条正净批准明细都已由独立 `inbound_postings` 绑定已过账库存事务时
才成立。拒收数量不计入个人仓，取消数量从应入账量扣除；不同物料不会互相抵消，已取消的空需求不会
因真空聚合被误标为已过账。

该读模型在发运、收货和个人仓过账事实提交后刷新；状态变化同步推进需求版本，避免旧出库事实在当前
版本仍被误判为同版本历史。相同收货幂等键在版本变化后先回读既有事实再返回，分批写入不会重复增加
版本或审计/Outbox 事实。个人仓入账单的 `posting_transaction_id` 与 `inbound_postings` 绑定同一
库存事务，后续审计或 Outbox 失败时随事务回滚。

本批本地定向回归：收货、发运、入账与状态聚合 `91 passed`；此前已取得准确 SHA 的最新提交
`3047344` 的 Client gate 与 PostgreSQL 16 release gate 均通过。状态聚合新增代码仍需随本批准确
提交 SHA 重新通过两条云端门禁后，才能作为发布证据。

## 7.21 入账历史绑定回读

入账单历史响应现在显式返回 `posting_transaction_id`。待入账单返回 `null`，已过账单返回与
`inbound_postings` 相同的不可变库存事务 UUID；状态只在该绑定存在时标记为 `posted`。前端严格
响应契约同步收紧并增加绑定回读测试，避免只凭状态文字判断个人仓入账事实。

## 7.22 入账目标沿用发运绑定

发运历史响应现在带回服务器已校验的目标位置和目标人员。入账页按收货单关联的发运单自动填充
这两个坐标并设为只读；没有完整发运目标绑定时不允许创建入账单。这样收货、个人仓入账和发运
目的地保持同一事实链，页面不再依赖人工复制 UUID。

## 7.23 正式退回链通知事实（2026-09-16）

正式退回单的提交、取消、发出、分包、验收和个人仓入账现在分别登记持久通知事实，
每条通知以对应业务事实 ID 和事件类型幂等；收件人只从目标保管责任的当前有效人员映射
解析微信 openid 或手机渠道。通知记录与退回、库存、验收和入账保持独立，当前实现不调用
微信/短信供应商，也不把排队或发送状态当作业务完成。退回链定向回归 137 项通过，通知
事件定向回归 3 项通过；该提交仍需随准确 SHA 重新取得 Client/PG16 门禁。

## 7.24 通知投递扩展 worker（2026-09-16）

新增 `app.notification_expander` 有界进程入口，负责按批次把 `pending` 通知事实扩展为
`queued` 投递事实，支持 `--once`、批量上限和受控轮询；事务失败会回滚并以明确错误码退出。
该进程不调用微信、短信或其他供应商，供应商调用仍必须由独立 worker 领取队列并写入精确
发送结果。worker 单测与通知扩展回归共 8 项通过，准确提交 SHA 仍需重新通过 Client/PG16
门禁。

## 7.25 通知供应商投递 worker 边界（2026-09-16）

新增 `app.notification_dispatcher` 有界投递进程：先提交 queued 投递的单 worker lease，
再在短事务外调用显式注入的微信/短信/飞书适配器，最后用同一 lease 记录一次精确 provider
结果。请求哈希只包含渠道、当前收件坐标、业务 payload 和尝试序号，不记录 token、Cookie 或
响应敏感字段；供应商超时、连接异常或适配器未配置均清除 lease 并记录失败/未知结果，未知
结果不会被 retry helper 自动重排，避免重复发送。

基础部署包不提供供应商凭据或默认网络适配器；未注册经过审核的适配器时 worker 在领取前
直接返回 `notification_provider_not_configured`。这使本地和门禁运行不会向外部系统发送消息，
同时给生产部署留下明确的适配器注入边界。当前新增 dispatcher 回归待准确 SHA 的 Client/PG16
门禁确认。

## 7.26 发运通知事实的 API ACL 修复（2026-09-16）

真实 PostgreSQL 16 收货链首次走 `star_oam_api` 时，发现发运事务在写入通知事实前只有
`notification_events` 读权限，`notification_recipients` 也没有运行时写权限，最终以 SQLSTATE
`42501` 失败。现有 `20261017_0107` migration 的 PostgreSQL 分支补充向 API 只授予这两张表的
`SELECT/INSERT`，不授予更新或删除；运行时安全 manifest 与静态 ACL 回归同步更新。这样通知事件和收件人坐标仍与
发运、收货、入账等业务事实在同一事务内原子提交，投递 worker 的 provider 边界保持不变。

本地迁移全量回归 `168 passed`，通知与 ACL 定向回归 `15 passed`，仓库安全检查通过；云端
PG16 动态门禁需以包含该修复的准确 SHA 重新执行。
