# 8.02 日终对账正式后端与迁移验证

本批把已验证的日终采集、比较、映射治理、不可变截止和进程入口移入正式后端，并新增
Alembic **0130**。最终 **39 个实际 PostgreSQL 16.15 场景通过**，权限/目录聚焦
**370 项、15 项子检查通过**，迁移链与 SQLite 完整空库升降级 **2 项通过**。
本批三个新建实例全部停止，尚未提交、推送或部署。

固定证据：[verification.json](../artifacts/daily-formal-integration-20260921/verification.json)，
SHA256 `faf2028b22f4b1ed70b5f96ef7cb3213c3228534a33acac9c892705fb3f2ff40`。本批 1,409 个非 Markdown 输入在最终 PG16 运行中保持摘要；
没有当前完整静态或 GitHub 发布门禁通过结论。

## 正式代码与事实边界

- [日终模块](../backend/app/daily_reconciliation/)采用 7.95 已修正的非目标仓覆盖适配、
  7.96 事务剩余期限和 7.97 独立进程入口。现有候选 JSON schema 标识保留，避免改变
  已审核的摘要协议；SQL 函数和触发器采用正式 0130 名称。
- [0130 迁移](../backend/alembic/versions/20261109_0130_daily_reconciliation_cutoffs.py)新增
  `daily_comparison_mapping_decisions` 与 `daily_reconciliation_cutoffs` 两张内部事实表。
  四个 SQL 函数与六个 ALWAYS 触发器进入正式安全目录；API、projector、Edge 不能读写
  新表，备份角色只读。空表可降级；存在日终事实或相关审计时拒绝降级。
- 两个延迟约束触发器在提交时重新检查总部权限、账号/身份/会话和授权期限。
  本批实际负向验证覆盖操作者权限及会话在提交前到期；来源、映射期限虽有代码检查，
  尚不能声称这些时钟边界均完成负向场景验证。
- CLI 与 GitHub 门禁读取的当前迁移及 readiness 摘要已更新为 0130。
  四个新增静态测试文件进入现有 CI 选择；没有在本机运行 GitHub 专用破坏性 PG16 gate。

## 当前实际验证

| 检查 | 证据与结果 |
| --- | --- |
| 正式迁移及 API 安全目录 | 实际 Alembic production 迁移、精确函数/触发器/ACL 校验通过；205 张原业务表摘要不变（不计 alembic_version） |
| 降级边界 | 空表 0130→0129→0130 保持原事实；有日终记录时降级失败且保留 HEAD、事实与审计 |
| SQL 独立拒绝 | 重算摘要的伪造报表、伪造账本、修改/删除/清空记录、越权角色均被拒绝 |
| 到期与未知提交 | 提交前权限或会话到期被数据库拒绝；精确恢复为未记录；实际提交后观察丢失则恢复到原记录，无自动重试 |
| 并发与期限 | 两调用只形成一份截止；会话/账本锁约 2.02 秒到期；各阶段共用剩余预算，提交前停顿回滚 |
| 撤权与历史 | 撤销来源或映射后拒绝新截止，历史记录保持；撤销登录会话后拒绝恢复请求 |
| 最终事实 | 四份截止对应四份审计；六张库存事实表前后摘要一致，未借对账改库存 |
| 未知目录项 | 未审核的 0130 触发器能被实际目录查询发现，API 安全校验拒绝；删除该测试探针后重新通过 |
| SQLite 与迁移链 | 实际从空库升迁、与 ORM 比对、降级及当前 HEAD/readiness 绑定通过 |

最终原生会话 **47170 退出 0**；最终目录聚焦 **23849 退出 0**；迁移链/SQLite
**28164 退出 0**。校验使用全新自有 Unix socket 实例，无 TCP 监听、外部 DSN、生产数据
或业务系统请求，未关闭正式数据库守卫。

## 原始失败与修复

首次聚焦缺少 PYTHONPATH，收集失败；正确调用后 92998 退出 0，79 项、15 子检查通过。
首次原生 43366 退出 1：函数族筛选遗漏 0130，已修正并保留未知成员拒绝。
同时发现生成器广泛改名误改两个 JSON schema 字符串，正式 SQL 已恢复原协议并加入检查。
第二次原生 98972 退出 0，38 场景通过；追加未知触发器检查后最终 39 场景通过。

扩展聚焦 76729 为 367 通过、3 失败，均为旧函数/触发器总数和迁移族断言；已修正。
71749 为 371 通过、1 失败：GitHub readiness 辅助函数仍读取 0129 文件；已改为 0130，
受影响的迁移链检查通过。所有日志、失败结果与旧源码快照保留于本批 artifacts，
不同选择的测试数量有交集，不合并为全量通过。

## 接下来必须补齐

1. 两个固定只读角色 `rsc_control_capture`（27 表）和 `rsc_reconciliation_capture`（6 表）
   尚无正式部署配置；本次仅在全新测试库中创建。**API 安全目录验证发生在这些 SELECT
   授权之前**；现行 private ACL 校验会拒绝它们对控制表的授权。下一批需精确白名单、
   角色能力/成员关系/表及列权限和 RLS 验证，证明采集与 API 校验可共存；不能全局放宽 ACL。
2. 内部执行仍依赖受控 owner 事务；不能把 migrator 连接暴露给 Web 请求。需要正式配置、
   进程管理/容器入口，以及带区域权限的日终状态投影、解释/复核 API 和查询页面。
3. 真实映射批准、真实来源、连续三天日终证据、容量及运行告警仍未验收。
   7.97 的真实网络、启动阶段父进程丢失等进程边界仍缺。
4. B5 8.01 备份/恢复证据保持，但旧 API 镜像不包含本批新模块和 0130，不能称为当前镜像。
   整合结束后再验证准确最终工件、完整静态及 GitHub PG16/Client；真实 OSS/PITR/灰度仍缺。

公开首页保持“交流备件知识大全”和星星入口 `/xx`；真实知识目录仍 pending/0。
额度剩余 23%，尚未达到用户要求的 10% 停止条件。原公开 CI 提交例外与 Edge 单次恢复
答复仍待处理；不据一般“继续”扩大外部写入授权。见[当前续开发与上线交接](CONTINUE_DEVELOPMENT.md)。
