# 正式 V1.0 基线缺口接续审计

2026-09-23 初审，2026-09-25 更新；范围为 `06f6/oam` 工作树
`codex/notification-delivery-worker`。依据项目
[正式 V1.0 基线](../../docs/RSC个人仓与物资运营扩展系统_正式生产版需求与架构设计_V1.0.md)
第 1、5、6 节，对当前正式路由、服务、部署交接和本地证据核对。下表区分“有候选实现”
与“可上线证明”；没有将旧原型或合成数据算作生产验收。

2026-09-25 当前复核：目标机隔离构建的 API/Web 均为 `linux/amd64`，Web
临时 HTTPS 路由与 API 非特权断网加载通过；原五个容器未变，真实站仍是旧登录首页。
本地 PG16.15 空库升迁至 `20261119_0140` 及运行角色/合成完整流通过，
`ciReleaseGate=false`。当前分支的 PostgreSQL 16 与客户端 CI push 触发已补，
测试工具及传递依赖的 13 项 Linux 哈希锁已核验；本地工作流拓扑、
`actionlint`、小程序 1,071 项和仓库安全扫描通过；
三份完整 Python 静态片均独立退出 0，合计 **6,853 passed / 3 skipped /
15 subtests passed**，覆盖 332 个互不重复的静态模块；
两份后来新增的 CI 契约测试已分别聚焦复跑；暂存范围复核后的小程序完整
**1,071 passed**、受影响的后端 **13 passed**，仓库安全扫描 1,685 文件通过。
GitHub 结果按下方准确 SHA 的回读状态记录。
这些证据见[开发交接](CONTINUE_DEVELOPMENT.md)。正式公开目录仍 `pending / 0`；
真实身份、供应商、OSS、期初、连续三天对账、500 用户/RPO/RTO、UAT 和书面上线验收
仍未完成。下文较早的“目标 x86 镜像缺失”描述属于 2026-09-24 时间点。

2026-09-25 当前 SHA `42d2d2a` 已推送；[Client release gate](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/36042618167)
已通过，[PostgreSQL 16 release gate](https://github.com/likexin12985/rsc-personal-warehouse/actions/runs/36042618264)
的最终结果为 failure：runtime 在 0044 并发探针超时；static 第 0、1 片通过，
第 2 片因 Linux 容量测试诊断解析及旧依赖安装断言共 3 项失败。
本地正在验证真实锁观测替代固定启动等待窗口的修复，并补了导入预检的
解析/授权绑定摘要，不能将原 SHA 的结果外推至新工作树或宣称 PG16 已通过。
新工作树的本地 PG16.15 空库迁移/运行权限、绑定摘要只读证明及合成期初/报表
完整流通过，实例停止；证据 `artifacts/local-current-head-pg16/checks/run-bgjxe5yt/checks.json`，
`ciReleaseGate=false`。CI 并发探针修复已在独立 PG16.15 实例验证通过：0044 的真实
`ShareRowExclusiveLock`、指定阻塞者与原子拒绝，以及版本维护锁顺序均成立，
证据 `artifacts/local-migration-lock-pg16/checks/run-gqoxp2f9/checks.json`；实例已停。
Alembic 入口新增不可变编译缓存，仍逐次执行迁移脚本；启用缓存后的迁移升降级、
离线 SQL、生产角色拒绝与加载器专项 22 项、期初/导入专项 150 项、拓扑 4 项通过。
新建 PG16.15 实例的迁移/当前权限与合成期初/报表业务流复核通过且实例已停，
证据 `artifacts/local-current-head-pg16/checks/run-79h2w4if/checks.json`。
上述修复已本地提交 `db5889c`。后续静态失败修复的四模块 48 项通过；
真实隔离 Ubuntu/dash 运行同仓库的容量测试函数 17 项通过。备份生产脚本未改，
字节限额与超限拒绝断言保留。本地补充不代替 GitHub runtime；尚无新 SHA 远端结果。

### P0 期初盘点 Excel 导入链的现行阻断

当前正式路由 `/imports/opening-count/format-check` 仅解析工作簿格式，
`/error-report` 只为本次请求返回格式错误文件；`/business-check` 已从当前上传者的
私有 OSS 文件读取并校验摘要、当前身份及任务/轮次/范围，调用正式实盘路径作只读
业务预检。这些结果没有持久任务、业务错误文件或确认执行权。
后续本地候选的预检额外返回 `binding_sha256`，捕获同一源文件、请求及任务版本下
可能变化的解析结果、追踪规则、目标账户与实际授权。期初服务及导入专项
150 项通过；该摘要还未存入受数据库守卫保护的导入任务，不能单独授权确认。
`FileJob` 模型虽已有 `import` 类型、`awaiting_confirmation` 状态和 `error_file_id`，
但 `0136` 迁移的 API 角色 INSERT/UPDATE 守卫只允许 `export`，故不能直接复用
导出任务 API 写入一个“导入已预检”记录。用户看到的预览不等于可确认的冻结来源。

下一实现需用独立迁移将导入任务绑定到源文件 ID/摘要、任务、轮次、范围、申请人及其
授权版本，并给 API/worker 仅够用的列权限和受控状态迁移；持久错误报告须绑定
专用私有文件用途。确认时重新核验源对象摘要、身份、任务/轮次/范围与正式实盘
规则，复用原幂等键提交，超时结果只能按原键回读，不得整表重放。迁移需验证
空库升降级、既有导出任务不变、API 角色直写伪造/越权/跳步拒绝，以及真实 PG16
并发和失败恢复。完成前正式 V1.0 的“预校验、错误报告、授权确认执行”仍为开放项。

2026-09-24 上一准确增量候选的冻结清单为 1,659 个 Git 可见文件、零漂移；完整后端
三片静态门禁各自退出 0，合计 **6,785 passed / 3 skipped / 15 subtests passed**，
Web **81 文件 / 1,382 项**、小程序 **1,070 项**及构建检查通过。合成 PG16.15
迁移至 `0139`、API 角色报表申请/worker/原键找回/下载门禁通过；详见
`artifacts/static-final-20260924/result.json` 和
`artifacts/local-current-head-pg16/checks/run-9kw5kso6/checks.json`。这只消除了该冻结
快照的“本地完整静态未验”缺口；GitHub runtime/Client、真实环境与 UAT 仍未验。
同一源码的当前 API 镜像已在隔离 Linux VM 构建，镜像内 338 个 COPY 输入逐字节
一致；报表 worker 无配置拒绝与 `reports` Compose profile 默认关闭在容器/Compose
实跑中通过，证据 `artifacts/api-image-final-20260924/result.json`。这是本地容器证据，
不证明真实 OSS 或目标机可用。
公开及 `/xx` 构件在隔离 Caddy 容器中完成 16 条 HTTP 路由核对，根路径与
`/login` 均返回知识查询页，证据 `artifacts/caddy-current-20260924/result.json`；
API 上游为本地替身。完整 Web Dockerfile 的试点镜像也已在隔离 Linux VM 构建，
镜像摘要 `sha256:5a07763d5709ec6524e7e801e82ccc930812538239bce8ed112f949a3a5850dc`；
镜像内公开/私有首页、目录和 Caddyfile 与本地构件哈希一致，镜像内本地 HTTPS
四路由通过，证据 `artifacts/web-image-final-20260924/build-result.json` 与
`verify-result.json`。目标机已只读确认当前 `rscwz.cn` 由旧 `star-oam-web`
占用 80/443，公开根页标题仍是“RSC个人仓”；新镜像未部署。新站真实 HTTPS、
实际鉴权和备案终态仍待核。
格式预检现已在 `/xx` 盘点中心提供模板、文件检查和错误报告入口；受控并发全量
Web **83 文件 / 1,387 项**通过，`build:warehouse` 及 `verify:pilot-release` 通过。
后者仍输出知识目录 `pending / 0`，只证明私有试点构件允许生成。当前仓库安全扫描
**1,667 文件 PASS**。这组客户端结果只覆盖当前 Web 代码，不补足下面列出的
导入持久任务、业务预校验和真实目标验收。
部署入口现额外检查宿主 TCP 监听并在异常输出时失败关闭；部署/回执绑定合成
聚焦 **99 passed**。目标机只读 `ss` 显示 IPv4 与 IPv6 的 80/443 仍共四条
监听，尚未执行端口释放或部署。此检查不替代可恢复切换方案或真实启动验收。
目标机再次只读回读确认仍是 `x86_64`，旧 Web 容器仍占用 80/443；部署入口
新增镜像 Linux OS/宿主架构门禁，在 `prepare` 启动数据库前和 `start` 执行
pin gate 前拦截错平台工件，部署绑定聚焦 **102 passed**。目标 Docker 对旧镜像
实际返回 `linux/amd64`，与新门禁的字段格式吻合。旧 `arm64` 镜像仍不能用于当前目标；这个门禁
只验证平台元数据，不等于当前候选已有 `amd64` 镜像或可上线回执。
新版目标冒烟补 `/`、`/login` 和 `/xx/` 的入口内容及静态资源命名空间核验；
本地假站 4 项正反例通过。旧首页仅有 HTTP 200 已不能使 `start` 冒烟成功。
这仍不是在真实设备上渲染页面或确认备案终态。
本轮当前源码的独立 PG16.15 空库升至 `0139`，运行角色、报表任务约束、九项短信
配置夹具与合成期初报表完整流退出 0，实例已停止；证据
`artifacts/local-current-head-pg16/checks/run-kpzgnsvw/checks.json`。
1,668 文件输入清单在该检查前后零漂移；`ciReleaseGate=false`，未替代 GitHub
或生产环境验证。完整期初 Excel 导入另受 `0136` 仅允许导出任务、`0137` 报表
专用文件目的及 OSS 适配器尚无受控读取接口的约束，需要独立迁移与 PG16 权限门禁。
随后新增期初盘点 XLSX 模板、格式检查及错误报告接口，聚焦 **86 passed**，见
[导入格式切片](OPENING_COUNT_IMPORT_FORMAT_20260924.md)。现有格式检查不绑定任务/范围，
不持久化 `FileJob`，不作主数据/业务预校验，也不提供确认执行；因此不计作完整 P0
导入。新增格式切片及说明后 Git 可见文件为 1,663，上述完整静态、PG16 与镜像属于旧快照。
后续 `0140` 已为期初盘点 XLSX 源文件增加独立私有用途、8 MiB 限额和当前
`stocktake.count` 权限数据库守卫；SQLite 升降级及新建 PostgreSQL 16.15 的
真实 API 角色允许/拒绝专项通过，见
`artifacts/local-current-head-pg16/checks/run-iceityfz/checks.json`。这只解决源文件
身份边界，不代替持久 `FileJob`、任务/轮次/范围绑定、业务预校验、错误报告和确认执行。
当前又新增复用正式实盘校验路径的内部只读业务预检，包含当前人员/范围、
冻结与物料/SN/批次检查；真实 PG16.15 API 角色下无事实变化且原幂等键随后
可正式提交，见 `artifacts/local-current-head-pg16/checks/run-pylub4f5/checks.json`。
工作簿保留真实行号，避免内部规范化序号错指源行。该结果只缩小业务预校验
算法缺口；内部组合入口也已核对预期源 SHA、格式与业务预检并生成待核实项
真实行号错误，但还没有私有对象与范围的授权绑定。`FileJob` 持久状态、
专用错误文件、私有 OSS 源与任务绑定、确认后重验
和 HTTP/Worker 仍未实现，正式 P0 导入尚未完成。
本次新建本地 PG16.15 空库另通过现有 `0139` 迁移和运行角色/报表权限专项，证据
`artifacts/local-current-head-pg16/checks/run-5reyib22/checks.json`；该专项
`ciReleaseGate=false`，不覆盖新增导入业务预校验，也不能替代当前候选完整发布门禁。
用户提供的备案申请详情截图显示审核意见通过及 `rscwz.cn`，仅据此登记为申请页
证据；最终备案状态和公开内容/目标 HTTPS 仍需单独回读。

| 基线域 | 当前可核查状态 | 正式放行缺口 |
| --- | --- | --- |
| 登录、组织与 OAM 只读来源（1.4–1.5、阶段 1） | 微信/PNVS、会话、范围授权、OAM 边缘采集/隔离投影有候选和本地测试；PNVS 专用 STS 已接入 | 真实唯一人员映射、服务/签名/模板与实发回读、当前 OAM 来源及历史数量/字段/附件核对、停用交接实测 |
| 库存、个人仓、期初和日终（1.6、阶段 2） | 不可变流水、期初、控制发布/映射和本轮六操作日终 CLI 有 PG16 合成证据 | 真实实盘开账、源/映射批准、首日及连续三天差异解释、任意余额重算和实际并发守恒 |
| 三级审批与分批履约（1.7–1.9、阶段 3–4） | 正式需求、审批、分配/占用、出库、发运、收货/入账、工单物料已有独立状态和大量局部测试 | 真实三方审批身份/证据、跨角色设备 UAT、多仓分批与异常、OAM 收货和个人仓入账分别回读 |
| 报损/报废、离职交接（1.11、阶段 4） | 库存模型预留 `handover_case_id`，且明确注释正式交接表尚不存在；当前正式路由无独立报损/报废/离职交接入口 | 正式单据、审批、冻结/冲销、双方确认与归零关闭链路仍需实现和验收；不可把退回或通用库存 `scrap` 类型视为完整业务流程 |
| 报表、Excel、打印/二维码（1.12、阶段 5） | 日终和库存查询、扫码标识校验存在；`0135–0139` 已补导出任务字段/权限、受控状态、专用 XLSX 文件用途与结果绑定；`/xx` 库存页已有导出申请、原键找回与下载。新增期初盘点 Excel 格式检查和错误报告接口，尚未任务绑定或执行 | 补真实私有 OSS、身份/来源与目标验收；导入需补业务预校验、持久错误文件及授权确认，再补单据打印并做越权/泄露测试；新候选完整门禁待重跑 |
| 通知与飞书机器人（1.13、阶段 5） | 事件扩展、投递租约、失败/结果未知、渠道回执模型及运维查询已实现；worker 在未配置适配器时明确拒绝启动 | 生产微信/短信/飞书渠道适配器、真实送达/已读回执、机器人验签回调与失败恢复尚无现场验收；登录短信不等于业务通知 |
| 发布、备份、非功能（阶段 5–6） | 试点 prepare/start 绑定、PG16 合成迁移、当前八角色合成容器联合恢复、Web `/xx` 和公开查询页已有本地验证；六张日终表全非空的当前容器恢复也已通过；目标 Ubuntu 24.04 / `x86_64` 上已有当前应用源码的隔离 `amd64` API/Web 预检镜像与本机路由探针 | 受控 Web 入口切换、真实 KMS/OSS、真实 HTTPS/设备、500 用户、RPO≤5 分钟/RTO≤2 小时、回滚/冲销演练、正式生产配置及完整 GitHub 门禁 |

对基线 1.11–1.13 和 3.9–3.10 的再次核对：`stock_operation_orders`、`file_jobs`、
`robot_inbound_events` 和 `qr_codes` 已有基础表/模型，但表存在不代表正式业务闭环。
当前 `main.py` 注册的正式路由中，库存作业覆盖退回而未见独立报损/报废或离职交接入口；
`inventory_models.py` 明确注明交接表尚未建立。`FileJob` 已有状态及唯一请求键约束，
`0135` 补了导出范围、授权版本、游标、结果哈希及下载意图次数；`0136` 收窄 API
角色的任务增改权限并封闭状态转移，`0137` 接纳报表专用 XLSX 目的及当前授权检查。
已有默认关闭的导出申请、任务状态和受控下载专用路由；新增导入格式检查路由，
但尚无任务/范围业务预校验和确认执行。`0138` 的唯一
索引防止一份结果文件归属两条任务；`0139` 又要求成功结果文件的幂等摘要与精确
任务一致。内部服务及一次性/持续 worker 已编码单任务事务绑定和失败恢复；PG16
本地完整合成期初已覆盖申请/worker/下载 HTTP，仍须真实 OSS、身份/来源和目标
环境验收，不能因本地合成链路宣称报表交付。
`RobotInboundEvent` 已有提供方事件唯一约束，但未见验签、发送人授权、幂等回调及业务接口
复验的正式入口。`scrap` 流水种类和 `scrapped` 成色仅是底层表示，不能代替报损冻结、
区域核实、总部终审、批准后报废及反向冲销单。

基线 P0 的[只读库存 Excel 导出任务](REPORT_EXPORT_IMPLEMENTATION_PLAN_20260923.md)
已在本地候选中固定申请人授权范围、筛选条件和账本游标，并经异步 worker 生成
SHA-256 结果；下载前重新校验人员权限、私有对象 HEAD 和同事务计数/审计。
`0135–0139` 的数据库边界、文件与任务精确绑定及合成 PG16 HTTP 全链路已有本地证据，
但生产开关仍关闭。下一切片应先验证真实私有 OSS、目标环境 API/worker 角色、身份/
期初与越权恢复，再决定是否开放受邀试点。之后独立实现导入预校验、错误报告及授权
确认，OAM 主数据不得被导入覆盖。基线 P1 的报损单应先冻结库存，再经过区域
及总部决议；拒绝时无库存过账，批准报废时只写一份不可变流水，误报以反向单保留原事实。
离职交接须以停用人员为键保持唯一未结案，限制新申请/投料，列清盘点、在途、占用及未结
事项，分批移交与双方确认后按双层复核归零关闭。机器人回调须在已验证渠道适配和身份
映射后单独上线。每个切片都需迁移、权限目录、负向越权/重放测试及 PG16 验证；
不得借本地空表或合成对象绕过真实试点验收。

本轮 D2 入口及证据见[日终运维候选](DAILY_RECONCILIATION_OPS_ENTRY_20260923.md)；
原受邀试点外部依赖见[真实证据审计](PILOT_REAL_EVIDENCE_GAP_AUDIT_20260922.md)。
正式全量 UAT 的逐项场景、权威回读及当前缺口见[正式 V1.0 验收证据矩阵](FORMAL_V1_UAT_AND_LAUNCH_EVIDENCE_20260925.md)。
受邀 HTTPS H5 `/xx` 是小范围试点目标；公开“交流备件知识大全”源仍按用户要求暂缓，
正式公开发布门禁保持 `pending / 0` 拒绝。即使试点门禁全绿，也不能宣称正式 V1.0 全量上线。

此前 `0134` 候选的本地日终/试点专项为 **561 passed、15 subtests passed**；
新建的 PG16.15 空库迁移至 0134、日终映射与截止正向链及 CLI 精确回读均已通过，实例停止。
第二个全新 PG16 实例还验证 CLI 独立进程/FD 的精确回读与四项负向拒绝，事实计数不变。
准确源码的 Linux API 镜像已构建，331 个 COPY 输入一致，隔离容器内日终 CLI 私有
FD/隐藏 TTY 凭据、准确回读和两项权限/摘要拒绝通过；隔离 Linux Compose 5.5.1
合成配置解析与当前八角色合成容器联合恢复通过，另有 212 表 / 788 行、六张日终表
全非空的新库恢复通过；
目标机 Compose 与真实 OSS 仍缺。`0135–0138` 报表基础切片另有全新 PG16 空库
升迁、精确 `report.export` 两角色授权、受控 `file_jobs` 列权限、文件守卫与结果唯一索引
的本地证据 `artifacts/local-current-head-pg16/checks/run-7r_5_s87/checks.json`；
新一轮空库 PG16.15 复核也通过，证据为
`artifacts/local-current-head-pg16/checks/run-l2dtbeea/checks.json`。库存报表内部申请、
快照、单任务领取、写后重验、结果绑定及单次/持续 worker 入口已编码，56 项库存/报表/文件和 HTTP 聚焦测试通过；`0139`
已增加数据库精确任务绑定守卫，新 HEAD 的全新 PG16.15 安全清单/权限门禁已通过，
证据 `artifacts/local-current-head-pg16/checks/run-4i1ebvxz/checks.json`；SQLite 全图空库
升降及最终源码的历史坏数据拒绝/触发器错绑拒绝与正绑放行测试通过。
API 运行身份伪造已完成导出任务的实际 PG16 反例也通过，证据
`artifacts/local-current-head-pg16/checks/run-hd_yxur0/checks.json`，实例停止；
这只覆盖一条负向状态，不等于申请/下载业务权限闭环。
新的全新 PG16.15 门禁还通过有效排队/领取/失败与跳步、不可变字段、停用申请人反例，
证据 `artifacts/local-current-head-pg16/checks/run-mfbt8ix6/checks.json`，实例停止；
再一轮 `artifacts/local-current-head-pg16/checks/run-7uq_h6rb/checks.json` 使用合成 XLSX
通过 API 运行角色的文件可用状态、成功任务绑定和下载计数单调转换，实例停止。
全新 `artifacts/local-current-head-pg16/checks/run-wlug5e73/checks.json` 还验证实际 API
角色调用下载意图服务后的私有对象 HEAD、签发计数、审计以及重放/停用拒绝，实例停止；
新 `artifacts/local-current-head-pg16/checks/run-8cm79ymz/checks.json` 在 PG16 实际
API 角色上补齐正式状态/下载 HTTP 的正向事务及重放、停用拒绝。可选全链路首次
`run-bwwf7ghw` 因可见范围漏建期初而拒绝；补齐真实盘点流程后，
`artifacts/local-current-head-pg16/checks/run-m04005fm/checks.json` 通过合成期初下的
申请 HTTP、API 角色 worker 和下载 HTTP。真实 OSS 对象传输、外部身份/来源和
目标环境验收仍缺。
新 HEAD 的独立日终 PG16 门禁也通过，证据
`artifacts/local-daily-ops-pg16/checks/run-3_5kt60m/checks.json`。
申请/状态/下载路由已挂载但默认关闭；`reports` worker profile 可选但未部署，真实 OSS、实际身份/期初与
受控下载运行环境闭环仍缺，不能视为正式报表交付。
不可与此前 `0134` 完整静态/镜像结果合并成当前候选全绿。
此前 Web 本地 80 文件 / 1,379 项、类型检查、公开/私有构建及试点检查通过。
2026-09-24 准确候选已冻结 1,655 个 Git 可见文件；三份测试夹具修正后，静态第二片
独立完整退出 0（2,408 passed / 2 skipped），第三片完整退出 0（2,287 passed）。
第三片调用的 HEAD 对照用例在第三份修正后定点复跑通过。第一片 v2 因参数化库存历史
用例重复加载迁移图而受控停止；结果缓存的定点用例通过，v3 独立完整退出 0
（2,089 passed / 1 skipped / 15 subtests passed）。三片完整结果合计 6,784 passed /
3 skipped / 15 subtests passed，第三片的变更调用点另有聚焦通过。并发客户端首轮因
多项超时受控退出 130；独立重跑 `client-v2.log` 完整退出 0，Web 80 文件 / 1,379 项、
小程序 1,070 项、类型检查、公开和 `/xx` 构建、公开入口及期初共享脚本检查均通过。
私有构建有单块超过 500 kB 的性能提示。原第一片旧哈希
断言失败及第二片迁移来源重复加载造成的中断日志保留，不与当前通过数相加。
当前静态运行详情、精确日志及迁移来源缓存边界见
[开发交接](CONTINUE_DEVELOPMENT.md)。GitHub PG16 runtime/Client、目标机试点
尚无当前候选的通过证据。测试夹具仅缩短同进程重复加载，没有改变业务迁移或
生产安全清单。
详见[开发交接](CONTINUE_DEVELOPMENT.md)、[运维入口证据](DAILY_RECONCILIATION_OPS_ENTRY_20260923.md)
及[当前镜像容器实跑](CURRENT_CONTAINER_DAILY_CLI_20260923.md)。

接下来的独立开发顺序：本地静态、客户端及合成 PG16 证据已具备，先取得当前候选
GitHub PG16 runtime/Client 结果，并完成目标机配置与真实 OSS 恢复；外部真实身份/
来源/期初明确后跑试点 UAT。产品开发应先把 P0 报表导出接入真实运行环境，再补
导入预校验/错误报告；随后补报损报废/离职交接的正式状态轴与打印；通知渠道需真实适配与回执。
所有真实写入仍须按准确对象授权，不能由本审计推定许可。

2026-09-24 导入缺口复核：格式预检、期初业务预校验和本人私有文件读取已具备
内部组合能力，源文件读取与业务预校验分开事务且无计数或库存写入，受限
`business-check` HTTP 已能返回短时业务错误预览；但正式
`file_jobs` 目前只允许报表导出 API 创建/更新，期初导入没有持久状态、错误报告
文件、确认入口或真实 OSS 集成证据，因此 1.12 的“预校验、错误报告、授权确认”
仍未达成。目标机已确认是旧备份 SSH 主机，现有健康的 PG16.15 预生产数据库
与旧 OAM 容器隔离；当前源码和镜像尚未在那里运行，不能把目标基础设施就绪
当作部署或业务验收完成。

2026-09-24 门禁与公开入口复核：本地后端三片完整静态合计 **6,845 passed /
3 skipped / 15 subtests passed**；公开页备案页脚修改后受影响的后端入口
**59 passed**，Web **1,387 passed**、小程序 **1,070 passed**，双构建与
试点构件检查退出 0。用户截图中的备案号已加入本地公开页底部中央并链接
工信部备案系统；当前公网 `rscwz.cn/` 仍是旧镜像的登录工作台，直接违反
用户规定的公开首页边界，`/xx/` 也由旧单页回退提供。目标机运行镜像的
Caddyfile 与本地双入口配置不一致，尚未切换。真实公网改正、公开资料导入、
GitHub 当前候选 CI、外部供应商/身份/库存资料及 UAT 仍是独立阻断项。
随后只读核验公网 `/api/auth/login-options`：密码开启，短信和微信均关闭，
不仅首页旧路由未切换，1.4 的正式无密码登录也未在目标站点生效。
部署烟测已加实际公开/私有 JS 读取与备案号、认证代码隔离检查；本地真实
双构建配合模拟 API 通过，公网旧站仍准确拒绝。见
[双入口烟测证据](PILOT_LIVE_ROUTE_SMOKE_20260924.md)。这不解除真实 PNVS/微信、
唯一身份映射、期初及 UAT 缺口。

目标 `amd64` 构建补充：Web 隔离预检镜像通过，API 官方 PyPI 两轮因依赖下载
超时未建成；使用阿里云镜像源的 API 预检镜像已建成，非特权、无网络
`pip check` 通过。两者没有启动试点服务，仍缺同一准确候选的依赖来源锁定、
独立生产配置、正式准备/迁移回执、备份恢复与回滚，以及真实业务 UAT。
因此“能在目标架构构建”只能缩小 B 类部署缺口，不能解除正式发布阻断。
最新本地 PG16.15 全新空库至迁移 `0140` 和合成期初/报表完整流通过，实例已停；
报告 `run-bs81fmwf` 仍标明 `ciReleaseGate=false`、`syntheticStorageOnly=true`。
实际供应商、真实数据迁移及生产权限仍须逐项验收。
目标机后续已验证官方 PyPI 哈希锁定的 72 项运行时发行包并构建新的 `amd64`
隔离 API 镜像，镜像内版本逐项一致；原有服务未切换。七个源码包的构建工具
又分别以官方哈希固定，最终仓库 Dockerfile 使用该模式通过目标机隔离构建。
基础镜像摘要尚未冻结，且缺少试点专用运行配置与真实 KMS/PNVS/OSS、身份和期初。
这缩小依赖漂移风险，不改变公开首页仍为旧登录站和正式放行阻断的结论。
