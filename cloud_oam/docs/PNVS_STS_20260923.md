# PNVS 专用 STS 凭据入口（2026-09-23）

`/xx` 的 PNVS 登录现可使用专用临时 AccessKey ID、AccessKey Secret、Security Token 三元组。配置项为 `OAM_SMS_ACCESS_KEY_ID`、`OAM_SMS_ACCESS_KEY_SECRET`、`OAM_SMS_SECURITY_TOKEN`、`OAM_SMS_SECURITY_TOKEN_EXPIRES_AT`，最后一项必须是带时区的 ISO 8601 时间。保留原有专用 RAM AK 模式；只要设置 token，就必须同时设置有效期，临近过期 60 秒时启动/预检和提供者构造拒绝，实际发送/校验前不足 15 秒也拒绝。PNVS 不读取 KMS/OSS 使用的 `ALIBABA_CLOUD_*` 默认凭据链。

部署侧须把同一临时身份的三个值和到期时间一起更新，并在到期前重启 API。每次 HTTP 发送或校验都会构造新的提供者和配置副本；现阶段没有从云端自动获取/无停机刷新临时凭据，也没有跨阿里云账户轮换证明。任何发送超时继续按结果未知隔离，不能因换凭据重发。验证码挑战的方案摘要不包含凭据，签名、模板、方案等请求参数变化仍按原方案绑定规则拒绝旧挑战。

阿里云 Python V2 SDK 的 `Config.security_token` 承载独立 STS token；构造和调用前后禁用 SDK 自带的 DEBUG 输出。提供者异常只暴露固定业务错误，不透传手机号、验证码、凭据或原始异常。上述只说明本地配置与接口行为；没有调用真实 PNVS、执行收费发送或验证 RAM 授权。

本批聚焦：`test_sms_provider.py`、`test_sms_profile_binding.py`、`test_startup_security_boundary.py`、`test_pilot_preflight.py`、`test_production_adapter_composition.py` 共 **186 passed / 1 条第三方弃用警告**。覆盖 STS 传给 SDK、凭据轮换不改变请求方案摘要、缺项/无效/过期拒绝、发送和校验前停用以及 SDK handler 清理。含权限、通知和日终迁移的最终联合聚焦 **276 passed / 1 条第三方弃用警告**。STS 修改后新的本地 PG16.15 空库迁移/权限门禁及 9 个真实行锁挑战场景通过，库存和截止事实未变、实例停止，证据为 `artifacts/local-current-head-pg16/checks/run-vpqlza62/`；完整静态、GitHub PG16 runtime 或真实短信联调仍未通过，不得据此宣称短信认证已上线。
