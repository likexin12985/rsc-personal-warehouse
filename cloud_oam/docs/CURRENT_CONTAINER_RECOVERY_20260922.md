# 当前候选的容器联合恢复验证

2026-09-23 复核第三轮归档：`normal/stderr.log` 也含 `File size limit exceeded`，对应 `deployment/backup/capacity.sh` 的容量单位探针；不能将该行单独认定为备份失败根因。`control/worker-observations.jsonl` 显示 `db-deadline` 只完成对象 worker 预检，没有 `db-stall-ready.json`，而 10 秒总期限在进入真实数据库阻塞点前到期。保留失败结论，下一轮仅在新目录、新库、当前准确镜像中调整**验证夹具**的总期限与屏障等待，并要求实际 `pg_sleep` 会话/锁、124 退出及会话清理三重证据；正式备份代码不因这段日志改变。第二新库恢复及其余故障场景仍待执行。

2026-09-22。状态：**当前镜像、源库八角色和正常备份通过，完整容器恢复未通过；额度停止交接。**
本批受 `full-static-release-20260922-binding-fix` 的 1,472 个非 Markdown 输入冻结约束，
只新增隔离验证产物与文档，不修改候选源码，也不接触生产数据或真实 OSS。

## 已完成的前置证据

- [当前正式 API 镜像](CURRENT_API_IMAGE_REBUILD_20260922.md)已真实构建，330 个构建输入和
  6 个 worker 前后无漂移，镜像内 328 个 COPY 文件的字节、权限、大小一致。
  `pip check`、共享定义及 worker 导入通过，包源和 VM 已停止。
- 新源使用 8.01 已验合成样本的准确 SQL、真实附件字节及摘要，在**新建且独占的原生 PG16**
  中恢复。原样本准确 HEAD 为 `20261108_0129`，206 表 / 572 行；没有重启旧数据目录。
- 正式 Alembic 升至 `20261113_0134`，变为 212 表 / 579 行；补齐下述合成范围后为 **212 表 / 584 行**。原业务表内容摘要一致；
  0132 的 3 条权限、4 条角色授权按迁移的固定种子逐条核验，原配置行保持原样。随后用正式工单来源配置脚本增加 1 个合成来源与 4 条准确范围绑定，旧行不变，其他全部表不变；库存采集和工单投影的现行完整权限检查均通过。
- 正式 capture provision 增加两个独立只读账号；当前共 8 个数据库角色、7 个 LOGIN 角色，
  `star_oam_edge` 保持 NOLOGIN。完整 API/capture 安全检查通过。
- 当前正式 `deployment/backup/backup_database.sh` 导出成功；3 个 available 对象的实际
  字节、长度、SHA256 与目录一致，1 个 pending 意图不作为已上传对象。
- 1,472 个输入运行前后保持摘要。源实例已停止，退出 0。该步骤不证明七个密码登录实测，
  更不证明远端生产权限或真实 OSS 身份。

第三轮源会话 **15232 退出 0**；该轮仅验证 API/capture 与附件。补齐工单范围后的最终源会话 **28562 退出 0**，产物目录为
`artifacts/current-container-source-20260922-attempt5/`。
[源结果](../artifacts/current-container-source-20260922-attempt5/result.json) SHA256：
`4c7b5b516258f995915f4f268bd41f15399112644af1557a632bd41345820972`。
独立复核 16 个源产物摘要及全部冻结输入无漂移。下游逐表比较统一使用 `Asia/Shanghai`，
避免把 `timestamptz` 的文本显示时区差异当成数据改变。

## 保留的失败与范围

首轮 **91882 退出 1**：验证夹具错误要求权限种子也完全不变；停止后查明 0132 的正常新增。
第二轮 **17110 退出 1**：夹具把文件目录实际 `pending` 误写成 `upload_pending`，在备份完成后拒绝。
两轮原日志与数据目录保留，两个自有 PG 实例均已停止。第三轮只修正合成验证断言，
没有修改业务迁移、正式备份、文件状态定义或放宽数据权限。

当前新增的六张日终表均为空；本批可验证当前结构、ACL/RLS、角色恢复及已有附件数据链，
**不包含非空日终映射、截止、解释、审核和请求封存的容器恢复**。后续必须单独补齐，
不能用旧候选恢复或当前空表数量替代。

## 实际容器接续

首轮实际容器会话 **70207 退出 1**：投影账号缺少工单来源范围，正式权限检查拒绝。8.01 旧样本仅有库存采集绑定。引擎、VM 和源 PG 均已停止，容器清理成功，归档不含明文临时密码。原目录完整保留。原生诊断 **56085 退出 1** 进一步记录库存采集已通过、仅投影账号 `revision_and_binding` 失败；它未执行配置，实例已停止。第五轮源按准确诊断经正式脚本补齐后通过，未放宽权限检查。

第二轮实际容器 **44039 退出 1**：八角色、七个新密码与错误口令拒绝、API 就绪均已通过；随后固定 API 角色探针缺少 `/app` 模块搜索路径而失败。只在第三轮新夹具补此路径，正式镜像、源码、权限未修改。第二轮源库、容器、引擎和 VM 均已停止，未产生备份成功结论。恢复库拒绝源库旧密码的检查仍待后续执行。

当前新执行器位于 `artifacts/current-container-recovery-20260922-attempt3/`，会话 **47541 已退出 1**。正常联合备份通过（34.102 秒、退出 0），API 使用准确 `star_oam_api`/目标库，源库八角色及七个新密码/错误口令拒绝通过。随后 `db-deadline` 场景未到达注入屏障便失败，日志同时出现 `File size limit exceeded (core dumped)` 和期限退出 124；根因尚未确定，不能将其作为超时清理用例通过。取消、父进程终止、对象损坏与第二新库恢复尚未执行。51 个归档成员、56 个绑定文件及 1,472 个正式输入打包前复核通过。

源库与恢复库都使用新实例，按基础六角色 → 正式 prepare-restore → 单事务加载准确 SQL →
restored_disabled → 数据/对象核对 → 新凭据 activate 的顺序执行。使用本批新 API 镜像，
不能换回 8.00 的旧标签。外层仅操作已确认归属的隔离 VM、全新私有 Docker 引擎和本次项目。

正常联合导出、超时、取消、父进程终止、对象损坏拒绝及二次新库恢复，均须以实际终态、
数据摘要、实例身份、会话/锁清理和资源停止记录验收。合成 SDK transport 与测试配置 API
继续明确标注；真实 OSS、生产启动、异地归档、非空日终恢复及 RPO/RTO 仍未通过。

第三轮源 PG、所有本次容器、私有引擎和 VM 已停止；源库无 PID/socket 残留，清理错误列表为空。归档未包含临时明文密码文件。完整静态 6070 / pytest 68895 / supervisor 68872 仍在后台运行，1,472 输入交接时无漂移。周额度已剩 **9%**，按用户 10% 停止约定停止新增工作。本批没有提交、推送、部署或修改系统证书信任。

下次先读取[本批交接验真](../artifacts/current-container-recovery-20260922-attempt3/handoff-verification.json)，再检查 6070 的实际终态。当前恢复故障现场在第三轮 `guest-evidence.tar` 的 `db-deadline/`、`chain-commands.log` 与 `control/`；先查注入屏障未到达原因及文件限制，不能直接扩大期限或改正式备份代码掩盖失败。重跑必须新目录、新库和本批准确镜像；非空日终、真实 OSS 和生产恢复仍缺。仓库安全会话 84708 退出 0，1,616 候选文件通过。
