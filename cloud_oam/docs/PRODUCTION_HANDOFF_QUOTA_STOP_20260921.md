# 上线开发整体进度与额度停止交接

更新时间：2026-09-22。当前由本任务统一接管此前并行任务的代码、证据和上线缺口；旧任务不再作为独立决策源。用户要求额度剩余 10% 停止；**产品仍未上线，目标未完成。**

## 当前工作区

**当前完整静态会话 6070 正在运行：pytest PID 68895 / 监督 PID 68872，2026-09-22 23:03（北京时间）启动。自动收集 6,710 项 / 1 模块跳过，313 个候选测试文件，冻结 1,472 个非 Markdown 输入。** 证据目录 `artifacts/full-static-release-20260922-binding-fix/`；保持同一会话，等实际终端与源码稳定结果，不重复启动、不改冻结输入。当前尚无完整通过结论。

- 工作树：`~/.codex/worktrees/06f6/oam`。
- 分支：`codex/notification-delivery-worker`；HEAD：`f713999d4693aedae11b5ebb379ef994aa50aeea`。
- 不 reset、revert 或丢弃未提交改动。尚未提交、推送、部署，禁止把当前本地通过结果当作正式发布。
- 正式 V1.0 基线未变，SHA256：`52e7e490cb83eb238e14e3c0e653e87bc1d42238af159dd32d4a6821fba188fc`。
- 完整静态会话 **52984** 已受控停止、退出 2：**11 passed / 1 skipped**，1,470 输入无漂移；两个 PID 已结束。该轮停止修复的部署缺陷现已通过 8.23 专项，新候选完整验证为本节顶部的 6070。详见[当前门禁接续](STATIC_RELEASE_GATE_20260922.md)。旧进程不能凭记录推断仍在运行，也不能重启旧 PG 数据目录。
- 最近账户周额度剩余 **9%**；已触发用户要求的 10% 停止条件，开发暂停；完整静态 6070 的后台进程仍保留。

## 当前统一接管边界

- 另一个 Codex 任务的本地候选改动已纳入本工作树审计；其最近状态为系统错误，不能作为上线完成证据。
- 预检脚本、试点计划、前端产物和日终 PG16 门禁统一以本工作树当前文件和工件为准；不按两个任务的历史口径重复计数。
- 本次接管没有向 OAM、RSC、Workflow、飞书、生产数据库或真实通知供应商写入数据。

## 整体进度

| 范围 | 已有可复核结果 | 上线前仍缺 |
| --- | --- | --- |
| 公开网站/小程序 | 公开入口为“交流备件知识大全”；网页星星进入 `https://rscwz.cn/xx`，小程序仅知识查询 | 用户已暂缓飞书知识源；当前 pending、0 条，严格公开发布继续阻断，个人仓试点开发继续 |
| 私有个人仓基础业务 | 收发、入账、工单替换/旧件、退回、盘点、SN、权限及原请求恢复已有实现和专项证据 | 当前完整候选门禁、真实组织/人员/SKU/历史迁移及多角色设备 UAT |
| 日终对账与审核 | 正式采集、映射、截止、查询、逐项解释、退回及独立审核、0134 原请求终结、网页操作已实现 | 浏览器附件至总部审核完整链路、跨标签/设备验证、实际进程管理和连续三天真实对账 |
| 通知运维 | 原目标保留、扩展权限、锁后刷新与受控重排的本地 PG16/聚焦证据保留 | N1—N5 真实渠道适配、可信回调、双向机器人及实际送达证据 |
| 备份与恢复 | 正式入口/worker/Compose、合成 DB/API/worker 容器全链路、原生八角色恢复和本地 PITR/SN 专项 | 当前八角色容器联合恢复、真实 OSS、异地归档、生产 RPO/RTO 演练 |
| 身份、运行与灰度 | 正式身份/权限边界和 PNVS-01 计划已有文档与局部验证 | 真实身份/微信/SMS/KMS、500 用户压测、监控告警、灰度及生产验收 |

开发模块和生产验收不是同一层，不能按测试数量给出“上线完成百分比”。更多证据与历史失败见[持续开发交接](CONTINUE_DEVELOPMENT.md)及[正式基线审计](NOTIFICATION_OPERATIONS_BASELINE_AUDIT_20260919.md)。

## 近期证据

8.24 当前镜像及正常容器备份已有证据：330 构建输入/328 COPY 文件一致；0134 合成源 **212 表 / 584 行**，八角色及七个新密码/错误口令验证、API 实际角色、正常联合备份通过。第三轮容器会话 **47541 退出 1**，`db-deadline` 未到注入屏障，日志出现文件大小限制和超时；后续故障场景及第二新库恢复尚未执行，完整恢复未通过。所有自有 PG/容器/引擎/VM 已停止，原失败保留；新增六张日终表为空，不声称覆盖非空日终恢复。详见[本批恢复交接](CURRENT_CONTAINER_RECOVERY_20260922.md)。


**8.22 HTTPS：** 40 项夹具边界测试及 9 个实际 HTTPS API 场景通过；两次上传/回读、一次下载，6 条审核事件，库存/截止保持，1,109 输入无漂移，服务和 PG 停止。仅测试进程信任准确叶证书，系统信任未改，浏览器仍待授权及联验。首轮 driver 错误保留，见[实际 API 证据](DAILY_REVIEW_HTTPS_API_20260922.md)。

8.23 部署候选绑定已完成：原子 prepare 回执、准确源码/配置/镜像 ID/DB/HEAD 绑定、项目锁、唯一内部构建标签、静态挂载快照及固定本机数据库查询已实现。**155 项最终聚焦通过，7 输入零漂移**；最终 helper 的真实 Compose 5.5.1 整体快照再解析通过，CI 拓扑 2 项、仓库安全 1,613 文件扫描和 diff 检查通过。3 个原始错误放行反例及三轮中间证据保留，无新迁移或目标机执行。详见[部署绑定专项](PILOT_PREPARE_BINDING_20260922.md)、[配置再解析证明](PILOT_IMMUTABLE_COMPOSE_20260922.md)。

**8.20 短信登录修复：** 发送/校验绑定准确挑战及供应商实际配置，配置漂移不计用户错误；历史两种验证码模式不能用当前 PNVS 创建会话。158 项聚焦、9 个本地 PG16 场景通过，875 输入无漂移，三个自有实例全部停止。无新迁移或真实短信；[专项证据及保留失败](SMS_PROFILE_BINDING_20260922.md)。

**8.21 完整静态范围修复：** 原 233 文件白名单漏了 77 个可本地执行的后端测试，现改为整个 backend/tests 与 edge_sync 自动发现、仅排除独立 GitHub PG16 runtime。初次收集 6,563 项；新短信/HTTPS夹具也自动进入，收集成功不是测试通过。CI 触发覆盖整个 cloud_oam；当前分支 push 不自动触发，应使用面向 main 的 PR 或明确 dispatch 后核验准确 SHA。

**8.19 日终操作提示：** 增加逐档案可执行动作及当前人员/授权版本绑定，历史解释人即使后来取得总部角色也不显示自审入口；部分解释可定向退回，混合无效选择整次拒绝。后端 **209 项**、前端完整 **1,379 项 / 80 文件**及类型/双构建/试点校验通过。首轮前端并发编辑失败已保留，冻结输入后完整复跑通过；无新迁移。详见[专项证据](DAILY_REVIEW_ACTIONS_20260922.md)。

同批新建 PG16.15 空库通过 **12 场景、16 次实际动作读取、4 个非空降级保留拦截**；审核前后 7 张库存/截止事实表不变，7 条事件、1 份请求封存。876 个后端/迁移/夹具输入无漂移，最终退出 0 且集群已停止。首轮错误合成人员组织被正式授权规则拒绝，失败现场保留；第二轮仅修正夹具后独立重跑。结果位于 `artifacts/daily-action-capabilities-pg16-20260922-attempt2/result.json`，仍不是 GitHub 或生产验收。

**8.18 部署入口修复：** `prepare` 构建并迁移独立数据库后停止，等待 KMS 双人登记；`start` 先过持久化 pin gate 再启动应用。补齐项目/卷/网络/镜像隔离、API/迁移/pin gate 数据库绑定、Docker 子网冲突、端口查询失败与域名绑定检查；无自动清理、回退或真实部署。新测试及脚本路径已接入 PostgreSQL 16 静态 CI。详见[部署入口专项](PILOT_DEPLOY_ENTRY_20260922.md)与[执行单](PILOT_RELEASE_PLAN_20260921.md)。

**8.13 浏览器子集：** 当前网页、真实 API 与全新本地 PG16 的六项联验通过；正常开启、提交后断线恢复、未送达保留、明确终结和外区隔离得到实际页面及数据库证据。修复切换档案残留旧提示。59 项聚焦、完整前端 1,346 项/80 文件、TypeScript、私有构建通过；2 条开启事件、1 份终结证明。首轮收尾 SQL 列名错误退出 1，原记录保留；第二轮退出 0。两个实例和标签页均清理，见[8.13 专项](DAILY_REVIEW_BROWSER_20260921.md)。

**8.15 PG16 runtime 接线与空库验证：** 新增 `backend/tests/pg16_daily_review_runtime.py`，并将共享门禁接入 `test_postgresql16_release_gate.py`。全新本地 PG16 空库从 `upgrade head` 开始，两个只读捕获角色先验不存在再随机创建；4 个非空 downgrade 保留拦截点、12 个日终场景和最终事实不变检查全部通过。静态工作流拓扑 2 项通过，1,459 项源码清单无摘要漂移。详见[日终 PG16 runtime 证据](DAILY_REVIEW_CI_RUNTIME_20260921.md)。

**8.16 统一发布预检：** 新增 `backend/tests/test_pilot_preflight.py` 并纳入 PostgreSQL 16 静态安全清单；纯本地配置契约和工作流拓扑共 **4 passed**。启动安全/RBAC/数据库部署检查 **77 passed**，前端 `/xx` 试点校验通过，但公开知识目录仍为 `pending / 0`，严格生产门禁继续拒绝。

**8.17 当前候选复核：** 公开站点与 `warehouse` 构建、试点发布预检通过；新增数据库 URL 密码长度拒绝用例，预检/工作流拓扑 **5 passed**，仓库 `.venv` 聚焦集合（启动安全、RBAC、数据库部署、正式访问）**81 passed**，通知/库存通知/目标恢复专项 **178 passed / 1 warning**（832.38 秒），Web **80 文件 / 1,346 passed**、小程序 **1,070 passed**，开发模式公开入口校验、`git diff --check` 与仓库安全检查通过。带 `--release` 的严格公开门禁按设计因 `pending / 0` 拒绝。该结果仍属于本地候选证据，未推送、未触发远端 CI、未部署。

公开知识源按用户最新要求暂缓，目录保持 `pending / 0`。用户另已确认官方本机 `127.0.0.1:8765` 托管只读路径及专用本机连接凭据仅内存使用；后续按 `nio-chat-cli/references/hosted-read.md` 使用托管客户端，不再裸 CLI 重试，不导出企业/Desktop/tool token，不扩大业务写入。本任务未复测通道或探测源表。

本地证据目录：`artifacts/daily-review-ci-runtime-20260921/`，结果 `status=passed`、`emptyDatabase=true`、`restoredArchive=false`、`githubReleaseGate=false`，本次集群 `native-checks/run-5nrxyj4a` 已停止。`attempt-02-failed/` 仅保留一次测试环境子进程导入异常的原始记录，不作为通过证据；随后独立子进程探针和完整空库复跑均通过。

历史完整后端会话 9496 为 **4,960 passed / 1 failed / 3 skipped**；失败 CLI 固定版本已局部修复，但后续整合后的完整后端和准确候选 GitHub PG16/Client 仍未通过。不能将各批聚焦结果拼成当前完整通过。

## 下次最短接续顺序

1. 先核对额度及用户恢复指令，再读正式基线、AGENTS、当前 diff 和本交接。保留全部本地变更。
2. 52984 已终端退出，不再等待；接续新完整静态 6070，保持 1,472 输入冻结，读取实际终端结果及漂移检查。准确最终候选仍需 GitHub `pg16_runtime` 与 Client 门禁；本地空库结果不能代替远端检查。
3. 补浏览器有效 HTTPS 附件上传、部分解释、总部退回/补证/独立审核完整联验及跨标签/授权变化/窄屏验证。不得放宽正式 HTTPS 校验或绕过浏览器证书警告。
4. 完成当前八角色容器联合恢复、真实来源/OSS、真实身份/SMS/KMS、监控告警、灰度和生产验收。全部必要证据齐全后才考虑提交。

完整发布证据尚未齐全，保持用户“证据齐全后才提交”的约束。原 Edge 后续只读检查已通过，旧阻断不再作为当前待答复项；知识源暂停。不得发送真实通知/短信、写生产库或业务系统、开通付费服务。真实来源失败保持未知，不当作零库存或目录为空的业务事实。
